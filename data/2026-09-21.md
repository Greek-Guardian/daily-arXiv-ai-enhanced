<div id=toc></div>

# Table of Contents

- [cs.IR](#cs.IR) [Total: 3]
- [cs.AI](#cs.AI) [Total: 22]
- [cs.CL](#cs.CL) [Total: 19]
- [cs.LG](#cs.LG) [Total: 22]


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [1] [Predictable Failure in Multi-Hop Retrieval: Score-Distributional Confidence Scoring and Abstention](https://arxiv.org/abs/2609.22056)
*Bacellar, Andre*

Main category: cs.IR

TL;DR: 论文研究多跳检索中可预测的失败子群，提出RCS置信度分数和RegimeAbstain弃权策略，在多个基准上减少自信错误答案，且无需额外LLM调用。


<details>
  <summary>Details</summary>
Motivation: 多跳检索中的失败并非均匀分布，而是聚集在结构可预测的查询子群中；如何利用无需额外LLM调用的结构特征来预测失败并实现校准弃权，以减少自信错误答案。

Method: RegimeAbstain系统接收多跳检索查询及其ANN检索结果，提取最多九个查询-ANN结构特征（如查询长度、跳数集中度等），这些特征不依赖于任何额外的LLM调用。通过一个逻辑函数计算检索置信度分数（RCS），该分数用于校准弃权策略：当RCS低于阈值时系统放弃回答以避免自信错误。训练目标为优化RCS参数以最大化区分成功与失败的能力，具体损失函数未详细说明。推理时，RCS在线计算并用于决定是否弃权，无需修改现有检索架构。

Result: {"offline_gains": [{"metric": "AUC-AC", "value": "最佳或并列最佳", "setting": "在五个失败制度、八个置信度基线对比中"}, {"metric": "CWAR", "value": "从39.5%降至20.6%（50%覆盖率）", "setting": "MuSiQue数据集，LLM-judge架构，相对降低47.8%"}, {"metric": "ECE", "value": "0.035", "setting": "MuSiQue数据集，LLM-judge架构"}, {"metric": "AUC损失", "value": "-0.5pp", "setting": "从MuSiQue迁移到2WikiMultiHopQA数据集的跨域测试"}], "online_gains": []}

Conclusion: 证明CWAR可归约性：自信失败降低可行当且仅当检索特征携带关于成功的互信息，解释了LLM评委与纯稠密检索在AUC-AC上的差距。；提出特征制度互补性：没有单一的ANN分数特征在所有失败制度中表现最佳，不同数据集的主导特征相异，并通过构造性见证对证明各特征在特定制度下必要。；实例化RegimeAbstain系统，计算检索置信度分数（RCS）——最多九个查询-ANN结构特征的逻辑函数，无需额外LLM调用，并实现校准弃权策略。；定义Confident-Wrong-Answer Rate（CWAR）指标，并在三个多跳基准和两种检索架构上验证。

Abstract: Multi-hop retrieval failures are not uniformly distributed across queries: they cluster in structurally predictable subpopulations. We prove two results formalizing this structure. First (CWAR Reducibility): confident-failure reduction is achievable if and only if retrieval features carry mutual information about success, a condition satisfied by LLM-judge pipelines but substantially weaker in dense-only settings, explaining the AUC-AC gap between regimes. Second (Feature Regime Complementarity): no single ANN score feature achieves best predictive performance across all failure regimes; the dominant feature differs between datasets (query length on MuSiQue, hop-1 concentration on HoVer), and a constructive witness pair shows each is necessary in one regime and non-contributory in the other. We instantiate these principles in RegimeAbstain, which computes a Retrieval Confidence Score (RCS), a logistic function of up to nine query-ANN structural features, all available without any additional LLM call, and uses it to implement a calibrated abstention policy. We define the Confident-Wrong-Answer Rate (CWAR) metric and evaluate across three multi-hop benchmarks (MuSiQue, 2WikiMultiHopQA, HoVer) and two retrieval architectures (LLM-judge and dense-only), covering five failure regimes with CWAR from 14.5% to 62.1%. RCS achieves best or co-best AUC-AC in all five conditions against eight confidence baselines. On MuSiQue (LLM-judge), RCS reduces CWAR from 39.5% to 20.6% at 50% coverage (47.8% relative reduction), with ECE=0.035. A model trained on MuSiQue transfers to 2WikiMultiHopQA with only -0.5pp AUC loss, confirming the domain-agnostic structure of regime features.

</details>


### [2] [Auto-Bidding with Disentangled Advertiser Profiles and Train-Free Adaptation](https://arxiv.org/abs/2609.21308)
*Cai, Songyue,Gu, Shan,Chen, Wei,Xu, Ziru,Wang, Lianyu,Xu, Jian,Zhu, Xiaofeng*

Main category: cs.IR

TL;DR: ADAPT 提出两阶段自动出价框架：先用对比学习在广告主记忆库上抽取静态/动态画像，再将动态画像解耦为共性+私有并与静态画像联合驱动出价策略；训练后支持免训练的新广告主冷启动画像构建与已有广告主画像更新。在大规模自动出价基准上实验验证有效，并做了消融。


<details>
  <summary>Details</summary>
Motivation: 如何为广告主构建并利用画像以实现个性化自动出价，同时解决纯画像提取困难、共性与私有信息难以同时建模、画像更新与冷启动适配困难等问题。

Method: ADAPT 是一个面向自动出价的两阶段训练框架，并支持免训练适配。输入为广告主相关的历史行为/出价数据，输出为每个广告主的个性化出价策略。阶段1：维护一个广告主记忆库(advertiser memory bank)，在其上使用对比学习抽取“纯”的静态画像与动态画像，旨在把画像信号中混杂的噪声与共性成分分离出来，得到更干净的广告主表征。阶段2：将阶段1得到的动态画像进一步解耦为“共性画像”(多个广告主共享的模式)与“私有画像”(该广告主独有的模式)，并与静态画像一起联合条件化(condition)出价策略的生成，从而同时建模广告主之间的共享信息与个体差异。推理/适配阶段：模型一旦训练完成，对于新广告主可直接基于其历史数据构建画像，对于已有广告主可随新数据更新画像，全程无需重新训练模型，从而缓解冷启动与分布漂移问题。目标是让出价策略在个性化与可迁移性之间取得平衡，并通过对比学习与解耦设计提升画像质量。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 两阶段训练范式：阶段1在广告主记忆库上通过对比学习抽取纯静态与动态画像；阶段2将动态画像解耦为共性画像与私有画像，并与静态画像联合条件化出价策略；免训练自适应：训练完成后无需重训练即可为新广告主构建画像、为已有广告主更新画像

Abstract: Auto-bidding is a key component of modern advertising systems that provides a personalized bidding strategy for each advertiser. By characterizing each individual, profile-based methods achieve personalization and have proven effective in domains such as recommendation. However, despite the diverse bidding behavior of advertisers, their application to auto-bidding remains limited. A primary reason is that constructing and leveraging advertiser profiles face several challenges: extracting pure profiles is non-trivial, modeling common and private information simultaneously is difficult, and profile updating and cold-start adaptation remain challenging. To tackle these issues, we propose \textbf{ADAPT}, an \underline{\textbf{A}}uto-bidding framework with \underline{\textbf{D}}isentangled \underline{\textbf{A}}dvertiser \underline{\textbf{P}}rofiles and \underline{\textbf{T}}raining-free adaptation. ADAPT introduces a two-stage training paradigm and supports training-free adaptation. Specifically, (i) the stage 1 extracts pure static and dynamic profiles via contrastive learning over the advertiser memory bank; (ii) the stage 2 disentangles the dynamic profile into a common profile and a private profile, and combines them with the static profile to jointly condition the bidding strategy; (iii) once trained, ADAPT constructs profiles for new advertisers and updates profiles of existing advertisers without retraining. Our experiments on a large-scale auto-bidding benchmark demonstrate that ADAPT consistently achieves superior performance, and ablation studies further validate the effectiveness of each module. The source code will be released at https://github.com/YuzunoKawori/ADAPT.

</details>


### [3] [Verify, Don't Trust: Agentic Model Development for Video Discovery Retrieval at Scale](https://arxiv.org/abs/2609.21257)
*Fu, Hao,Zhu, Baiting,Chen, Minglei,Huang, Yinjie,Ding, Shuai*

Main category: cs.IR

TL;DR: Meta 的 EvoPilot 提出人工把关的长周期在线 autoresearch 方法，用角色化 agent、版本化领域技能与类型化适配器驱动在线检索模型的自动迭代，并以持久化记录和确定性检查保证实验有效性。在 Video Deep Dive 检索系统的 37 天活动中，方法定位并修复了导致 22 个百分点命中率下降被错误归因的评估缺陷，修复后获得 3.20 个百分点离线提升，并以 7 天在线随机评估测得 GSRR 在 VDD 切片上 0.66% 的相对提升。


<details>
  <summary>Details</summary>
Motivation: 长周期在线 autoresearch（异步系统、数小时变体、数周活动）中，LLM agent 提出的模型改动即使跑完也可能得出无效结论：代码改动是 no-op、数据窗口泄漏、评估器语义漂移、或两组实验走了不同服务漏斗。

Method: EvoPilot 是面向长周期在线 autoresearch 的人工把关方法。每轮实验由按角色分工的 agent 执行，agent 通过版本化的领域技能（versioned domain skill）与类型化的适配器（typed adapter）操作异步在线系统。持久化记录（durable records）保存每次实验与失败，形成可复用的状态，使被中断的轮次可以恢复、artifact 可以被复用（节省约 5 GPU 小时）。确定性检查（deterministic checks）根据已记录的教训强制执行约束，防止重复已知错误。方法还引入人工把关的验证环节，对实验做后验重放与变异测试，用来拒绝无效对比并接纳有效对比。研究在 Meta Video Deep Dive（VDD）检索系统上执行 37 天活动，覆盖七个方向，使用每小时刷新的数亿视频索引；此前人工实验未证明 interaction head 有收益，原始 autoresearch 尝试错误地把 22 个百分点的离线命中率下降归因于 interaction head，人工把关验证最终把下降定位到已有评估缺陷（输出深度 3000 与 600）；修复后匹配对比测得 3.20 个百分点离线改善，并另以 7 天随机在线评估测得 GSRR 在 VDD 切片上 0.66% 的相对提升。

Result: {"offline_gains": [{"metric": "hit-rate 相对提升", "value": "3.20 个百分点（离线改善）", "setting": "修复评估缺陷后的匹配对比（matched comparison）"}, {"metric": "hit-rate 下降错误归因", "value": "22 个百分点", "setting": "原始 autoresearch 尝试错误地把 22 个百分点离线命中率下降归因于 interaction head；实际为已有评估缺陷，输出深度为 3000 与 600"}, {"metric": "GPU 时长节省", "value": "约 5 GPU 小时", "setting": "artifact 复用"}], "online_gains": [{"metric": "GSRR（Good Search Result Rate for Retention）VDD 切片相对提升", "value": "0.66% 相对提升", "setting": "为期 7 天的随机在线评估"}]}

Conclusion: EvoPilot：人工把关（human-gated）的长周期在线 autoresearch 方法；按角色分工的 agent，通过版本化领域技能（versioned domain skill）与类型化适配器（typed adapter）执行每轮实验；持久化记录保存实验与失败，确定性检查强制执行已记录的经验教训；对已有评估缺陷的定位与修复：发现输出深度 3000 与 600 导致命中率下降被错误归因；后验重放与变异测试（post-study replay and mutation tests）：拒绝无效对比、接纳有效对比；持久化状态恢复被中断的轮次；artifact 复用节省约 5 GPU 小时

Abstract: Large language model (LLM) agents can propose, implement, and evaluate model changes. Autoresearch loops demonstrate this capability through minutes-scale iterations on a self-contained program. Online autoresearch instead spans asynchronous systems, hours-long variants, and weeks-long campaigns that can influence a product. A completed run can still support an invalid conclusion when a code change is a no-op, data windows leak, evaluator semantics drift, or the two arms traverse different serving funnels. We present EvoPilot, a human-gated method for long-horizon online autoresearch. Role-specific agents execute each round through a versioned domain skill and typed adapter. Durable records preserve experiments and failures; deterministic checks enforce recorded lessons. We study a 37-day campaign for the retrieval system that powers Video Deep Dive (VDD), an online experience for discovering follow-on videos after a user opens a seed video. The campaign covered seven directions and used an hourly refreshed index of hundreds of millions of videos. Earlier manual experiments had not established a benefit from an interaction head. A primitive autoresearch attempt revisited the direction but incorrectly attributed an offline hit-rate decline of 22 percentage points to the head. We then introduced EvoPilot. Its human-gated verification traced the drop to a pre-existing evaluation defect that produced output depths of 3,000 and 600. After repair, a matched comparison measured an offline improvement of 3.20 percentage points. Post-study replay and mutation tests rejected invalid comparisons while admitting valid counterparts. Durable state recovered an interrupted round, and artifact reuse avoided approximately five GPU-hours. Separately, a seven-day randomized online evaluation estimated a 0.66% relative increase in the VDD slice of Good Search Result Rate for Retention (GSRR).

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [4] [AutoRecLab: Describe the Experiment, Get the Code!](https://arxiv.org/abs/2609.21863)
*Baumgart, Moritz,Meister, Philipp,Krell, Justus,Schmidt, Michael,Gipp, Bela,Beel, Joeran*

Main category: cs.AI

TL;DR: AutoRecLab通过自然语言描述自动生成并执行推荐系统实验代码，结合RAG、静态类型验证和执行引导树搜索，在演示中成功实现显式到隐式反馈转换研究，平均成本约1美元/次。


<details>
  <summary>Details</summary>
Motivation: 将自然语言描述的推荐系统实验设计自动转化为可执行、可验证的代码，减少人工编写和调试错误。

Method: AutoRecLab是一个基于Python的自主推荐系统实验室。输入为自然语言研究想法，系统首先提取明确的实验需求，然后通过检索增强生成（RAG）查找相关文档，构建并验证一个原型，最后通过执行引导的树搜索迭代扩展为完整的实验。工作流结合静态类型验证确保代码正确性。在演示中，系统自主实现了显式到隐式反馈转换研究，并在6种算法和3个数据集上进行了基线比较，9次运行中8次成功，平均成本约1美元。

Result: {"offline_gains": [{"metric": "实验成功率", "value": "8/9 runs succeed", "setting": "在6种算法和3个数据集上的基线比较"}, {"metric": "平均成本", "value": "approximately $1 per run", "setting": "使用GPT-5.4-mini运行"}], "online_gains": []}

Conclusion: 结合RAG的文档查找、静态类型验证和执行引导树搜索的自动化实验工作流；从实验需求推导到原型构建再到完整实验迭代扩展的分阶段自主实现

Abstract: Empirical evaluation is central to recommender-systems (RecSys) research, but turning experimental designs into executable code remains a manual and error-prone task. We present AutoRecLab, a Python-based autonomous RecSys lab that automates RecSys experiments from natural-language prompts. Given a research idea, AutoRecLab derives explicit experiment requirements, builds and validates a prototype, and iteratively expands it into the requested full experiment. The workflow combines retrieval-augmented generation (RAG) for documentation lookup, static type verification, and execution-steered tree search. In our demonstration, AutoRecLab autonomously implements an explicit-to-implicit feedback conversion study. In a baseline comparison across six algorithms and three datasets, 8 of 9 runs succeed at an average cost of approx- imately $1 per run with GPT-5.4-mini.

</details>


### [5] [Designer-RSI: Evolving Procedural Memory from User Traffic for Agentic Graphic Design](https://arxiv.org/abs/2609.22086)
*Du, Hongyang,Yan, Lan,Flores, Christian,Kadav, Asim*

Main category: cs.AI

TL;DR: 提出Designer-RSI框架，通过外部程序性记忆的拓宽与深化机制，使冻结的LLM智能体在专业图形设计任务中持续适应，无需权重更新或人工标签，在多个基准上显著提升执行成功率和生成质量。


<details>
  <summary>Details</summary>
Motivation: 专业图形设计缺乏可靠的程序化评分函数，导致智能体难以通过传统强化学习或人类反馈进行持续改进。

Method: 该系统包含一个冻结的前沿模型作为智能体，操作专业设计软件的超过230个工具。外部程序性记忆存储自然语言技能，从76个文档衍生技能开始。记忆拓宽机制：当检测到反复出现的未覆盖子任务时，获取新的程序并加入记忆。记忆深化机制：针对已有程序，根据其成功和失败执行进行修订。匹配重放门控确保只有能修复失败且不降低已成功执行的更改被接受。经过五轮迭代，使用1,406个真实用户简报和1,869个自动评分轨迹进行训练，无权重更新，无人工标签。在推理时，智能体根据当前设计任务从记忆中检索相关技能，并用于指导工具调用，最终生成结构化可编辑的设计工件。

Result: {"offline_gains": [{"metric": "GenEval2 execution success", "value": "从72.7%提升至99.3%", "setting": "在Claude-Sonnet-4上，五轮迭代，1,406个真实用户简报和1,869个自动评分轨迹"}, {"metric": "generation quality", "value": "+11.99 points", "setting": "相对于无技能基线，在Claude-Sonnet-4上"}, {"metric": "win rate against no-skill agent", "value": "61.8%", "setting": "在四个专门设计基准上，Claude-Sonnet-4"}, {"metric": "win rate against no-skill agent", "value": "67.6%", "setting": "在四个专门设计基准上，Claude-Opus-4.6"}, {"metric": "win rate on 200 held-out briefs", "value": "拓宽单独达到49.4%，深化单独达到48.6%，组合达到58.5% (p=0.025)", "setting": "用户流量基准的200个保留简报，对比无技能智能体"}], "online_gains": []}

Conclusion: 外部自然语言程序性记忆，从用户流量经验中积累并精炼可复用的设计流程；记忆拓宽机制：为反复出现的未覆盖子任务获取新程序；记忆深化机制：根据自身成功与失败执行修订已有程序；匹配重放门控：仅接受能修复失败且不回退已观察成功的更改

Abstract: Professional graphic design is a long-horizon agentic task in which structured, editable artifacts emerge from many interdependent actions, yet outcomes admit no reliable programmatic oracle. We introduce a continual adaptation framework in which a frozen frontier model operates professional design software through more than 230 tools, while an external procedural memory of natural-language skills accumulates and refines reusable design procedures from experience. The memory widens by acquiring procedures for recurring uncovered subtasks and deepens by revising existing procedures against their own successful and failed executions, while a matched replay gate admits only changes that repair failures without regressing observed successes. Five rounds over 1,406 real user briefs and 1,869 automatically graded trajectories, with no weight updates and no human labels, grow the bank from 76 documentation-derived skills to 139 and raise GenEval2 execution success on Claude-Sonnet-4 from 72.7% to 99.3% (+11.99 points in generation quality), with 61.8% and 67.6% win rates against the no-skill agent across four specialized design benchmarks on Claude-Sonnet-4 and Claude-Opus-4.6. We further show the two mechanisms are effective in combination: on 200 held-out briefs from user-traffic benchmark, widening or deepening alone reaches a 49.4% / 48.6% win rate over the no-skill agent, while their combination reaches 58.5% (p = 0.025). Procedural memory offers a practical route to continual adaptation of agents under noisy, unverifiable feedback.

</details>


### [6] [AutoViewMem: Self-Configuring Orthogonal Views for Conversational Long-Term Memory](https://arxiv.org/abs/2609.21940)
*Cao, Zijie,Qu, Xijun,Gu, Zhicheng,Chen, Xiaoshu,Yuan, Duanyang,Hou, Yanning,Zhou, Sihang,Gong, Jianxing,Huang, Jian,Mei, Yang*

Main category: cs.AI

TL;DR: AutoViewMem通过自配置正交视图在写入时解耦长期对话记忆，减少语义干扰，提升top-K检索质量，在LoCoMo和PersonaMem上超越强基线，且推理流程简单。


<details>
  <summary>Details</summary>
Motivation: 现有长期记忆系统使用固定粒度或静态模式，当异构信息（偏好、事件、约束、时序更新）嵌入单一混合表示时产生语义干扰，导致top-K检索对噪声敏感且相关证据排序不佳。

Method: AutoViewMem是一个数据驱动的框架，在索引前将长期对话记忆组织成自配置、低重叠的语义视图。首先，从交互轨迹中发现候选视图；然后，选择紧凑的互补视图集；接着，利用这些视图指导写入时的结构化提取，生成带来源的细粒度记忆，从而在写入阶段实现语义解耦，使标准top-K相似度搜索能够检索到专注的证据，无需显式路由或迭代检索；最后，应用离线巩固来改善记忆的紧凑性与一致性。整个推理流程保持简单。实验在LoCoMo和PersonaMem基准上进行，使用Qwen3-8B和Qwen3-14B骨干模型，表明该方法在长程问答和个性化任务上优于强记忆基线。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 自配置低重叠语义视图：从交互轨迹中发现候选视图并选择紧凑互补视图集；写入时结构化提取：利用视图指导带来源的细粒度记忆提取，将语义解耦从检索时移至写入时；离线巩固：提升记忆紧凑性与一致性

Abstract: Long-term memory is essential for large language model (LLM) agents to maintain consistency and personalization over extended interactions. Existing memory systems typically rely on fixed granularities or static schemas, but these designs struggle when heterogeneous information, such as preferences, events, constraints, and temporal updates, is embedded in a single mixed representation. The resulting semantic interference makes top-K retrieval sensitive to noise and often leaves relevant evidence poorly ranked. We present AutoViewMem, a data-driven framework that organizes long-term conversational memory into self-configuring, low-overlap semantic views before indexing. AutoViewMem discovers candidate views from interaction traces, selects a compact complementary view set, and uses these views to guide write-time structured extraction of provenance-grounded memories. This representation-first design moves semantic disentanglement from retrieval time to write time, allowing standard top-K similarity search to retrieve focused evidence without explicit routing or iterative retrieval. We further apply offline consolidation to improve memory compactness and consistency. Experiments on the LoCoMo and PersonaMem benchmarks, under both Qwen3-8B and Qwen3-14B backbones, show that AutoViewMem improves long-horizon question answering and personalization over strong memory baselines while preserving a simple inference pipeline.

</details>


### [7] [What Should We Ask Next? Retrieval-Aware Question Learning under Partial Evidence](https://arxiv.org/abs/2609.21924)
*Qian, Lyucheng,Zhang, John Yuehan,Wang, Pingyu*

Main category: cs.AI

TL;DR: RAVEL 将交互式检索中的提问选择建模为在线强化学习问题，利用问答-检索闭环的排序反馈训练提问策略，替代离线模仿学习；在 Interactive-PEDES 上五轮交互逐步提升检索效果，并把提问预算转向局部开放式属性。


<details>
  <summary>Details</summary>
Motivation: 在部分证据下，如何决定下一个问题，使其产生的回答对下一轮检索更新最有价值；现有方法通过模仿离线问答顺序来训练提问决策，而问题价值实际由回答内容及其对下游检索的影响决定。

Method: RAVEL 面向交互式人物再识别检索任务。输入为当前检索状态，agent 直接观察当前 Top-4 候选；提问策略以监督式问题生成模型初始化。策略将问题选择建模为序贯决策，通过完整问答-检索闭环获得排序反馈（rank feedback）作为奖励信号，采用在线强化学习优化问题策略，使所选问题能最大化下一轮检索更新的收益。每一轮中，agent 选择问题、获得回答、执行检索更新，循环五轮。训练目标为在闭环排序反馈下最大化检索性能，而非模仿离线问答顺序。

Result: {"offline_gains": [{"metric": "交互式检索性能（五轮交互）", "value": "逐轮增强（未给出具体数值）", "setting": "Interactive-PEDES 数据集上的实验"}], "online_gains": []}

Conclusion: 形式化提问价值由回答及其下游检索效果决定，指出候选判别性与感知有用性是弱监督信号；提出 RAVEL：检索感知的在线强化学习框架，从监督式提问生成初始化，直接观察当前 Top-4 候选，用完整问答-检索闭环的排序反馈优化提问策略；分析发现 RAVEL 将提问预算重新分配到局部化的开放式属性上，在初始困难查询上收益最大

Abstract: Interactive retrieval under partial evidence is a sequential information-acquisition problem: an agent must decide which question will create the most useful evidence for the next retrieval update. Existing systems train this decision by imitating an offline ordering of candidate QA pairs, although question value is determined by the response it elicits and its downstream effect on retrieval. We establish that candidate discriminativeness and perceived usefulness provide weak supervision for this objective, then introduce RAVEL, a retrieval-aware online reinforcement learning framework for interactive person re-identification. RAVEL initializes from supervised question generation, observes the current Top-4 candidates directly, and optimizes the question policy with rank feedback from the full question-answer-retrieval loop. Experiments on Interactive-PEDES show that RAVEL delivers progressively stronger retrieval performance across five interaction rounds. Further analysis shows that RAVEL reallocates the questioning budget toward localized open-ended attributes, which provide more useful retrieval evidence and yield the largest gains on initially difficult queries.

</details>


### [8] [EnterpriseVal: Quantifying the Efficacy, Reliability and Value of Generative AI in the Enterprise](https://arxiv.org/abs/2609.21841)
*Ali, Abbas Raza,Siddiqui, Muhammad Ajmal,Zahid, Moona*

Main category: cs.AI

TL;DR: EnterpriseVal是一套面向企业生成式AI用例的评估与部署决策系统，包含用例规范、多维指标目录、混合专家与LLM评判的评分协议、阈值门控算法和价值-风险模型。在全球银行三个工作流试点中，信贷备忘录起草达到88%引用精度和1.6%幻觉率，程序转换将分析员精修时间从估计27.4小时降至2.9小时。


<details>
  <summary>Details</summary>
Motivation: 企业生成式AI部署缺乏能回答“该工作流在当前数据、控制与合规条件下是否适合、可靠、安全且值得规模化”的用例级量化评估与决策方法，导致大量项目无法证明业务效果或被迫取消。

Method: EnterpriseVal是一个用例级评估系统，其架构包含五个顺序交互的组件。首先，形式化用例规范定义被评估的工作流及冻结的社会-技术配置，包括模型、提示、检索、工具、护栏和人工监督，并通过自主性等级和后果级别共同设定所需的评估强度。其次，指标目录提供横跨保真度、效用、效率、可靠性、保证和监督六个维度的指标集合。第三，评分协议结合盲评专家判断与校准的LLM-as-judge评分，使用预测驱动推断以在有限标注下扩展评估。第四，两级阈值门控以可执行算法实现，接收带置信界的指标向量并输出REJECT、CONDITIONAL或SCALE决策。第五，价值-风险模型将审核者捕获率作为可测量参数纳入，以量化部署的商业价值与风险。推理或执行流程为：输入具体用例及其配置，系统按自主性-后果矩阵选择评估强度，运行指标收集，通过混合评分协议生成带置信界的指标值，最后经门控算法给出部署决策，并由价值-风险模型辅助判断是否规模化。

Result: {"offline_gains": [], "online_gains": [{"metric": "citation precision", "value": "88%", "setting": "全球银行信贷备忘录起草工作流，最佳模型，人工评分，门控要求为70%"}, {"metric": "hallucination rate", "value": "1.6%", "setting": "全球银行信贷备忘录起草工作流，最佳模型，人工评分，门控要求为5%"}, {"metric": "analyst refinement effort", "value": "从估计的27.4小时/文档降至2.9小时/文档", "setting": "全球银行程序转换工作流"}]}

Conclusion: 形式化用例规范与冻结的社会-技术配置描述，配备自主性等级与后果级别以共同决定评估强度。；覆盖保真度、效用、效率、可靠性、保证与监督的多维指标目录。；通过预测驱动推断将盲评专家判断与校准LLM-as-judge评分相结合的可扩展评分协议。；以可执行算法表述的两级阈值门控，将带置信界的指标向量映射为REJECT/CONDITIONAL/SCALE决策。；将审核者捕获率作为可测量参数纳入价值-风险模型。

Abstract: Frontier language models now produce professional deliverables that expert graders judge to match human work on a substantial share of economically valuable tasks, yet most enterprise GenAI initiatives fail to show a measurable business effect and a large fraction of agentic projects are expected to be cancelled. We argue that this is substantially a measurement problem: public benchmarks answer "what can the model do?", whereas a deployment decision requires "is this workflow fit, reliable, safe and worth scaling - here, on our data, under our controls?". We present EnterpriseVal, a use-case-level evaluation system that closes this gap. It comprises (i) a formal specification of the use case and of the frozen socio-technical configuration under test, model, prompts, retrieval, tools, guardrails and human oversight, with an autonomy level and consequence tier that jointly set the required evaluation intensity; (ii) a metric catalogue spanning fidelity, utility, efficiency, reliability, assurance and oversight; (iii) a grading protocol that scales blinded expert judgement with calibrated LLM-as-judge scoring through prediction-powered inference; (iv) a two-tier threshold gate, stated as an executable algorithm, that maps metric vectors with confidence bounds to REJECT/CONDITIONAL/SCALE decisions; and (v) a value-and-risk model in which the reviewer catch rate is a measured parameter. We report a pilot across three workflows in a global bank. In credit-memo drafting, human-graded citation precision reached 88% and hallucination rate 1.6% for the best model against gates of 70% and 5%; in procedure transformation, analyst refinement effort fell from an estimated 27.4 to 2.9 hours per document. We separate established results, documented pilot evidence, the proposed system and open hypotheses, and specify the experiments required for full validation

</details>


### [9] [World Modeling in Transformers](https://arxiv.org/abs/2609.21748)
*Beckmann, Pierre,Queloz, Matthieu,Freitas, Andre*

Main category: cs.AI

TL;DR: 该论文以 TaxiGPT 为受控环境，用机制分析与因果干预证明 Transformer 的导航失败源于交叉路口特征叠加干扰，而非缺乏世界模型，并引入 Affordance Packing 与训练阶段涌现分析。


<details>
  <summary>Details</summary>
Motivation: Transformer 在行为上失败时，是否代表其内部没有学到忠实的环 境世界模型？如何机制性地刻画并区分表征缺陷与行为缺陷？

Method: 论文围绕 TaxiGPT 构建受控实验：模型在曼哈顿街道的随机游走序列上训练，输入为当前状态（如位置、目标等 token），输出为导航动作。作者通过机制分析与因果干预检查模型内部表征，发现模型确实编码了交叉路口和街道、跟踪自身位置，并借助目标罗盘进行导航。失败被追溯到交叉路口特征在叠加表示中的相互干扰，这种干扰破坏了模型在内部地图中的定位。Affordance Packing 将具有相同合法移动的交叉路口表征分组，以限制定位错误的传播。此外，作者提出机制性指标来比较不同模型，并观察到世界建模能力在训练的不同阶段逐步涌现。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 以 TaxiGPT（在曼哈顿随机游走上训练的 Transformer）为受控环境，证明行为失败并不等于缺乏世界模型；通过机制分析与因果干预，定位到交叉路口特征的叠加干扰导致内部地图中的定位失败；提出 Affordance Packing：将具有相同合法移动的交叉路口表征分组，缓解叠加干扰带来的后果；提出机制性指标，用于跨模型比较世界建模能力，并揭示该能力在训练不同阶段涌现

Abstract: Behavioral failures can make a transformer appear to lack a world model even when it has learned faithful representations of its environment. We demonstrate this in TaxiGPT, a transformer trained on random walks through Manhattan whose failures have been interpreted as evidence of an incoherent internal map. Through mechanistic analysis and causal interventions, we show that the model represents intersections and streets, tracks its position, and uses a goal compass to navigate. We trace its failures to interference between superposed intersection features, which disrupts localization within the internal map. Affordance packing, which groups representations of intersections with the same legal moves, helps limit the consequences of these errors. Finally, we propose mechanistic indicators that we use to compare models and show that world-modeling capacities emerge at different stages of training. Our findings motivate a shift from asking whether a model has a world model to mechanistically studying its world modeling: the interacting capacities through which it represents its environment and uses those representations to guide behavior.

</details>


### [10] [GUARD: Natural Forgetting in Large Reasoning Models via Guided Answer-Reasoning Distillation](https://arxiv.org/abs/2609.21677)
*Yan, Zeyu,Zhou, Guanghao,Qiu, Minghui,Gao, Ming,Chen, Cen*

Main category: cs.AI

TL;DR: GUARD提出一种针对大型推理模型的自然遗忘方法，通过引导令牌对齐和蒸馏，将不安全披露转化为安全的拒绝式回答，并引入NFRS评估遗忘质量。在R-TOFU和STAR-1衍生数据集上验证了减少泄露且保持推理能力。


<details>
  <summary>Details</summary>
Motivation: 如何让大型推理模型进行自然遗忘，即生成不披露敏感信息的连贯推理链和拒绝式回答，同时保持推理能力，避免现有遗忘方法导致的幻觉、重复输出等问题。

Method: GUARD方法整体分为三个阶段：1) 数据构建：使用原始模型生成可能包含不安全披露的推理轨迹和答案；2) 引导对齐：在冻结的LRM上，通过引导令牌（guidance tokens）控制生成过程，将不安全轨迹转化为安全退出轨迹，即连贯的非披露CoT和拒绝式回答；3) 蒸馏：将引导产生的安全行为蒸馏到模型参数中，使模型在无引导时也能自然输出安全轨迹。训练目标包括对齐损失和蒸馏损失，旨在让模型学会自然的遗忘轨迹。推理时，模型直接生成安全的CoT和拒绝回答。具体模块细节、训练数据规模、硬件资源未披露。

Result: {"offline_gains": [{"metric": "unsafe and privacy disclosures", "value": "substantially reduced", "setting": "R-TOFU and STAR-1-derived harmful-intent, two distilled LRMs"}, {"metric": "reasoning utility", "value": "preserved", "setting": "R-TOFU and STAR-1-derived harmful-intent, two distilled LRMs"}], "online_gains": []}

Conclusion: 提出引导答案-推理蒸馏（GUARD），将模型生成的不安全披露转换为安全退出轨迹，通过引导令牌对齐冻结的LRM，并将引导行为蒸馏到模型参数中。；引入自然遗忘推理评分（NFRS），衡量遗忘输出的结构稳定性、流畅性和无支撑替代内容。

Abstract: Recent advances in large reasoning models (LRMs) have made machine unlearning more challenging, as protected facts or unsafe rationales may surface in intermediate chain-of-thought (CoT) traces before the final answer is produced. Existing unlearning objectives typically suppress the target content or redirect internal representations, but they never specify how the post-forgetting trajectory should continue, which can lead to hallucinated substitutes, malformed boundaries, or repetitive outputs. We argue that LRM unlearning should instead learn a natural forgetting trajectory: a coherent non-disclosing CoT followed by a stable refusal-style answer that replace the original disclosure. To this end, we propose Guided Answer-Reasoning Distillation (GUARD), which converts model-generated unsafe disclosures into safe-exit trajectories, aligns a frozen LRM via guidance tokens, and distills the guided behavior into model parameters. To address the lack of metrics for replacement quality beyond leakage, we further introduce Natural Forgetting Reasoning Score (NFRS), which captures structural stability, fluency, and unsupported substitutes in forgotten outputs. Extensive experiments on R-TOFU and a STAR-1-derived harmful-intent setting show that GUARD substantially reduces unsafe and privacy disclosures across two widely adopted distilled LRMs while preserving reasoning utility.

</details>


### [11] [Accelerating Dense LLMs via L0-regularized Mixture-of-Experts](https://arxiv.org/abs/2609.21672)
*Zhang, Zhenyu,Yang, Jiudong,Tao, Zhaowen,Chen, Meng*

Main category: cs.AI

TL;DR: L0-MoE 通过 L0 正则化把稠密 LLM 改造成轻量 MoE，并配合领域感知数据筛选与动态批处理，在几乎不损失性能的情况下实现最高 2.5x 推理加速，实验优于现有 LLM 加速基线。


<details>
  <summary>Details</summary>
Motivation: 稠密 LLM 推理速度慢、成本高，而现有加速方法往往带来明显性能下降，传统 MoE 又需要大量计算资源，如何在几乎不损失性能的前提下加速稠密 LLM。

Method: 论文提出 L0-MoE：在稠密 LLM 的基础上，通过 L0 正则化学习稀疏的专家激活/路由结构，将原稠密前馈或专家计算重构成轻量 MoE 模块。训练侧引入 cluster confusion matrix 进行领域感知的数据集筛选，以更好地组织训练数据并指导专家分化；同时使用动态批处理提升训练吞吐。推理时仅激活被路由选中的少量专家，从而降低计算量，相对稠密模型最高获得 2.5x 加速，同时保持有竞争力的下游性能。摘要未披露具体的路由机制、正则化目标函数形式、专家数量、参数规模与部署硬件。

Result: {"offline_gains": [{"metric": "推理加速比", "value": "最高 2.5x", "setting": "相对稠密模型，未披露具体硬件与基准配置"}, {"metric": "性能保持", "value": "保持有竞争力性能，优于现有 LLM 加速基线", "setting": "未披露具体评测任务与数据集"}], "online_gains": []}

Conclusion: 使用 L0 正则化将稠密 LLM 转化为轻量 MoE，实现近乎无损加速；引入 cluster confusion matrix 用于领域感知的数据集筛选；采用动态批处理（dynamic batching）提升训练效率

Abstract: Large language models (LLMs) achieve strong performance but suffer from slow and costly inference. Existing acceleration methods often lead to noticeable performance degradation, while Mixture-of-Experts (MoE) models require extensive computational resources. In this paper, we propose L0-MoE, a lightweight MoE approach using L0-regularization to accelerate dense LLMs nearly without performance loss. Our method introduces a cluster confusion matrix for domain-aware dataset curation and applies dynamic batching for efficient training. Experiments show that L0-MoE achieves up to 2.5x speedup over dense models while maintaining competitive performance, outperforming existing LLM acceleration baselines.

</details>


### [12] [One Prompt Does Not Fit All: Self-Meta-Evolve for Personalized Information Extraction](https://arxiv.org/abs/2609.21626)
*Li, Hongliang,Wang, Lu,Xu, Yong,Chen, Hanyang,Hou, Zhitao,Qin, Xiaoting,Ge, Song,Lin, Qingwei,Zhang, Dongmei*

Main category: cs.AI

TL;DR: 提出Self-Meta-Evolve分层框架，通过双循环为每个用户维护并演化个性化提示，解决企业信息抽取中的用户异质性问题，在自建基准上显著超越全局提示优化基线，并获人类评估验证。


<details>
  <summary>Details</summary>
Motivation: 现有提示优化方法依赖全局单一提示，无法适应企业环境中用户异质性导致的信息需求差异。

Method: Self-Meta-Evolve采用层次化结构：为每个用户维护一个专属提示。系统包含内循环与外循环。内循环接收用户角色（persona）条件化的反馈，对结构化提示进行编辑；外循环则从内循环的成功编辑案例中蒸馏出通用的编辑模式，用以更新元提示。元提示指导后续编辑操作。整个过程通过交互反馈持续优化，最终得到每个用户适配的提示。推理阶段，针对特定用户，利用其专属提示及元提示生成个性化抽取结果。论文未披露模型参数、硬件或延迟等细节。

Result: {"offline_gains": [{"metric": "成功率", "value": "74.58%", "setting": "在自建基准上，相比最强提示优化基线提升13.56个百分点"}, {"metric": "两轮迭代成功率", "value": "52.54%", "setting": "仅两次迭代后达到的成功率"}, {"metric": "人类评估胜率", "value": "71%", "setting": "20位真实专业人士双盲配对比较中，本框架胜过静态基线"}], "online_gains": []}

Conclusion: 提出自元演化（Self-Meta-Evolve）分层框架，为每个用户维护独立提示并通过双循环迭代优化；内循环基于角色条件反馈编辑结构化提示，外循环通过提炼成功编辑模式演化元提示；发布基于O*NET职业分类法、包含292个模拟用户的可复现个性化信息抽取基准

Abstract: Large language models (LLMs) are increasingly deployed for enterprise information extraction (IE), where the same document must be reorganized differently for each user. Existing prompt optimization methods, however, rely on a single prompt optimized against a global objective, which is misaligned with the inherent user heterogeneity of real workplaces. We formulate enterprise IE as per-user prompt adaptation under interaction feedback and propose Self-Meta-Evolve, a hierarchical framework that maintains a dedicated prompt for each user and continuously refines it through a dual-loop process: an inner loop that edits structured prompts based on persona-conditioned feedback, and an outer loop that evolves the meta-prompt itself by distilling successful editing patterns. To enable scalable training and evaluation, we release a persona-driven IE benchmark of 292 simulated enterprise users, paired with a reproducible persona-generation pipeline grounded in O*NET occupational taxonomies. On this benchmark, Self-Meta-Evolve achieves a 74.58% success rate, outperforming the strongest prompt-optimization baseline by 13.56 absolute points, and reaches 52.54\% within only two iterations. A double-blind human study with twenty real professionals further confirms that prompts adapted by our framework win against static baselines in 71% of pairwise comparisons.

</details>


### [13] [Calibrating Teacher--Student Discrepancy for On-Policy Distillation](https://arxiv.org/abs/2609.21619)
*He, Qiangqiang,Li, Jin,Chen, MingCai*

Main category: cs.AI

TL;DR: Cal-OPD 针对 on-policy distillation 中 teacher-student 差异包含教师自身偏差的问题，通过正负特权干预估计并剔除该偏差，仅保留真实能力差距信号进行优化。在数学推理基准上仅用约 52-65% 的原始差异信号即超越标准 OPD 及其变体，跨模型尺度一致有效。


<details>
  <summary>Details</summary>
Motivation: 标准 on-policy distillation 中观察到的 teacher-student 逐 token 差异包含教师自身偏差（teacher self-deviation），导致学生 indiscriminately 学习教师偏差而非真实能力差距；privileged OPD 下该问题更严重。

Method: Cal-OPD 基于 on-policy distillation 框架。输入为教师模型和学生模型在 on-policy 生成轨迹上的逐 token 对数似然。标准 OPD 直接最小化 teacher-student 对数似然差异。Cal-OPD 首先通过正负 privileged intervention 扰动教师（例如提供/屏蔽特权信息），测量教师在不同干预下的似然偏移，从而估计教师自身偏差区域（self-deviation region）。然后，原始 teacher-student discrepancy 被分解为两部分：教师自身偏差成分和真实能力差距成分。校准后的优化信号仅保留超出该偏差区域的差异部分（实验中使用原差异的约 52-65%）。训练目标是最小化校准后的 token 级差异。推理时直接使用学生模型，无额外模块。

Result: {"offline_gains": [{"metric": "数学推理基准（具体指标未在摘要中列出）", "value": "Cal-OPD 一致优于标准 OPD 及其变体，跨模型尺度", "setting": "offline 实验，具体数据集、模型规模、基线名称未在摘要中提供"}], "online_gains": []}

Conclusion: 通过正负 privileged intervention 估计教师的自身偏差区域（self-deviation region）；校准 teacher-student discrepancy：仅保留超出该区域的成分作为优化信号；在数学推理基准上，仅用原差异信号的约 52-65% 即超越标准 OPD 及其变体

Abstract: On-policy distillation (OPD) improves reasoning models by learning the token-level discrepancy between a stronger teacher and an on-policy student. However, this discrepancy does not purely reflect the capability gap between the teacher and the student: it also contains deviations arising from the teacher itself, which are consequently mixed into the observed teacher--student discrepancy and indiscriminately learned by standard OPD during training. This issue is further exacerbated by privileged OPD, where privileged information induces larger teacher-side likelihood shifts, thereby encouraging the student to learn more of the teacher's own deviation. We introduce \textbf{Calibrated On-Policy Distillation (Cal-OPD)}, which estimates the teacher's self-deviation region through positive and negative privileged interventions and calibrates the original teacher--student discrepancy by retaining only the component that lies beyond this region. Experiments on mathematical reasoning benchmarks show that, while retaining only about 52--65\% of the original teacher--student discrepancy as the optimization signal, Cal-OPD consistently outperforms standard OPD and its variants across model scales.

</details>


### [14] [Dual-Interest Sequential Product Recommendation With Multi-Granular SSM](https://arxiv.org/abs/2609.21548)
*Liao, Shuiying,Mok, P. Y.*

Main category: cs.AI

TL;DR: DSRec是一个双兴趣交叉SSM模型，通过长期Mamba和短期时间调制SSM分别建模用户稳定偏好与会话意图，并利用残差交叉融合实现跨粒度对齐，在公开基准上优于现有方法，为高效序列推荐提供了新思路。


<details>
  <summary>Details</summary>
Motivation: 现有序列推荐方法（包括Transformer和多数SSM方法）将每个物品编码为单一静态上下文角色，忽略物品多义性（同一物品在不同用户上下文中有不同语义角色），且难以跨不同时间粒度捕获动态行为，同时Transformer方法面临成本效率问题。

Method: DSRec输入为用户历史交互序列（物品ID序列）及对应的点击时间间隔。模型包含两个并行的兴趣编码分支：长期兴趣分支通过历史聚合将序列物品编码为长期兴趣嵌入，捕获稳定偏好；短期兴趣分支则强调局部会话意图，并由相邻点击的时间间隔进行调制。两个分支分别送入不同的SSM编码器：长期分支使用full-sequence Mamba进行长程序列建模；短期分支使用时间调制SSM，根据时间间隔动态调整状态转移矩阵，使状态演化对时间间隔敏感。为融合跨粒度信息，模型采用残差交叉融合机制，在两个分支之间交换上下文表示，同时通过残差连接保留语义独立性，避免相互干扰。最终基于融合后的双兴趣表示预测下一个交互物品。训练目标为序列推荐中常用的下一物品预测损失（具体损失函数未在摘要中披露）。推理时，模型可直接根据用户历史序列和实时时间间隔计算推荐分数，无需额外复杂模块，保持了SSM的高效推理特性。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 提出双兴趣分支结构，分别建模长期稳定偏好和短期会话意图，并解耦物品在不同语义上下文中的角色；长期分支使用全序列Mamba进行长程建模，短期分支使用时间调制SSM，根据点击时间间隔动态调整状态演化；设计残差交叉融合机制，在两个分支间交换上下文信息，同时保持语义独立性，实现跨粒度对齐

Abstract: Sequential recommendation aims to predict the next item a user will interact with based on their historical behavior. Advances in Transformers have significantly improved sequential recommendation but are still limited by cost efficiency. Although State Space Models (SSMs) have recently enabled efficient long-range modeling, most existing methods encode each item with a single static contextual role, overlooking the phenomenon of item polysemy. In fact, the same item often plays different semantic roles depending on user context, and existing methods are limited in capturing dynamic behavior across different temporal granularities. In this work, we propose DSRec, a novel dual-interest cross-SSM model that explicitly disentangles item roles across long-term and short-term semantic context. Sequential items are encoded into long-term interest embeddings that capture stable preferences via historical aggregation, and a short-term interest branch that emphasizes local session intent modulated by inter-click time intervals. These interest embeddings are processed through distinct SSM encoders: a full-sequence Mamba for long-term modeling, and a time-modulated SSM that dynamically adjusts state evolution based on temporal gaps. To enable effective cross-granularity alignment, we adopt a residual cross-fusion mechanism that exchanges contextual information between the two branches while preserving semantic independence. Experiments on public benchmarks demonstrate that DSRec outperforms other state-of-the-art methods.

</details>


### [15] [LogicTrack: Auditing Reasoning Trajectories of Large Language Models with Formal Logic Solvers](https://arxiv.org/abs/2609.21492)
*Hu, Jingyu,Yang, Shu,Liu, Weiru,Wang, Di*

Main category: cs.AI

TL;DR: LogicTrack是一个神经符号框架，通过自动形式化和定理证明器验证LLM推理步骤的逻辑有效性，并利用求解器驱动的回溯奖励（SBR）引导推理时树搜索，进而构造SFT数据使模型内化逐步审计。在8个推理基准和7个LLM上实验，提升了推理链的可验证性和最终答案通过率。


<details>
  <summary>Details</summary>
Motivation: 现有链式思维（CoT）优化方法主要依赖结果反馈，中间推理步骤的逻辑有效性未被验证，导致模型可能通过逻辑有缺陷的推理链得到正确答案，在高风险领域缺乏可信度。

Method: LogicTrack是一个神经符号框架，用于审计LLM的推理轨迹。输入为LLM生成的链式思维（CoT）轨迹。核心模块包括：(1) 自动形式化模块：将每个推理步骤转换为符号表示（如逻辑公式）；(2) 符号验证模块：使用自动定理证明器（ATP）检查形式化步骤相对于前提的逻辑有效性；(3) 求解器驱动的回溯奖励（SBR）：根据步骤的逻辑健全性计算逐步奖励，该奖励用于引导推理时的回溯树搜索，以探索更可靠的推理路径；(4) SFT数据构造：从LogicTrack生成的轨迹中提取回溯轨迹，构造监督微调数据，训练模型内化逐步审计能力。训练目标包括：SFT阶段使模型学习生成逻辑有效的推理步骤并具备回溯能力；推理时使用SBR引导搜索。推理流程：LLM生成步骤 -> 自动形式化 -> ATP验证 -> SBR评分 -> 根据评分决定是否回溯并探索替代步骤。该架构旨在将逻辑验证与LLM推理结合，提升推理链的可验证性和最终答案通过率。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: LogicTrack：神经符号框架，自动将每个推理步骤形式化为符号表示，并用自动定理证明器验证逻辑有效性；Solver-Based Backtracking Reward (SBR)：逐步评分机制，量化逻辑可靠性并引导推理时的回溯树搜索；利用LogicTrack生成的回溯轨迹构造SFT数据，使微调后的模型内化逐步审计能力

Abstract: Chain-of-Thought (CoT) reasoning has been shown to improve the performance of large language models (LLMs), yet existing optimization methods largely rely on outcome-based feedback, leaving the logical validity of intermediate reasoning steps largely unverified. To address the gap whereby LLMs arrive at correct final answers through logically flawed intermediate reasoning chains, we propose LogicTrack, a neuro-symbolic framework that audits reasoning trajectories by auto-formalizing each reasoning step into symbolic representations and verifying it with automated theorem provers. LogicTrack introduces Solver-Based Backtracking Reward (SBR), a step-wise scoring mechanism that quantifies logical soundness and guides backtracking tree search at inference time. We further extend LogicTrack to construct supervised fine-tuning (SFT) data with backtracking traces from its trajectories, enabling fine-tuned models to internalize step-wise auditing as an intrinsic capability. Extensive experiments across 8 reasoning benchmarks and 7 LLMs demonstrate that LogicTrack effectively improves both the verifiability of reasoning chains and final answer pass rate, thereby enhancing overall CoT quality and trustworthiness in high-stakes domains.

</details>


### [16] [GVPO++: Group Variance Policy Optimization for LLM Post-Training and On-Policy Distillation](https://arxiv.org/abs/2609.21432)
*Zhang, Kaichen,Hong, Yuzhong,Bao, Junwei,Jiang, Hongfei,Song, Yang,Hong, Dingqian,Xiong, Hui*

Main category: cs.AI

TL;DR: GVPO++提出一种新的LLM后训练方法GVPO，通过将KL约束奖励最大化的解析解融入梯度加权，解决了重要性采样导致的训练不稳定问题。方法具有唯一最优解保证，无需重要性采样，并可扩展至在线策略蒸馏。


<details>
  <summary>Details</summary>
Motivation: 现有后训练方法（如GRPO）因依赖重要性采样导致训练不稳定，难以实际部署。

Method: GVPO的核心架构是将KL约束奖励最大化的解析解集成到梯度加权方案中。具体地，该方法推导出梯度对应于隐式奖励的中心距离与实际奖励中心距离之间的均方误差。训练时，无需重要性采样即可从任意采样分布中估计梯度，从而避免了因重要性采样带来的高方差和训练不稳定。GVPO保证存在唯一最优解，精确对应原始KL约束奖励最大化目标。此外，该框架可自然扩展到在线策略蒸馏（OPD），并允许优化一类扩展的OPD目标，为多样化目标设计提供理论基础。推理路径与标准LLM一致，未涉及额外推理开销。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 将KL约束奖励最大化的解析解融入梯度加权，提出GVPO方法；梯度等价于隐式奖励中心距离与实际奖励中心距离之间的均方误差；保证唯一最优解且无需重要性采样，支持灵活采样分布；自然扩展到在线策略蒸馏，并支持优化广义OPD目标族

Abstract: Post-training plays a pivotal role in enhancing the reasoning capabilities and task-specific expertise of large language models (LLMs). Despite recent advances in post-training methods, such as Group Relative Policy Optimization (GRPO), their practical deployment remains impeded by training instability arising from the reliance on importance sampling. We introduce Group Variance Policy Optimization (GVPO), a novel post-training method that integrates the analytical solution of KL-constrained reward maximization into its gradient weighting scheme. This formulation provides an intuitive interpretation: GVPO's gradient corresponds to the mean squared error between the central distance of implicit rewards and that of actual rewards. GVPO offers two key advantages: (1) it guarantees a unique optimal solution, exactly to the KL-constrained reward maximization objective, and (2) it enables flexible sampling distributions without requiring importance sampling. Beyond general post-training, we show that GVPO naturally extends to on-policy distillation (OPD). Furthermore, GVPO enables the optimization of a broad family of extended OPD objectives, providing a principled foundation for diverse objective design. By unifying theoretical guarantees with practical adaptability, GVPO establishes a new paradigm for reliable and versatile LLM post-training and on-policy distillation.

</details>


### [17] [DENSE: Distilling Agent Trajectories into Evidence-Grounded Shortcut Trees for Self-Refinement](https://arxiv.org/abs/2609.21423)
*Liu, Siyuan,Yu, Fan,Ru, Dongyu,Liu, Yizhu,Yang, Yifan,Cao, Xuezhi,Cai, Xunliang,Cao, Yixin*

Main category: cs.AI

TL;DR: 提出DENSE，将智能体执行轨迹组织为证据接地的嵌套捷径树，在无外部结果监督下实现自我精炼；在Terminal-Bench 2.1上，借助REFIT评估，四个模型严格通过率相比初始尝试提升7.12-15.64个百分点，重试时token使用降低19.0-43.6%，并在困难任务多轮反馈中优于Self-reflection基线。


<details>
  <summary>Details</summary>
Motivation: 在没有外部结果奖励或正确性标签的情况下，如何从智能体执行轨迹中提取有用经验并用于重试同一任务，以提升任务成功率并降低推理开销。

Method: DENSE的输入是智能体在任务执行过程中产生的原始轨迹（执行痕迹），不依赖事后奖励或正确性标签。首先，将轨迹按嵌套子任务执行结构进行组织，形成证据接地的嵌套捷径树：树节点代表子任务，边代表执行顺序或依赖关系；重复的尝试被压缩为捷径，已解决的子问题被标记为解除，有用的执行步骤和未完成需求被保留。训练或推理时，智能体在重试同一任务时参考该捷径树，从已解决的节点跳过重复探索，聚焦未完成需求以指导下一步行动。REFIT评估框架通过比较初始尝试与反馈后重试的严格成功率变化来衡量反馈效果。整体目标是在无外部监督下提取轨迹内证据，实现自我精炼和持续自我改进，同时减少冗余推理步骤以降低token消耗。由于摘要未披露具体模型架构细节，以上为基于摘要的概括。

Result: {"offline_gains": [{"metric": "严格通过率提升", "value": "7.12-15.64 百分点", "setting": "Terminal-Bench 2.1，四个智能体模型，与初始尝试相比"}, {"metric": "智能体token使用降低幅度", "value": "19.0-43.6%", "setting": "Terminal-Bench 2.1，四个智能体模型的重新尝试过程中"}, {"metric": "累计通过率", "value": "优于Self-reflection基线", "setting": "探索性困难任务扩展，四个模型，多轮反馈迭代后"}], "online_gains": []}

Conclusion: DENSE：将轨迹证据组织成证据接地的嵌套捷径树，压缩重复尝试、追踪已解决的问题，并保留有用步骤与未完成需求。；REFIT：一种评估框架，通过比较从初始尝试开始的单次任务成功率变化来衡量反馈对智能体重试的帮助效果。；在无外部结果监督的测试方法中，于Terminal-Bench 2.1上取得最高的严格通过率，并在多轮反馈迭代中优于Self-reflection基线。

Abstract: Online agent deployments produce abundant execution traces, while task-specific verification and expert annotation are costly to scale. This gap raises a question: without post-execution rewards or correctness labels, what useful experience can we extract from the trajectories themselves? To efficiently use this information, we introduce DENSE (Distilling Evidence from Nested Subtask Executions), which organizes trajectory evidence into evidence-grounded nested shortcut trees. DENSE condenses repeated attempts, tracks resolved problems, and preserves useful steps alongside unfinished requirements. To evaluate how feedback helps agents retry the same task, we introduce REFIT, which compares changes in task success rates from the initial attempt. Among tested methods without external outcome supervision, DENSE achieves the highest strict pass rate across four agent models on Terminal-Bench 2.1. Compared with the initial attempts, strict pass rate increases by 7.12-15.64 percentage points, with 19.0-43.6% lower observed agent token use during the new attempts. In an exploratory hard-task extension, DENSE also outperforms the Self-reflection baseline in cumulative pass rate after multiple feedback iterations across all four models. These findings suggest DENSE's potential for continual agent self-improvement without external annotations or post-execution outcome supervision.

</details>


### [18] [Offline Multimodal Large Language Models for Decision Support in Air Operations](https://arxiv.org/abs/2609.21390)
*Dantas, Joao P. A.,Cunha, Jelton A.,Dietzsch, Gabriel*

Main category: cs.AI

TL;DR: 针对航空作战无网络环境，提出离线多模态RAG决策支持系统，可处理文本和图像输入并保持答案溯源。初步试验显示系统在 doctrinal 知识测试中匹配人类得分（8/10），且任务时间从26.5分钟缩短至7.1分钟。


<details>
  <summary>Details</summary>
Motivation: 如何在无互联网连接、严格安全约束的边缘环境中，为航空作战分析人员提供可追溯至原始来源的多模态 doctrinal 知识检索与决策支持，并评估其对任务效率和认知负荷的影响。

Method: 论文提出一个模块化检索增强架构，适用于无互联网连接的操作环境。系统接受来自技术手册的文本和图像输入。架构包含检索模块，基于 doctrinal 知识库进行检索，并将检索结果作为上下文输入大语言模型生成答案。答案保持可追溯至原始来源。系统在隔离环境中部署，不依赖外部计算资源。具体模块细节（如嵌入模型、向量数据库、多模态融合方式、LLM 具体型号、推理硬件）在摘要中未明确披露。

Result: {"offline_gains": [{"metric": " doctrinal 知识评估得分", "value": "8/10（系统） vs 8/10（人工）", "setting": "基于电子目标识别 doctrine 的同一测试，4名巴西空军图像分析师参与"}, {"metric": "任务完成时间", "value": "7.1分钟（系统） vs 26.5分钟（人工平均）", "setting": "同上评估任务"}, {"metric": "心理需求评分", "value": "6.0/7", "setting": "人工手动生成 REMIR 报告的认知负荷测量（NASA-TLX 可能）"}, {"metric": "努力程度评分", "value": "5.0/7", "setting": "人工手动生成 REMIR 报告的认知负荷测量"}], "online_gains": []}

Conclusion: 面向离线无网络环境的多模态RAG架构，支持文本和图像输入，并保持答案溯源至原始文档。；结合军事 doctrinal 知识评估与认知负荷测量的混合评估方法，对比人工与系统在电子目标识别任务上的表现。；提出未来系统化比较人工与AI辅助工作流的评估协议。

Abstract: Air operations rely on complex rules, established procedures, and time-critical analysis under limited connectivity and strict security constraints. In such environments, analysts must combine written doctrine with images, often without access to external computing resources. This paper studies offline large language models as decision support tools, deployed in isolated and restricted environments to give analysts access to doctrinal knowledge that remains traceable to its original sources through natural language interaction. We describe a modular retrieval-augmented architecture suitable for operation without Internet connectivity, supporting both text and image input from technical manuals. As a first step toward evaluating this architecture, we report a pilot study with four image analysts of the Brazilian Air Force, combining (i) a doctrinal knowledge assessment based on their electronic-target identification doctrine, comparing human and proposed system performance on the same test, and (ii) a measurement of the cognitive workload involved in manually producing a reconnaissance target report (Relat\'orio de Miss\~ao de Reconhecimento - REMIR) without AI assistance. The results show a demanding manual task, especially in terms of mental demand (6.0/7) and effort (5.0/7), while the proposed system matches the human score (8/10) and completes the assessment in 7.1 minutes (compared to a human average of 26.5 minutes), establishing a baseline for future AI-assisted evaluation. Finally, we describe a future evaluation protocol to systematically compare manual and AI-assisted workflows.

</details>


### [19] [Efficient Benchmarking in Production: A Study of an Evolving LLM Agent](https://arxiv.org/abs/2609.21267)
*She, Yining,Lin, Lei*

Main category: cs.AI

TL;DR: 论文基于生产分析型LLM智能体的574次历史基准测试运行，研究高效周期性评估策略。比较随机采样、历史缓存、固定子集和IRT自适应测试，发现多维2PL自适应测试保真度最佳（200题即38.5%运行达到1.03 pp MAE），但实际部署了难度分层固定子集，因其操作简单、跨五个智能体家族无需重校准、且一天校准窗口即稳定。


<details>
  <summary>Details</summary>
Motivation: 生产LLM智能体在持续演进时，完整基准测试重跑成本过高，如何高效地进行周期性评估并保证评分保真度。

Method: 论文未提出新的模型架构，而是研究评估流程架构。输入为生产分析型LLM智能体的历史基准测试运行记录：共574次完整运行，按时间顺序划分为校准期和留出期。评估方法包括：随机采样（从完整题库中随机选取子集）、历史缓存（复用历史问题结果）、固定代表性子集（基于校准期选择的固定问题集）和IRT自适应测试（基于项目反应理论的多维2PL模型，根据智能体作答动态选择下一问题）。评估目标是在减少问题数量的同时保持与完整基准测试评分的高保真度（以MAE衡量）。推理/评估流程：对于每个待评估的智能体版本，使用选定的子集策略执行有限数量的问题，然后基于子集评分估计完整评分。论文报告了不同策略在留出期上的MAE，并最终部署难度分层固定子集，因其操作简单且在校准窗口短至一天时仍稳定，且可无需重新校准迁移到其他智能体家族。

Result: {"offline_gains": [{"metric": "MAE", "value": "1.03 pp", "setting": "多维2PL自适应测试，执行200个问题（完整运行的38.5%）"}, {"metric": "训练/校准窗口稳定性", "value": "短至1天", "setting": "难度分层固定子集在不同校准窗口下的稳定性验证"}, {"metric": "跨智能体家族迁移", "value": "无需重新校准", "setting": "固定子集迁移到其他五个智能体家族"}], "online_gains": [{"metric": "部署采用", "value": "已部署", "setting": "难度分层固定子集作为周期性评估方案，用于服务数万月活用户的生产分析型智能体"}]}

Conclusion: 基于574次历史生产运行数据，按时间顺序划分校准期与留出期，系统比较随机采样、历史缓存、固定代表性子集和IRT自适应测试四种评估策略。；发现多维2PL自适应测试评分保真度最佳：仅执行200个问题（完整运行的38.5%）即可达到1.03个百分点的MAE。；尽管自适应测试效果最好，仍因操作简单性部署了难度分层固定子集，并验证其无需重新校准即可迁移到其他五个智能体家族。；验证固定子集在短至一天的校准窗口下依然稳定。

Abstract: Production LLM agents are evaluated repeatedly as they evolve, but full agent benchmarks are costly to rerun. We study efficient recurring evaluation for a production analytics agent serving tens of thousands of monthly active users and report first-hand deployment experience. Using 574 historical runs of the production benchmark, split chronologically into calibration and held-out periods, we compare random sampling, historical caching, fixed representative subsets, and IRT-based adaptive testing. The results show that multidimensional 2PL adaptive testing achieves the best overall score fidelity: executing 200 questions, 38.5% of a full run, yields 1.03 pp of MAE. We nevertheless deployed difficulty-stratified fixed subsets because of their operational simplicity, and show they transfer without recalibration to five other agent families and remain stable across calibration windows as short as one day. Drawing on this deployment experience, we report practical recommendations for recurring production-agent evaluation.

</details>


### [20] [Information-Gain Rewards over Diversity-Pruned Tests: GT-Anchored Verifier Co-Training for Reliable Code Generation](https://arxiv.org/abs/2609.21208)
*Nunez, Ana,Najafirad, Peyman*

Main category: cs.AI

TL;DR: CoVer 用单策略 GRPO 共训练 coder 与测试作者：以 ground-truth 锚定的信息增益奖励加协方差符号门控筛选有判别力的自生成测试，并用三阶段多样性剪枝控制采样集中偏差。在 7B/14B 上相对 Qwen2.5-Instruct 提升 one-shot pass@1 +5.8/+7.1 点，且作为 CodeT 排序 backbone 再涨 +3.5 点。


<details>
  <summary>Details</summary>
Motivation: 自博弈式代码生成 RL 中测试作者与代码生成器共同训练时出现的两类失效：permissiveness collapse（通过率奖励被平凡、无判别力的测试刷高）与 concentration bias（i.i.d. 采样测试集中在众数输入上导致估计方差膨胀），从而无法可靠地替代固定测试集。

Method: CoVer 是基于单策略 GRPO 的共训练框架，同一语言模型策略同时输出代码解与自生成测试。训练流程：对每个问题，策略采样代码与候选测试，测试在候选解上执行产生通过/失败向量；同时构造一个经 ground-truth 锚定的分级正确性信号 y∈[0,1]^m（由 ground truth 测试或标注分级给出）。信息增益奖励计算该通过/失败向量与分级正确性信号 y 之间的互信息，并用两者协方差符号作为门控，只有当测试对正确性具有正向判别力时才给予奖励，从而抑制 permissiveness collapse。为缓解 concentration bias，候选测试池先经三阶段多样性感知剪枝：无效性（invalidity）过滤剔除无法运行或恒定的测试，输入字符串（input-string）过滤去除重复输入，执行剖面（execution-profile）过滤去除执行行为冗余的测试，得到行为非冗余套件，在固定执行预算下提高 IG 估计的有效样本量。策略用 GRPO 以该 IG 奖励（对测试作者）以及通过率类奖励（对 coder）联合更新，形成 coder 与 verifier 相互促进的闭环。推理时，CoVer-7B 既可作为生成模型直接输出代码，也可作为 CodeT 排序管线的 backbone 对候选解打分重排，实现生成与选择双重收益。

Result: {"offline_gains": [{"metric": "one-shot pass@1", "value": "+5.8 points", "setting": "7B 模型，相对 Qwen2.5-Instruct 基线，五个基准（LiveBench、MBPP、LiveCodeBench、CodeContests、CodeForces）"}, {"metric": "one-shot pass@1", "value": "+7.1 points", "setting": "14B 模型，相对 Qwen2.5-Instruct 基线，五个基准"}, {"metric": "macro-average", "value": "所有对比方法中最高", "setting": "7B 与 14B 两个规模"}, {"metric": "CodeT 排序管线指标", "value": "+3.5 points", "setting": "CoVer-7B 作为 CodeT 排序管线即插即用 backbone"}], "online_gains": []}

Conclusion: 信息增益（IG）奖励：用自生成测试的通过/失败向量与经 ground-truth 锚定的分级正确性信号 y∈[0,1]^m 之间的互信息来打分，并用协方差符号门控，只奖励正判别性测试；三阶段多样性感知选择：对候选池做 invalidity、input-string、execution-profile 过滤，剪枝为行为上非冗余的测试套件，在固定执行预算下提高 IG 估计器的有效样本量；单策略 GRPO 框架 CoVer：同一策略同时充当 coder 与 verifier/test author，共训练生成与选择两个用途；作为 CodeT 排序管线的即插即用 backbone，验证共训练对生成与选择双重收益

Abstract: Self-play methods that co-train a single language model as both coder and test author promise to move code-generation RL beyond fixed test suites, but they suffer from two coupled pathologies: permissiveness collapse, where pass-rate rewards are maximised by trivial, non-discriminative tests, and concentration bias, where i.i.d. sampled tests cluster on modal inputs and inflate estimator variance. We introduce CoVer (Co-trained Coder and Verifier), a single-policy GRPO framework that addresses both failure modes. First, an information-gain (IG) reward scores each self-generated test by the mutual information between its pass/fail vector and a graded, ground-truth-anchored correctness signal y [0, 1] m, gated by the sign of their covariance so that only positively discriminative tests receive reward. Second, a three-stage diversity-aware selection step prunes a candidate pool to a behaviourally non-redundant suite (invalidity, input-string, execution-profile filtering), raising the effective sample size of the IG estimator at fixed execution budget. On five benchmarks (LiveBench, MBPP, LiveCodeBench, CodeContests, Code-Forces), CoVer raises one-shot pass@1 by +5.8 points at 7B and +7.1 points at 14B over the Qwen2.5-Instruct backbone, and achieves the highest macro-average among all compared methods at both scales. As a drop-in backbone inside the CodeT ranking pipeline, CoVer-7B adds +3.5 points, demonstrating the dual benefit of co-training for both generation and selection.

</details>


### [21] [TinyCeNN-LM: Quality-Gated Conversion of Pretrained Attention with CeNN-Inspired Cellular-Recurrent Layers](https://arxiv.org/abs/2609.21139)
*Mohsenzadegan, Kabeh,Tavakkoli, Vahid,Kyamakya, Kyandoghere*

Main category: cs.AI

TL;DR: TinyCeNN-LM提出了一种质量门控的训练后转换框架，用受CeNN启发的细胞循环层替换预训练LLM中的注意力，并在小型模型上报告了NLL变化、KV缓存压缩及下游准确率。


<details>
  <summary>Details</summary>
Motivation: 在不破坏后续层所期望表示的前提下，将预训练语言模型中的注意力替换为替代的循环/局部处理结构。

Method: 该框架在训练后将预训练语言模型中的注意力层替换为受CeNN启发的细胞循环层。候选层包含有界局部处理、紧凑循环记忆、路由与融合模块。执行逐层转换，候选层只有在满足固定的表示保真度和NLL阈值时才会被接受，否则通过接受或回滚机制回滚。评估了三种实现：Integrated Memory、MemoryFusion以及PDelta3-GDN2-CLVR+Local32。在SmolLM2-135M上，第0-2层通过严格PDelta3准则被接受，累积ΔNLL为+0.01209；第3层尽管NLL可接受，但因表示保真度不达标而被拒绝。在Qwen3.5-0.8B上，全注意力层3、7和11被接受，最终ΔNLL为+0.02073。报告了Integrated Memory变体的困惑度和缓存减少情况。200项下游健全性检查给出了转换后Qwen版本的准确率。

Result: {"offline_gains": [{"metric": "累积ΔNLL", "value": "+0.01209（第0-2层接受）", "setting": "SmolLM2-135M，训练后转换"}, {"metric": "最终ΔNLL", "value": "+0.02073（第3、7、11层接受）", "setting": "Qwen3.5-0.8B，全注意力层转换"}, {"metric": "困惑度变化", "value": "-0.07% 至 +0.93%", "setting": "Integrated Memory变体，相对于原始模型"}, {"metric": "总缓存减少", "value": "最高6.01%", "setting": "Integrated Memory变体"}, {"metric": "下游准确率", "value": "28.5%–32.0%", "setting": "转换后的Qwen版本，200项抽样健全性检查"}], "online_gains": []}

Conclusion: 质量门控的训练后转换框架，采用逐层接受或回滚；受CeNN启发的细胞循环层，具有有界局部处理、紧凑循环记忆、路由与融合；三种实现：Integrated Memory、MemoryFusion、PDelta3-GDN2-CLVR+Local32；严格的PDelta3接受准则，要求同时满足表示保真度与NLL阈值

Abstract: Replacing attention in a pretrained language model is a compatibility problem: a plausible substitute may alter representations expected by later layers. TinyCeNN-LM introduces a \emph{quality-gated post-training conversion} framework using CeNN-inspired cellular-recurrent layers with bounded local processing, compact recurrent memory, routing, fusion, and accept-or-rollback validation. Three implementations are studied: Integrated Memory, MemoryFusion, and PDelta3-GDN2-CLVR+Local32. Strict PDelta3 conversion accepts a layer only when representation and NLL criteria pass fixed thresholds. On SmolLM2-135M, layers 0-2 are accepted with cumulative $\Delta\mathrm{NLL}=+0.01209$, while layer 3 is rejected despite acceptable NLL because representation fidelity fails. On Qwen3.5-0.8B, full-attention layers 3, 7, and 11 are accepted with final $\Delta\mathrm{NLL}=+0.02073$. Integrated Memory keeps perplexity within $-0.07\%$ to $+0.93\%$ while reducing total cache by up to $6.01\%$. A sampled 200-item downstream sanity check gives $28.5\%$--$32.0\%$ overall accuracy for converted Qwen releases. The results support conservative, quality-gated structural conversion rather than universal attention replacement or speedup.

</details>


### [22] [Decoupling Internal Representational Changes and Causal Importance in Fine-Tuned Large Language Models](https://arxiv.org/abs/2609.21113)
*Li, Lingfang,Sen, Procheta,Das, Shubham,Bollegala, Danushka*

Main category: cs.AI

TL;DR: 该论文实证分析了LLM微调对内部表示和任务相关组件（EAP识别）的影响，发现组件功能局部化且与表示变化层不相关，跨任务组件重叠不保证性能迁移，甚至可能因任务性质差异导致退化。


<details>
  <summary>Details</summary>
Motivation: 微调如何重塑LLM的内部表示，以及这些变化是否与EAP识别的任务相关组件（注意力头和logit级激活）相关联。

Method: 论文通过分析微调前后的LLM内部表示（包括注意力模式和分层激活）来研究微调的影响。使用EAP方法识别驱动任务性能的关键组件（注意力头和logit级激活），并检查这些组件在层间的分布。进一步比较EAP组件跨任务的重叠与跨任务性能迁移的关系。未披露具体模型架构、训练目标或推理流程细节。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 发现EAP识别的组件集中在特定层，表明功能局部化；EAP组件的层分布与表示变化最大的层基本不相关；跨任务EAP组件重叠不带来性能迁移；若任务类型差异大（分类vs生成），重叠甚至导致性能退化

Abstract: Fine-tuning has emerged as a widely adopted approach for adapting LLMs to a variety of downstream tasks. However, how it reshapes their internal mechanisms remains poorly understood. To address this, we investigate how fine-tuning alters internal representations in LLMs, including attention patterns and layer-wise activations, and examine whether these changes are linked to task-relevant components identified by EAP (e.g., attention heads and logit-level activations) that drive task performance. We find that EAP-identified components are concentrated within specific layers, indicating a degree of functional localisation in how models internalise task-specific behavior. Notably, the distribution of these components across layers is largely uncorrelated with the layers undergoing the most substantial representational changes during fine-tuning. Furthermore, we observe that overlap in EAP-identified components across tasks does not translate into cross-task performance transfer if the tasks are different in nature (e.g. classification vs. generative tasks). More specifically, fine-tuning on one task can lead to a degradation of performance on another when the two tasks exhibit a high degree of overlap in their EAP-identified components.

</details>


### [23] [Detecting Hallucination in LLMs: Tracing the Topological Signatures of Impaired Context Sharing](https://arxiv.org/abs/2609.21096)
*Jalilifard, Amir,Rocha, Anderson,Wong, Eric,Raimundo, Marcos Medeiros*

Main category: cs.AI

TL;DR: 论文通过分析注意力图的Forman-Ricci曲率，揭示幻觉与信息流拓扑异常（如自注意力过度、上下文扩散、信息过压缩）的关联，提出一种单次前向传播的幻觉检测方法，在多个LLM和基准上优于现有注意力基线和多次响应方法。


<details>
  <summary>Details</summary>
Motivation: 如何有效区分LLM生成的幻觉与非幻觉响应。

Method: 该方法以预训练LLM的注意力图为输入，首先对每个注意力头提取Forman-Ricci曲率，用以量化信息流的结构瓶颈。然后，结合半局部（单个注意力头内部）和全局（跨层/跨头）信息流特征，构建一个分类器或检测器。整个检测过程仅需一次前向传播（single-pass），无需生成多个响应。训练时可能使用标注的幻觉/非幻觉数据对，优化目标是最大化分类准确率。推理时，给定一个响应，计算其注意力图的拓扑特征并输出幻觉概率。关键发现：幻觉与最后一层中自注意力过度使用、上下文检索分散以及信息过压缩高度相关。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 利用Forman-Ricci曲率分析注意力图的信息流瓶颈，识别幻觉相关的拓扑结构。；提出捕获注意力头半局部和全局信息流特征的方法，实现单次前向传播的幻觉检测。；发现幻觉响应与自注意力过度依赖、上下文扩散检索及信息过压缩的强关联，特别是在最后一层Transformer中。

Abstract: In this work, we examine the topology of information flow patterns within attention graphs to effectively distinguish hallucinated from non-hallucinated responses. We analyze the Forman-Ricci curvature to identify structural patterns indicating information bottlenecks in attention graphs. We then introduce a method that captures both semi-local and global information-flow characteristics of attention heads associated with hallucinated responses. We evaluate our approach extensively across several LLMs and established benchmarks. Empirical results demonstrate that our proposed single-pass approach provides consistent improvements over existing attention-based and multi-response baselines across two hallucination-detection benchmarks, while achieving competitive performance across diverse LLM architectures. Further analysis reveals that impaired context sharing among tokens during causal generation is strongly associated with hallucination occurrences in LLMs. In particular, hallucinated responses are consistently characterized by an over-reliance on self-attention, diffused context retrieval from earlier tokens, or information over-squashing, especially in the final transformer layer.

</details>


### [24] [Attention-Aware Routing: Coupling Routing and Attention in MoEs](https://arxiv.org/abs/2609.20974)
*Kosmopoulou, Despoina,Tsetsilas, Anastasios,Georgiou, Efthymios,Karamanolakis, Giannis,Roy, Swastik,Potamianos, Alexandros*

Main category: cs.AI

TL;DR: 本文提出Attention-Aware Routing (AAR)，通过利用注意力权重的滑动窗口特征增强MoE路由器，仅训练路由参数并冻结基座Transformer。在OLMoE上GSM8K提升3.37个百分点，并揭示路由与注意力的耦合电路及深度敏感性，为MoE路由优化和LLM推理改进提供新视角。


<details>
  <summary>Details</summary>
Motivation: MoE语言模型中，路由器通常仅基于token的隐藏状态选择专家，利用的上下文信息有限，导致路由决策与模型实际上下文状态割裂，影响下游推理性能。

Method: AAR在标准MoE Transformer基础上，保持基座Transformer（包括注意力机制和前馈专家网络）完全冻结，仅训练路由参数。路由器输入不再仅依赖当前token的隐藏状态，而是额外引入一个滑动窗口内注意力权重提取的时序和频谱特征。这些特征被视为模型上下文状态的摘要，与隐藏状态解耦。路由决策基于增强特征选择并加权专家。训练时仅优化路由参数，实现路由作为唯一变量的隔离实验。推理时，路由器根据当前及历史注意力信息动态分配专家。论文还通过分析发现，第l层的路由变化会通过残差流传播，影响第l+1层的注意力汇强度，形成路由-注意力的耦合电路。

Result: {"offline_gains": [{"metric": "GSM8K accuracy", "value": "+3.37 pp", "setting": "OLMoE模型，与routing-only SFT基线对比"}], "online_gains": []}

Conclusion: 提出Attention-Aware Routing (AAR)，从注意力权重滑动窗口中提取时序和频谱特征，作为与隐藏状态解耦的上下文摘要，增强路由器输入。；保持基座Transformer完全冻结，仅训练路由参数，隔离路由作为唯一变量，实现可控的因果分析。；揭示路由与注意力形成耦合电路：第l层的路由改变经过残差流传播，放大第l+1层的注意力汇，重塑注意力而无需直接更新注意力机制。；发现AAR对深度敏感： indiscriminate应用会损害事实检索，而数学推理收益在深层网络持续，揭示检索-推理张力。；AAR减少长发散生成：错误答案变短，正确答案长度不变。

Abstract: In Mixture-of-Experts language models, the router typically selects and weights experts based on the token's hidden state, utilizing limited contextual information. We propose Attention-Aware Routing (AAR), which augments the router with temporal and spectral features extracted from a sliding window of attention weights that represent a summary of the model's contextual state, disentangled from the hidden state. Keeping the base transformer entirely frozen, we train only the routing parameters, isolating routing as the sole variable. AAR improves GSM8K by +3.37 pp over a routing-only SFT baseline on OLMoE. Beyond performance, we show that routing and attention form a coupled circuit: routing changes at layer l propagate through the residual stream to amplify attention sinks at layer l+1, reshaping attention without any direct update to the attention mechanism itself. Further, AAR reduces long diverging generation, with incorrect answers getting shorter, while correct answers remain unchanged in length. Finally, AAR is strongly depth-sensitive: applying it indiscriminately across layers can degrade factual retrieval, whereas mathematical reasoning gains persist when it is introduced deeper in the network. This sensitivity exposes a retrieval--reasoning tension across depth and makes layer-selective AAR a controlled probe of the routing-relevant information carried by attention at different layers.

</details>


### [25] [RBS-Attention: Radius-Bounded Sparse Prefill for Long-Context Large Language Models](https://arxiv.org/abs/2609.20971)
*Song, Chuxu,Wei, Jiuqi,Peng, Zhencan*

Main category: cs.AI

TL;DR: RBS-Attention 是一种面向长上下文 LLM 的免训练稀疏 prefill 方法，针对 block 稀疏选择中的均值稀释问题，设计质心基分支与基于最大 key-block radius 的 rescue 分支，独立阈值化后组合 mask，并保持规则 block-sparse FlashAttention 执行。在 H100 上对 Qwen3-30B-A3B-Instruct-2507-FP8 取得 20.65× standalone prefill-attention、11.92× vLLM prefill-attention、5.97× 端到端 TTFT 加速（128K），在 Qwen3-32B 上 RULER 总体准确率 88.65 对比稠密 89.52。


<details>
  <summary>Details</summary>
Motivation: 长上下文大语言模型推理中，稠密自注意力在 prefill 阶段处理整个 prompt 的开销过高；现有稀疏 block 选择方法以 block 质心代表整体相关性，会因均值稀释而漏掉 block 内个别高相关 token，导致重要信息被低估。

Method: RBS-Attention 是一种免训练的稀疏 prefill 方法，在 prefill 阶段替代或增强稠密自注意力。输入为长上下文 prompt，经 LLM 的注意力层处理时，不再对全部 key block 计算稠密注意力，而是先进行 block 级稀疏选择。选择由两个互补分支构成：(1) centroid base branch 计算每个 key block 质心的平均相关性，用以捕捉整体相关 block，保留常规 block-sparse 选择行为；(2) rescue branch 使用最大 key-block radius 及其 prompt、layer、head 相关分布来识别那些质心相关性低但块内可能存在高相关 token 的 block，即可能因 mean dilution 被低估的 block。两个分支分别独立阈值化，生成各自 mask 后组合，以控制 rescue block 对最终注意力的贡献。最终 mask 保持规则的 block-sparse FlashAttention 执行模式，避免引入不规则计算。推理流程为：在 prefill 时先进行双分支 block 评分与阈值化，合并 mask，然后按 mask 执行 block-sparse FlashAttention 计算注意力输出，再进入后续层与自回归生成。论文未披露训练目标（方法训练免），也未见对模型权重或额外模块的训练。

Result: {"offline_gains": [{"metric": "standalone prefill-attention speedup", "value": "20.65×", "setting": "H100 GPUs, 128K context"}, {"metric": "vLLM prefill-attention speedup", "value": "11.92×", "setting": "H100 GPUs, 128K context, vLLM"}, {"metric": "end-to-end time-to-first-token (TTFT) speedup", "value": "5.97×", "setting": "H100 GPUs, 128K context, Qwen3-30B-A3B-Instruct-2507-FP8"}, {"metric": "RULER overall accuracy", "value": "88.65", "setting": "dense Qwen3-32B model, versus 89.52 for dense attention"}], "online_gains": []}

Conclusion: 提出并形式化 mean dilution 问题：block 质心平均相关性可能掩盖 block 内高相关 token。；提出 RBS-Attention：训练免的双分支稀疏 prefill 方法，包含 centroid base branch 与 rescue branch。；rescue branch 使用最大 key-block radius 及其 prompt、layer、head 相关分布，识别可能被低估的 block。；对两个分支独立阈值化并组合 mask，在保留规则 block-sparse FlashAttention 执行方式的同时控制 rescue block 的贡献。

Abstract: Long-context large language model inference is increasingly limited by prefill, where dense self-attention processes the entire prompt before generation begins. Sparse block selection can reduce this cost, but a block centroid may hide a highly relevant token among many irrelevant ones. We call this failure mode mean dilution and propose RBS-Attention, a training-free sparse-prefill method with two complementary selection branches. A centroid base branch captures average relevance, while a rescue branch uses the maximum key-block radius and its prompt-, layer-, and head-dependent distribution to identify blocks at risk of underestimation. Independently thresholding the two branches and combining their masks controls the contribution of rescue blocks while preserving regular block-sparse FlashAttention execution. On H100 GPUs, RBS-Attention achieves 20.65$\times$ standalone prefill-attention speedup, 11.92$\times$ vLLM prefill-attention speedup, and 5.97$\times$ end-to-end time-to-first-token speedup at 128K on Qwen3-30B-A3B-Instruct-2507-FP8. On the dense Qwen3-32B model, it obtains 88.65 overall RULER accuracy versus 89.52 for dense attention; LongBench-v2, InfiniteBench, and Video-MME provide additional quality evaluation. Supporting experiments measure actual retention, compare selectors at matched density, and characterize block-size, threshold, and memory behavior. Together, these results support radius-adaptive dual-branch selection as an effective approach to long-context prefill.

</details>


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [26] [DiaVLo: Diagnosing Behaviours of Vision-Language Models](https://arxiv.org/abs/2609.22008)
*Corti, Lorenzo,Yang, Jie*

Main category: cs.CL

TL;DR: DiaVLo是一个诊断视觉语言模型行为的框架，通过人工策展与VLM生成构建行为规格，并用因果估计识别关键概念。实验表明其行为标签与模型性能相关，能揭示对齐与错位模式，助力可靠部署。


<details>
  <summary>Details</summary>
Motivation: 如何系统性地识别视觉语言模型的行为规格（期望与不良行为），并定位导致这些行为的关键概念，以支持可靠部署。

Method: DiaVLo框架利用人工策展构建期望行为的规格，同时借助VLM的生成能力生成观察到的行为规格。通过比较两者，表面化潜在的错位。框架还包含因果估计模块，用于定量识别哪些概念对VLM行为的影响最大。在多种开源VLM上，于分类和生成两种条件下进行实验，结果显示行为标签与模型性能相关，并揭示了对齐与错位的行为模式以及VLM感知、组织和优先处理概念的规律。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 结合人工策展与VLM生成能力自动构建期望行为与观察行为的规格，以发现潜在错位；提供因果估计以识别对VLM行为影响最大的概念；在分类与生成两种设定下对多个开源VLM进行诊断评估，展示行为标签与模型性能的相关性

Abstract: Vision-language models (VLMs) rely on storing and transferring appropriate information across their sub-components. Verifying that the VLMs exhibit desired behaviours, while avoiding harmful ones, is central to their reliable deployment. Yet, methods that identify VLM behaviours remain scarce. We present DiaVLo, a diagnostic framework that leverages human curation and VLMs' generation capabilities to construct specifications of desired and observed VLM behaviours, surfacing potential misalignments. Beyond this, DiaVLo also provides causal estimates to identify the most influential concepts steering VLM behaviours. We evaluate DiaVLo on several open-source VLMs under both classification and generation conditions. Our experiments show that DiaVLo produces behaviour labels that correlate with model performance and provide context for measured performance. DiaVLo surfaced behaviours that are clearly aligned and misaligned, alongside patterns in how VLMs perceive, organise, and prioritise concepts.

</details>


### [27] [NemotronLabs VoiceChat: An Open Full-duplex Speech-to-Speech Model with Tool Calling Capabilities](https://arxiv.org/abs/2609.21967)
*Balam, Jagadeesh,Bartley, Travis,Casanova, Edresson,Chauhan, Sanjay,Chen, Chen,Chen, Zhehuai,Chen, Zijia,Ciannella, Francesco,Deng, Slyne,Desta, Mikyas,Dubey, Harishchandra,Essid, Slim,Ferchichi, Nourchene,Ginsburg, Boris,Fuenmayor, Mariana Graterol,Habibi, Negar,Hu, Kevin,Joseph, Anand,Karandikar, Viraj,Kim, Myungjong,Klimkov, Viacheslav,Narasimhan, Seelan Lakshmi,Lee, Lily,Li, Jason,Long, Eileen,Mahabaleshwarkar, Ameya,Malte, Aditya,Margolin, Adi,Meister, Sasha,Mendelev, Valentin,Olabiyi, Oluwatobi,Pasad, Ankita,Peng, Yifan,Rastorgueva, Elena,Ritchie, Jayda,Roche, Jason,Srihari, Nikhil,Su, Yuanhang,Suhara, Yoshi,Trinh, Viet Anh,Wang, Jinhan,Zelasko, Piotr,Wang, Hui,Meng, Puhui,Zhang, Chaosen,Liu, Yunsheng,Wang, Shawn,Li, Wenjing,He, Zhonglei*

Main category: cs.CL

TL;DR: NemotronLabs VoiceChat是一个开源的全双工语音到语音模型，原生支持工具调用。它集成了流式语音编码器、仅解码器语言模型、并行输出流（代理文本与结构化函数调用）、辅助RNN-T转录分支和流式TTS解码器。在Full-Duplex-Bench 1.0、1.5和3.0以及VoiceBench上进行了评估，展示了在暂停处理、中断恢复、工具选择等方面的性能。


<details>
  <summary>Details</summary>
Motivation: 如何在统一的流式架构中集成全双工语音交互、语音识别与生成、语言理解以及外部工具调用，同时保持自然对话所需的时序行为。

Method: NemotronLabs VoiceChat采用统一的流式架构，包含以下模块：流式语音编码器将输入音频编码为时序表示；仅解码器语言模型作为核心处理单元，接收编码后的语音表示并生成输出；语言模型驱动并行专用输出流，分别生成代理文本（用于对话）和结构化函数调用（用于工具调用）；辅助RNN-T分支对用户语音进行增量转录，提供实时文本转录；流式TTS解码器将模型生成的文本表示转换为语音输出。模型按流式方式逐帧处理输入，各模块协同工作以保持自然对话的时序行为，实现边听、边转录、边推理、边调用工具、边说话的全双工交互。训练目标包括语言建模、转录（RNN-T损失）和语音生成，但具体优化细节未在摘要中披露。推理时，模型实时接收用户语音，同时进行转录、生成响应文本和工具调用，并通过TTS流式输出语音，支持用户中断和反馈。

Result: {"offline_gains": [{"metric": "takeover rate", "value": "最低", "setting": "Full-Duplex-Bench 1.0 暂停处理，权重开放系统对比"}, {"metric": "takeover following user interruptions", "value": "100%", "setting": "Full-Duplex-Bench 1.0"}, {"metric": "post-interruption response-quality score", "value": "4.33/5", "setting": "Full-Duplex-Bench 1.0"}, {"metric": "resumes response after user backchannels", "value": "93%", "setting": "Full-Duplex-Bench 1.5"}, {"metric": "normalized average", "value": "55.1", "setting": "VoiceBench"}, {"metric": "tool-selection F1", "value": "82.5%", "setting": "Full-Duplex-Bench 3.0"}], "online_gains": []}

Conclusion: 统一的流式全双工语音到语音模型，集成流式语音编码器、仅解码器语言模型、并行专用输出流（代理文本与结构化函数调用）、辅助RNN-T转录分支和流式TTS解码器。；并行专用输出流设计使模型能够同时生成代理文本和结构化函数调用，支持工具调用能力。；辅助RNN-T分支实现增量用户转录，与主模型并行工作。

Abstract: We introduce NemotronLabs VoiceChat, an open full-duplex speech-to-speech model with native tool-calling capabilities. NemotronLabs VoiceChat combines a streaming speech encoder and decoder-only language model with parallel specialized output streams for agent text and structured function calls, an auxiliary RNN-T branch for incremental user transcription, and a streaming TTS decoder. This design enables the model to listen, transcribe, reason, invoke tools, and speak within a unified streaming architecture while preserving the temporal behavior required for natural conversation. On Full-Duplex-Bench 1.0, NemotronLabs VoiceChat achieves the lowest pause-handling takeover rates among evaluated open-weight systems, 100\% takeover following user interruptions, and a 4.33/5 post-interruption response-quality score. On Full-Duplex-Bench 1.5, it resumes its response after user backchannels in 93\% of cases. NemotronLabs VoiceChat obtains a 55.1 normalized average on VoiceBench and, on Full-Duplex-Bench 3.0 (FDB 3.0), achieves 82.5\% tool-selection F1, while argument accuracy and end-to-end tool execution remain areas for improvement. These results demonstrate that full-duplex interaction, speech recognition and generation, general language capabilities, and external tool use can be integrated in a single open speech-to-speech model without sacrificing real-time conversational behavior.

</details>


### [28] [When Steering Fails in Latent Reasoning: A Latent-to-Language Transition Gap](https://arxiv.org/abs/2609.21662)
*Huang, Gaoxiang,Qi, Lei*

Main category: cs.CL

TL;DR: 论文发现激活引导在潜在链式推理中效果远弱于显式CoT，即使用于引导的隐表示变化相当。通过任务信息可识别性、输出分布突变和双向控制实验，提出‘潜在到语言转换鸿沟’假说，指出转换接口是未来潜在引导方法设计和评估的关键目标。


<details>
  <summary>Details</summary>
Motivation: 激活引导在潜在链式推理（latent CoT）中对后续语言生成的控制效果显著弱于显式CoT，即使隐表示变化幅度相当，存在隐空间到语言生成的转换鸿沟。

Method: 论文未提出新的模型架构，而是对现有语言模型在潜在链式推理（latent CoT）与显式链式推理（explicit CoT）模式下进行激活引导实验。输入为任务提示，模型通过连续隐状态进行多步推理（latent CoT）或生成显式文本推理步骤（explicit CoT）。在推理过程中，对隐层激活施加引导向量，并测量其对后续语言生成输出的影响。通过探测任务信息在连续思维中的可识别性、分析输出分布在转换边界的突变、以及对比任务相关方向在两种模式下的双向控制强度，论证‘潜在到语言转换鸿沟’的存在。训练目标与常规语言模型一致，推理流程中增加了干预与测量步骤。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 发现并形式化定义‘潜在到语言转换鸿沟’问题；证明连续思维中任务信息仍可识别，排除信息丢失解释；观察到输出分布在该转换边界处发生突变；发现任务相关方向在潜在CoT中的双向控制弱于显式CoT

Abstract: Activation steering has become a widely used approach for controlling language models during explicit chain-of-thought (CoT) reasoning, motivating its extension to latent CoT. However, we find that steering continuous thoughts produces substantially weaker effects on subsequent language generation than steering explicit CoT, even when the hidden representations are moved by comparable amounts. We first show that task information remains identifiable in continuous thoughts. Hence, we hypothesize a \textbf{latent-to-language transition gap}, in which an intervention effect in latent space fails to transfer to language generation. Two further results support this hypothesis: the output distribution changes abruptly at the transition boundary, and task-related directions exert much weaker bidirectional control in latent CoT than in explicit CoT. These findings identify the transition interface as a central target for evaluating and designing future latent-steering methods.

</details>


### [29] [Hallucination-R1: Robustness-Oriented Paraphrase Generation for Factual Consistency](https://arxiv.org/abs/2609.21227)
*Yu, Wenhan,Wu, Wenxin,Wang, Hao,Sha, Lei*

Main category: cs.CL

TL;DR: 该论文提出HALLUCINATION-R1，一个两阶段优化的复述生成框架，用于生成语义忠实但能挑战下游QA模型事实一致性的复述，从而暴露模型在语义不变性下的幻觉。在三个数据集上验证了有效性，并表明生成数据可用于鲁棒性微调。


<details>
  <summary>Details</summary>
Motivation: 模型在语义等价的复述下出现事实不一致（复述诱导幻觉），而通用复述无法提供有效的鲁棒性监督信号。

Method: HALLUCINATION-R1是一个两阶段优化的复述生成框架。输入为事实问题及其原始表述，输出为语义忠实且能挑战下游QA模型鲁棒性的复述。第一阶段优化旨在稳定生成既保持语义又具有多样性的复述，避免接近复制的弱信号或过度多样破坏语义等价。第二阶段优化以揭示下游QA模型事实一致性退化为奖励信号，鼓励生成能诱使模型出错但语义不变的复述。最终使用生成的复述数据对问答模型进行轻量微调，提升其在复述变化下的鲁棒准确率。推理时模型生成复述，用于评估或对抗性训练下游QA模型。具体网络结构、训练数据、硬件资源未披露。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 形式化复述诱导幻觉设定：原始问题回答正确但语义等价复述下回答错误；提出两阶段优化框架：第一阶段稳定语义保真且多样的复述生成，第二阶段奖励能揭示下游QA模型事实一致性退化的复述；在SimpleQuestions、PopQA、TruthfulQA上验证一致性-多样性权衡，并跨模型家族暴露鲁棒性失败；轻量微调研究表明生成数据可提升复述变化下的鲁棒准确率

Abstract: Factual hallucination is commonly defined by incorrect factual outputs. We study a paraphrase-induced hallucination setting, where a model answers a factual question correctly in its original form but generates an incorrect answer under a semantically equivalent paraphrase. Such inconsistencies expose latent factual instability under semantic invariance. However, general-purpose paraphrases are often insufficient as robustness-oriented supervision: near-copy paraphrases provide weak signals, while overly diverse paraphrases may break semantic equivalence. In this paper, we propose HALLUCINATION-R1, a robustness-oriented paraphrase generation framework that learns to produce semantically faithful yet robustness-challenging paraphrases for factual consistency. Through two-stage optimization, it first stabilizes meaning-preserving and diverse paraphrasing, then rewards paraphrases that reveal factual consistency degradation in downstream QA models. Experiments on SimpleQuestions, PopQA, and TruthfulQA show that HALLUCINATION-R1 achieves a strong consistency--diversity trade-off and exposes robustness failures across multiple model families and datasets. Further analyses indicate that these failures are not reducible to surface-level artifacts or semantic drift, but reveal non-trivial factual instability under meaning-preserving variation. A lightweight fine-tuning study also shows that HALLUCINATION-R1-generated data improves robust accuracy under paraphrase variations, suggesting its utility for robustness-oriented training. Our code and models are publicly available at https://github.com/yuwenhan07/Hallucination-R1.

</details>


### [30] [An Interpretable Memory Decision Controller for LLM Agents Based on Three-Signal Complementarity: Decoupling Confidence and Consistency](https://arxiv.org/abs/2609.22043)
*Zhang, Yiming,Zhang, Jinghong,Zhao, Haoran,Ma, Yiren,Zhao, Chunlei*

Main category: cs.CL

TL;DR: 提出 MDL：一个零参数、白盒的记忆决策控制器，置于 RAG 检索与生成之间。通过融合相关性、可靠性和任务风险三信号（QR 正交子空间投影 + 元工作记忆信号）得到可解释的信任表征，显式解耦置信度与一致性，并引入风险反转与显式弃权。在多款主流 LLM 和开源数据集上，冲突记忆下的幻觉率降低约 56.04%，高风险场景接近零幻觉。每次决策仅约 0.14 ms，比嵌入检索快约 50 倍，比 LLM 自评估快 4-5 个数量级。


<details>
  <summary>Details</summary>
Motivation: Standard RAG blindly injects retrieved memories; when the memory store contains conflicting positions, this amplifies hallucinations. The paper addresses whether retrieved memories should be trusted and how to decide at low cost.

Method: MDL is inserted between the retrieval and generation stages of a RAG pipeline. The controller receives the retrieved memory candidates and encodes three complementary signals: relevance, reliability, and task risk. A QR-based orthogonal subspace projection is applied to fuse these signals into an interpretable decision representation. A meta-working-memory signal is also incorporated. The resulting representation quantifies the trustworthiness of retrieved memories. MDL then explicitly decouples confidence from consistency and applies risk inversion and explicit abstention. Because the entire controller relies purely on geometric operations, it has no trained parameters and can produce decisions in about 0.14 ms. The decision determines whether the retrieved memories are injected into the LLM prompt or whether the system abstains, thereby preventing memory-induced hallucinations.

Result: {"offline_gains": [{"metric": "hallucination rate reduction under conflicting memories", "value": "about 56.04% reduction in general scenarios", "setting": "evaluations on mainstream large language models and multiple open-source datasets"}, {"metric": "hallucination rate", "value": "approaches zero in high-risk scenarios", "setting": "high-risk scenario evaluation on mainstream large language models and multiple open-source datasets"}, {"metric": "decision latency", "value": "about 0.14 ms per decision", "setting": "controller overhead compared to embedding-retrieval and LLM self-evaluation"}, {"metric": "speedup vs embedding-retrieval step", "value": "roughly 50x faster", "setting": "decision latency comparison"}, {"metric": "speedup vs LLM self-evaluation call", "value": "four to five orders of magnitude faster", "setting": "decision latency comparison"}], "online_gains": []}

Conclusion: Memory Decision Layer (MDL): a zero-parameter decision controller between retrieval and generation；Three-signal complementary encoder fusing relevance, reliability, and task risk via QR-based orthogonal subspace projection plus a meta-working-memory signal；Explicit decoupling of confidence from consistency for interpretable trust quantification；Risk inversion and explicit abstention mechanism；White-box geometric operations only, no trained parameters, very low decision latency

Abstract: Memory systems for large language models have focused predominantly on efficient retrieval, whereas the decision of whether retrieved memories should be trusted has received comparatively little attention. When the memory store contains conflicting positions, standard retrieval-augmented generation (RAG) blindly injects memories and amplifies hallucinations: in models susceptible to memory injection, the RAG hallucination rate under conflicting memories is markedly higher than that of a memory-free baseline. Inspired by memory signaling mechanisms in the prefrontal cortex, we propose the Memory Decision Layer (MDL), a zero-parameter memory decision controller situated between the retrieval and generation stages. Its core is a three-signal complementary encoder that fuses relevance, reliability, and task risk through QR-based orthogonal subspace projection and a meta-working-memory signal into an interpretable decision representation that quantifies the trustworthiness of retrieved memories. Building on this encoder, MDL explicitly decouples confidence from consistency and introduces risk inversion and explicit abstention. Evaluations on mainstream large language models and multiple open-source datasets show that MDL reduces the hallucination rate under conflicting memories by about 56.04% in general scenarios and approaches zero hallucination in high-risk scenarios. The controller is fully white-box: it relies purely on geometric operations, requires no trained parameters, and adds only about 0.14 ms per decision -- roughly 50x faster than the embedding-retrieval step that precedes it and four to five orders of magnitude faster than an LLM self-evaluation call.

</details>


### [31] [Reusing Latent Speech Representations for Query-Conditioned Topic Localization in Transcripts](https://arxiv.org/abs/2609.21844)
*Freisinger, Steffen,Seeberger, Philipp,Ranzenberger, Thomas,Bocklet, Tobias,Riedhammer, Korbinian*

Main category: cs.CL

TL;DR: 论文提出查询条件化的主题定位任务，通过复用 ASR 编码器状态作为句子级表示并与文本嵌入融合，使轻量跨度定位器无需额外音频编码器即可利用语音信息，在公开数据集上取得一致增益，尤其在严格边界匹配下；增益在结构化语音上更明显，自发性语音上有限。


<details>
  <summary>Details</summary>
Motivation: 长转写文本作为下游 NLP 输入成本高且包含大量无关上下文；如何在给定主题标题查询时，准确预测转写文本中最好地回应该查询的句子跨度。

Method: 输入为长转写文本以及一个主题标题查询。系统首先使用 ASR 编码器处理音频，并复用其编码器状态作为句子级语音表示；同时计算文本嵌入。两类表示进行融合，形成句子级多模态表示。随后使用轻量级的 span locator，在融合表示上预测最符合查询的主题句子跨度。训练目标为跨度定位（边界匹配），推理时无需单独运行音频编码器，直接复用 ASR 已产生的编码器状态，降低额外计算开销。论文在两个公开数据集上评估，并在严格边界匹配标准下比较文本基线，跨数据集实验进一步分析结构化语音与自发性语音上的差异。

Result: {"offline_gains": [{"metric": "span localization 性能", "value": "consistent gains over text-only baselines, especially under strict boundary-matching criteria", "setting": "两个公开数据集上的实验，严格边界匹配标准"}, {"metric": "跨数据集增益", "value": "benefits strongest for structured or semi-structured speech; gains on spontaneous speech limited and mixed", "setting": "跨数据集实验"}], "online_gains": []}

Conclusion: 复用 ASR 编码器状态作为句子级表示，无需单独运行音频编码器即可引入语音信息；将 ASR 句子级表示与文本嵌入融合，使轻量 span locator 能够利用语音信息；在严格边界匹配标准下，验证融合语音信息对跨度定位的增益；跨数据集分析表明增益在结构化/半结构化语音上最强，在自发性语音上有限且混合

Abstract: Long transcripts are costly inputs for downstream NLP systems and often contain irrelevant context. We study query-conditioned topic localization: predicting the sentence span in a transcript that best addresses a topic-title query. To improve span localization, we reuse ASR encoder states as sentence-level representations and fuse them with textual embeddings. This lets lightweight span locators exploit speech information without running a separate audio encoder. Experiments on two public datasets show consistent gains over text-only baselines, especially under strict boundary-matching criteria. Cross-dataset experiments further indicate that the benefits are strongest for structured or semi-structured speech, while gains on spontaneous speech are limited and mixed.

</details>


### [32] [Evaluating In-Context Learning and Retrieval Strategies for Devanagari Post-OCR Correction](https://arxiv.org/abs/2609.21595)
*Bhandari, Abhishek,Harit, Gaurav*

Main category: cs.CL

TL;DR: 该论文首次系统评估了LLM在Devanagari OCR后校正中的ICL效果，提出CharBM25检索策略，通过字符n-gram BM25匹配OCR错误模式来检索示例。在20,000句基准上，CharBM25显著优于领域随机选择，且字符三元组最佳。研究发现检索策略是决定性因素，模型规模需8B以上才能可靠提升，Gemma-3-27B在印地语和马拉地语上分别降低WER 55.0%和33.3%。CharBM25无需GPU，计算成本极低。


<details>
  <summary>Details</summary>
Motivation: 在无训练微调条件下，如何利用大语言模型的上下文学习能力，通过检索合适的示例来提升Devanagari文字（印地语和马拉地语）的OCR后校正质量。

Method: 论文采用无训练的ICL框架进行OCR后校正。输入是包含OCR错误的句子。系统首先从训练集中检索相似示例，然后构建提示，将检索到的示例作为上下文输入给LLM，LLM输出校正后的句子。检索策略包括：领域随机选择、稠密语义检索和作者提出的CharBM25。CharBM25基于字符n-gram的BM25相似度检索示例，通过匹配共享的OCR错误模式（如特定字符混淆）来选择示例。实验中使用了字符三元组，并发现其效果最佳。LLM使用3B到32B的不同模型，包括Gemma-3-27B。评估在20,000句的基准上进行，涵盖五个新闻领域。主要发现是检索策略对校正质量影响巨大，CharBM25在印地语和马拉地语上均优于领域随机选择，且模型规模需达到8B以上才能可靠地超越OCR基线。

Result: {"offline_gains": [{"metric": "WER绝对降低", "value": "2.8-4.0pp", "setting": "印地语，CharBM25对比领域随机选择"}, {"metric": "WER绝对降低", "value": "2.9-3.8pp", "setting": "马拉地语，CharBM25对比领域随机选择"}, {"metric": "WER降低百分比", "value": "55.0%", "setting": "印地语，Gemma-3-27B，CharBM25-5"}, {"metric": "WER降低百分比", "value": "33.3%", "setting": "马拉地语，Gemma-3-27B，CharBM25-5"}], "online_gains": []}

Conclusion: 首次系统评估LLM在Devanagari OCR后校正中的ICL效果，覆盖3B-32B模型。；提出CharBM25检索策略：基于字符n-gram的BM25相似度检索示例，以匹配测试句的OCR错误模式。；发现检索策略是校正质量的决定性因素，CharBM25优于领域随机选择，且字符三元组普遍优于二元组和一元组。；揭示模型规模与ICL增益的关系：8B以下模型不可靠，3B在马拉地语上甚至导致更多句子退化。；构建并公开20,000句基准数据集，覆盖五个新闻领域。

Abstract: In-context learning using Large Language Models (LLMs) offers a compelling path to training-free post-OCR correction, yet its effectiveness for Devanagari script remains entirely unexplored. We present the first systematic evaluation of LLMs (3B-32B) for post-OCR correction in Hindi and Marathi, comparing three in-context example retrieval strategies: domain-random selection, dense semantic retrieval, and our proposed CharBM25, which retrieves examples by character n-gram BM25 similarity over OCR inputs to target shared error patterns with the test sentence. Across a 20,000-sentence benchmark spanning five news domains, retrieval strategy is the decisive factor in correction quality: CharBM25 outperforms domain-random selection by 2.8-4.0pp absolute WER on Hindi and 2.9-3.8pp on Marathi, using character trigrams, which consistently outperform bigrams and unigrams. Scale dominates performance: Gemma-3-27B achieves WER reductions of 55.0% for Hindi and 33.3% for Marathi under CharBM25-5. Few-shot gains are capacity-gated: models below 8B do not reliably improve over the OCR baseline, and on Marathi the smallest models (3B) degrade more sentences than they improve. Marathi is persistently harder to correct than Hindi across all scales, reflecting its greater morphological complexity. These findings establish CharBM25 as an effective, GPU-free retrieval strategy that matches or exceeds dense retrieval at negligible computational cost, and show that combining it with a general-purpose LLM of 12B+ parameters delivers reliable, training-free Devanagari post-OCR correction without task-specific fine-tuning. Dataset: https://huggingface.co/datasets/AbhishekBhandari/Devanagari-OCR-ICL-Benchmark

</details>


### [33] [MIRAGE: Multi-Perspective Creative Language Model Reasoning with Reinforcement Learning Guidance](https://arxiv.org/abs/2609.21554)
*Lagzian, Arash,Anumasa, Srinivas,Liu, Dianbo*

Main category: cs.CL

TL;DR: MIRAGE 提出一种无需训练的推理时多视角推理框架，通过 Selector 选择概念视角、Reasoner 顺序求解并聚合答案，在多个推理基准上以较低开销显著优于 Chain-of-Thought 等方法。


<details>
  <summary>Details</summary>
Motivation: LLM 在复杂数学、科学和逻辑推理任务上表现不足，需要一种能动态切换概念视角并在推理时整合多视角的框架来提升准确率。

Method: MIRAGE 是一个推理时框架，不修改基础 LLM 参数。其端到端流程为：给定任务输入，Selector 模块根据任务类型选择有效的概念视角（如代数、概率等），并将选定的视角指令与原始问题组合成提示；Reasoner 模块按顺序使用选定的视角尝试求解任务，每一步生成候选解答并评估置信度；若某个视角的求解达到置信阈值则直接输出该解答，否则继续切换至下一个视角，最终将多个视角的解答进行聚合以得到最终答案。该架构通过 Selector 与 Reasoner 的交替协作实现动态多视角推理，避免单一视角的局限，同时将推理开销控制在最小程度，以提升复杂推理任务的准确率。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 提出 MIRAGE 推理时框架，包含 Selector（选择有效概念视角，如代数、概率）和 Reasoner（顺序求解直到高置信解答出现）协同工作。；无需额外训练即可在推理时动态多视角探索，并通过聚合多个视角输出最终答案。；在 GSM8K、MATH500、MMLU-Pro、Game-of-24 上以最小推理开销显著超越 Chain-of-Thought 和多样提示集成。

Abstract: Recent advances in Large Language Models (LLMs) have revolutionized artificial intelligence and how human interact with AIs. Despite impressive advancements, LLMs struggle with complex mathematical, scientific, and logical tasks. Inspired by human cognitive flexibility - our ability to dynamically switch mental perspectives - we propose MIRAGE (Multi-perspective Inference-time Reasoning via Agent-Guided Exploration), a novel inference-time creative thinking framework. MIRAGE includes a Selector that prioritizes effective conceptual perspectives (e.g., algebraic, probabilistic) and a Reasoner that sequentially solves tasks until a confident solution emerges, otherwise aggregating multiple perspectives. Tested on GSM8K, MATH500, MMLU-Pro, and Game-of-24 benchmarks, MIRAGE consistently outperforms methods like Chain-of-Thought and diverse prompting ensembles, significantly boosting accuracy with minimal inference overhead, providing a scalable solution for practical applications.

</details>


### [34] [Omni Demand Understanding: A Benchmark for Contextual User-Intent Inference in Multimodal Interaction](https://arxiv.org/abs/2609.21392)
*Chen, Qi,Chu, Yunfei,He, Haolin,Yang, Yifan,Liu, Zihan,Wang, Yuxuan,Ma, Ziyang,Xu, Ruiyang,Gao, Meng,Yan, Yinsong,Wang, Ling,Wang, Hui,Huang, Wen,Chen, Yiheng,Yang, Guanrou,Kong, Qiuqiang,Xu, Jin,Chen, Xie*

Main category: cs.CL

TL;DR: 该论文提出Omni Demand Understanding (ODU)问题及配套基准ODU-Bench，用于评估多模态大模型从语音、视觉和对话历史中推断用户潜在需求的能力。评测显示14个先进MLLM存在系统性缺陷：最强模型仅恢复44.7%的关键推断信息，11个模型在非需求场景误触发率超50%。该基准对搜索/推荐系统的多模态用户意图理解有直接参考意义。


<details>
  <summary>Details</summary>
Motivation: 现有交互能力基准主要关注响应质量，而忽视了更根本的问题：模型能否从复杂的多模态交互中正确推断用户的潜在需求。真实需求在语音中常表达不充分，需结合多模态线索和对话历史推断，且受歧义、不流畅表达和噪声环境影响；同时，请求式语音不一定是需求，易导致误触发。

Method: 论文未提出新模型架构，而是构建了一个评测基准ODU-Bench。基准构建流程包括：1）基于挑战驱动分类法定义评测维度；2）使用智能体视频生成方法合成多模态交互数据；3）结合人类录制的真实交互；4）进行媒体接地标注和人工验证。评测任务设定为：给定一个交互流（可能包含语音、视觉和对话历史），模型需检测是否存在用户需求，并在存在时推断用户意图。评测涵盖单轮和多轮交互的五个维度。对14个原生MLLM的评测表明，最强模型仅能恢复44.7%的关键上下文推断信息，且多数模型在非需求场景下误触发率高。

Result: {"offline_gains": [{"metric": "关键信息恢复率", "value": "44.7%", "setting": "最强模型Gemini 3.1 Pro在需要从视觉、声学或对话上下文推断的关键信息上的恢复率"}, {"metric": "误触发率", "value": ">50%", "setting": "14个模型中有11个在非需求场景下的误触发率高于50%"}], "online_gains": []}

Conclusion: 将Omni Demand Understanding (ODU)定义为新的多模态上下文推断问题：判断交互流中是否存在用户需求并推断意图。；从五个维度（覆盖单轮和多轮交互）构建ODU-Bench基准。；采用挑战驱动分类法、分类法引导的智能体视频生成和人类录制交互，结合媒体接地标注与人工验证。；对14个原生多模态大模型进行系统评测，揭示当前模型在上下文需求推断上的系统性能力差距。

Abstract: Natural audio-visual interaction is emerging as an important interface for AI assistants, allowing users to communicate through speech and vision rather than carefully composed text prompts. However, existing benchmarks of interactive capabilities still focus primarily on response quality, leaving a more fundamental question underexplored: can a model correctly infer the user's underlying demand from complex multimodal interaction? Real-world user demands are often underspecified in speech and must be inferred from multimodal cues and dialogue history. This inference is further complicated by ambiguous or disfluent expression and noisy acoustic environments. Conversely, request-like speech may not constitute a demand to the assistant, leading to false triggers. We establish Omni Demand Understanding (ODU) as a distinct multimodal contextual inference problem: given an interaction stream, a model must detect whether a user demand is present and infer intent from multimodal and conversational context. ODU evaluates this capability along five dimensions, covering both single-turn and multi-turn interactions. We construct ODU-Bench using a challenge-driven taxonomy, taxonomy-guided agentic video generation, and human-recorded interactions, followed by media-grounded annotation and human verification. We evaluate 14 native MLLMs. Even the strongest, Gemini 3.1 Pro, recovers only 44.7% of key information that must be inferred from visual, acoustic, or conversational context. Moreover, 11 of the 14 models exhibit false-trigger rates above 50% on non-demand scenarios. These results reveal a systematic capability gap in current MLLMs' ability to infer contextual user demands. We hope ODU can establish the evaluation of a previously underexplored yet essential capability in multimodal interaction: correctly understanding user demands before generating an appropriate response.

</details>


### [35] [Prediction Dynamics in Depth-Recurrent Language Models](https://arxiv.org/abs/2609.21383)
*Luo, Xinyue,Yu, Fei*

Main category: cs.CL

TL;DR: 论文研究深度循环语言模型的预测动态，通过锐利边缘刻画将更新分解为共同平移、相对胜者方向与竞争者配对，并在 Huginn-3.5B 和 Ouro-1.4B 上将平均最早合格深度相对总深度进一步降低 22.5-34.4%。该工作为 LLM 推理中的自适应计算与提前退出提供了理论解释。


<details>
  <summary>Details</summary>
Motivation: 深度循环语言模型通过重复潜在更新精炼预测，但为何中间答案可能与最终答案一致而分数仍在变化？论文旨在刻画这种预测动态，并为有限深度下的答案保持与自适应计算提供理论解释。

Method: 论文分析的是深度循环语言模型（Depth-Recurrent Language Models），这类模型通过重复的潜在更新（latent updates）逐步精炼预测。模型在固定或可变深度上生成中间预测，最终输出端点答案。作者从理论上推导出一个锐利的边缘刻画（sharp margin characterization），将幅度边界（magnitude bound）的保守性分解为三个组成部分：共同平移（common translation）、相对胜者方向（direction relative to the winner）以及每个竞争者更新与其分数差距的配对（pairing of each competitor's update with its score gap）。在此基础上，作者进一步在共享预测分布的设置下，将预测分数的变化分解为正交的共同运动（common motion）和对比运动（contrast motion），并利用候选集质量（candidate-set mass）与集内集中度（within-set concentration）来表达共同分量。论文通过回顾性比较（retrospective comparison）在 Huginn-3.5B 和 Ouro-1.4B 的完整轨迹上验证这些理论分解，使用全答案文本评分和标签评分两种设置，度量去除不同分量后平均最早合格深度（mean earliest qualifying depth）的变化。该分析旨在解释有限深度下答案保持的机制，并为自适应深度选择或提前退出策略提供几何与组合层面的理解。

Result: {"offline_gains": [{"metric": "mean earliest qualifying depth reduction (relative to total depth)", "value": "22.5-34.4%", "setting": "Huginn-3.5B 与 Ouro-1.4B，全答案文本评分，在去除共同平移后进一步考虑更新方向与竞争者配对"}, {"metric": "substantial contributions under label scoring", "value": "未量化具体数值", "setting": "label scoring 条件"}], "online_gains": []}

Conclusion: 推导出锐利的保守性边界刻画，将幅度边界分解为共同平移、相对胜者方向以及每个竞争者更新与其分数差距的配对；在 Huginn-3.5B 与 Ouro-1.4B 上验证：在去除共同平移的基础上，进一步考虑更新方向与竞争者配对可将平均最早合格深度降低总深度的 22.5-34.4%（全答案文本评分）；在共享预测分布下，将共同运动与对比运动正交分离，并通过候选集质量和集内集中度表达共同分量；发现共同能量与对比能量可以以不同速率衰减，从而允许偏好变化份额增长与绝对更新收缩共存

Abstract: Depth-recurrent language models refine predictions through repeated latent updates. Why can intermediate answers agree with the endpoint while their scores continue to change? We derive a sharp margin characterization that decomposes the conservatism of a magnitude bound into common translation, direction relative to the winner, and the pairing of each competitor's update with its score gap. Across Huginn-3.5B and Ouro-1.4B, accounting for update direction and competitor pairing reduces the mean earliest qualifying depth by a further 22.5-34.4% of the total depth beyond translation removal under full answer-text scoring. This retrospective comparison uses completed trajectories. Substantial contributions also occur under label scoring. For shared predictive distributions, we separate common and contrast motion orthogonally and express the common component through candidate-set mass and within-set concentration. Common and contrast energies can attenuate at different rates, allowing a growing preference-change share to coexist with shrinking absolute updates. These findings explain finite-depth answer preservation through the geometry and composition of observed score changes.

</details>


### [36] [ArenaFlow: From Trajectory Ranking to Hierarchical Credit Propagation for Open-Ended Agent RL](https://arxiv.org/abs/2609.21378)
*Zhang, Qiang,Ding, Ruixue,Zhang, Fanrui,Chen, Xi,Chen, Boli,Wang, Shihang,Huang, Yinfeng,Zheng, Yi,Xie, Pengjun,Zhang, Kaipeng,Liu, Jiawei,Zha, Zheng-Jun*

Main category: cs.CL

TL;DR: ArenaFlow 提出一种用于开放式智能体强化学习的分层信用传播框架。它通过锦标赛式相对排序获得轨迹级奖励，并利用结构化反思评估提取关键成功步骤、可复用技能及技能使用归因；随后将轨迹优势按生存深度传播到步骤级，并基于群体使用归因维护全局技能记忆，实现技能的更新、剪枝与检索，从而为后续探索提供策略先验。实验验证了其在开放式智能体任务上的有效性，但未披露公司、线上部署或生产指标。


<details>
  <summary>Details</summary>
Motivation: Applying reinforcement learning to open-ended agent tasks is difficult because solutions are diverse and reliable scalar rewards are hard to obtain; existing pairwise evaluation methods compress rich comparative feedback into a single trajectory-level reward, obscuring decisive intermediate steps and preventing successful behaviors from being consolidated into reusable skills.

Method: ArenaFlow is a hierarchical credit propagation framework for open-ended agent reinforcement learning. It operates on trajectories generated by an LLM agent interacting with an environment. At the top level, a tournament-based relative ranking mechanism compares pairs of trajectories to produce trajectory-level reward signals, avoiding pointwise scalar reward collapse. Each comparison is accompanied by structured reflective evaluation that yields three supervision signals: pivotal success steps, reusable strategy skills, and usage attribution of retrieved skills. At the step level, the trajectory-level advantage is propagated to high-confidence pivotal steps according to tournament survival depth, enabling targeted optimization of local reasoning behaviors. At the skill level, skill utility is estimated from group-level usage attribution, and a global skill memory is maintained through utility-aware updating, pruning, and retrieval. High-utility skills are then used as policy priors to guide future exploration. The framework does not disclose the base LLM, parameter scale, training data, hardware, or inference costs.

Result: {"offline_gains": [], "online_gains": []}

Conclusion: Tournament-based relative ranking to derive trajectory-level reward signals, with each comparison augmented by structured reflective evaluation.；Structured reflective evaluation extracts three supervision types: pivotal success steps, reusable strategy skills, and usage attribution of retrieved skills.；Step-level credit propagation that distributes trajectory-level advantages to high-confidence pivotal steps according to tournament survival depth.；Skill-level utility estimation from group-level usage attribution with a global skill memory that supports utility-aware updating, pruning, and retrieval, and provides policy priors for future exploration.

Abstract: Reinforcement learning has substantially improved large language model (LLM) agents in verifiable domains, but remains difficult to apply to open-ended agent tasks, where solutions are diverse and reliable scalar rewards are hard to obtain. Recent pairwise evaluation methods alleviate reward discrimination collapse by replacing pointwise scoring with relative preferences. However, they still compress rich comparative feedback into a single trajectory-level reward, obscuring decisive intermediate steps and preventing successful behaviors from being consolidated into reusable skills. We propose ArenaFlow, a hierarchical credit propagation framework for open-ended agent reinforcement learning. ArenaFlow leverages tournament-based relative ranking to derive trajectory-level reward signals. Each comparison is further equipped with structured reflective evaluation, which reveals three types of supervision: pivotal success steps, reusable strategy skills, and usage attribution of retrieved skills. At the step level, ArenaFlow propagates trajectory-level advantages to high-confidence pivotal steps according to tournament survival depth, enabling more targeted optimization of local reasoning behaviors. At the skill level, ArenaFlow estimates skill utility from group-level usage attribution and maintains a global skill memory through utility-aware updating, pruning, and retrieval. The resulting high-utility skills further serve as policy priors for future exploration. Extensive experiments validate ArenaFlow's effectiveness on open-ended agent tasks.

</details>


### [37] [Beyond Atomic Tokens: Factorizing Syllables for Language Model Pretraining](https://arxiv.org/abs/2609.21362)
*Nguyen, Nghia Hieu,Huynh, Thai Bao,Dinh-Le, Binh-An,Hoang, Phu Gia,Nguyen, Dat Tien,Van Nguyen, Kiet,Nguyen, Ngan Luu-Thuy*

Main category: cs.CL

TL;DR: 论文提出 Phonemic Tokenizer，将越南语/中文音节转为 IPA 并确定性地分解为声母、韵母、声调三个音系分量，三分量共占一个上下文位置，词表仅112（中文）与256（越南语）项，无需语料相关词表学习。基于该分词器的 PhonemicBERT 使用因子化组件嵌入与三个预测头重建被掩码音节。内在评测显示 Rényi 效率显著更高、越南语标准音节词典生育率恰为1、序列更短；在受控中文预训练及越南语评测中与字符、子词、SubChar 和现有多语言模型相比具有竞争力或更优。


<details>
  <summary>Details</summary>
Motivation: 传统字符或子词分词器忽略音节内部音系结构，导致词表庞大且无法在音系相关音节间共享表示；如何用语言学驱动的确定性分词与预训练架构实现紧凑词表、音节级序列长度保持和可解释的表示共享。

Method: 输入为原始文本（越南语或中文）。Phonemic Tokenizer 首先将每个音节转换为国际音标（IPA），再确定性地分解为三个音系组件：声母（onset）、韵母（rime）和声调（tone）。这三个组件不属于三个独立位置，而是共同占据一个上下文位置，从而保持与音节级序列长度一致；由于组件级别共享，音系相关的音节可以复用组件表示。对于非音系单元或分词器不支持的字符，采用字符级回退处理。词表由所有可能的音系组件构成，无需从语料中学习，因此中文词表仅112项、越南语256项。预训练模型 PhonemicBERT 在输入侧为每个音节位置构造三个因子化的组件嵌入（onset、rime、tone），组合后送入 Transformer 编码器。预训练目标是为被掩码的音节设置三个预测头，分别预测其 onset、rime 和 tone 组件，从而重建完整音节，而非仅预测单一原子 token。推理时，文本经相同分词流程转为组件序列，模型照常编码并用于下游语言理解任务。该设计通过将音节拆分为可共享的音系因子，在极小词表下同时保持序列长度、表示共享能力和语言学可解释性。

Result: {"offline_gains": [{"metric": "Rényi efficiency", "value": "substantially higher than baselines", "setting": "中文与越南语内在分词评测"}, {"metric": "分词生育率（Fertility）", "value": "exactly 1", "setting": "标准越南语音节词典内每个词条"}, {"metric": "序列长度", "value": "generally shorter", "setting": "越南语序列与现有预训练分词器比较"}, {"metric": "语言理解任务表现", "value": "competitive with or outperforms character, subword, and SubChar alternatives", "setting": "受控中文预训练设置"}, {"metric": "越南语与多语言模型对比表现", "value": "competitive or superior", "setting": "既有越南语和多语言预训练模型"}], "online_gains": []}

Conclusion: Phonemic Tokenizer：将越南语和汉语音节转换为IPA并确定性分解为声母（onset）、韵母（rime）、声调（tone）三个音系分量，三分量共同占用一个上下文位置，保持音节级序列长度并实现音系相关音节间的表示共享。；极简确定性词表：无需语料相关词表学习，中文词表仅112项，越南语256项；非音系或不支持单元采用字符级回退。；PhonemicBERT：结合因子化分量嵌入，并使用三个预测头（对应onset、rime、tone）重建完整被掩码音节。；在受控中文预训练设置下与字符、子词、SubChar基线比较，并在越南语上与既有越南语及多语言预训练模型比较，验证竞争力。

Abstract: Conventional tokenizers represent text as characters or statistically derived subwords, overlooking the internal phonological structure of syllables and often requiring large vocabularies. We introduce \textbf{Phonemic Tokenizer}, a linguistically motivated tokenizer for Vietnamese and Chinese that converts each syllable into IPA and factorizes it into three phonological components: onset, rime, and tone. The three components jointly occupy one contextual position, preserving syllable-level sequence length while enabling representation sharing across phonologically related syllables. Non-phonological and unsupported units are handled through character-level fallback. This deterministic design requires no corpus-dependent vocabulary learning and yields vocabularies of only 112 entries for Chinese and 256 for Vietnamese. Intrinsic evaluation shows that the tokenizer achieves substantially higher R\'enyi efficiency in both languages, represents every entry in a standard Vietnamese syllable dictionary with a Fertility of exactly one, and generally produces shorter Vietnamese sequences than existing pretrained tokenizers. We further instantiate the tokenizer in \textbf{PhonemicBERT}, which combines factorized component embeddings and reconstructs complete masked syllables using three prediction heads. Under a controlled Chinese pretraining setup, PhonemicBERT-Zh is competitive with or outperforms character, subword, and SubChar alternatives across diverse language-understanding tasks. PhonemicBERT-Vi also achieves competitive or superior results to established Vietnamese and multilingual pretrained models. These results establish phonemic factorization as a compact, efficient, and interpretable alternative to atomic and statistically segmented text representations.

</details>


### [38] [$\mu^2$-Bench: A Multilingual Machine Unlearning Benchmark](https://arxiv.org/abs/2609.20945)
*Hwang, Kyomin,Kim, Hyeonjin,Lee, Hyunho,Kim, Yearim,Song, Yeji,Kwak, Nojun*

Main category: cs.CL

TL;DR: μ²-Bench是一个多语言机器遗忘基准，模拟记忆、遗忘、评估全流程，覆盖多语言并区分训练与留出语言评估，分析表明MMU需要反映多语言特性的方法。


<details>
  <summary>Details</summary>
Motivation: 多语言机器遗忘（MMU）缺乏统一的评估基准，无法判断遗忘方法是否真正跨语言消除了目标知识。

Method: 论文摘要未披露模型架构、训练目标、数据流等具体技术细节。基准的流程为：首先在多语言数据上训练LLM使其记忆目标知识，然后应用多种机器遗忘方法移除这些知识，最后在训练语言和留出语言上评估知识是否被消除。评估关注知识在多语言间的分散性。具体模型、遗忘算法、评估指标、数据规模等信息均未在摘要中提供。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 构建覆盖多种语言的记忆-遗忘-评估全流程基准μ²-Bench；在训练语言和留出语言上同时评估遗忘效果；评估知识在多语言间的分散性；分析表明成功的MMU需要反映多语言特性的方法

Abstract: Undesired information such as harmful content and private data propagates through Multilingual Large Language Models (LLMs) via direct training and indirect cross-linguistic spread. Multilingual Machine Unlearning (MMU) aims to remove such information, yet its evaluation remains underexplored, leaving unclear whether unlearning truly eliminates target knowledge across all languages. To bridge this gap, we introduce $\mu^2$-Bench, an MMU benchmark that simulates the full pipeline of memorization, unlearning, and evaluation across diverse languages. It 1) spans a broad set of languages, 2) evaluates on both training and hold-out languages, and 3) assesses knowledge as dispersed across multiple languages. We show that successful MMU requires methods that reflect multilingual characteristics, and conduct analysis to provide deeper insights into MMU.

</details>


### [39] [Rewarding Efficient Reasoning Improves Abstention on Underspecified Tasks in Reasoning Models](https://arxiv.org/abs/2609.20846)
*Tsvilodub, Polina,HÃ¶th, Max,Franke, Michael,Deiseroth, BjÃ¶rn,Kauf, Carina*

Main category: cs.CL

TL;DR: 针对大推理模型在信息不足任务上过度推理、不愿弃答的问题，作者设计了一种受资源理性启发的GRPO奖励，微调多个4B模型后平均提升12.8%弃答性能，并将CoT平均缩短44%，同时不损失回答能力。


<details>
  <summary>Details</summary>
Motivation: 大推理模型在任务信息不足（underspecified/不可回答）时仍生成冗长思维链并强行作答，缺乏类似人类的“何时弃答”能力，浪费推理算力且不可靠。

Method: 论文在多个4B大推理模型上使用GRPO进行微调。输入为可回答或信息不足的任务提示；模型先生成Chain-of-Thought再产生最终输出。核心改动在奖励函数：除正确性奖励外，引入资源理性启发的效率奖励，鼓励模型判断任务信息是否充分——当信息不足时应更短地推理并弃答，当信息充分时保持正常推理。训练目标是让模型在不可回答任务上的推理努力不超过可回答任务，从而与人类行为一致。推理流程与标准LRM相同（生成CoT后输出），但微调后弃答行为的CoT显著缩短。未披露具体数据规模、硬件或训练时长。

Result: {"offline_gains": [{"metric": "弃答性能（abstention performance）", "value": "+12.8% (平均)", "setting": "多个4B LRM经该GRPO奖励微调后，与基线比较"}, {"metric": "CoT长度", "value": "平均缩短44%", "setting": "同上微调模型，同时保留回答能力"}], "online_gains": []}

Conclusion: 发现LRM在不可回答任务上的CoT比可回答任务更长，与人类“推理努力在不可回答任务上有上界”的行为相反；从资源理性认知角度设计新的GRPO奖励，鼓励对“任务是否包含足够解答信息”进行高效推理；在多个4B LRM上微调后同时提升弃答性能并缩短CoT

Abstract: While modern large reasoning models (LRMs) excel at providing correct answers in many tasks, we provide additional evidence for the observation that they often struggle with a critical capability: knowing when to abstain from answering. We analyze this gap by comparing LRM behavior to results from a human study, revealing that human reasoning effort on unanswerable tasks is upper-bounded by answerable tasks, whereas LRMs waste computational resources by generating longer Chains of Thought (CoTs) on unanswerable than on answerable prompts. To overcome this inefficiency, we take inspiration from a resource-rational perspective on human cognition and introduce a novel GRPO reward that encourages efficient reasoning about whether the task contains all the information needed to solve it. Fine-tuning several 4B LRMs with this reward leads to human-like abstention performance gains (+12.8% on average) while retaining answering capabilities and boosting the models' efficiency (44% shorter CoTs on average).

</details>


### [40] [Reading Less While Writing: A Closed-Form Bandwidth Dial for Streaming Multimodal Decoders](https://arxiv.org/abs/2609.20845)
*Mehmood, Yasir,Javed, Kashif*

Main category: cs.CL

TL;DR: ZENDAYA用单一参数γ的闭式调度替换固定wait-k，实现流式多模态解码；理论证明结构依赖安全，并在三个语料上使29M小模型在低延迟下匹配或超越固定调度，Streaming METEOR显著提升。


<details>
  <summary>Details</summary>
Motivation: 传统解码器需读取完整输入后才能生成，流式场景（视频/音频转文本）不可行；现有固定wait-k调度与输入长度和节奏无关，低延迟下性能崩溃。

Method: 输入为视频或音频流，经编码得到源token序列；解码器自回归生成目标文本。ZENDAYA调度根据当前生成进度（已输出token数）和输入预测长度，通过闭式函数确定可见源前缀长度，该函数由单一标量γ控制。γ决定平均每生成一个词所消费源token的比例，均值约为1/(1+γ)。训练时从零训练一个29M参数解码器，目标为文本生成（如METEOR评估）。推理时，系统按ZENDAYA调度逐步扩展可见源前缀，新到达的源token可被后续生成步骤利用，保证已生成token不依赖未来未到达的输入。该结构将固定wait-k的离散固定偏移替换为随生成进度连续变化的可见窗口，从而在低延迟下避免因源信息过载导致的注意力稀释，并在无界异步流下保持正确性。

Result: {"offline_gains": [{"metric": "Streaming METEOR", "value": "统计显著提升", "setting": "Charades-STA, ActivityNet Captions, LibriHeavy；29M参数解码器从零训练，在最低延迟设置下对比固定调度"}], "online_gains": []}

Conclusion: 提出ZENDAYA：由单一连续参数γ控制的流式解码调度，将可见源前缀设为生成进度的闭式函数，并按输入预测长度缩放。；证明结构依赖定理：任何预先固定的非递减调度下，已输出token无法依赖未到达输入，保证训练/未训练权重及异步无界流均成立。；揭示反直觉经验现象：看到更少源可能产生更好文本，因源洪流在模型缺乏自身输出锚点时稀释注意力。；将离线解码与实时流式解码统一为一个连续家族的两个端点，γ同时作为延迟旋钮和可解释的源消费预算。

Abstract: A decoder that turns video or audio into text conventionally consumes the entire input before emitting a word. Offline this is merely more than the task requires; live it is impossible, since a caption cannot wait for a match to end. Streaming systems bolt on a fixed rule such as wait-$k$, which waits for the same number of input tokens before every word, regardless of the input's length or pace. We replace the fixed offset with ZENDAYA, a schedule governed by a single continuous parameter $\gamma$. It makes the visible source prefix a closed-form function of generation progress, scaled by the input's own predicted length, so an ordinary offline decoder and a real-time streaming decoder become two endpoints of one family rather than separate models. The same scalar fixes, in closed form, the mean fraction of source consumed per emitted word, $\bar{E}(\gamma) \approx 1/(1+\gamma)$, making it at once a latency dial and an interpretable budget. We prove a structural dependency theorem: under any schedule fixed in advance and non-decreasing, no emitted token can depend on input that has not yet arrived. The guarantee holds for trained and untrained weights alike, and extends to unbounded streams under arbitrary asynchronous arrival. The empirical result is counterintuitive: seeing less can produce better text, because a flood of source dilutes attention exactly when the model has the least of its own output to anchor on. Trained from scratch across two modalities and three public corpora (Charades-STA, ActivityNet Captions, LibriHeavy), a compact 29M-parameter decoder matches or beats the fixed schedule while reading less of the source, with the sharpest gains at the lowest latencies, where a fixed offset collapses. Streaming METEOR gains are statistically significant on all three corpora.

</details>


### [41] [Boosting Deepresearch and LongContext Ability with Self-Generated Deepresearch Rollouts Traces](https://arxiv.org/abs/2609.20844)
*Wang, Zihan,Wang, Hao,Jiang, Boyuan,Zhang, Yiqun,Feng, Shi,Yang, Xiaocui,Ye, Yiwen,Lin, Jianghang,Ji, Xiaozhong,Lin, Jinghao,Wu, Kai*

Main category: cs.CL

TL;DR: 论文发现 DR-RL 后 61.6% 的残余错误源于长上下文理解不足，提出 DR-to-Long 方法：复用 DR-RL 轨迹，将网页摘要与证据片段替换为对应 URL 全文，零标注成本构造长多文档 QA 数据；并设计 DLD(DR→LongQA→DR)-RL 三阶段训练流程。实验显示相对标准 DR-RL，在三个 Deepresearch 基准上提升 7.3%，在三个长上下文基准上提升 13.5%。属长上下文后训练与检索智能体结合的可复用方法工作，但无公司或在线部署证据。


<details>
  <summary>Details</summary>
Motivation: Deepresearch 智能体在多轮搜索与网页访问后上下文急剧增长，即使经过 DR-RL 训练，残余预测错误中仍有 61.6% 可归因于长上下文理解不足（长上下文幻觉与跨文档证据整合失败）。有效长上下文训练的关键瓶颈是缺少高质量长多文档训练数据。

Method: 整体为三阶段 RL 训练架构（DLD-RL），数据流与模块如下：
1) 短 DR-RL 阶段（轨迹收集）：Deepresearch 智能体在真实网络环境中进行多轮 search 与 visit 交互，产生 rollout 轨迹。每条轨迹天然包含搜索历史、访问过的网页、被压缩的证据片段、网页摘要以及最终答案监督信号。
2) DR-to-Long 数据构造模块：将轨迹中每一步的紧凑证据片段与网页摘要替换为对应 URL 的完整正文，从而在保持原有证据引用关系与最终答案监督不变的前提下，把上下文扩展为显著更长的多文档上下文，生成 LongQA 训练实例。该过程复用已有 RL rollout，标注成本为零，直接解决长上下文训练数据缺口。
3) LongQA-RL 阶段：在生成的 LongQA 实例上做强化学习，优化目标指向长上下文理解，抑制长上下文幻觉、提升跨文档证据整合能力。
4) 全量 DR-RL 阶段：在长上下文能力增强后，继续进行完整的 DR-RL，恢复并继续提升 deepresearch（多轮检索—访问—作答）能力。
各模块的交互逻辑是前向串行：DR-RL 产出轨迹 → DR-to-Long 把轨迹转化为长上下文数据 → LongQA-RL 提升长上下文 → 再次 DR-RL 提升 DR 能力，形成 DR→LongQA→DR 的闭环，使长上下文理解与智能体检索推理能力相互增强而非此消彼长。推理阶段仍为 DR 智能体范式：多轮 search/visit，上下文随轮次增长，模型需在长多文档上下文中定位证据并生成最终答案。论文未披露具体模型规模、训练数据总量、硬件、时长或推理延迟/吞吐。

Result: {"offline_gains": [{"metric": "Deepresearch 基准平均性能", "value": "相对标准 DR-RL 提升 7.3%", "setting": "三个 Deepresearch 基准"}, {"metric": "长上下文基准平均性能", "value": "提升 13.5%", "setting": "三个长上下文基准"}], "online_gains": []}

Conclusion: 观察到 DR-RL 后 61.6% 残余错误源于长上下文理解不足，据此提出先补长上下文能力再继续 DR-RL 的动机；DR-to-Long：把 DR-RL 轨迹中压缩片段与网页摘要替换为对应 URL 的完整内容，零标注成本地生成保留原证据关系的长多文档 QA 实例；DLD(DR→LongQA→DR)-RL 三阶段训练流程：短 DR-RL 收集轨迹 → 转换为 LongQA 实例 → LongQA-RL 强化长上下文 → 全量 DR-RL 继续提升 DR 能力

Abstract: Deepresearch (DR) agents interact with real-world web environments through multi-turn search and visit, causing their contexts to grow rapidly over time. We observe that, even after DR Agentic Reinforcement Learning (DR-RL), 61.6% of the model's remaining prediction errors can still be attributed to insufficient long-context understanding, including longcontext hallucination and failures in cross-document evidence integration. It motivates us to further break the bottleneck of DR-RL by strengthening the model's long-context ability. However, effective LongContext training requires more than simply increasing context length. To bridge the data gap, we propose `DR Rollouts to LongContext-QA (DR-to-Long)'. The method repurposes DR-RL trajectories, which naturally contain search histories, visited webpages, evidence snippets, and final-answer supervision. It then replaces the compact snippets and webpage summaries in each trajectory with the full contents of their corresponding URLs, producing substantially longer multi-document contexts while preserving the original evidence relationships. Building on DR-to-Long, we introduce DLD (DR -> LongQA -> DR)-RL. DLD-RL first performs a short DR-RL stage to collect rollout trajectories, which are then converted into LongQA instances at zero annotation cost. The model is subsequently optimized with LongQA-RL to strengthen LongContext ability, followed by full DR-RL to continue improving its DR capability. Experiments show that DLD-RL outperforms standard DR-RL by 7.3% on three Deepresearch benchmarks and improves performance by 13.5% on three long-context benchmarks.

</details>


### [42] [Recursive Language Models Generalize Out of Domain](https://arxiv.org/abs/2609.20831)
*Yang, Chenxiao,Li, Zhiyuan,McAllester, David,Srebro, Nathan*

Main category: cs.CL

TL;DR: 论文研究递归语言模型（RLM）通过子任务上下文隔离提升域外泛化，理论分析表明IID下CoT与RLM泛化相当，但OOD下CoT依赖上下文捷径而失效，RLM隔离机制可消除该失败模式，对LLM推理系统设计有启示。


<details>
  <summary>Details</summary>
Motivation: 在子任务隔离的递归语言模型与标准Chain-of-Thought之间，研究限制模型可见上下文如何影响域内（IID）和域外（OOD）泛化，并解释CoT在OOD下依赖上下文捷径而失效的机制。

Method: 论文提出递归语言模型（RLM），将复杂问题分解为子任务，每个子任务在独立的上下文中由语言模型求解，最终组合结果。模型通过递归调用自身来完成整体推理。对比的标准Chain-of-Thought（CoT）允许模型访问完整推理轨迹。论文从学习理论角度分析：IID下，CoT可高效模拟递归规则，泛化界限仅差常数因子；OOD下，CoT倾向于利用训练数据中跨子任务的上下文线索作为捷径，当测试时这些线索变化则失败，而RLM的上下文隔离迫使模型学习可泛化的规则。理论分析基于简单性偏差和覆盖规则，但未披露具体模型参数、训练数据、硬件等细节。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 提出递归语言模型（RLM）的上下文隔离机制，每个子任务在独立上下文中求解。；理论证明IID下CoT可高效模拟递归规则，泛化保证仅差常数因子，但OOD下CoT易利用跨子任务上下文捷径，而递归隔离可消除该失败模式。；揭示经典学习理论中覆盖正确规则不足以保证OOD泛化的不足，强调简单性偏差导致选择捷径。

Abstract: We study when limiting what a language model can see improves learning. We compare standard CoT, the more general learner that reads the full trace, with recursive language models, which restricts itself by solving each subtask in an isolated context. In-distribution, this generality comes for free: CoT can efficiently simulate the recursive rule, so the IID generalization guarantee changes only by a constant factor, and recursion does not offer much. But out of domain, CoT can fit training by relying on context outside the current subtask, i.e. a shortcut that breaks once those tokens change; recursive context isolation rules out this failure mode. Even though CoT's class still covers the recursive rule, simplicity bias picks the shortcut over the truth. Thus, to go beyond distributional accuracy and truly reason, covering the right rule is not enough; this contrasts with classical learning theory.

</details>


### [43] [Reviser: Revision-Capable Text Generation via Autoregressive Cursor Actions](https://arxiv.org/abs/2609.20830)
*Diab, Sean*

Main category: cs.CL

TL;DR: Reviser是一种基于游标动作的自回归文本生成模型，在可变画布上通过插入、移动和停止动作实现非单调生成。在延续基准测试中，它优于SEDD和MDLM，与同规模自回归基线竞争，且推理计算量显著降低。


<details>
  <summary>Details</summary>
Motivation: 现有文本生成模型难以在生成过程中灵活地插入或修改先前的内容，而非自回归和编辑方法通常需要重复的序列级计算，计算开销大。

Method: Reviser是一个仅解码器的Transformer，将响应生成为在可变画布上的一系列游标相对动作。在每个步骤，模型预测一个动作token：INSERT(token)、MOVE(Δ)或STOP。模型自回归于编辑历史动作序列，而非最终文本顺序。画布初始为空，通过动作逐步构建和修改文本。INSERT在当前游标位置插入token，MOVE将游标移动Δ个位置（可正可负），STOP结束生成。训练目标是最大化动作序列的似然。推理时，模型逐步采样动作，更新画布和游标，直到STOP。这种设计允许非单调生成，模型可在生成过程中回退游标并插入内容。与标准自回归生成相比，Reviser的动作序列长度可变，但推理计算量因无需重复处理整个序列而降低。

Result: {"offline_gains": [{"metric": "人类偏好（arena评估）", "value": "强烈优于SEDD和MDLM", "setting": "延续基准测试，arena评估"}, {"metric": "与大小匹配的自回归基线对比", "value": "在100M和300M规模上具有竞争力", "setting": "延续基准测试"}, {"metric": "推理计算量", "value": "显著低于代表性多轮细化和扩散式基线", "setting": "共享FLOPs约定"}], "online_gains": []}

Conclusion: 提出基于游标相对动作的自回归生成框架，在可变画布上执行插入、移动和停止操作。；模型自回归于编辑历史动作而非最终文本顺序，实现真正的非单调生成。；在相同FLOPs约定下，推理计算量显著低于多轮细化和扩散式基线。

Abstract: Revision-capable generation is appealing because it can insert or revise earlier content, but many non-autoregressive and edit-based approaches obtain this flexibility through repeated sequence-level computation. We propose Reviser, a decoder-only Transformer that generates a response as a sequence of cursor-relative actions on a mutable canvas. At each step, Reviser predicts exactly one action token: INSERT(token), MOVE($\Delta$), or STOP, and is autoregressive over edit-history actions rather than final text order. This design enables genuinely non-monotonic generation while preserving a simple next-action interface. On a continuation benchmark, Reviser is strongly preferred to SEDD and MDLM in our arena evaluations, and trajectory statistics confirm that the model performs frequent backward moves and mid-canvas insertions rather than merely emulating end-append decoding. Against size-matched autoregressive baselines, Reviser is competitive at both the 100M and 300M scales. Under our shared FLOPs convention, Reviser also requires substantially less inference compute than representative multi-pass refinement and diffusion-style baselines.

</details>


### [44] [Do small language models know what they don't know?](https://arxiv.org/abs/2609.20824)
*Mudgal, Prashant*

Main category: cs.CL

TL;DR: 本文探讨小语言模型（<3B参数）中基于熵的置信度信号，发现令牌级熵在SLM中几乎无效（91%的组合中接近零），而语义熵能有效恢复置信度信号。通过语义熵进行不确定性感知路由到更大专家模型，可实现高达+50个百分点的准确率提升，跨家族路由平均提升+22.0%，同家族仅+6.8%。这表明熵方法的优势在于智能计算分配而非节省计算。


<details>
  <summary>Details</summary>
Motivation: 小语言模型（<3B参数）在消费级硬件上运行时，如何利用基于熵的置信度信号提高准确性？特别是令牌级熵在SLM中是否有效，以及语义熵能否用于不确定性感知路由到更大的专家模型。

Method: 论文评估了七种基于熵的置信度信号方法，在7个模型对和5个标准NLU基准上测试。主要架构包括：1）令牌级熵早停：计算每个令牌的熵，当熵值超过阈值时提前停止解码，但发现在SLM中平均令牌熵接近零，与正确性无关；2）语义熵估计：通过多次采样生成答案，按语义聚类，计算分布不确定性；3）不确定性感知路由：使用语义熵作为置信度，将不确定的查询路由到更大的专家模型（如从SmolLM 360M到Phi-3.5-mini）。推理流程为：SLM首先生成多个样本并计算语义熵，若语义熵超过阈值则路由到专家模型处理，否则使用SLM答案。训练目标未涉及，因为方法主要是推理时的置信度估计和路由。

Result: {"offline_gains": [{"metric": "准确率提升", "value": "+50个百分点", "setting": "使用语义熵选择性路由不确定查询到更大专家模型，7个模型对和5个NLU基准"}, {"metric": "平均准确率提升（跨家族路由）", "value": "+22.0%", "setting": "跨家族路由（如SmolLM 360M到Phi-3.5-mini）"}, {"metric": "平均准确率提升（同家族路由）", "value": "+6.8%", "setting": "同家族路由"}], "online_gains": []}

Conclusion: 发现SLM中令牌级熵在91%的数据集-模型组合中接近零且与答案正确性无关，表明其作为置信度信号无效；提出使用语义熵（通过多次采样生成、按语义聚类答案、测量分布不确定性）作为SLM中可行的置信度信号；利用语义熵进行不确定性感知路由，将不确定查询路由到更大的专家模型，实现高达+50个百分点的准确率提升；发现跨家族路由（如SmolLM 360M到Phi-3.5-mini）平均提升+22.0%，而同家族路由仅+6.8%，表明专家模型质量比架构兼容性更重要

Abstract: We explore whether entropy-based confidence signals can be leveraged to improve the accuracy of Small Language Models (SLMs) with fewer than 3 billion parameters, running entirely on consumer hardware. We evaluate seven distinct approaches, including token-level entropy early stopping, semantic entropy estimation, and uncertainty-aware routing to larger expert models, across 7 model pairs and 5 standard NLU benchmarks. Our key finding is that token-level entropy is effectively blind in SLMs: in 91% of dataset-model combinations, mean token entropy is near zero regardless of answer correctness, rendering token-based confidence signals unusable at this scale. We demonstrate that semantic entropy, computed by generating multiple samples, clustering answers by meaning, and measuring distributional uncertainty, recovers a viable confidence signal. Using semantic entropy to selectively route uncertain queries to a larger expert model yields accuracy improvements of up to +50 percentage points. Notably, cross-family routing (e.g., SmolLM 360M to Phi-3.5-mini) averages +22.0% improvement compared to +6.8% for same-family routing, revealing that expert model quality matters more than architectural compatibility. Our results suggest that the value proposition for entropy-based methods in SLMs is not computational savings but intelligent compute allocation: spending more tokens where they matter most.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [45] [Detecting Pretraining Data in Large Language Models from a Free-Energy Perspective](https://arxiv.org/abs/2609.21888)
*Ke, Chenye,Liu, Zirui,Liu, Qi,Zhuang, Yan,Zhang, Jintao,Huang, Zhenya,Wang, Shijin*

Main category: cs.LG

TL;DR: 论文从预测损失与预测熵的联合空间出发，发现仅用似然的水平边界会误判可预测非成员，进而提出带熵校正的倾斜边界判据，并从Helmholtz自由能角度将其解释为能量转移检测（ETD）。实验显示平均AUROC最多提升3.5%、TPR@5%FPR最多提升5.1%，在不同设定下稳健。


<details>
  <summary>Details</summary>
Motivation: 仅依赖预测似然（loss）的预训练数据检测方法使用水平决策边界，会将高似然但实际未参与训练的可预测非成员误判为成员，难以区分训练暴露与强泛化。

Method: 论文提出的ETD方法在推理阶段对候选文本分别计算两个统计量：预测损失（似然）与预测熵，将它们放入联合空间，并采用倾斜决策边界而非传统似然的水平边界。具体而言，检测分数被构造为对预测损失按预测熵进行校正的形式；理论分析表明该校正保持期望的成员信号、降低信号方差，从而在标准化后提高成员与非成员的可分性。作者进一步将均值-方差分析扩展到预测熵均值差非零的一般情形。该校正分数可写为Helmholtz自由能形式，即自由能等于平均能量减去熵项（对应温度与熵的乘积），因此检测过程被解释为从宏观残余自由能转移角度区分训练成员与非成员。最终以该自由能/能量分数作为成员判定依据，实验显示平均AUROC与TPR@5%FPR均优于仅似然的检测器且跨多种设置保持稳健。摘要未披露具体网络模块、训练目标与在线服务路径。

Result: {"offline_gains": [{"metric": "平均AUROC", "value": "提升最多3.5%", "setting": "预训练数据检测实验（基线与具体数据集未在摘要中披露）"}, {"metric": "TPR@5%FPR", "value": "提升最多5.1%", "setting": "预训练数据检测实验（基线与具体数据集未在摘要中披露）"}], "online_gains": []}

Conclusion: 提出在预测损失与预测熵联合空间中的倾斜决策边界，用预测熵对预测损失进行校正；证明熵校正能在保持期望成员信号的同时降低其方差，从而提升标准化后的成员-非成员可分性；将均值-方差分析推广到熵均值差非零的更一般设定；发现该熵校正分数具有Helmholtz自由能解释，提出能量转移检测（Energy Transfer Detection, ETD），从宏观残余自由能转移视角进行预训练数据检测

Abstract: Detecting pretraining data in large language models is challenging because high likelihood can reflect either training exposure or strong generalization. In the joint space of prediction loss and predictive entropy, a likelihood-only detector uses a horizontal boundary and can mistake predictable non-members for members. Motivated by this, we introduce an inclined boundary that evaluates prediction loss relative to predictive entropy. Our analysis shows that entropy correction can preserve the expected membership signal while reducing its variance, thereby improving standardized member--non-member separation. We further extend the mean--variance analysis to the more general setting with a nonzero mean entropy gap. Interestingly, this entropy-adjusted score admits a Helmholtz free-energy interpretation, leading to Energy Transfer Detection (ETD), which views pretraining data detection from a macroscopic residual free-energy transfer perspective. Extensive experiments show that ETD achieves the best average detection performance, improving average AUROC by up to 3.5\% and TPR@5\%FPR by up to 5.1\%, while remaining robust across diverse settings.

</details>


### [46] [Neural Cellular Automata Learn General Features in their Hidden Channels](https://arxiv.org/abs/2609.21870)
*Guichard, Etienne,Nichele, Stefano*

Main category: cs.LG

TL;DR: 本文探索神经细胞自动机（NCA）隐藏通道的内部动力学，提出一种将教师隐藏状态注入学生模型的迁移学习机制。在少样本MNIST基准上，NCA以约9,800参数实现优于循环和前馈架构的泛化。机制分析表明隐藏通道解耦特征提取与分类共识，捕获尺度不变拓扑基元，支持跨类别迁移。


<details>
  <summary>Details</summary>
Motivation: 在少样本场景下，现代深度学习模型因过参数化而容易过拟合和记忆；尽管神经细胞自动机（NCA）参数高效，但其内部隐藏通道的角色尚未被充分探索。

Method: 论文研究神经细胞自动机（NCA）的内部隐藏通道动力学。NCA是一种高度参数高效的模型，通过局部规则在网格上迭代更新细胞状态。每个细胞维护一个隐藏状态向量，该向量在多个时间步中与邻居交互。作者引入一种迁移学习机制：预训练教师模型的隐藏状态被注入学生模型，以指导学生的早期优化。在少样本和尺度变化的MNIST基准上评估，NCA表现出优于循环和前馈架构的泛化能力。机制分析显示，隐藏通道将特征提取与统一分类共识解耦，吸收形态复杂性并收敛到相互正交的状态。这些隐藏通道捕获尺度不变的拓扑基元，允许学生模型在未见类别上利用从仅训练0-5数字的教师模型迁移的特征实现强少样本性能。

Result: {"offline_gains": [{"metric": "少样本分类准确率", "value": "优于同类循环和前馈架构", "setting": "少样本和尺度变化的MNIST基准，约9,800参数"}], "online_gains": []}

Conclusion: 提出一种新的迁移学习机制，将预训练教师模型的隐藏状态注入学生模型以指导早期优化；通过机制分析揭示NCA隐藏通道将特征提取与统一分类共识解耦，吸收形态复杂性并收敛到相互正交的状态；证明隐藏通道捕获通用的尺度不变拓扑基元而非类别特定模板，支持从仅训练0-5数字的教师模型向未见类别的少样本迁移

Abstract: Modern deep learning models achieve impressive generalization through over-parameterization, but this paradigm often struggles with overfitting and memorization in few-shot regimes. Neural Cellular Automata (NCAs) offer a highly parameter-efficient alternative, yet research has focused primarily on their output, leaving the role of their internal hidden channels largely unexplored. In this paper, we investigate the internal dynamics of NCA hidden channels and introduce a novel transfer-learning mechanism that injects a pretrained teacher's hidden states into a student model to guide early optimization. Evaluated on few-shot and scale-variant MNIST benchmarks, NCAs outperform comparable recurrent and feed-forward architectures, demonstrating superior generalization with a minimal parameter budget (~9,800 parameters). Mechanistic analysis reveals that the hidden channels decouple feature extraction from uniform classification consensus by absorbing morphological complexity and converging to mutually orthogonal states. Furthermore, we demonstrate that these hidden channels capture general, scale-invariant topological primitives rather than class-specific templates. This allows a student model to achieve strong few-shot performance on unseen classes using features transferred from a teacher trained only on a subset of digits (0-5). Our results highlight the potential of utilizing hidden-state dynamics as a robust, decentralized computational substrate for parameter-efficient transfer learning

</details>


### [47] [On Repulsive and Attractive Teachers: Separating Correctness from Behavior in Self-Distillation](https://arxiv.org/abs/2609.21561)
*Baumann, Anton,Ashirmatov, Akmal,Schmidt-Traub, Leo,LÃ¼beck, Frederike,HÃ¼botter, Jonas,Buening, Thomas Kleine,Krause, Andreas*

Main category: cs.LG

TL;DR: 论文研究在线策略自蒸馏中的行为纠缠问题，提出吸引与排斥自蒸馏的对比分析，并设计对比自蒸馏目标（吸引正确教师、排斥错误教师）来抵消共享行为偏移，在多种模型上提升推理性能并保持响应长度稳定。


<details>
  <summary>Details</summary>
Motivation: 特权信息不仅改变教师模型的知识（正确性信号），也改变其行为（探索性、响应长度、思维模式切换），导致自蒸馏中正确性学习信号与意外行为偏移相互纠缠。

Method: 本文基于在线策略自蒸馏框架：通过在输入中拼接特权信息（如正确解或错误解）条件化教师模型，得到token级教师分布，再将当前模型（学生）的在线生成分布与教师分布进行蒸馏（学生从自身策略采样）。吸引式自蒸馏最小化学生与正确解条件教师的KL散度，使学生向教师靠拢；排斥式自蒸馏则最大化与特权教师分布的差异，将学生推离该分布。论文系统比较了两种目标对推理行为的影响，发现吸引抑制探索、缩短响应并提升置信度，排斥则增加长度、触发潜在思考模式并最终不稳定。提出的对比自蒸馏同时优化两项：吸引项指向正确解条件教师，排斥项远离错误解条件教师，使得仅由特权条件引起的行为偏移（如长度变化）在两项中方向相反并相互抵消，而正确性相关的token级信号得以保留。训练目标直接优化该对比损失，不依赖GRPO，推理时使用学生模型自身生成，无需教师模型。

Result: {"offline_gains": [{"metric": "推理性能", "value": "提升", "setting": "非思考模型、仅指令模型、已思考模型三类模型，对比自蒸馏目标结合不同条件教师"}, {"metric": "响应长度稳定性", "value": "保持稳定", "setting": "对比自蒸馏后应答长度未出现明显增长或缩短（与吸引/排斥单独作用相比）"}], "online_gains": []}

Conclusion: 系统对比吸引式自蒸馏（向特权教师靠拢）与排斥式自蒸馏（远离特权教师）对模型行为的相反影响。；提出对比自蒸馏：同时吸引正确解条件教师、排斥错误解条件教师，使两者共享的行为偏移相互抵消。；将对比自蒸馏目标与GRPO解耦，独立研究该目标本身的行为与性能影响。

Abstract: On-policy self-distillation provides dense, token-level supervision by conditioning a model on privileged information and distilling the resulting teacher distribution back into the model. However, privileged information can change not only what the teacher knows, but also how it behaves, entangling correctness-relevant learning signals with unintended behavioral shifts. We study this effect in reasoning tasks by contrasting attractive self-distillation, which moves the model toward a privileged teacher, with repulsive self-distillation, which moves it away from a privileged teacher. We find that both objectives can induce strong and opposing behavioral shifts: attraction suppresses exploratory reasoning and promotes shorter, more confident responses, whereas repulsion increases response length, can trigger unintended switches into a model's latent thinking mode, and ultimately becomes unstable. Motivated by these observations, we study contrastive self-distillation, which combines attraction toward a correct-solution-conditioned teacher with repulsion from an incorrect-solution-conditioned teacher. In contrast to prior work that combines such distillation signals with a GRPO objective, we isolate the self-distillation objective and study its behavior on its own. We find that the shared behavioral shifts of the two teachers largely cancel, leaving a token-level signal that more directly reflects correctness. Across non-thinking, instruct-only, and already-thinking models, this contrastive objective improves reasoning performance while maintaining stable response lengths.

</details>


### [48] [OneBid: A Unified Auto-Bidding Foundation Model for Diverse oCPX Advertising Scenarios](https://arxiv.org/abs/2609.21550)
*Li, Yewen,Jiang, Peng,Li, Yitian,Lv, Pengfei,Liu, Xialong,Jiang, Peng,Cai, Qingpeng*

Main category: cs.LG

TL;DR: OneBid 是快手提出的统一自动出价基础模型，将异质 oCPX 场景（注册、购买、ROAS 等）统一到单一 Decision Transformer 骨干，通过 Return-to-Go 与 Cost-to-Go 双条件、序列级 MoE 和 CROP 离线后训练实现多目标控制与安全策略改进。全量部署于快手，在线 A/B 测试显示 oCPX Ads 整体 ADVV +2.2%，ROAS 场景峰值 +13.1%。


<details>
  <summary>Details</summary>
Motivation: 现有 auto-bidding 方法从规则控制、强化学习到 Decision Transformer 等生成式方法，与主流的 oCPX 范式日益不匹配：oCPX 覆盖注册、购买等异质场景，每个场景单独建模导致 pipeline 碎片化，且缺乏跨场景建模。将多场景统一到一个模型面临三个挑战：多目标控制、严格延迟下的可扩展容量、安全的离线策略改进。

Method: OneBid 以 Decision Transformer 为骨干并扩展为统一自动出价基础模型。输入为异质 oCPX 场景的日志序列，动作条件由两个原子信号构成：conversion value 的 Return-to-Go 与 cost ratio 的 Cost-to-Go，同时引入 value-aware regularization 优化下一个动作预测。为吸收跨场景分布异质性，模型采用序列级 Mixture-of-Experts：共享专家编码跨场景知识，稀疏路由专家捕捉场景特定模式，在低延迟约束下随模型规模与数据扩展。训练分两阶段：先在大规模异质 oCPX 日志上学习可复用的 backbone，再通过离线后训练适配各场景部署。后训练阶段使用 Critic-guided Relative Offline Policy optimization（CROP）：学习得到的 critic 对候选动作进行组内相对打分，避免 GRPO 式在线探索的不安全性，并通过约束策略偏移降低 OOD 风险。推理时统一 backbone 结合场景特定适配进行出价动作生成，满足在线广告的严格延迟要求。

Result: {"offline_gains": [], "online_gains": [{"metric": "ADVV", "value": "+2.2%", "setting": "oCPX Ads 在线 A/B 测试整体；相对基线未明确"}, {"metric": "ADVV", "value": "+13.1%", "setting": "ROAS 场景在线 A/B 测试峰值；相对基线未明确"}]}

Conclusion: 将 Decision Transformer 的单 Return-to-Go 条件扩展为两个原子信号：conversion value 的 Return-to-Go 与 cost ratio 的 Cost-to-Go，并加入 value-aware regularization 的下一个动作预测。；设计序列级 Mixture-of-Experts 架构，共享专家编码跨场景知识，稀疏路由专家以低延迟捕捉场景特定模式，随模型规模与数据扩展。；提出 Critic-guided Relative Offline Policy optimization (CROP) 后训练方法，学习 critic 对候选动作进行组内相对打分，避免 GRPO 式微调的不安全在线探索并约束策略偏移以降低 OOD 风险。

Abstract: Auto-bidding is central to computational advertising, where strategies must maximize advertisers' conversion value under economic constraints. It has evolved from rule-based controllers to reinforcement learning and generative methods such as Decision Transformer (DT). Yet these methods increasingly mismatch the prevailing optimized cost-per-X (oCPX) paradigm, which spans heterogeneous scenarios (e.g., registration, purchase), each served by a separate model, leading to fragmented pipelines and underexploring cross-scenario modeling. Inspired by foundation models like LLMs, unifying these oCPX scenarios into one model raises three challenges: multi-objective control, scalable capacity under strict latency, and safe offline policy improvement. We present OneBid, a unified auto-bidding foundation model that learns a reusable backbone from heterogeneous oCPX logs and adapts it to scenario-specific deployments via offline post-training. Building on DT, OneBid extends single Return-to-Go conditioning to two atomic signals, Return-to-Go for conversion value and Cost-to-Go for cost ratio, plus value-aware regularization on next-action prediction. To absorb distributional heterogeneity, we design a sequence-level Mixture-of-Experts architecture, where shared experts encode cross-scenario knowledge and sparsely-routed experts capture scenario-specific patterns at low latency, yielding consistent scaling with model size and data. During post-training, we align the backbone with scenario preferences via Critic-guided Relative Offline Policy optimization (CROP): a learned critic scores candidate actions group-relatively, avoiding the unsafe online exploration of GRPO-style fine-tuning while constraining policy shift to reduce OOD risk. Validated via online A/B tests and fully deployed at Kuaishou, OneBid delivers an overall +2.2% ADVV gain on oCPX Ads, peaking at +13.1% in the ROAS scenario.

</details>


### [49] [BI-Agent and BI-Bench: Towards Automating End-to-End Business Intelligence](https://arxiv.org/abs/2609.20886)
*Hu, Chuxuan,He, Yeye,Zhou, Penny,Tok, Wee Hyong,Kang, Daniel,Chaudhuri, Surajit*

Main category: cs.LG

TL;DR: 论文针对企业 BI 中数据准备繁琐、前沿 LLM 端到端准确率不足 50% 的问题，提出 BI-Bench 基准（首个系统性评测 LLM 端到端 BI 能力的基准，从真实用户仪表盘抽取问题-答案对）与工具增强的 BI-Agent。BI-Agent 将 BI 工作流分解为 search、join、transform、answer 子任务并编排专门的数据管理方法，同时给出基于真实 BI 项目合成轨迹的 SFT+RL 后训练框架，使 vanilla LLM 准确率提升最多 40 个百分点，后训练后进一步提升最多 30 个百分点。


<details>
  <summary>Details</summary>
Motivation: 传统 BI 工作流需要用户手动完成识别相关表、数据转换、建立 join 关系等准备步骤，耗时且复杂；现有前沿 LLM 在端到端 BI 问题上表现很差（准确率低于 50%），缺少系统性的评测基准和有效的自动化方案

Method: BI-Agent 以端到端方式处理 BI 问题：输入是用户的业务问题以及底层企业数据（多张表）。Agent 不要求用户预先准备数据，而是将完整 BI 工作流分解为若干在结构化数据上的子任务，包括 relevant table search（识别相关表）、join（构建表间连接关系）、transform（数据转换）以及最终 answer（回答问题）。每个子任务由专门的数据管理方法/工具来执行，Agent 负责编排这些工具，在多个 BI 阶段之间传递中间结果：先检索出相关表，再确定并执行 join 与 transform 得到可用于分析的结构化数据，最后基于该结构化数据生成答案。为提升 Agent 能力，作者构建了后训练框架：从真实 BI 项目中合成训练轨迹，先用监督微调（SFT）在轨迹上训练，再用强化学习（RL）进一步优化。论文未披露具体的模型规模、训练硬件、数据规模与推理延迟。

Result: {"offline_gains": [{"metric": "端到端 BI 准确率", "value": "提升最多 40 个百分点", "setting": "BI-Agent 搭配 vanilla LLM 在 BI-Bench 上"}, {"metric": "端到端 BI 准确率", "value": "提升最多 30 个百分点", "setting": "经后训练的 BI-Agent（SFT+RL）"}, {"metric": "端到端 BI 准确率", "value": "低于 50%", "setting": "未增强的前沿 LLM 在 BI-Bench 上的基线表现"}], "online_gains": []}

Conclusion: BI-Bench：首个系统性评测 LLM 端到端 BI 能力的基准，从公开来源采集真实用户 BI 项目与仪表盘，人工抽取（问题，标准答案）对；BI-Agent：工具增强的 BI 智能体，将 BI 工作流分解为 search、join、transform、answer 等结构化数据子任务，并跨 BI 阶段编排专门的 data management 方法；面向 BI 的后训练框架：从真实 BI 项目中合成训练轨迹，结合监督微调（SFT）与强化学习（RL）对 BI-Agent 进行领域后训练

Abstract: Business intelligence (BI) is a cornerstone of enterprise decision-making and is widely used by enterprise users in software such as Power BI and Tableau. In traditional BI workflows, users need to prepare data by (1) identifying relevant tables, (2) performing data transformations, and (3) building join relationships, before they can (4) answer their business questions. These steps can be complex and time-consuming, making BI challenging. Given the strong capabilities of large language models (LLMs) in working with data, we study their ability to answer BI questions end-to-end, without requiring users to manually perform the tedious preparation steps. To do this, we harvest a large collection of real-world BI projects from public sources, and manually extract pairs of (questions, ground-truth answers) from real user dashboards. The resulting benchmark, BI-Bench, is the first benchmark to systematically study LLMs' ability on end-to-end BI. We find that even frontier LLMs perform poorly on BI-Bench, with less than 50% accuracy. To address their limitations, we design a tool-augmented BI-Agent that decomposes BI workflows into subtasks on structured data, such as search, join, and transform, and orchestrates specialized data management methods across BI stages. Furthermore, we develop a post-training framework that synthesizes training trajectories from real BI projects, enabling BI-Agent to be further post-trained using both supervised fine-tuning (SFT) and reinforcement learning (RL). BI-Agent achieves substantial accuracy gains of up to 40 percentage points with vanilla LLMs, and post-trained BI-Agent yields gains of up to 30 points. Our results highlight the importance of combining tool-augmented reasoning with domain-specific post-training in complex BI workflows, and point to promising directions for future research.

</details>


### [50] [Abstention and Noise Filtering: Two Missing Primitives of Softmax Attention](https://arxiv.org/abs/2609.22005)
*Wang, Richard Zhe*

Main category: cs.LG

TL;DR: 本文通过实验表明softmax注意力缺乏abstention和noise filtering两个原语。abstention允许注意力头输出空，通过可学习sink logit实现；noise filtering通过value门控抑制残差流干扰。在10M-350M参数匹配模型中，abstention收益随规模下降，noise filtering收益随规模上升，两者结合在所有规模上最佳。机制额外参数极少，且兼容KV缓存。


<details>
  <summary>Details</summary>
Motivation: softmax注意力缺乏abstention（允许注意力头输出空）和noise filtering（抑制残差流中叠加特征的干扰）两个原语，导致门控机制的有效性原因不明。

Method: 在标准softmax注意力中，注意力头的输出是value的加权和，权重和为1。本文在softmax中增加一个可学习的per-head sink logit，使得注意力头可以分配概率质量给一个'空'输出，从而实现abstention，绕过权重必须归一化的约束。同时，在value路径上为每个头增加一个门控，对value进行逐元素缩放，以过滤残差流中叠加特征的噪声干扰。模型训练使用匹配的基线（无门控、仅abstention、仅noise filtering、两者都有）从10M到350M参数进行对比，测量验证损失。推理时，两种机制均兼容KV缓存，因为sink logit和value门控是静态的或可预先计算的。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 提出abstention通过可学习的per-head sink logit实现，允许注意力权重不归一化，头可以输出空；提出noise filtering通过在value路径上加门控，抑制残差流中叠加特征的干扰；通过实验区分abstention和noise filtering在语言模型预训练中的不同作用，发现abstention的收益随规模下降，noise filtering的收益随规模上升；设计受控干扰注入实验，验证门控可消除干扰，并揭示不同门控形式的盲点

Abstract: Gating the value pathway of attention reportedly improves language model pretraining, and prior studies disagree on why. We argue and provide experimental evidence that such gates supply two different things that softmax attention lacks: abstention and noise filtering. The first is abstention, which allows an attention head to output nothing, bypassing the requirement that attention weights must sum to one. The second is noise filtering, which allows the value pathway of an attention head to suppress interference from superposed features in the residual stream. In our experiments in matched models from 10M to 350M parameters, we supply abstention through a learned per-head sink logit in the softmax and noise filtering through a gate on each value. We report three empirical findings. First, the benefit of abstention, measured as the reduction in validation loss relative to a matched baseline, declines as models grow, whereas the benefit of noise filtering increases with scale. In particular, abstention accounts for nearly all of the gain from gating at 10M and filtering for most of it at 350M. Second, the best model at every scale is the one with both primitives built in. Third, injecting controlled interference into the values a head reads confirms that the gate removes such interference, and reveals that each of the two gate forms we study has a characteristic blind spot. Supplying both primitives adds negligible parameters and remains compatible with the key-value cache.

</details>


### [51] [$\lambda$-Controlled GRPO: Turning Flow-Matching Ratio Instability into a Budgeted Resource](https://arxiv.org/abs/2609.22041)
*Wang, Yufeng,Priye, Parivesh,Marathe, Meeshawn,Pahwa, Ramit*

Main category: cs.LG

TL;DR: 针对 Flow-GRPO 多步去噪中重要性比漂移导致的训练不稳定问题，提出 λ-Controlled GRPO。发现不稳定源于单一可测的路径方差，将其视为可预算资源，基于预测律校准重要性比和梯度分配。在文本到图像模型上，文本准确率和偏好奖励均超过最强经验稳定器，且后期路径方差控制在预算内。方法不引入额外调参，可迁移至 LLM 后训练。


<details>
  <summary>Details</summary>
Motivation: Flow-GRPO 在多步去噪过程中策略更新不稳定：重要性比系统性漂移、随步骤扩散、裁剪率不一，导致后期可用样本减少。现有方法将各症状分别处理，依赖手工调参。

Method: 论文提出的 λ-Controlled GRPO 建立在 Flow-GRPO 之上，将 flow-matching 模型的去噪采样器视为随机策略，从奖励反馈中优化。核心架构围绕多步去噪过程中的路径方差（path variance）展开。该方差由采样器的高斯转移核在每一步精确决定，并可在训练中廉价估计。方法不依赖噪声经验统计，而是根据预测的路径方差律来校准重要性比（importance ratio）的行为：首先利用预测的路径方差确定每个去噪步骤的重要性比的理论分布，进而推导出控制更新尺度的两个参数（由标准策略选择固定，而非自由调参）；然后根据每步的预测成本分配梯度努力（即梯度预算），使得后期步骤不会因重要性比漂移而浪费可用样本。整个训练流程包括：前向采样得到轨迹，计算每步奖励和路径方差，根据预测律调整裁剪和梯度权重，更新策略网络。推理阶段与标准 flow-matching 采样一致，不增加额外计算开销。

Result: {"offline_gains": [{"metric": "文本准确率（OCR 评分）", "value": "优于最强经验稳定器", "setting": "文本到图像模型，困难目标文本渲染，OCR 评分"}, {"metric": "人类偏好奖励（偏好模型评分）", "value": "优于最强经验稳定器", "setting": "文本到图像模型，匹配人类偏好，偏好模型评分"}, {"metric": "后期步骤路径方差", "value": "保持在预期预算内，而基线系统性超出", "setting": "训练后期"}], "online_gains": []}

Conclusion: 发现多步去噪中重要性比漂移、扩散、裁剪差异等现象源于单一逐步骤量——路径方差（path variance），该量由采样器的高斯转移核精确决定且可廉价估计。；将不稳定性重新定义为可测量和预算的资源，而非需要逐一修复的症状集合。；提出 λ-Controlled GRPO：基于预测的路径方差律而非含噪经验统计来校准重要性比行为，并根据预测成本分配各去噪步骤的梯度努力。；控制更新的两个尺度由标准策略选择固定，而非引入自由调参参数。

Abstract: Reinforcement learning is increasingly used to align image generators with reward signals, and Flow-GRPO recently extended this paradigm to flow-matching models by treating the denoising sampler as a stochastic policy that can be optimized from reward feedback. Training in this setting is unstable in a way specific to multi-step denoising: the policy update changes systematically across denoising steps, with importance ratios drifting below one, becoming increasingly dispersed, clipping at different rates, and leaving fewer usable samples late in training. Prior work treats these effects as separate failure modes and addresses each with a hand-tuned stabilizer. We show instead that they arise from a single per-step quantity, which we call path variance. This quantity is determined exactly by the sampler's Gaussian transition kernel and can be estimated cheaply during training. This reframes instability as a resource that can be measured and budgeted rather than a collection of symptoms to repair. Our method, $\lambda$-Controlled GRPO, calibrates importance-ratio behavior from this predicted law rather than from noisy empirical statistics, and allocates gradient effort across denoising steps according to their predicted cost. The two scales governing the update are fixed by standard policy choices rather than introduced as free tuning parameters. On a text-to-image model under two reward settings, rendering difficult target text scored by optical character recognition and matching human preferences scored by a preference model, $\lambda$-Controlled GRPO improves both text accuracy and preference reward over the strongest empirical stabilizer. It also keeps late-step path variance within its intended budget, precisely where the baseline systematically overshoots. The result is a Flow-GRPO update calibrated by its own transition law rather than stabilized after instability appears.

</details>


### [52] [ExpBoN: Exponential-Noise Best-of-$n$ for Efficient Test-Time LLM Alignment](https://arxiv.org/abs/2609.21899)
*Liu, Yanxiao,Wan, Sicheng,GÃ¼ndÃ¼z, Deniz*

Main category: cs.LG

TL;DR: ExpBoN提出基于指数噪声的软Best-of-n推理时对齐方法，具有快速收敛的理论保证，并与引导投机推理结合为ExpGSI，在多个数学和STEM推理任务上以14%-45%的计算节省实现可比精度。


<details>
  <summary>Details</summary>
Motivation: Best-of-n采样在推理时对齐中难以精细控制奖励与分布偏移的权衡，且硬最大化导致收敛慢、计算成本高。

Method: ExpBoN基于指数噪声的report-noisy-max机制：从n个候选响应中，为每个候选添加独立指数噪声，然后选择加噪后奖励最大的候选。该方法可视为一种软Best-of-n，通过噪声控制奖励最大化与分布偏移的权衡。理论上，ExpBoN具有精确的有限n分解，表明其采样分布与KL正则化奖励最大化的最优分布之间的总变差距离、期望奖励差距以及KL散度均以指数速度收敛。ExpGSI进一步将ExpBoN嵌入引导投机推理（GSI）框架：GSI利用一个草稿模型生成候选序列，目标模型进行验证/重排序；ExpGSI在重排序阶段应用ExpBoN选择最终输出，从而减少所需的前向计算量。实验在MATH500、MMLU-STEM和Minerva Math上评估Qwen2.5-Math和Qwen3模型，通过估计计算量度量效率，显示ExpGSI在不同候选预算下减少14%-39%的计算量（Qwen2.5-Math），在n=16时最多减少45%（Qwen3），同时保持与基线相当的准确率。

Result: {"offline_gains": [{"metric": "估计计算量减少", "value": "14%-39%", "setting": "MATH500, MMLU-STEM, Minerva Math数据集，Qwen2.5-Math系列模型，不同候选预算n"}, {"metric": "估计计算量减少", "value": "最高45%", "setting": "n=16时，Qwen3模型系列"}], "online_gains": []}

Conclusion: 提出ExpBoN：基于指数噪声report-noisy-max机制的软Best-of-n方法，提供精确有限n分解，在总变差、期望奖励和双向KL散度上指数快速收敛。；对ExpBoN的收敛性和regret行为进行全面理论分析。；将ExpBoN集成到引导投机推理（GSI）框架中，形成ExpGSI，在保持精度的同时大幅降低计算成本。

Abstract: Best-of-$n$ (BoN) sampling is a simple yet effective inference-time alignment method, but hard maximization provides only coarse control over the trade-off between reward and distribution shift. Soft Best-of-$n$ (Verdun et al. 2025) provides smoother control and converges to the optimal distribution associated with KL-regularized reward maximization. In this paper, we introduce ExpBoN, an alternative soft BoN method based on the exponential-noise report-noisy-max mechanism. It admits an exact finite-$n$ decomposition, which yields exponentially fast convergence in total variation, expected reward, and both directions of KL divergence. We provide comprehensive theoretical analyses of its convergence and regret behavior. We further integrate ExpBoN into the guided speculative inference (GSI) framework (Geuter, Mroueh, and AlvarezMelis 2025), resulting in ExpGSI, for efficient reward-guided LLM alignment. ExpGSI yields substantial reductions in computational cost while maintaining comparable accuracy. Experiments on MATH500, MMLU-STEM, and Minerva Math with the Qwen2.5-Math and Qwen3 model families show that ExpGSI reduces estimated computation by $14\%$-$39\%$ across candidate budgets for Qwen2.5-Math and by up to $45\%$ at $n=16$ for Qwen3. Overall, our results provide a theoretical and algorithmic foundation for exponential-noise BoN and efficient test-time LLM alignment.

</details>


### [53] [LLMs as Feature Engineers for Text-and-Tabular Prediction](https://arxiv.org/abs/2609.21894)
*Barlier, Merwan,Skrlj, Blaz*

Main category: cs.LG

TL;DR: 该论文提出一种迭代式LLM框架，自动从非结构化文本中提取可解释、模式绑定的类别特征以提升表格预测。通过生成器LLM提出语义定义、提取器LLM物化特征、下游模型评估性能，并利用模型误差的自然语言反馈引导搜索，在三个公开数据集上实现最高3倍的特征发现加速和严格的多视图互补性能。


<details>
  <summary>Details</summary>
Motivation: 如何自动化地从非结构化文本中提取可解释、模式绑定的类别特征，以提升表格预测模型的性能，同时保证实例级可解释性。

Method: 框架包含三个核心组件，按迭代顺序运行：1）生成器LLM：接收当前特征集和模型误差反馈，提出新的语义特征定义（如类别标签及其描述）；2）提取器LLM：根据生成的定义，从非结构化文本中为每个实例提取具体的特征值，物化为表格列；3）下游表格模型：使用包含新特征的表格数据训练并评估预测性能，计算AUC等指标。系统将模型误差（如AUC排序反转）翻译为自然语言反馈，指导生成器LLM在下一轮迭代中修正特征定义。迭代持续进行，直到性能收敛或达到预设轮数。最终特征集与TF-IDF和稠密嵌入结合，形成多视图输入。推理时，对于新实例，提取器LLM根据已学习的特征定义从文本中抽取特征值，输入表格模型进行预测。目标函数是最大化下游表格模型的预测性能（如AUC），同时保证特征的可解释性（通过模式绑定和SHAP分析验证）。

Result: {"offline_gains": [{"metric": "特征发现速度", "value": "最高3倍加速", "setting": "与无引导搜索相比，在三个公开数据集上的评估"}, {"metric": "预测性能", "value": "严格优于任何子集", "setting": "生成特征与TF-IDF和稠密嵌入结合的多视图互补实验"}], "online_gains": []}

Conclusion: 迭代式生成-提取-评估框架：生成器LLM提出语义特征定义，提取器LLM物化特征，下游表格模型评估性能。；基于模型误差的自然语言反馈回路：将AUC排序反转等显式误差转化为自然语言，引导LLM针对性解决预测失败。；模式绑定的可解释特征：生成的特征在SHAP重要性中占主导，提供透明的语义审计路径。

Abstract: We introduce an iterative framework that automates the extraction of interpretable, schema-bound categorical features from unstructured text for tabular prediction models. To navigate the feature space, a generator LLM proposes semantic definitions, a separate extractor LLM materializes the features, and a downstream tabular model evaluates their predictive performance. We optimize this search by translating explicit model errors, such as AUC ranking inversions, into natural-language feedback, steering the LLM to resolve specific predictive failures. Evaluated across three public datasets, this error-driven loop accelerates feature discovery by up to $3\times$ compared to unguided search. Empirically, the generated features demonstrate strong multi-view complementarity, strictly outperforming any subset when combined with TF-IDF and dense embeddings. Finally, the framework guarantees instance-level interpretability: the discovered features dominate SHAP importance rankings and provide a fully transparent, semantic audit trail for every prediction.

</details>


### [54] [The Weight Is Over - Interactive Diffusion on Consumer GPUs](https://arxiv.org/abs/2609.21849)
*Ganz, Frieder,MÃ¼ller, Maximilian*

Main category: cs.LG

TL;DR: Adobe 与 NVIDIA 合作的端侧扩散推理系统论文，提出嵌入翻译器以用小文本编码器替代大编码器来降低权重与延迟，给出速度/质量/内存权衡的可复现扫描方法，并在消费级 GPU 上实现亚秒级 TTFI 的交互式图像生成与编辑。


<details>
  <summary>Details</summary>
Motivation: 扩散流水线内存占用高、延迟敏感，且需要编排 embedder、transformer、decoder 与后处理，难以像 LLM 推理循环那样标准化；需要在性能、质量与模型占用之间取得平衡，以覆盖尽可能多的客户端设备。

Method: 扩散流水线由文本嵌入器（embedder）、扩散 transformer、解码器（decoder）以及可能的后处理模块组成。为降低端侧部署的权重与延迟，作者引入嵌入翻译器，将一个小型文本编码器的输出映射到大型文本编码器的嵌入空间，从而在保持与原有大编码器兼容的条件下仅需加载小编码器并额外做一次轻量翻译。整体流程为：用户输入文本与交互编辑信号 → 小型文本编码器产生嵌入 → 嵌入翻译器将其对齐到大型编码器空间 → 扩散 transformer 在潜空间进行去噪迭代 → 解码器将潜表示还原为图像 → 可选后处理输出。论文关注在端侧消费级 GPU 上编排这些异构模块，通过可复现的扫描配方在速度、质量、内存三者之间选取配置点，目标是在有限显存与算力下达到亚秒级 TTFI 的交互式编辑体验。具体训练目标、损失函数、模块参数量与硬件配置未在摘要中披露。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 嵌入翻译器：将小型文本编码器映射到大型编码器空间，以减少权重与延迟；可复现的速度/质量/内存三角权衡扫描配方；交互式端侧图像生成编辑器，在近期 GPU 上实现亚秒级 TTFI

Abstract: On-device inference is booming, but the momentum is almost all in language models. Diffusion pipelines are memory hungry, latency-sensitive, and require orchestrating an embedder, a transformer, a decoder, and often further postprocessing that is not as standardized as LLM inference loops are. We navigate the trade-off between performance, quality, and model footprint to reach as many client devices in the wild as possible. We make three contributions: an embedding translator that maps a small text encoder into a large encoder space to cut weight and latency; a reproducible sweep recipe for navigating the speed/quality/memory triangle in diffusion pipelines; and an interactive on-device image generation editor achieving sub-second TTFI on recent GPUs.

</details>


### [55] [GraphSkillEvo: Evolutionary Optimization of Graph-Structured Agent Skills](https://arxiv.org/abs/2609.21749)
*Sun, Rui,Zheng, Zhi,Wang, Zhenkun,Lu, Zhichao*

Main category: cs.LG

TL;DR: GraphSkillEvo 提出将 Agent 技能表示为图结构化的自然语言工件（节点为执行步骤，有向边为上下文转移），并基于该表示设计种群进化优化框架，利用变异和交叉算子探索结构化技能空间。在五个 Agent 基准上，相比 SkillOpt 基线，GPT-5.4-nano 平均准确率提升 4.01%，GPT-5.4 提升 1.76%。


<details>
  <summary>Details</summary>
Motivation: 现有技能优化方法将技能表示为非结构化自然语言指令，缺乏显式工作流指导且存在大量冗余，同时无约束的自然语言搜索空间过大，导致技能优化效率低下，LLM Agent 难以有效执行。

Method: GraphSkillEvo 将技能表示为图结构化的自然语言工件：每个节点代表一个执行步骤及其操作指导（自然语言描述），有向边编码步骤之间上下文相关的转移条件，从而形成显式的工作流级指导。基于该表示，框架采用基于种群的进化优化：维护多个候选图结构技能，通过针对图结构的变异（如修改节点内容、增删节点、改变边连接）和交叉（如交换或组合不同候选技能的子图）算子生成新候选；在每一代中，根据在任务基准上的执行表现评估候选技能，选择有效组件进行组合，从而在结构化的技能空间中更广泛、更全面地探索。推理时，LLM Agent 按照优化后的图结构技能中定义的步骤和转移条件执行任务。训练目标本质上是最大化技能在任务上的执行准确率，通过进化过程迭代优化技能结构，而非更新 LLM 参数。论文未披露模型参数规模、训练硬件或推理成本细节。

Result: {"offline_gains": [{"metric": "average accuracy", "value": "提升 4.01%", "setting": "GPT-5.4-nano 模型，五个 Agent 基准，相比基线 SkillOpt"}, {"metric": "average accuracy", "value": "提升 1.76%", "setting": "GPT-5.4 模型，五个 Agent 基准，相比基线 SkillOpt"}], "online_gains": []}

Conclusion: 提出图结构化的自然语言技能表示，节点表示执行步骤及其操作指导，有向边编码上下文相关的步骤转移，提供显式的工作流级指导并减少冗余；基于图结构技能表示，提出 GraphSkillEvo 种群进化优化框架，包含针对图结构技能的变异和交叉算子，维护多个候选技能并组合有效组件，实现比纯 LLM 迭代自我精炼更广、更全面的结构化搜索

Abstract: Skills can improve the performance of Large Language Model (LLM) agents by providing task-specific procedural guidance, while skill optimization further improves their effectiveness through iterative refinement. However, existing skill optimization methods typically represent skills as unstructured natural-language instructions, creating two key challenges: 1) Unstructured skills often lack explicit workflow-level guidance and contain substantial redundancy, making them difficult for LLMs to execute; 2) the vast search space of unconstrained natural-language skills makes skill optimization ineffective. To address these challenges, we propose representing skills as graph-structured natural-language artifacts. In graph-structured skills, each node represents an execution step together with its operational guidance, while directed edges encode context-dependent transitions between steps. Compared to unstructured skills, graph-structured skills can provide clear workflow-level guidance. Moreover, the proposed graph-structured skill can also facilitate skill optimization. Building on this structured representation, we introduce GraphSkillEvo, a population-based evolutionary optimization framework with mutation and crossover operators for graph-structured skills. By maintaining multiple candidate skills and combining effective components, GraphSkillEvo enables broader and more comprehensive exploration of the structured skill space than purely LLM-based iterative self-refinement. Extensive experiments across five agent benchmarks demonstrate that GraphSkillEvo consistently outperforms the strong skill optimization baseline SkillOpt, improving average accuracy by 4.01% on GPT-5.4-nano and 1.76% on GPT-5.4. Our code is available at https://github.com/ruisun7/GraphSkillEvo.

</details>


### [56] [SpecQuant: Speculative Decoding with Multi-Parent Quantization for Adaptive LLM Inference](https://arxiv.org/abs/2609.21704)
*KB, Harish,M, Jagadeeswaran,P, Pradheep,S, Yuvanesh,T, Sivakumar*

Main category: cs.LG

TL;DR: SpecQuant提出了一种无需训练的自适应LLM推理框架，通过多父量化生成共享权重的INT4/FP8/FP16变体，并结合投机解码与动态路由，在Qwen2.5模型上实现了35-43%的加速且精度损失小于2%，适用于消费级硬件部署。


<details>
  <summary>Details</summary>
Motivation: 在消费级硬件上本地运行LLM受限于计算和内存，现有加速技术（量化、投机解码、自适应推理）通常需要重新训练、逐架构调优或额外的草稿模型，缺乏无需训练的通用高效方案。

Method: SpecQuant是一个训练无关的框架，结合了投机解码与多父量化。它从共享的基础模型（如Qwen2.5）中派生出多个量化变体：INT4、FP8和FP16。这些变体共享权重，因此无需单独的草稿模型即可保证投机解码的token接受率，避免了兼容性问题。在推理时，系统根据预测的查询复杂度动态路由：对于简单或事实性任务，使用轻量级量化变体（如INT4）进行快速推理；对于复杂推理任务或长上下文输入，则使用全精度模型（如FP16）以保证准确性。这种自适应路由机制实现了在多种硬件上的高效推理，无需特殊基础设施或专业知识。

Result: {"offline_gains": [{"metric": "speedup", "value": "35-43%", "setting": "Qwen2.5模型，MMLU、AlpacaEval、GSM8K基准测试"}, {"metric": "accuracy degradation", "value": "<2%", "setting": "同上基准测试"}], "online_gains": []}

Conclusion: 训练无关（training-free）框架，结合投机解码与多父量化；从共享基础模型派生多个量化变体（INT4、FP8、FP16），保证投机解码的token接受率且无需独立草稿模型；基于预测复杂度的动态查询路由，将简单/事实性任务导向轻量变体，复杂推理或长上下文输入使用全精度模型

Abstract: Running large language models (LLMs) locally continues to be limited by restrictions of compute and memory on consumer hardware. The popular acceleration technologies, such as quantization, speculative decoding, and adaptive inferencing, offer substantial speed boosts but usually necessitate retraining, per architecture tuning, or draft models. SpecQuant is a trainingfree framework, that combines speculative decoding with multiparent quantization to perform adaptive, efficient inference of LLMs. SpecQuant derives multiple quantized variants (INT4, FP8, FP16) from a shared base model, and dynamically routes queries based on predicted complexity; lightweight variants are used for simple or factual tasks, and full-precision models are used for complex reasoning tasks or long-context inputs. The shared-weight design of SpecQuant ensures sufficient token acceptance for speculative decoding without compatibility issues using separate draft parent models. We evaluate SpecQuant on Qwen2.5 based models on the MMLU, AlpacaEval, and GSM8K datasets, or benchmarks, demonstrating 35-43% speedups without degrading accuracy greater than 2%, substantial within the LLM community. SpecQuant enables practical on-device LLM deployment across diverse hardware without special infrastructure or expertise.

</details>


### [57] [Trading Depth for Time in Recurrent Transformers](https://arxiv.org/abs/2609.21605)
*Huang, Zeyi,He, Xuehai,Lee, Yong Jae,Shen, Yelong*

Main category: cs.LG

TL;DR: 论文比较循环Transformer中时间循环（latent thought token）与物理深度对性能的影响。在MoE NanoChat骨干上，1个thought token使较浅模型以约48%更少参数达到接近双倍深度模型的bits per byte，为参数高效的LLM架构设计提供了实证。


<details>
  <summary>Details</summary>
Motivation: 在循环Transformer中，增加计算量应该投入到更多的时间循环步数（latent thought token）还是更大的物理深度（更多Transformer层）？

Method: LRT在解码时每个词表token执行一次骨干前向传播。具体地，在连续词表token之间插入一个latent thought token，该thought token与词表token一样通过相同的L层Transformer块，共享骨干参数，从而在不增加物理深度的情况下增加一个隐状态精炼阶段。与无thought token的2L层LRT对比，两者在解码每个词表token时都执行2L个Transformer块，但thought-token模型使用更少的参数。训练目标仍为下一词预测，thought token的隐状态参与下一个词表token的预测。推理时，每个词表token生成后，额外执行一次L层前向传播计算thought token，再用于预测下一个词表token，从而以时间维度换取参数效率。

Result: {"offline_gains": [{"metric": "bits per byte差距", "value": "0.006 bits per byte", "setting": "16层MoE NanoChat骨干，1个thought token的LRT与2L层无thought token的LRT对比"}, {"metric": "bits per byte差距", "value": "0.004 bits per byte", "setting": "20层MoE NanoChat骨干，1个thought token的LRT与2L层无thought token的LRT对比"}, {"metric": "恢复改进比例", "value": "67%", "setting": "16层MoE NanoChat骨干，1个thought token相对双倍深度模型"}, {"metric": "恢复改进比例", "value": "81%", "setting": "20层MoE NanoChat骨干，1个thought token相对双倍深度模型"}, {"metric": "参数减少比例", "value": "约48%", "setting": "相比双倍深度模型的总参数量减少"}], "online_gains": []}

Conclusion: 提出Latent Recurrent Transformer（LRT），在连续词表token之间插入latent thought token，每个thought token与词表token共享相同的L层骨干参数，提供额外的隐状态精炼阶段。；在解码时每个词表token保持一次骨干前向传播，控制变量比较时间循环与物理深度。；在16层和20层MoE NanoChat骨干上，用1个thought token使较浅模型接近双倍深度对应模型的性能。

Abstract: Recurrent Transformers increase computational depth through temporal recurrence, feeding each token's high-level hidden state into the computation of the next. This raises a natural question: is additional computation better spent on more temporal steps or greater physical depth? We investigate this question using Latent Recurrent Transformers (LRTs), which retain one backbone forward pass per vocabulary token during decoding and provide a controlled setting for comparing these two ways of adding computation. Specifically, we insert a latent thought token between consecutive vocabulary tokens. Each thought token passes through the same $L$ layers as a vocabulary token, sharing the backbone parameters and providing an additional stage of hidden-state refinement before predicting the next token. We compare this $L$-layer LRT against a $2L$-layer LRT without thought tokens. Both execute $2L$ Transformer blocks per vocabulary token during decoding, but the thought-token model uses fewer parameters. On 16- and 20-layer mixture-of-experts NanoChat backbones, one thought token brings the shallower model within 0.006 and 0.004 bits per byte of its double-depth counterpart, recovering 67% and 81% of the improvement with approximately 48% fewer total parameters. These results suggest that temporal thinking offers a parameter-efficient alternative to increasing physical depth in recurrent Transformers.

</details>


### [58] [MACE: Memory-Agent Co-Evolution with Adaptive Memory Graphs for Multi-Agent Systems](https://arxiv.org/abs/2609.21533)
*Yang, Kairui,An, Minghao,Li, Xunkai,Yi, Ziheng,Chen, Zekai,He, Guangyuan,Li, Rong-Hua*

Main category: cs.LG

TL;DR: MACE提出记忆-智能体协同进化框架，通过自适应记忆图组织多智能体协作知识，在记忆预算下选择功能单元与呈现格式，并利用执行反馈更新记忆与选择策略。在八个基准上平均得分81.11%，优于最强基线SAGE的78.97%。


<details>
  <summary>Details</summary>
Motivation: 如何使LLM多智能体系统在协作过程中有效复用历史协作轨迹，同时保留动作前提与后续智能体所需输出，并根据执行反馈自适应调整记忆组织与使用方式。

Method: MACE是一个记忆-智能体协同进化框架。其MemGoG结构将多智能体协作轨迹中的功能单元表示为子图，每个子图包含条件、动作和输出节点，子图之间通过支持、冲突、修复三种关系连接。MACE Loop在每一步根据当前任务和记忆预算，从记忆图中选择相关的功能单元及其关系，并为每个智能体提供指令或检查清单格式的呈现（呈现格式的选择也通过学习决定）。智能体执行后产生的输出与任务结果被记录下来，用于更新功能单元的质量分数和关系权重，进而影响后续检索和呈现格式选择。整个流程在没有预先指定记忆组织的情况下，通过执行反馈不断优化记忆结构和智能体的记忆使用方式。

Result: {"offline_gains": [{"metric": "average score", "value": "81.11%", "setting": "八个基准上的平均得分，对比最强基线SAGE的78.97%"}], "online_gains": []}

Conclusion: MemGoG记忆图结构：将功能单元表示为条件、动作、输出的子图，并通过支持、冲突、修复关系连接。；MACE Loop：在记忆预算内选择任务相关单元与关系，并为每个智能体提供指令或检查清单两种呈现格式。；协同进化更新机制：记录所选单元、呈现格式、智能体输出和任务结果，更新单元分数与关系，用于检索和后续呈现选择。

Abstract: LLM-based multi-agent systems generate collaboration traces that record how agents plan tasks, verify intermediate results, and repair failures. Reusing these procedures requires preserving an action's prerequisites and the outputs needed by subsequent agents. Our empirical studies show that grouping these dependencies into functional memory units improves their retention, while connecting units increases retrieval of the units and links jointly required by a task. The preferred combination of units also changes between instructions and checklists, even when each combination's content is fixed across formats. Updating choices from the outcomes of each combination and format pairing outperforms scoring combinations and formats separately. These findings motivate MACE, a memory-agent co-evolution framework that adapts memory organization and agent memory use through execution feedback. Its MemGoG structure represents functional units as subgraphs of related conditions, actions, and outputs, connecting them through support, conflict, and repair relations. MACE Loop selects task-relevant units and relations within a memory budget and provides each agent with instructions or checklists for its current operation. It records the selected units, presentation formats, agent outputs, and task outcomes to update unit scores and relations for retrieval and inform subsequent presentation choices. Across eight benchmarks, MACE outperforms ten baselines with an average score of 81.11%, compared with 78.97% for the strongest baseline, SAGE.

</details>


### [59] [OpenMAS-GCom. A Diagnostic Benchmark for Graph-enhanced Multi-Agent Systems](https://arxiv.org/abs/2609.21527)
*Yang, Kairui,Li, Xunkai,Zhang, Kaixiang,An, Minghao,Chen, Zekai,Ba, Yuxuan,Li, Rong-Hua*

Main category: cs.LG

TL;DR: OpenMAS-GCom是一个诊断图增强多智能体系统的评测基准，通过固定任务/模型/提示/预算并只修改一个组件（通信边、专家或批评者角色、中间消息、worker执行）来归因性能差异；评测17种配置、29个数据集、6个领域，并新增400个跨文档整合与冲突消解任务，同时报告准确率与每token准确率。


<details>
  <summary>Details</summary>
Motivation: G-MAS的最终分数比较混合了模型、通信模式、角色与计算成本的差异，导致性能差异难以归因到具体的通信结构、角色分配与信息流；即评估归因问题。

Method: OpenMAS-GCom不是一个新的模型或训练系统，而是一个诊断性评测框架。其表示层将G-MAS形式化为四类要素：协作单元（即承担特定职责的智能体，如专家或批评者）、通信链路（定义智能体之间信息传递方向的图边）、共享中间信息（在链路间传递的中间消息内容）以及执行规则（决定智能体何时被调度、何时被禁用）。在此基础上，基准通过受控干预构造对比组：对原始系统仅修改一个组件，同时固定任务、模型、提示与预算上限，从而把最终分数的差异归因到被修改的该组件。具体干预包括：重连通信边以改变通信拓扑；移除专家智能体或批评者智能体以改变角色分配；用错误内容替换中间消息以切断或污染信息流；在执行过程中禁用worker以模拟执行故障。评测对象覆盖三类配置：单智能体、普通多智能体、图增强多智能体，共17种配置，在29个数据集、6个领域上运行。任务输入方面，除既有数据集外，还构造了400个G-MAS-Complex任务，要求智能体跨多文档整合信息、消解冲突记录，并输出带有来源标识的指定值，从而对信息聚合与冲突处理能力施加压力。评测输出包括准确率与accuracy per token，后者将计算成本纳入比较，避免仅凭最终分数排序。实验结论层面的数据流是：先测原始系统得分，再测单组件修改后的得分，通过损失差值解释各组件贡献，例如专家移除比批评者移除带来更大的平均损失，错误消息与worker失败虽在原始分数上相似但退化模式不同，以及在G-MAS-Complex上最高准确率与最高每token准确率由不同配置获得。

Result: {"offline_gains": [{"metric": "专家移除后的平均性能损失", "value": "大于批评者移除后的平均损失", "setting": "受控干预对比，固定任务、模型、提示与预算"}, {"metric": "错误消息与worker失败下的性能退化", "value": "两者退化模式不同，尽管原始分数相似", "setting": "受控干预对比"}, {"metric": "G-MAS-Complex上的最高准确率与每token准确率", "value": "由不同配置分别取得", "setting": "400个G-MAS-Complex任务"}], "online_gains": []}

Conclusion: 提出OpenMAS-GCom诊断基准，以协作单元、通信链路、共享中间信息与执行规则形式化G-MAS。；通过单组件受控干预（重连通信边、移除专家或批评者、替换错误中间消息、禁用worker）对比原系统与修改版本，固定任务、模型、提示与预算。；在29个数据集、6个领域上评测17种单智能体、普通多智能体与图增强配置。；新增400个G-MAS-Complex任务，要求跨多文档整合信息、消解冲突记录并返回指定值与来源标识。；引入accuracy per token作为兼顾计算成本的诊断指标。

Abstract: Graph-enhanced multi-agent systems (G-MAS) coordinate large language model agents through communication graphs and role assignments, which determine how agents exchange information and divide responsibilities. However, final-score comparisons across systems combine differences in models, communication patterns, roles, and computation costs, making performance differences difficult to attribute to specific communication structures, role assignments, and information flows. To address this evaluation attribution problem, we introduce OpenMAS-GCom, a benchmark for diagnosing how these components affect G-MAS performance through controlled interventions. We represent systems through collaboration units, communication links, shared intermediate information, and execution rules. OpenMAS-GCom compares original systems with versions modified by changing one component while keeping tasks, models, prompts, and budget limits fixed. We rewire communication edges, remove specialist or critic agents, replace intermediate messages with incorrect content, and disable workers during execution. The benchmark evaluates 17 single-agent, ordinary multi-agent, and graph-enhanced configurations on 29 datasets across six domains. We add 400 G-MAS-Complex tasks requiring agents to combine information from multiple documents, resolve conflicting records, and return specified values with source identifiers. Experiments show larger mean losses after specialist removal than after critic removal, different performance degradation under incorrect messages and worker failures despite similar original scores, and different configurations achieving the highest accuracy and accuracy per token on G-MAS-Complex.

</details>


### [60] [What Must Survive? Exact Task-Information--State Frontiers for Resource-Sufficient Learning](https://arxiv.org/abs/2609.21523)
*Katende, Ronald*

Main category: cs.LG

TL;DR: 论文从信息论与线性代数角度刻画了在任务信息部分提前到达时，压缩状态所需的最小保留维度，给出了精确秩前沿、近似前沿、下界与 NP 难性，并用注意力压缩、数字孪生和多任务模型三个例子说明其实际意义。


<details>
  <summary>Details</summary>
Motivation: 在下游任务仅部分已知（先收到有限任务信息，之后才暴露精确任务）时，压缩系统必须保留多少状态，以及有限提前任务信息最多能节省多少状态。

Method: 论文考虑如下设定：存在有限个线性任务 T_U（U 为任务索引集），系统在形成压缩状态前先收到一条任务建议信息（advice，字母表大小 K），之后才收到精确任务 C。目标是确定在给定 advice 下必须保留的最小状态维度。核心结果是精确前沿 p*(K) = min_{分区 P, |P|≤K} max_{C∈P} rank(T_C)，即把任务集划分为至多 K 个子集，每个子集对应一个低秩联合任务算子，状态维度至少为各子集最大秩。b 比特 advice 对应 K = min(2^b, |U|)。进一步给出近似奇异值前沿（允许近似误差时的状态-信息权衡）、共同核心下界（所有任务共享的核心维度下界）以及精确直和定律（组合任务的状态需求可加）。计算复杂性方面，证明寻找最优 advice 划分是强 NP 难的，且在任何固定正近似容差下仍难。三个示例展示了该理论：1) 软注意力构造，9 比特 advice 将 524,288 个坐标压缩到 1,024 个；2) 域分解数字孪生，界面加局部状态；3) 分层多任务模型，两阶段前沿，3 比特信息使状态从 3136 降至 448，最终趋近 328。推理路径为：收到 advice 后选择对应分区，根据该分区的联合任务算子秩确定所需状态维度，再接收精确任务并执行线性任务。

Result: {"offline_gains": [{"metric": "状态坐标数", "value": "524,288 → 1,024", "setting": "条件良好的 softmax 注意力构造，9 比特建议信息区分 512 种延续之一"}, {"metric": "状态坐标数", "value": "3136 → 448", "setting": "分层多任务模型，3 比特任务信息"}, {"metric": "状态坐标数", "value": "328", "setting": "单任务不可约下限"}], "online_gains": []}

Conclusion: 对有限线性任务族，精确刻画给定建议字母表大小 K 的状态秩前沿 p*(K)=min_{P} max_{C∈P} rank(T_C)。；给出 b 比特前沿，通过 K=min(2^b,|U|) 关联比特预算与状态秩。；提出近似奇异值前沿、共同核心下界与精确直和定律。；证明寻找最优建议划分的强 NP 难性，且该难性在所有固定正近似容差下持续存在。；用条件良好的 softmax 注意力构造给出 524,288 到 1,024 坐标的精确前沿（9 比特区分 512 种延续之一）。；域分解数字孪生给出界面加局部状态定律及异构区域的加权划分问题。；分层多任务模型给出两阶段前沿：3 比特将所需状态从 3136 坐标降至 448 坐标，更多任务信息可接近不可约的 328 坐标单任务下限。

Abstract: A system may be compressed before its downstream task is fully known. We ask how much retained state is then necessary and how much can be saved by limited advance task information. For a finite family of linear tasks, a task message is revealed before state formation and the exact task only afterwards. For an advice alphabet of size $K$, the exact frontier is \[ p^*(K)= \min_{\substack{\Pcal\text{ partition of }\U\\|\Pcal|\le K}} \max_{C\in\Pcal}\rank(T_C), \] with the $b$-bit frontier obtained by setting $K=\min(2^b,|\U|)$. Thus advance task information reduces state through partitions whose joint task operators have low rank. We also give an approximate singular-value frontier, a common-core lower bound and exact direct-sum law, and strong NP-hardness of finding an optimal advice partition. The hardness persists at every fixed positive approximation tolerance. Three examples illustrate the result. A well-conditioned softmax attention construction gives an exact $524{,}288\to1{,}024$ coordinate frontier when nine bits resolve one of $512$ continuations. A domain-decomposed digital twin yields an interface-plus-local-state law and a weighted partition problem for heterogeneous regions. A hierarchical multi-task model gives a two-stage frontier in which three bits reduce the required state from $3136$ to $448$ coordinates, with further task information approaching the irreducible $328$-coordinate single-task floor.

</details>


### [61] [Understanding LLM Quantization through Activation-Guided Compensation and Orthogonal Residuals](https://arxiv.org/abs/2609.21450)
*Narita, Yamato,Sato, Issei*

Main category: cs.LG

TL;DR: 该论文通过精确误差分解统一理解W4A4量化中的权重补偿与正交残差，导出随机Hadamard旋转、符号选择与通道缩放的实用指南，在8个Llama/Mistral模型上无梯度训练达到与SpinQuant相当的性能。


<details>
  <summary>Details</summary>
Motivation: 激进W4A4后训练量化中激活异常值导致量化分辨率下降，现有方法（权重优化、通道缩放、正交旋转）的误差成分及关系不明确。

Method: 论文针对LLM后训练量化提出理论框架：首先将局部权值-激活量化误差精确分解为激活引导的权重补偿项和正交残差，利用持续通道离群值与规则激活量得到残差上界。基于该上界，分析随机Hadamard旋转中随机符号如何抑制持续离群通道间的构造性干涉，并推导二阶矩平衡下的L2缩放规则，松弛后恢复SmoothQuant式L∞缩放。实际量化流程为无反向传播配置：应用随机Hadamard旋转、符号选择和通道缩放，通过采样多符号模式选择较优变换。在8个Llama和Mistral模型上评估，性能与梯度训练的SpinQuant相当。

Result: {"offline_gains": [{"metric": "性能", "value": "与梯度训练的SpinQuant相当", "setting": "8个Llama和Mistral模型，无反向传播配置"}], "online_gains": []}

Conclusion: 将局部权值-激活量化误差精确分解为激活引导的权重补偿项与正交残差，并用持续通道离群和规则激活量给出残差上界；基于残差界导出随机Hadamard旋转、符号选择与通道缩放的实用指南，解释随机符号抑制持续离群通道的构造性干涉；提出多符号模式采样改进变换选择，并证明二阶矩平衡推出L2缩放规则，进一步松弛可恢复SmoothQuant式L∞缩放

Abstract: Post-training weight-activation quantization reduces the memory and inference costs of large language models, but aggressive W4A4 quantization remains difficult because activation outliers degrade effective quantization resolution. Although weight optimization, channel-wise scaling, and orthogonal rotation mitigate this problem, the error components they address and their relationship remain unclear. Using an exact decomposition of local weight-activation quantization error into an activation-guided weight compensation term and an orthogonal residual, we bound the residual using persistent channel-wise outlier and regular activation quantities. This decomposition clarifies which error components can be addressed by weight compensation and which require transformation design. We then use the residual bounds to derive practical guidelines for applying randomized Hadamard rotation, sign selection, and channel scaling. In particular, the analysis explains how random signs suppress constructive interference among persistent outlier channels, how sampling multiple sign patterns can improve transformation selection, and how second-moment balancing leads to an $L_2$ scaling rule while a further relaxation recovers SmoothQuant-style $L_\infty$ scaling. We evaluate these guidelines through backpropagation-free configurations across eight Llama and Mistral models, obtaining performance competitive with gradient-trained SpinQuant.

</details>


### [62] [IntBMoE: Integrating Block-Level Conditioning into Expert Composition for Full-Participation Mixture-of-Experts](https://arxiv.org/abs/2609.21346)
*Cheng, Ran,Xu, Longfei,Liu, Zheng,Liu, Kaikui,Chu, Xiangxiang*

Main category: cs.LG

TL;DR: IntBMoE是一种新型混合专家架构，通过块级条件与稀疏块执行，解耦专家参与度、计算量和存储量，在图像分类、语言建模和推荐任务上验证有效，并在AMap生成式推荐系统中全量部署，在线A/B测试取得2.4%相对UVCTR提升。


<details>
  <summary>Details</summary>
Motivation: 现有MoE设计无法独立控制每个token的专家参与度、执行计算量和参数存储量，导致稀疏路由参与度低或密集执行计算成本高。

Method: IntBMoE采用块条件MoE架构，核心创新在于块（block）的概念。块由一个小型学习码本生成，每个码本条目对应一个块。在每个内部层，轻量级超网络将该层专家池中的所有专家基合并为一个组合专家。每个组合专家从整个专家池中抽取知识，因此参与度是满的；但路由器只将每个token发送到少数几个块，从而保持执行稀疏；而块的数量由码本固定，不依赖输入，因此物化参数存储有界。此外，双路径残差门控（DPRG）通过乘法门控耦合两条独立组合路径，增强模型表达能力。训练时，模型学习码本和超网络参数，优化目标为任务损失（如图像分类、语言建模或推荐）。推理时，对于每个token，路由器选择少量块，然后由超网络动态生成组合专家进行计算，在保证满参与度的同时降低计算和存储成本。

Result: {"offline_gains": [], "online_gains": [{"metric": "UVCTR", "value": "2.4% 相对提升", "setting": "在线A/B测试，AMap生成式推荐系统"}]}

Conclusion: 块条件MoE (Block-Conditioned MoE)：通过小型学习码本生成块，轻量超网络合并每个内部层的所有专家基，实现密集专家组合但稀疏块执行，解耦参与度、执行和物化。；双路径残差门控 (DPRG)：通过乘法门控耦合两条独立组合路径。

Abstract: Mixture-of-Experts (MoE) scales capacity, but existing designs cannot set three quantities independently. For a single token, participation is how many experts contribute knowledge to its output, execution is how many are actually computed (compute cost), and materialization is how many expert-sized parameter sets must be built and stored (memory cost). Sparse routing keeps execution and materialization low, but shrinks participation: for each token, only a few experts contribute. Dense output-mixing restores full participation, but its execution grows with the number of experts. Parameter-merging keeps execution at one expert, but its materialization grows with the number of routing decisions. We propose IntBMoE, a block-conditioned MoE that decouples all three by pairing dense expert composition with sparse block execution. Its blocks come from a small learned codebook, one per entry. At each internal layer, a lightweight hypernetwork merges all expert bases in that layer's pool into one composed expert. Participation is full, because every composed expert draws on the entire pool. Execution stays sparse, because a router sends each token to only a few blocks. Materialization is bounded, because the codebook, not the input, fixes how many blocks exist. Dual-Path Residual Gating (DPRG) further couples two independently composed paths through multiplicative gating. Experiments on image classification show consistent gains over representative sparse and dense MoE baselines. Additional experiments on language modeling and sequential recommendation validate its generalization beyond vision. IntBMoE is fully deployed in AMap's generative recommendation system, serving hundreds of millions of users under a 60ms latency budget, with a 2.4% relative UVCTR gain in online A/B testing. Our code is available at https://github.com/AMAP-ML/DreamX-Rec/.

</details>


### [63] [An Introduction to Compression-Based Machine Learning](https://arxiv.org/abs/2609.21309)
*Hurwitz, John,Raff, Edward,Nicholas, Charles K.*

Main category: cs.LG

TL;DR: 系统综述并形式化压缩与机器学习互为转化的方法论，提出压缩基ML设计框架，在常规分类基准上与基线竞争，在恶意软件检测上显著更强，设计选择最高带来0.62准确率增益。


<details>
  <summary>Details</summary>
Motivation: 如何系统化地将无损压缩算法（如gzip）与机器学习分类器相互转化，并厘清不同设计选择对性能的影响。

Method: 论文以信息论为出发点构建压缩与机器学习之间的双向映射。正向路径：任意无损压缩算法（如gzip/LZ77）可定义归一化压缩距离（NCD），或依据最小描述长度（MDL）原则构建分类器——输入样本与各类模板拼接后压缩，以压缩长度作为距离或码长度，最近类或最短描述长度类作为预测输出。反向路径：任意自回归模型（包括Transformer语言模型）可通过算术/熵编码转化为无损压缩器——模型输出下一token概率分布，熵编码器依此将序列编码为接近交叉熵下界的比特流，因此压缩率与模型预测性能直接相关。设计框架将压缩方法拆解为压缩器选择、距离/编码方式、分类聚合策略、预处理等设计维度，在分类基准（常规数据集与恶意软件数据集）上进行系统性消融，揭示不同选择对准确率的影响，最高可达0.62的增益。论文以综述加形式化为主，未披露具体训练资源、推理硬件或部署规模。

Result: {"offline_gains": [{"metric": "accuracy", "value": "up to 0.62 improvement", "setting": "varying compression-based ML design choices on classification benchmarks"}], "online_gains": []}

Conclusion: 提出压缩基机器学习的设计框架，形式化NCD与MDL两种转化路径；将自回归模型通过熵编码逆向转化为无损压缩器的循环依赖关系梳理；通过设计空间遍历实验发现设计选择可带来最高0.62的准确率增益

Abstract: Any lossless compression algorithm (like gzip) may be converted into a machine learning method, via either Normalized Compression Distance or the Minimum Description Length principle. Any auto-regressive model may be converted into a lossless compression method via entropy coding. This seemingly circular dependence has unrealized potential in modern artificial intelligence and machine learning, and we survey and formalize the various strategies that have been used to leverage compression for machine learning. We introduce and empirically validate a design framework for compression-based ML, finding compression-based methods competitive with conventional baselines and decisively stronger on malware. We find that varying these design choices yields accuracy gains of up to 0.62.

</details>


### [64] [TierKV: Long-Context On-Device LLMs via Predictive Multi-Tier KV Caching](https://arxiv.org/abs/2609.21172)
*Shu, Zhihao,Sanim, Md Musfiqur Rahman,Hu, Jie,Yuan, Kun,Qin, Minghai,Agrawal, Gagan,Niu, Wei*

Main category: cs.LG

TL;DR: TierKV提出预测性多层级KV缓存优化（PMCO），针对移动端长上下文LLM推理的KV缓存显存瓶颈，在解码前预测缓存需求并联合分配token到精确、低秩、闪存卸载三层，用闭合形式求解器运行时选边界与秩；在三个移动SoC上八种模型实现最高17.6倍prefill吞吐提升，RAM驻留KV缓存减少12.5-34%，精度损失轻微。


<details>
  <summary>Details</summary>
Motivation: 移动设备上长上下文LLM推理时，KV缓存随序列长度线性增长并每步解码被访问，成为主导的显存瓶颈；已有低秩压缩、token驱逐、闪存卸载等方法存在重建开销、不可逆token丢失或I/O停顿问题。

Method: TierKV是移动端LLM推理框架，核心为预测性多层级缓存优化（PMCO）。在解码开始前，PMCO从prefill阶段的隐藏状态预测未来的缓存需求，然后在设备内存预算和精度预算的约束下，联合将token分配到三个层级：exact（精确存储）、low-rank（低秩压缩存储）和flash-offloaded（闪存卸载）。该形式化保留了完整上下文访问，消除了反应式驱逐的循环依赖，并允许闭合形式求解器在运行时选择层级边界和每层秩。推理时解码步骤从这些层级中按分配访问KV，从而在有限内存下支持更长上下文，提高prefill吞吐并减少RAM驻留KV缓存。

Result: {"offline_gains": [], "online_gains": []}

Conclusion: 提出预测性多层级缓存优化（PMCO），在解码前从prefill隐状态预测未来缓存需求；在设备内存与精度预算下，联合将token分配到exact、low-rank、flash-offloaded三个层级；闭合形式求解器在运行时选择层级边界与每层秩，避免反应式驱逐的循环依赖；保留完整上下文访问，同时降低RAM驻留KV缓存

Abstract: Large language models (LLMs) are moving onto mobile devices for increasingly diverse workloads over text, images, video, and audio. These applications often require long contexts, making the Key-Value (KV) cache a dominant memory bottleneck because it grows linearly with sequence length and is accessed at every decoding step. Prior work reduces KV-cache footprint through low-rank compression, token eviction, or flash offloading, but the resulting reconstruction overhead, irreversible token loss, or I/O stalls can offset the benefit of saving memory. We present TierKV, a mobile LLM inference framework built on Predictive Multi-Tier Cache Optimization (PMCO). Before decoding starts, PMCO predicts future cache demand from prefill hidden states and jointly assigns tokens to exact, low-rank, and flash-offloaded tiers under the device memory and accuracy budgets. This formulation retains access to the full context, removes the circular dependency of reactive eviction, and admits a closed-form solver that selects tier boundaries and per-layer ranks at runtime. Across eight text, vision, and audio models on three mobile SoCs, TierKV improves prefill throughput by up to 17.6x over existing mobile LLM frameworks, reduces RAM-resident KV cache by 12.5-34%, thereby enabling substantially longer contexts under the same memory budget, while incurring only minor accuracy degradation.

</details>


### [65] [Stiefel-AdamW: Geometry-Aware AdamW for Linear Factorization Blocks](https://arxiv.org/abs/2609.21039)
*Zangrando, Emanuele,Sutti, Marco,Tudisco, Francesco*

Main category: cs.LG

TL;DR: Stiefel-AdamW是一种面向线性分解块（LoRA、低秩压缩层、QK^T）的几何感知优化器：将一因子约束在Stiefel流形、另一因子保持Euclidean，保留AdamW逐坐标预条件的同时消除因子爆炸，实现开销极小。在GPT2、ViT、Mistral 7B的LoRA微调和GPT2全量预训练上报告一致改进，但未披露具体指标数值与生产部署，适合作为LLM微调/低秩压缩训练的优化器改进参考。


<details>
  <summary>Details</summary>
Motivation: 线性分解块 W=BA 具有非唯一分解（GL(r)规范对称性），标准Euclidean优化器（如AdamW）忽略其几何结构，易导致因子爆炸、训练不稳定并限制可用学习率。

Method: 该工作针对深度学习中普遍存在的线性分解块 W=BA（两个参数矩阵直接相乘、无非线性），例如LoRA适配器、低秩压缩层、自注意力中的query-key乘积。此类分解非唯一，存在GL(r)规范对称性，导致训练不稳定和可用学习率受限。Stiefel-AdamW的架构可概括为：（1）输入/表示：将分解块中的一因子（通常为A或B之一）视为位于Stiefel流形上的正交/半正交矩阵，另一因子保持Euclidean空间；（2）主模块与数据流：矩估计仍在环境Euclidean空间中进行，保持AdamW的逐坐标对角预条件；几何信息仅通过两个操作引入——将梯度投影到当前点的切空间，以及通过retraction将更新后的点映射回流形；（3）目标/优化：将原始GL(r)规范对称性松弛为紧正交对称性，理论上排除因子范数无界增长，并继承Riemannian方法的稳定性与标准收敛保证；（4）推理/服务路径：作为AdamW的近似drop-in替换，额外计算仅为切空间投影与retraction，实现开销极小，不改动模型推理结构。实验在LoRA微调GPT2、ViT、Mistral 7B及GPT2全量预训练上验证，报告相对强baseline有一致改进，但未披露具体数值、模型参数量与训练资源。

Result: {"offline_gains": [{"metric": "未具体披露指标名（论文摘要称consistent improvements）", "value": "未披露具体数值", "setting": "GPT2、ViT、Mistral 7B的LoRA-style微调以及OpenWebText上GPT2全量预训练，对比强baseline（含AdamW）"}], "online_gains": []}

Conclusion: 将其中一个因子约束在Stiefel流形上、另一个保持Euclidean，把全GL(r)规范对称性松弛为紧正交对称性，从结构上排除因子爆炸（若成立可保留因子范数有界）。；在环境Euclidean空间中进行矩估计，仅通过切空间投影与流形retraction引入几何信息，从而保留AdamW的逐坐标对角预条件优势。；作为AdamW的近似无缝替换，实现开销极小，并同时继承Riemannian方法的稳定性与标准收敛保证（论文声称）。

Abstract: A pervasive structural pattern in modern deep learning is the linear factorization block: a submodule of the form $W = BA$ in which two parameter matrices are multiplied directly, with no intervening nonlinearity. Such blocks appear in LoRA adapters, low-rank compressed layers, query-key products of self-attention, and share a common pathology: the factorization is non-unique, which can destabilize training and limit usable learning rates. Despite this, factorization blocks are typically optimized with standard Euclidean methods that ignore the underlying geometry. We introduce Stiefel-AdamW, a near drop-in replacement for AdamW for use wherever such blocks appear. By constraining one factor on the Stiefel manifold while leaving the other Euclidean, Stiefel-AdamW relaxes the full $\mathrm{GL}(\mathbb{R}^r)$ gauge symmetry to a compact orthogonal symmetry, ruling out factor blow-up while retaining the coordinate-wise diagonal preconditioning that gives AdamW its practical strength. Moment estimation is performed in the ambient Euclidean space, with geometry entering only through a tangent-space projection and a manifold retraction. The implementation overhead over AdamW is minimal, and we show that the resulting optimizer inherits both the stability benefits of Riemannian methods and standard convergence guarantees. We validate Stiefel-AdamW on LoRA-style fine-tuning of GPT2, ViT, and Mistral 7B and on full pretraining of GPT2 on OpenWebText, showing consistent improvements over strong baselines at essentially no additional cost over AdamW.

</details>


### [66] [Elastic Threshold Attention: Learned Contextual Sparsity for Long-Context Decoding](https://arxiv.org/abs/2609.20888)
*Haris, Themistoklis,Li, Henry,Karimzadehgan, Maryam*

Main category: cs.LG

TL;DR: ETA提出可端到端训练的动态阈值稀疏注意力，通过乘性抑制logits和Triton解码内核，在1.45B模型上以约38%活跃解码密度达到与稠密注意力相当的质量，并实现最高2.5倍解码加速，同时给出离线校准方案进一步降低计算。


<details>
  <summary>Details</summary>
Motivation: 长上下文解码时KV缓存导致内存带宽瓶颈，现有稀疏注意力方法依赖刚性启发式阈值，可能丢弃必要上下文导致质量下降，且难以硬件加速。

Method: ETA在标准Transformer注意力基础上引入动态阈值机制。模型接收查询表示，通过一个轻量预测器为每个查询头生成上下文相关的阈值。训练时，对低于阈值的注意力logits执行乘性抑制使其趋近于零，而非直接删除，从而形成一个平滑的均匀注意力下限，避免表示坍塌，并消除初始token上的局部注意力汇。预训练目标与标准语言建模一致，但训练过程在约85%稀疏度下完成。推理时，ETA利用缓存的几何概率界对KV块进行O(1)时间筛选，硬剪枝无信息KV块，并吸收粗粒度GPU块选择附带引入的无关token，保持约38%的活跃解码密度。此外，针对领域特定部署，提供离线校准算法，冻结每头常数阈值以消除预测器运行时开销，进一步降低注意力计算。

Result: {"offline_gains": [{"metric": "语言建模、常识推理、长上下文检索性能", "value": "与稠密注意力相当", "setting": "1.45B预训练模型，约85%训练稀疏度，约38%活跃解码密度"}, {"metric": "训练稀疏度", "value": "约85%", "setting": "1.45B预训练模型"}, {"metric": "活跃解码密度", "value": "约38%", "setting": "1.45B预训练模型"}, {"metric": "墙钟解码加速比", "value": "最高2.5倍（对比FlashAttention-2）", "setting": "序列长度最高512K token"}, {"metric": "注意力计算额外降低", "value": "27%", "setting": "离线校准冻结每头常数阈值后"}], "online_gains": []}

Conclusion: Elastic Threshold Attention：从查询表示端到端预测动态上下文阈值，按难度自适应分配稠密上下文或剪枝常规token；训练时通过乘性抑制低于阈值的logits而非删除，避免表示坍塌，并消除初始token上的局部注意力汇；推理时实现Triton自定义解码内核，利用缓存几何概率界以O(1)时间筛选KV块，并硬剪枝无信息KV块；离线校准算法：冻结每头常数阈值以消除预测器开销，为领域特定部署进一步降低注意力计算

Abstract: Massive KV caches can cause severe memory-bandwidth bottlenecks during long-context decoding. Sparse attention methods mitigate this via selective loading, but that comes at a cost: rigid heuristics drop necessary context, leading to quality degradation. We introduce \textbf{Elastic Threshold Attention (ETA)}, an end-to-end trainable architecture that achieves hardware-accelerated decoding speed without sacrificing dense model quality. ETA predicts dynamic, contextual thresholds directly from query representations, allowing the model to allocate dense-like context to difficult retrieval or reasoning steps while pruning routine tokens. To learn this policy from scratch without representation collapse, ETA \emph{multiplicatively suppresses} sub-threshold logits toward zero during training rather than deleting them. Training against this smooth uniform attention floor provides a distributed probability reservoir that \textbf{causes localized attention sinks on initial tokens to disappear}. It also enables the model to hard-prune uninformative KV blocks at inference time and absorb incidental tokens co-admitted by coarse GPU block selection. As a result, a 1.45B pretrained ETA model rivals dense attention across language modeling, commonsense reasoning, and long-context needle retrieval at $\approx 85\%$ training sparsity and $\approx 38\%$ active decode density. At inference time, we implement a custom decode kernel in Triton that screens KV blocks in $O(1)$ time using cached geometric-probabilistic bounds, delivering up to $2.5\times$ wall-clock decode speedups over FlashAttention-2 on sequences up to 512K tokens. Finally, we introduce an offline calibration algorithm for domain-specific deployments that freezes per-head constant thresholds to eliminate predictor overhead, cutting attention compute by an additional $27\%$.

</details>

<div id=toc></div>

# Table of Contents

- [cs.CL](#cs.CL) [Total: 18]
- [cs.LG](#cs.LG) [Total: 12]
- [cs.IR](#cs.IR) [Total: 9]
- [cs.AI](#cs.AI) [Total: 13]


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [1] [Automated Extraction of Records of Processing Activities (RoPA) Using Hybrid RAG and Locally Deployed Large Language Models](https://arxiv.org/abs/2609.27359)
*To Duy Hinh,Nguyen Le Quoc Anh,Phan Van Tri,Khuong Nguyen-An*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司/机构与团队。该工作面向越南《个人数据保护法》与 Decree No. 356/2025/ND-CP（2026年1月1日生效）要求组织建立并维护 RoPA（处理活动记录）这一强制性合规业务场景，管线阶段属于"信息提取与文档自动化"（结合检索增强生成与本地 LLM 推理部署）。系统 RoPA Manager 采用混合检索（tsvector 词法排序 + 稠密向量检索 + Reciprocal Rank Fusion）配合本地部署 LLM 完成自动化 RoPA 信息提取。核心结论：自动化评分器在扰动数据上的鲁棒性 F1=0.9493；端到端提取 token 覆盖率 50.04–55.25%；在 24GB GPU 上本地 Qwen3.5-27B-GPTQ-Int4 与云端 DeepSeek-V4-Flash 无显著差异（差值 0.20 个百分点，p=0.72），为数据主权约束下的本地部署提供选型依据。


<details>
  <summary>Details</summary>
Motivation: 业务场景为法律合规自动化：越南《个人数据保护法》与 Decree No. 356/2025/ND-CP 自 2026 年 1 月 1 日生效，强制要求组织建立并维护 RoPA（处理活动记录）。生产瓶颈在于人工编制 RoPA 高度劳动密集（labor-intensive），而将数据交给云端托管 LLM 可能与数据主权（data-sovereignty）要求相冲突，因此需要在本地部署 LLM 的前提下实现自动化 RoPA 信息提取。问题的重要性体现在合规义务的强制性、人工成本高、以及数据主权约束限制了云端 LLM 的使用；这些均为摘要披露的具体动因。

Method: 系统名为 RoPA Manager，用于自动化 RoPA 信息提取。核心架构为"混合检索 + 本地 LLM"：混合检索由三部分组成——基于 tsvector 的词法排序（lexical ranking over tsvector）、稠密向量检索（dense-vector search）、以及 Reciprocal Rank Fusion (RRF) 融合；随后由本地部署的 LLM 完成最终信息提取。摘要还构造了一个越南语 RoPA 基准（含 32 家组织、77 项处理活动、12 个字段组、4,338 个参考值），并设计了自动化评分器（scorer），用于在不同层级评估系统表现。作者明确声明评分器在扰动数据上的测试衡量的是"评分器鲁棒性"而非端到端提取精度，这一区分是作者主动澄清的。选型对比涉及本地 Qwen3.5-27B-GPTQ-Int4、Gemma-4-31B 与云端 DeepSeek-V4-Flash。

Result: 离线指标（均非在线 A/B 增益）：
- 自动化评分器鲁棒性：在扰动数据上且不调用 LLM，F1 = 0.9493，95% CI [0.9436, 0.9548]（作者声明衡量评分器鲁棒性而非端到端提取精度）。
- 端到端提取：token 覆盖率 50.04–55.25%（相对参考标签）。
- 人工复核：两位独立专家复核 4,338 个参考值中的 1,558 个（占基准 35.9%），未发现错误值，专家间一致率 99.68%，PABAK = 0.9936。
- 值级精度（value-level precision）：论文摘要未披露（作者明确说明"未测量"）。
- 模型选型对比（32 组配对场景，24 GB GPU）：本地 Qwen3.5-27B-GPTQ-Int4 与云端 DeepSeek-V4-Flash 无统计学显著差异，差值为 0.20 个百分点（有利于 DeepSeek），95% CI [-0.93, 1.32]，p = 0.72；Gemma-4-31B 显著更差（p < 0.01）。

在线 A/B 测试增益：论文摘要未披露。

模型规模：本地部署 Qwen3.5-27B-GPTQ-Int4（27B、GPTQ Int4 量化）、Gemma-4-31B、云端 DeepSeek-V4-Flash；训练数据、训练硬件、训练时间：论文摘要未披露。
推理硬件：24 GB GPU（用于模型对比）；推理延迟、吞吐量、内存、成本：论文摘要未披露。

Conclusion: 该工作解决的是数据主权约束下、强制性法律合规场景中 RoPA 记录的自动化提取问题，主要可复用价值在于三点：一是"tsvector 词法排序 + 稠密向量 + RRF"混合检索与本地 LLM 的端到端组合方案，可直接迁移到其他受数据主权约束的文档自动化/信息提取场景；二是量化方式（GPTQ Int4）、本地小体量模型与云端大模型的选型方法论——通过 32 组配对实验与统计分析（置信区间、p 值）证明本地 Qwen3.5-27B 在 24GB GPU 上可与云端 DeepSeek-V4-Flash 效果相当，为"本地替代云端"的决策提供了可复制的评估范式；三是构造了越南语 RoPA 基准并区分"评分器鲁棒性"与"端到端精度"的严谨评测分层，对工业评估体系有借鉴意义。证据局限与不足：论文摘要未披露具体公司/机构与团队、模型训练细节、推理延迟/吞吐/成本，且端到端 token 覆盖率仅 50.04–55.25%，值级精度未测量，说明纯离线覆盖指标偏低、缺乏端到端精度与在线/生产验证；此外该场景属于法律文档信息提取而非搜索/广告/推荐的核心排序链路，工业搜索/广告/推荐团队可主要借鉴其检索融合与本地 LLM 选型评估方法。

Abstract: Vietnam's Personal Data Protection Law (Law No. 91/2025/QH15) and Decree No. 356/2025/ND-CP, effective January 1, 2026, require organizations to establish and maintain Records of Processing Activities (RoPA). Manual RoPA preparation is labor-intensive, while cloud-hosted large language models (LLMs) may conflict with data-sovereignty requirements. We propose RoPA Manager, a system for automated RoPA information extraction using hybrid retrieval that combines lexical ranking over tsvector, dense-vector search, Reciprocal Rank Fusion (RRF), and locally deployed LLMs. We introduce a Vietnamese RoPA benchmark with 32 organizations, 77 processing activities, 12 field groups, and 4,338 reference values. Evaluation is reported at three distinct levels. The automated scorer, tested on perturbed data without invoking an LLM, achieved F1 = 0.9493 [0.9436, 0.9548]; this measures scorer robustness rather than end-to-end extraction accuracy. End-to-end extraction achieved token coverage of 50.04-55.25% against the reference labels. Two independent experts reviewed 1,558 reference values (35.9% of the benchmark), found no incorrect values, and achieved 99.68% agreement with PABAK = 0.9936. Value-level precision was not measured. Across 32 paired scenarios on a 24 GB GPU, locally deployed Qwen3.5-27B-GPTQ-Int4 showed no statistically significant difference from cloud-based DeepSeek-V4-Flash (difference 0.20 percentage points in favor of DeepSeek, 95% CI [-0.93, 1.32], p = 0.72), while Gemma-4-31B performed significantly worse (p < 0.01).

</details>


### [2] [COMED: The Missing Middle Between Routing and Collaboration in Multi-LLM Inference](https://arxiv.org/abs/2609.26913)
*Norah Alballa,Wenxuan Zhang,Salma Kharrat,Fares Fourati,Zafar Ayyub Qazi,Mohamed Elhoseiny,Marco Canini*

Main category: cs.CL

TL;DR: 公司/机构与团队：论文摘要未披露具体公司，作者来自学术团队（涉及KAUST等，但摘要未明示生产部门）。业务场景：多模型LLM推理服务——在单一LLM无法对所有查询均匀可靠的前提下，如何在路由（只选一个模型）与稠密协作（每次查询都调用所有peers）之间做选择性升级调用，以在回答质量和推理成本之间取得平衡。流水线阶段：推理/服务阶段的模型选择与调用调度（post-anchor controller）。提出系统：COMED（Controlled Model Escalation for Multi-LLM Deliberation），基于anchor self-consistency、router margin和轻量peer probe的后置控制器，对置信答案直接接受，对歧义答案验证，仅在协作可能有益时升级。核心结果：在16个open-weight设置中全部超过固定与路由anchor，MedQA最高+10.7个百分点，同时调用更少模型、解码更少token；在HLE前沿模型上将GPT-5.5从23.1%提升到28.1%，优于稠密协作。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈：没有任何单一LLM对所有查询都可靠，因此需要多模型推理系统。现有方案的两难：路由（routing）在选出初始模型后即停止，无法处理后续失败；稠密协作（dense collaboration）在每个查询上都调用全部peers，带来高成本。关键洞察：协作具有非单调性——peers既能恢复单独无法解决的错误（rescue），也可能把原本正确的答案改错（corrupt/harm）。因此问题在于：缺少在成本与收益之间进行选择性升级的中间层（"missing middle"），需要一个控制器来决定何时协作划算、何时不划算。该问题对多模型推理服务的成本、延迟、token消耗和回答质量都直接相关。

Method: COMED是一个post-anchor controller，用于选择性的跨模型协作（selective cross-model collaboration）。核心信号来源（据摘要披露）：1）anchor self-consistency——衡量初始回答的自一致性/置信度；2）router margin——路由器的置信边际；3）轻量peer probe——对peer进行轻量探测。工作流程：接受置信答案（confident answers）、验证歧义案例（ambiguous cases）、仅在协作可能有益时升级（escalate）。理论刻画：提出rescue-harm decomposition（救回-损害分解），将协作的净收益分解为"被救回的错误"与"协作引入的损害"，选择性协作在"被救回错误多于协作引入损害"时才有提升。作者声称的创新在于：在路由与稠密协作之间引入受控升级的中间机制，并用可量化分解形式化权衡。需要注意：上述"输入-输出流、训练目标、模块细节"多基于摘要推断，摘要未完整披露具体实现细节（如peer probe具体架构、margin阈值设定方式等）。

Result: 离线指标：在medical、scientific、general reasoning benchmark上，COMED在16个open-weight设置中全部优于fixed和routed anchors；MedQA最高提升+10.7个百分点，同时比dense collaboration调用更少模型、解码更少token。在HLE（前沿模型）上，COMED将GPT-5.5从23.1%提升到28.1%，优于dense collaboration，并在该基准上取得最佳结果。在线A/B实验：论文摘要未披露。模型规模：论文摘要未披露。训练数据：论文摘要未披露。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露（仅提及"fewer decoded tokens"和"fewer models"的相对成本节省，未给出绝对值）。延迟：论文摘要未披露。吞吐：论文摘要未披露。内存：论文摘要未披露。成本：论文摘要未披露（仅定性说明减少模型调用次数和token解码量）。

Conclusion: 解决的问题：在单模型不可靠、纯路由能力有限、纯稠密协作成本过高的多模型LLM推理服务中，提供一种选择性升级机制，以较低成本获得接近甚至超过稠密协作的准确率。实用价值：对需要低成本高可靠多模型推理的团队有参考意义——通过置信度/边际/轻量探测三信号控制协作调用，直接减少模型调用与token消耗。局限性与证据缺口：摘要未披露具体公司或生产团队、未披露在线A/B效果、未披露具体资源消耗（延迟/吞吐/成本/硬件）的绝对值，也未给出peer probe和控制器本身的实现细节与开销；"frontier models"和"HLE"的具体环境与评测协议细节摘要未完整说明。可复用经验：rescue-harm decomposition可作为评估协作收益的形式化框架；anchor self-consistency + router margin + light peer probe的三信号组合，为工业多模型推理的运行时调度提供了一个可迁移的设计思路。

Abstract: No single Large Language Model (LLM) is uniformly reliable across queries, motivating multi-model inference systems that either route among models or combine their outputs. However, routing stops after selecting an initial model, while dense collaboration invokes peers on every query. We show that collaboration is non-monotonic: peers can recover failures that no model solves alone, but can also corrupt initially correct answers. We introduce COMED (Controlled Model Escalation for Multi-LLM Deliberation), a post-anchor controller for selective cross-model collaboration. COMED uses anchor self-consistency, router margin, and a lightweight peer probe to accept confident answers, verify ambiguous cases, and escalate only when collaboration is likely beneficial. We formalize this trade-off with a rescue-harm decomposition showing that selective collaboration improves when rescued errors outweigh collaboration-induced harms. Across medical, scientific, and general reasoning benchmarks, COMED improves fixed and routed anchors in all 16 open-weight settings, with gains up to +10.7 percentage points on MedQA while invoking fewer models and using fewer decoded tokens than dense collaboration. On HLE with frontier models, COMED improves GPT-5.5 from 23.1% to 28.1%, outperforming dense collaboration and achieving the best results.

</details>


### [3] [Evaluating Open-Weight LLMs for Turkish Domain Documents Under Retrieval and Hardware Constraints](https://arxiv.org/abs/2609.28007)
*Imtiaz Ul Hassan,Öykü Akbulut,Onur Kaya,Ardhendu Behera,Swagat Kumar,Peter Matthew,Yonghuai Liu*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司名，团队为 Imtiaz Ul Hassan、Öykü Akbulut、Onur Kaya、Ardhendu Behera、Swagat Kumar、Peter Matthew、Yonghuai Liu。真实业务场景是工业/公共部门长文档（109页工业R&D报告、112页公共部门报告）上的土耳其语文档问答，管线阶段为检索（retrieval/召回）与推理（reasoning）分离的端到端评测，并叠加硬件受限的本地量化部署约束。所提系统为证据标注评价协议，用以区分检索失败与下游模型推理失败且不需额外模型调用；在 7B-8B 五款开源模型上端到端准确率 49%–75%，并对比 7 种检索配置，字符级 TF-IDF 基线未被任何配置显著超越。


<details>
  <summary>Details</summary>
Motivation: 生产痛点：现有土耳其语 LLM 评价多依赖通用基准（general-purpose benchmarks），而非长、结构复杂的领域文档；工业/公共部门场景需要在资源受限的本地环境部署开源模型。关键约束包括：硬件受限（仅笔记本 6GB VRAM 的本地部署）、检索与推理耦合难以定位失败来源，以及不同文档间证据召回饱和行为差异导致部分文档受检索或上下文容量瓶颈约束。

Method: 架构与流程：选五款开源 7B-8B 权重模型，在 NVIDIA RTX 3050 6GB VRAM 笔记本 GPU 上本地运行，采用受控提示（controlled prompting）、受控解码（decoding）与 4-bit 量化的统一协议。评测对象为 100 道经系统验证的问题（源自 109 页工业报告），并以第二份 112 页公共部门报告及独立构建的 100 问集复现该协议。核心方法创新（作者主张）：证据标注（evidence-annotated）评价协议，将"检索失败"与"下游模型推理失败"分离，且无需额外模型调用。检索侧对比 7 种词法（lexical）、稠密（dense）、混合（hybrid）检索配置，所有比较使用 95% Wilson 区间与精确配对 McNemar 检验。以上流程描述均来自摘要陈述，未做额外推断。

Result: 离线指标：主基准端到端准确率 49%–75%（五款 7B-8B 模型区间）；检索对比中，7 种词法/稠密/混合配置在两份文档上均无任一配置显著优于字符级 TF-IDF 基线（依据 95% Wilson 区间与精确配对 McNemar 检验）；证据召回（evidence recall）在两份报告上的饱和行为不同，表明检索与有效上下文容量对某些文档构成约束而对另一些文档不构成。未报告在线 A/B 增益。模型规模：论文摘要未披露（仅知为 7B-8B 量级开源模型）；训练数据、训练硬件/时间、推理硬件已披露为 NVIDIA RTX 3050 6GB VRAM 笔记本 GPU；推理延迟、吞吐量、内存与成本：论文摘要未披露。

Conclusion: 论文解决的核心问题是在无号模型就能回答的通用基准之外，为土耳其语长领域文档建立检索与推理分离、且适配本地硬件受限部署的评价方法，并提供证据召回饱和证据。实用价值：为工业/公共部门在资源受限环境下选型开源 LLM、配置检索器提供了可复现协议；明确了"模型选择、检索行为、硬件限制必须分开评估"。局限与证据缺口：摘要未披露模型名称、具体量化位数细节（仅"4-bit quantisation"）、延迟/吞吐/内存/成本，也未披露企业内部生产数据或在线实验。可迁移经验：检索层在字符 TF-IDF 基线上的"无显著提升"提示工业团队不应默认复杂检索器优于简单基线；证据召回饱和差异提醒需按文档类型单独评估检索与上下文容量的瓶颈归属。

Abstract: Most Turkish-capable large language models (LLMs) are evaluated using general-purpose benchmarks rather than long, structurally complex domain documents. This paper evaluates five open-weight 7B-8B models for Turkish document question answering under a resource-constrained local deployment setting. The primary benchmark contains 100 systematically validated questions derived from a 109-page industrial R&D report, and the evaluation protocol is replicated using a second 112-page public-sector report and an independently constructed 100-question set. All models are evaluated locally on an NVIDIA RTX 3050 laptop GPU with 6 GB VRAM using controlled prompting, decoding, and 4-bit quantisation. The principal methodological contribution is an evidence-annotated evaluation protocol that separates retrieval failure from downstream model reasoning failure without requiring additional model calls. On the primary benchmark, end-to-end accuracy ranges from 49% to 75%. Seven lexical, dense, and hybrid retrieval configurations are additionally compared using 95% Wilson intervals and exact paired McNemar tests; none significantly outperforms the character TF-IDF baseline on either document. Evidence recall saturates differently across the two reports, showing that retrieval and effective context capacity can be binding constraints for some documents but not others. These results demonstrate that model selection, retrieval behaviour, and hardware limits must be evaluated separately when deploying open-weight LLMs for Turkish domain documents.

</details>


### [4] [Semantic Self-Distillation for Language Model Uncertainty](https://arxiv.org/abs/2602.04577)
*Edward Phillips,Sean Wu,Fredrik K. Gustafsson,Boyan Gao,David A. Clifton*

Main category: cs.CL

TL;DR: 论文摘要披露的作者单位为论文摘要未披露的机构/团队（作者Edward Phillips、Sean Wu、Fredrik K. Gustafsson、Boyan Gao、David A. Clifton）；业务场景是论文摘要未披露的具体产品，但明确针对"latency-critical applications"（延迟关键型应用）中的大语言模型不确定性量化，管线阶段为推理前的prompt级不确定性估计与答案级可靠性评测。提出Semantic Self-Distillation（SSD）框架，将采样的语义分布蒸馏到轻量级学生模型，使其在语言模型生成答案token之前预测prompt条件密度，以熵作为prompt级不确定性信号、概率密度用于答案级可靠性评估。在TriviaQA与MMLU上的幻觉预测任务中，学生模型性能与教师模型的采样语义分散具有竞争力，同时提供分布外检测与多选答案选择等额外原语。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于大语言模型的不确定性量化困难，而语义分散（多组采样答案在语义上的方差）作为模型不确定性的有效代理，其计算成本过高，无法用于延迟关键型（latency-critical）应用中。摘要指出现有方案受限于采样带来的高计算开销，阻碍了在线/低延迟场景下的部署，因此需要一种更轻量、可在生成前提前给出不确定性信号的方法。

Method: Semantic Self-Distillation（SSD）将教师模型采样的语义分布蒸馏到轻量级学生模型，学生模型在语言模型生成答案token之前预测一个prompt条件下的语义分布（prompt-conditioned density）。该分布的熵作为prompt级不确定性信号，概率密度支持答案级可靠性评测。摘要主张该方法可作为在语言之外复杂输出空间中蒸馏预测不确定性的一般框架。作者声称的创新在于以自蒸馏方式将采样语义分散压缩为生成前可得的密度估计，从而避免重复采样；关于学生模型的具体架构、输入输出具体形式、训练目标细节，论文摘要未披露。

Result: 离线指标：在TriviaQA与MMLU上的幻觉预测任务中，学生模型相对教师模型的采样语义分散表现"competitive"（竞争力相当）；并在分布外检测与多选答案选择上提供额外不确定性原语，具体数值论文摘要未披露。在线A/B测试：论文摘要未披露。模型规模、训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本均论文摘要未披露。

Conclusion: 该方法解决的痛点是：在延迟敏感场景下用轻量学生模型替代昂贵的采样语义分散来获取不确定性信号，并将不确定性估计前移到生成阶段之前。实用价值在于为工业LLM在线推理提供一种低延迟的不确定性量化原语，可用于幻觉预测、分布外检测与多选答案选择。局限性与证据缺口：摘要未报告任何在线/生产环境的验证、未披露延迟相对采样方案的量化改善、未披露学生模型规模与资源开销，且仅验证于TriviaQA与MMLU两个数据集。可复用点：工业LLM团队可将SSD作为推理前轻量不确定性估计模块，用于风险控制、拒识、以及低延迟排序或可靠性评测，但需自行验证其在真实产品流量与延迟预算下的收益。

Abstract: Large language models present challenges for principled uncertainty quantification, in part due to their complexity and the diversity of their outputs. Semantic dispersion, or the variance in the meaning of sampled answers, has been proposed as a useful proxy for model uncertainty, but the associated computational cost prohibits its use in latency-critical applications. We show that sampled semantic distributions can be distilled into lightweight student models which estimate a prompt-conditioned density before the language model generates an answer token. The student model predicts a semantic distribution over possible answers; the entropy of this distribution provides a prompt-level uncertainty signal, and the probability density allows answer-level reliability evaluation. Across experiments on TriviaQA and MMLU, we find our student models perform competitively relative to the teacher's sampled semantic dispersion on a hallucination prediction task, whilst offering additional uncertainty primitives for out-of-domain detection and multiple-choice answer selection. We term this technique Semantic Self-Distillation (SSD), which can serve as a general framework for distilling predictive uncertainty in complex output spaces beyond language.

</details>


### [5] [Agent-Editing World Model: Rethinking World Modeling for LLM Agents](https://arxiv.org/abs/2609.28416)
*Shuang Sun,Guoxin Chen,Fanzhe Meng,Jia Deng,Huatong Song,Jinhao Jiang,Wayne Xin Zhao,Hongteng Xu,Ji-Rong Wen*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司与团队。论文面向 LLM 智能体在 Search、Terminal、Software Engineering 三类长期任务的决策改进场景，处于智能体推理与训练环节（决策状态建模、噪声轨迹修正、拒绝采样微调），提出 Agent-Editing World Model（AEWM），用 Action Judge 对动作分级 + State Revision 编辑噪声续写 + EditAct 在真实执行中改写后续决策所依赖的状态。AEWM 在自建 Action Judge 基准上 macro-F1 达 70.5%，较最强前沿基线高 10.6 个百分点；EditAct 在六项基准、三种智能体骨干上较最强基线平均提升 3.2–6.7 分；AEWM-RFT（基于已验证 EditAct 轨迹的拒绝采样微调）在三个领域较 Self-RFT 提升 2.2–2.6 分，且无需在线 AEWM 引导。


<details>
  <summary>Details</summary>
Motivation: 现有语言世界模型通常通过预测环境观察来辅助智能体，但当真实反馈可获得时，重建高熵、依赖执行的工具响应价值有限；同时智能体存在"任务状态污染"（task-state contamination）问题——历史中无根据的假设与过时计划持续存在并扭曲后续决策。该问题直接影响长时程任务中的决策质量与失败恢复，对 Search/Terminal/代码工程等真实工具交互场景的生产可用性至关重要。论文摘要未披露具体业务方、用户规模与线上指标。

Method: AEWM 不再模拟工具响应，而是建模"推理与动作如何塑造未来任务进展"。核心由三部分组成：1）Action Judge，将决策区分为 Critical（关键）、Exploratory（探索性）、Noisy（噪声）三类；2）State Revision，从同一段观测历史出发，对噪声的推理-动作续写进行编辑改写；3）EditAct，将上述能力与真实执行结合，不是仅仅给出批评，而是直接改变后续决策所依据的状态。训练方式为在 Search、Terminal、Software Engineering 三个领域上做 mid-training 与监督微调（SFT），并进一步用已验证的 EditAct 轨迹进行拒绝采样微调得到 AEWM-RFT。（上述为作者声明的架构与训练流程；输入输出张量形式、模型基座、训练目标函数、参数规模等，论文摘要未披露。）

Result: 离线指标：AEWM 在自建 Action Judge 基准上 macro-F1 为 70.5%，较最强前沿基线高 10.6 个百分点；EditAct 在六项基准、三种智能体骨干上较最强基线平均提升 3.2–6.7 分；AEWM-RFT 在三个领域较 Self-RFT 提升 2.2–2.6 分，且推理时不需在线 AEWM 引导。在线 A/B 测试指标：论文摘要未披露。模型规模、训练数据、训练硬件/时长、推理硬件、延迟、吞吐、显存与成本：论文摘要均未披露。

Conclusion: 论文解决的核心问题是长时程 LLM 智能体中由历史噪声与过时计划导致的状态污染，提出用"编辑式世界模型"替代"观察重建式世界模型"，并通过动作分级、状态改写与真实执行耦合来提升决策质量，最后用拒绝采样微调把能力固化到主模型、免去在线引导开销。可复用的思路包括：动作重要性三分法、状态层（而非仅批评层）的修正、以及"在线引导蒸馏到离线微调"的训练范式，适用于搜索、终端操作、软件工程等工具型智能体。局限与证据缺口：论文摘要未披露公司/生产环境、是否做过线上 A/B、模型与资源成本及延迟，三类任务的实际业务转化与可扩展性需结合原文进一步验证。

Abstract: Recent advances in large language models (LLMs) have enabled agents to tackle long-horizon tasks across diverse environments. To further improve agent performance, existing language world models typically predict environment observations, yet reconstructing high-entropy, execution-dependent tool responses offers limited value when real feedback is available. Meanwhile, agents suffer from \emph{task-state contamination}, where unsupported assumptions and outdated plans persist in history and distort subsequent decisions. We propose the \textbf{Agent-Editing World Model (AEWM)}, which models how reasoning and actions shape future task progress rather than simulating tool responses. AEWM combines \textbf{Action Judge} to distinguish \textsc{Critical}, \textsc{Exploratory}, and \textsc{Noisy} decisions with \textbf{State Revision} to edit noisy reasoning--action continuations from the same observed history. \textbf{EditAct} integrates these capabilities with real execution, directly changing the state underlying subsequent decisions rather than merely providing critiques. We train AEWM across Search, Terminal, and Software Engineering through mid-training and supervised fine-tuning. AEWM achieves 70.5\% macro-F1 on our Action Judge benchmark, exceeding the strongest frontier baseline by 10.6 points. Across six benchmarks and three agent backbones, EditAct improves average scores by 3.2--6.7 points over the strongest baseline. Furthermore, rejection sampling fine-tuning on verified EditAct trajectories, termed \textbf{AEWM-RFT}, improves over Self-RFT by 2.2--2.6 points across three domains without online AEWM guidance.

</details>


### [6] [Computation Over Geometry: Meaning Identity Is Computed, Not Shipped in the Embeddings](https://arxiv.org/abs/2609.28290)
*Jiaqi Deng*

Main category: cs.CL

TL;DR: 本文为单一作者（Jiaqi Deng）研究，论文摘要未披露所属公司/团队。业务场景是检索与RAG中的句子“意义身份判定”（两句改述后是否同义），处于召回/向量检索与重排序(dual-encoder/reranker)环节。核心发现是：对于冻结的离线和商用编码器与LLM，意义身份不是向量嵌入几何的固有属性，而是两句在同一前向传播中共享计算时才被算出的算子；仅靠独立编码向量（含晚期融合）在重叠匹配的PAWS-X上AUC仅为0.55–0.65（稠密峰值0.70），而共享前向探针达0.90–0.96（1.5B到32B），并可由1.5B联合阅读器从无标签教师分数蒸馏得到。


<details>
  <summary>Details</summary>
Motivation: 针对检索和RAG中“先独立编码、再以向量距离/余弦相似度作为同义或相关判据”的主流范式提出质疑：生产系统常假设句子向量几何天然承载意义身份，而对改写、同义表达鲁棒性不足。摘要揭示的核心瓶颈是冻结离线的编码器（BGE、E5、GTE、MiniLM、E5-Mistral-7B）以及LLM（Llama 3、Mistral、Qwen）的独立编码向量在意义身份判定上接近随机（0.55–0.65），晚期融合亦接近机会水平，说明单纯把句义“装进”向量并靠几何度量会系统性丢失身份信息，影响召回质量与RAG检索。双编码器虽可微调拟合PAWS（0.87–0.93），但迁移与STS-B性能受损，呈现“拟合训练分布 vs 泛化语义相似度”的工程张力。

Method: 作者的核心方法是“计算 vs 几何”的对照实验而非提出新生产系统：对同一句对分别在两种编码方式下探测意义身份——(1)独立前向分别得到两向量后做距离/晚期融合/线性或非线性读者；(2)两句共享一次前向传播（joint forward pass），取中间层或末层状态由探针判定。验证了共享前向中的身份信号为中层（mid-depth）、随模型规模在3B附近饱和(≈0.94)、对“搭档洗牌”(partner shuffle)敏感（洗牌后崩塌），且跨模型类别普遍成立：因果LM之外的DeBERTa、RoBERTa（双向编码器）与Flan-T5、T5、BART（编码器-解码器）同样保持该差距。进一步发现：线性/固定读者无法从冻结独立编码解锁身份；全量49k PAWS训练集上非线性成对读者可恢复部分（0.68–0.87）；现成重排序器分化——BGE-reranker-large达0.94，而MS-MARCO与Jina仅0.55–0.64；1.5B联合阅读器可从无标签教师分数蒸馏出该算子，而教师自身独立向量的任何线性函数均无法表达。作者主张：余弦相似度度量的是“措辞邻域”，意义身份应被视为一种廉价的计算算子而非句子向量属性。

Result: 离线指标（PAWS-X重叠匹配、英语确认AUC）：冻结商用编码器BGE/E5/GTE/MiniLM/E5-Mistral-7B为0.55–0.65（稠密峰值0.70）；Llama 3/Mistral/Qwen独立末token状态同样不佳；两向量晚期融合接近机会水平；共享前向探针0.90–0.96（1.5B到32B），3B附近饱和约0.94，GPT-2 XL上较弱为0.76；非线性成对读者在全部49k个PAWS训练对上达0.68–0.87；现成重排序器中BGE-reranker-large 0.94，MS-MARCO与Jina 0.55–0.64；双编码器微调可拟合PAWS（0.87–0.93）但STS-B与迁移下降。在线A/B收益：论文摘要未披露。模型规模：探测信号覆盖1.5B至32B，联合阅读器最小1.5B；训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: 论文解决的核心问题是纠正检索/RAG对向量几何承载意义身份的默认假设：冻结双编码器向量的几何度量对改述同义几乎不敏感，而共享前向中的身份信号是廉价可算且可蒸馏的算子。实用价值在于为召回、向量检索、重排序与RAG链路提供可复用的经验结论：依赖独立编码+余弦/内积的范式在改写鲁棒性上存在根本短板，重排序器选择至关重要（BGE-reranker-large达0.94而MS-MARCO/Jina近随机），双编码器硬拟合同义数据会伤害STS-B与迁移。证据缺口与局限：单一作者、无机构佐证，paper摘要未披露任何在线实验、实际生产规模、延迟/成本数据，结论基于PAWS-X等学术基准，缺乏真实线上检索系统的端到端效果证据；论文侧重负面/诊断性证据，未给出生产可落地的完整替代系统。工业检索/推荐/RAG团队可借鉴：在改写同义/语义等价敏感的召回场景下，不应仅依赖冻结双编码器向量几何，需引入共享上下文计算或强重排序器（如BGE-reranker一类），并对“语义身份”与“语义相似度”的训练目标做解耦权衡。

Abstract: Meaning identity (whether two sentences say the same thing after wording changes) is treated in retrieval and RAG as a geometric fact about independently encoded sentence vectors. We show that, for frozen off-the-shelf encoders and language models, it is not: identity is computed when both sentences share one forward pass, and is not a property of the embedding geometry those systems ship. On overlap-matched PAWS-X, purpose-built encoders (BGE, E5, GTE, MiniLM, E5-Mistral-7B) reach English confirm AUC only 0.55-0.65 (dense peak 0.70). Independently encoded last-token states of Llama 3, Mistral, and Qwen do no better; late fusion of the two vectors stays near chance. The same probe on a joint forward pass reaches 0.90-0.96 from 1.5B to 32B, collapses under partner shuffle, is mid-depth, saturates near 0.94 by 3B, and appears more weakly in GPT-2 XL (0.76). The gap holds beyond Llama-style models on other causal LMs, bidirectional encoders (DeBERTa, RoBERTa), and encoder-decoders (Flan-T5, T5, BART). Fixed or linear readers over frozen independent encodings never unlock identity; nonlinear pair readers recover part of it only on the full 49k-pair PAWS train split (0.68-0.87). Off-the-shelf rerankers split: BGE-reranker-large reaches 0.94, while MS-MARCO and Jina stay at 0.55-0.64. Independently trained families compute the same relation and a 1.5B joint reader can distill it from unlabelled teacher scores, while no linear function of the teachers own independent vectors can. Bi-encoders can be fine-tuned to fit PAWS (0.87-0.93), but transfer and STS-B suffer. Cosine compares wording neighbourhoods; identity is a cheap computed operator, not a property of either sentence vector.

</details>


### [7] [Exact Feedback Is Not Control: Evaluating Text-based Closed-Loop Revision in LLMs](https://arxiv.org/abs/2609.28150)
*Haitong Jiang,Chunlin Liu,Yile Wang,Yuhong Feng*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司与团队（作者单位未在摘要中说明）。研究针对LLM应用中的文本闭环修订（closed-loop revision）这一生产场景，处于生产LLM系统的行为评估与可控性环节：核心问题是"精确反馈是否等于可控修订"。作者提出固定预算修订协议与确定性验证器，完整报告精确长度、词汇、组合约束下的全部违规，从而隔离出模型侧的修订行为。在19个开源与闭源模型上，controller级平均最终联合成功率从17.4%到99.8%不等，相同初始草稿下仍存在显著跨模型差异；失败轨迹常重复早期输出，且先前重复与后续可恢复性下降相关；去除早期对话仅改变重复逃逸而非稳定提升最终成功率。结论是精确反馈使修订错误可见，但并未使闭环可靠。代码与复现说明已公开。


<details>
  <summary>Details</summary>
Motivation: 生产LLM应用（智能体、受约束文本生成、需迭代修稿的任务）广泛依赖闭环修订，但失败可能源于两类原因：反馈不完整，或模型无法有效响应正确反馈。现有研究往往混淆这两者，难以定位责任。本工作通过"固定反馈正确性与完整性"，专门隔离模型侧的修订行为，从而回答"即便给了完全精确的反馈，模型是否仍改不好"这一核心工程问题。摘要未披露具体业务领域、规模约束或线上痛点细节（论文摘要未披露）。

Method: 核心思路（作者主张）：引入"固定预算修订协议"（fixed-budget revision protocol），配合确定性验证器（deterministic verifiers），在精确长度（exact-length）、词汇（lexical）、组合（compositional）三类约束上报告所有剩余违规，从而固定反馈的正确性与完整性，单独测量模型侧的响应。评测流程：给定初始草稿→验证器输出全部违规→模型在固定预算内修订→验证器复查，最终以controller级平均联合成功率（joint success）度量。此外引入"匹配状态干预"（matched-state interventions）：在保持当前草稿与反馈不变的前提下移除早期对话，观察"重复逃逸"（recurrence escape）与最终成功率的变化。分析维度包括：跨模型成功率差异、失败轨迹中早期输出的重复（recurrence）、post-training与模型规模对修订行为的影响。作者主张"精确反馈使修订错误可观测，但不使闭环可靠"。

Result: 离线评测（论文未涉及线上A/B）：在19个开源与闭源模型上，controller级平均最终联合成功率为17.4%到99.8%，相同初始草稿下仍存在显著跨模型差距；失败轨迹常重复早期输出，且先前重复与较低后续可恢复性相关；匹配状态干预下去除早期对话可改变重复逃逸，但不能可靠提升最终成功率，效果取决于模型、任务与触发状态组成。模型规模、训练数据量、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本等：论文摘要未披露。

Conclusion: 本工作解决了"精确反馈是否等于可控修订"这一生产LLM闭环应用中的基础性问题：通过固定反馈正确性与完整性，揭示了模型侧修订行为本身的不可靠性——高精确反馈并不自动带来可靠闭环，且重复早期输出是预测后续失败的信号。可复用的点：①确定性验证器+固定预算协议可作为生产LLM受约束生成/智能体工作流的行为评测与回归基准；②"先前重复→低可恢复性"可作为在线检测修订失败/触发降级策略的启发式特征；③匹配状态干预提示仅靠裁剪对话历史不足以修复失败轨迹。局限与证据缺口：摘要未披露具体公司/产品、任务落地场景、线上收益、模型规模与资源开销；且"去除早期对话不能稳定提升最终成功率"说明该方向尚未形成可直接上线的干预方案，需进一步结合模型/任务/触发状态做条件化处理。

Abstract: Closed-loop revision is increasingly used in large language model (LLM) applications, but failures may reflect incomplete feedback or ineffective responses to correct feedback. We introduce a fixed-budget revision protocol with deterministic verifiers that report all remaining violations across exact-length, lexical, and compositional constraints. Fixing feedback correctness and completeness isolates model-side revision behavior. Across 19 open- and closed-source models, controller-level mean final joint success ranges from 17.4% to 99.8%, with substantial cross-model gaps persisting under identical initial drafts. Controlled experiments reveal reproducible model-specific responses to exact feedback. Post-training and scale reshape these responses without consistently bringing them closer to exact correction. Across all constraint families, failed trajectories often repeat earlier outputs, and prior recurrence is associated with lower subsequent recoverability. Matched-state interventions show that removing earlier dialogue while holding the current draft and feedback fixed changes recurrence escape without reliably improving final success; effects depend on the model, task, and trigger-state composition. Exact feedback makes revision errors observable, but does not make the closed loop reliable. Code and reproduction instructions: this https URL .

</details>


### [8] [TEMPS: Temporal Sentence Embeddings for Temporal Information Retrieval](https://arxiv.org/abs/2609.28048)
*Mourad Hassani,Julien Romero,Amel Bouzeghoub,Christian Jacquelinet*

Main category: cs.CL

TL;DR: 论文摘要未披露公司及团队；针对临床、新闻、法律等对时间敏感的搜索场景，提出 TEMPS（Temporal Embedding Model for Precise Search），定位在稠密检索/RAG管线的检索匹配阶段，解决"主题相关但时间错误"的召回问题。TEMPS 是一个附加在冻结语义检索器上的模块化时间分支，通过锚定时间表达式解析与高斯矩匹配提供免人工标注的监督信号，并在推理时与语义分数融合；在三项时间基准上提升了每个语义骨干的 MRR，并在 TS-Retriever 上将 R@1 从 19.92 提升至 25.39。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于现代 IR 系统很少表示时间，而临床、新闻、法律等场景的查询相关性高度依赖事件发生时间；现有稠密检索器和 RAG 管线在主题匹配上表现良好但在时间匹配上表现差，导致召回"主题对但时间错"的内容。该问题直接影响检索与生成结果的可信度与业务可用性；规模约束及具体线上损失论文摘要未披露。

Method: TEMPS 采用模块化时间分支，附加在冻结（frozen）的语义检索器之上，仅在该时间信号上训练，不破坏原有语义检索能力。流程为：解析锚定时间表达式（anchored temporal expressions）并解析为区间，将每个区间用高斯分布做矩匹配（moment-matching）；由此得到的排序作为"锚定日期条件编码器"（anchor-date-conditioned encoder）的监督信号。由于监督来自时间锚定（grounding）本身，训练无需人工标注的时间数据。推理时，时间分数与语义分数进行融合。摘要指出时间分数本身是分布嵌入中的高斯-KL 包含度量（Gaussian-KL inclusion measure），TEMPS 的增量在于提供了 grounding 与矩匹配监督。注：编码器内部结构、输入输出细节、融合方式及具体损失函数论文摘要未披露。

Result: 离线指标：在三项时间基准（temporal benchmarks）上，TEMPS 提升了每一个所测语义骨干（semantic backbone）的 MRR；在 TS-Retriever 上，R@1 从 19.92 提升至 25.39，优于此前的时间 SOTA。线上 A/B 增益：论文摘要未披露。模型规模：论文摘要未披露。训练数据规模：论文摘要未披露（仅说明无需人工标注的时间数据）。训练硬件/时间：论文摘要未披露。推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: TEMPS 解决了检索与 RAG 系统中"主题相关但时间错误"的召回缺陷，通过在冻结语义检索器上附加时间分支并以时间锚定自监督（免人工标注）训练，具有低侵入、可插拔、可复用特点，适合时间敏感的工业搜索、问答与 RAG 系统。局限与证据缺口：仅报告了离线 MRR/R@1，未见线上 A/B 效果；模型规模、训练与推理资源、延迟及时间骨干的具体实现均未披露，规模化部署成本与线上收益尚待验证。

Abstract: Modern information retrieval (IR) systems rarely represent time, yet many information needs depend on it: in clinical, journalistic, and legal search, when an event occurred can decide whether a document is relevant. Dense retrievers and Retrieval-Augmented Generation (RAG) pipelines match queries to documents well on topic but poorly on time, so they surface content that is on-topic yet temporally wrong. We introduce Temporal Textual Similarity (TTS), a task that measures how well two anchored texts align in time, independent of their topical similarity. We then present TEMPS (Temporal Embedding Model for Precise Search), a modular temporal branch that attaches to a frozen semantic retriever and trains on that signal. It resolves anchored temporal expressions to intervals and moment-matches each one to a Gaussian; the resulting ordering supervises an anchor-date-conditioned encoder, whose score we fuse with the semantic score at inference. Grounding supplies the supervision, so training uses no hand-labeled temporal data. The temporal score itself is the Gaussian-KL inclusion measure from distributional embeddings; what TEMPS adds is the grounding and the moment-matched supervision. On three temporal benchmarks, TEMPS improves MRR for every semantic backbone tested and, on TS- Retriever, lifts R@1 from 19.92 to 25.39 over the prior temporal state of the art.

</details>


### [9] [Risk-Controlled KV-Cache Eviction: From Memory Budgets to Risk Targets](https://arxiv.org/abs/2609.27981)
*Beomgu Kang,SoJin Yun,Hojoon Kim,Hyunseok Seo*

Main category: cs.CL

TL;DR: 论文作者所属公司与团队：论文摘要未披露。业务场景：LLM 推理服务中的 KV-cache 内存压缩与部署可靠性控制；流水线阶段：推理/serving 阶段（KV-cache 驱逐与内存预算管理），非检索/召回/精排链路。提出系统：一种与压缩器无关（compressor-agnostic）的部署风险控制框架，将 KV-cache 驱逐从“内存预算”问题重构为“风险目标”问题，通过事后认证（post-hoc certification）在有限样本保证下从校准数据中选取保留策略，无法认证时回退到完整 KV。核心结果：同一可靠性契约下，Llama 上 SnapKV 在 LongBench 可认证到 75% 保留率，而 RULER-32K 上无压缩策略可通过认证并触发全 KV 回退；经验上达标率低于 5% 目标的策略仍可能认证失败。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于：现有 KV-cache 驱逐通常以“平均质量-内存权衡”评估，但较小的平均损失可能掩盖个别请求效用显著退化的尾部风险。业务需求是：在真实部署中，运维方需要的是一个部署级别的可靠性承诺（目标风险水平与置信度要求），而非单纯的平均质量指标。现有方案局限：基于经验的阈值选择可能在经验降解率低于目标时仍通过不了有限样本认证，说明仅靠平均指标无法保证生产可靠性。论文摘要未披露具体规模、并发、延迟或成本等量化约束。

Method: 方法层面（作者主张）：将驱逐重定义为一个部署风险控制问题——当驱逐相对于同请求全 KV 推理使任务效用下降超过“部署指定容差”时，即发生“实质退化”，部署风险即此类事件在总体中的发生频率。输入输出流：给定一个“可靠性契约”（目标风险水平 + 置信度要求），使用与压缩器无关的事后认证过程，从校准数据中基于有限样本保证选取保留策略；若无压缩策略通过认证，则回退到完整 KV。核心创新：①将内存预算框架改写为风险目标/可靠性契约框架；②提供 compressor-agnostic 的有限样本事后认证；③显式的全 KV 回退作为安全机制。我的解读：这一框架本质是把统计学习中的 conformal prediction / finite-sample guarantee 机制迁移到 KV-cache 压缩策略选择上，属于决策规则层面的创新，而非新的压缩算法本身。

Result: 离线指标：在 Llama 上，同一契约下 SnapKV 在 LongBench 可认证到 75% 保留率，但在 RULER-32K 上没有任何测试的压缩策略通过认证，因而触发全 KV 回退。经验层面：在 Llama LongBench 上，经验阈值选择会挑出未认证的策略，这些策略在固定预算方法上比认证策略少保留 5-10 个百分点的 cache。其他关键事实：目标风险水平示例为 5%；测试覆盖多个驱逐方法、Llama 与 Mistral 模型、LongBench 与 RULER-32K 两个基准。在线 A/B 测试增益：论文摘要未披露。模型规模：论文摘要未披露。训练数据：论文摘要未披露（本方法使用校准数据，但数据规模未披露）。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟/吞吐/内存/成本：论文摘要未披露（仅给出保留率等相对内存指标）。

Conclusion: 解决的问题：将 KV-cache 驱逐从“平均质量-内存权衡”升级为“带有限样本保证的部署风险控制”，为生产推理提供了可验证的可靠性契约，避免平均指标掩盖尾部退化。实践价值：对工业 LLM 服务团队而言，可复用该框架将运维侧的可靠性 SLA（目标风险 + 置信度）自动转化为内存保留操作点，并在认证失败时安全回退到全 KV。局限与证据缺口：论文摘要未披露所属公司/团队、模型规模、校准数据规模、真实在线效果、延迟/吞吐/成本影响；也未见生产系统验证，认证结果依赖 LongBench/RULER-32K 等离线基准。可迁移经验：①尾部风险（per-request 退化频率）应作为压缩策略的部署级指标；②有限样本认证可约束经验阈值选择的乐观偏差；③压缩器无关的认证 + 全 KV 回退是稳健的工业部署范式。

Abstract: KV-cache eviction is typically evaluated through average quality-memory trade-offs, yet a small average loss can hide requests whose utility degrades materially. We reformulate eviction as a deployment risk-control problem: a material degradation occurs when eviction lowers task utility by more than a deployment-specified tolerance relative to full-KV inference on the same request, and deployment risk is the population frequency of such events. Given a reliability contract specifying a target risk level and confidence requirement, we use a compressor-agnostic post-hoc certification procedure to select a retention policy from calibration data with a finite-sample guarantee, falling back to full KV when no compressed policy is certified. Across multiple eviction methods, Llama and Mistral models, and LongBench and RULER-32K, the same contract supports substantially different levels of eviction: on Llama, it certifies SnapKV at 75% retention on LongBench but no tested compressed policy on RULER-32K, triggering full-KV fallback. Policies with empirical degradation rates below the 5% target can still fail finite-sample certification; on Llama LongBench, empirical thresholding selects uncertified policies that retain 5-10 percentage points less cache across fixed-budget methods. The proposed framework converts a deployment-level reliability requirement into a KV-memory operating point.

</details>


### [10] [Learning When Not to Listen: Selective Anti-Interference Pretraining for Language Models](https://arxiv.org/abs/2609.27925)
*Jinchang Zhu,Haowei He,Yi Ding,Rong Fu,Nie Xiaojian,Shuangyong Song,Zhongjiang He,Menglin Yang*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司/团队归属（仅有作者署名，含疑似工业界成员），业务场景为长、packed 或干扰密集上下文中语言模型被无关远前缀干扰的问题；管线阶段属于预训练/持续预训练（训练目标层面）。作者提出 SPAR（Selective Prefix Anti-Interference Regularization）：同时跑原始序列与仅篡改远前缀的 corrupt-prefix 输入，借助 short-context sufficiency gate 与 gated KL 目标稳定局部可支撑的 suffix 预测。在 Qwen2.5-0.5B/3B、Llama-3.2-1B、Llama-3.1-8B、GPT2-XL 上以相同计量训练算力做继续预训练，SPAR 提升 RULER；从零预训练同时在 RULER 与 NoLiMa 上取得增益。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于语言模型会过度依赖不相关的前置文本：当预测已由局部上下文充分支撑时，扰动遥远且无关的 prefix token 仍会改变后续预测。该问题在长文档、packed 序列或充斥干扰项（distractor-heavy）的上下文中尤为严重，而这些场景正是工业界长上下文 serving、检索增强（多文档拼接）与上下文打包的实际形态。现有训练目标缺乏针对"何时不该听远前缀"的选择性鲁棒信号，导致模型上下文利用不稳。论文摘要未披露具体业务产品、线上指标或规模数据。

Method: SPAR（Selective Prefix Anti-Interference Regularization）是一种面向"选择性抗干扰"的预训练目标。流程为：对每个样本同时运行原始序列与 corrupt-prefix 输入（仅篡改距离较远的前缀），通过一个 short-context sufficiency gate 估计远前缀是否为目标 token 提供额外信息（即局部是否已足够），再用 gated KL 目标仅对局部已可支撑的 suffix 预测施加正则，迫使其在远前缀被扰动时保持稳定。核心创新在于"门控"——不盲目对所有 token 抗干扰，而由模型自身估计局部充分性后选择性稳定。机制分析表明该 gate 能识别局部充分 token，并在 gate 选中 token 上显著降低 prefix 敏感度。作者声称这是目标级（objective-level）信号；具体 gate 参数化、KL 权重、模型架构与训练细节论文摘要未披露。

Result: 离线结果：在 Qwen2.5-0.5B、Qwen2.5-3B、Llama-3.2-1B、Llama-3.1-8B、GPT2-XL 五类预训练基座上的继续训练（equal counted training compute 条件下），SPAR 提升 RULER；从零预训练实验在 RULER 与 NoLiMa 上均取得增益。摘要未给出具体数值、基线对照、离线指标幅度或方差。无线上 A/B 实验结果。模型规模：上述 0.5B/3B/1B/8B 及 GPT2-XL（论文摘要未披露其余训练数据规模、训练硬件与时长的具体数值）。推理硬件、延迟、吞吐、内存与成本：论文摘要未披露。

Conclusion: SPAR 面向"选择性抗干扰"提供了一种目标级正则化方案，可缓解无关远前缀对局部已确定预测的干扰，且在不同规模的开源基座上以同等算力取得 RULER 与 NoLiMa 增益，说明其可复用为长上下文、多文档/检索增强、上下文打包场景下的通用预训练或持续预训练组件。对工业 LLM 团队的可迁移点：以低成本目标级修改提升长上下文鲁棒性。局限与证据缺口：摘要未披露具体数值提升、训练/推理资源开销、是否引入额外推理延迟或内存、gate 额外计算成本，以及是否在真实产品测试集或线上 A/B 上验证，故实际生产收益与线上影响尚不可见。

Abstract: Language models can over-condition on irrelevant preceding text: predictions already supported by local context may still change when distant, unrelated prefix tokens are perturbed. This interference is especially consequential in long, packed, or distractor-heavy contexts, where useful evidence and irrelevant spans coexist. We propose Selective Prefix Anti-Interference Regularization (SPAR), a pretraining objective for selective anti-interference. SPAR runs the original sequence and a corrupt-prefix input in which only the far prefix is changed, then uses a short-context sufficiency gate and a gated KL objective to stabilize locally supported suffix predictions. The gate operationalizes a model-based estimate of whether the far prefix supplies additional information about the target token. Mechanism analyses show that the gate identifies locally sufficient tokens and sharply reduces prefix sensitivity on gate-selected suffix tokens. In continued training on pretrained base models, SPAR improves RULER across Qwen2.5-0.5B, Qwen2.5-3B, Llama-3.2-1B, Llama-3.1-8B, and GPT2-XL under equal counted training compute; pretraining experiments further show gains on both RULER and NoLiMa. These results show that selective anti-interference is an effective objective-level signal for robust context use.

</details>


### [11] [Improving LLM-based Autonomous Web Agents with Filtering](https://arxiv.org/abs/2609.27770)
*Zhitong Guo,Jing Yu Koh,Ruiyu Li*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司/团队所属机构。业务场景为基于LLM的自主网页智能体（自动化完成多步网页任务），所处环节为LLM智能体的输入预处理/上下文过滤与候选元素召回阶段。提出的系统为两类检索过滤策略：一是对HTML元素按与任务相关性排序的DeBERTa/T5排序模型，二是一个零样本ColBERT检索器。核心结果：DeBERTa模型将LLaMA-2-70B智能体在WebArena上的成功率从1.97%提升至2.96%；ColBERT检索器在Mind2Web上召回0.52、WebArena上召回0.47（ground-truth元素）。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈：LLM上下文窗口有限，原始HTML源码包含大量冗长且无关的细节，直接输入会增加噪声、难以在有限窗口内定位关键元素，导致智能体任务成功率低下。现有方案局限：论文首先复现GPT-3.5与LLaMA-2-70B在WebArena上的基线并识别常见失败模式，表明不经过滤直接输入HTML效果差。规模约束与业务意义：对自动化网页操作类产品（如RPA、客服自动化、企业工作流）而言，能否在受限上下文内精准提供与任务相关的页面元素，直接决定任务完成率与端到端可用性。

Method: 整体架构：LLM自主网页智能体 + 网页内容过滤前置模块。输入为网页HTML与任务，输出为过滤后的候选HTML元素供LLM决策。核心思路：以"相关性检索/排序"替代原始HTML全量输入，降低上下文噪声。关键模块：(1) DeBERTa-based 排序模型与 T5-based 排序模型，对HTML元素按与任务的匹配分数排序；(2) 训练方式为在Mind2Web轨迹数据上微调后迁移到WebArena（跨基准迁移）；(3) 零样本ColBERT-based检索器，用于直接检索ground-truth元素，未使用目标域标注。创新点（作者主张）：将检索过滤作为LLM智能体输入的预处理手段，结合排序式过滤与零样本检索两种策略，并验证跨数据集的迁移能力。论文摘要未披露模型具体输入表示、排序目标函数、ColBERT的编码与检索细节。

Result: 离线指标：DeBERTa-based过滤模型使LLaMA-2-70B智能体在WebArena上的成功率从1.97%提升至2.96%（相对提升约50%，绝对值+0.99个百分点）；零样本ColBERT检索器在Mind2Web上recall=0.52、WebArena上recall=0.47（ground-truth元素召回）。在线A/B测试：论文摘要未披露。模型规模：使用GPT-3.5、LLaMA-2-70B、DeBERTa-based、T5-based、ColBERT-based模型，但各过滤模型的参数量论文摘要未披露。训练数据：Mind2Web轨迹数据（规模未披露）。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟：论文摘要未披露。吞吐：论文摘要未披露。内存：论文摘要未披露。成本：论文摘要未披露。

Conclusion: 解决的问题：在LLM网页智能体场景下，通过检索过滤降低原始HTML上下文噪声与长度，提升任务成功率。实用价值：为任何"LLM处理长且含噪声的结构化/半结构化文档"的工业场景提供了一种轻量、可训练的输入过滤范式——以小型排序模型或零样本检索器精简上下文，再交给大LLM决策。局限与证据缺口：成功率绝对值仍极低（2.96%），且未披露在线业务收益、延迟/成本、过滤模型规模、训练资源；仅在学术基准WebArena/Mind2Web上验证，未体现真实企业产品流程。可复用点：工业搜索/推荐/LLM团队可将该"过滤优先于LLM"的思路迁移到长文档问答、网页/表格理解、RAG上下文裁剪等任务，用小型编码器做相关性预筛以节省窗口与算力；但采用前需自行验证延迟与成本收益。

Abstract: Autonomous web agents, powered by Large Language Models (LLMs), have garnered significant attention for automating various web-based tasks with multi-step reasoning and decision-making capabilities. An open research question in the development of these agents lies in the format of the webpage input. Raw HTML source code, with its extensive and often irrelevant details, poses difficulties for LLMs with limited context windows. To address this challenge, we first reproduce baseline models such as GPT-3.5 and LLaMA-2-70B on the WebArena (Zhou et al., 2023) benchmark, identifying common failure modes. We then propose two retrieval strategies to filter out irrelevant context for LLM agents. We develop DeBERTa-based and T5-based models that rank HTML elements by their relevance to the task. We fine-tune them on Mind2Web trajectory data and transfer them to WebArena. Experiments show that our DeBERTa-based model improves the success rate of the LLaMA-2-70B LLM agent on WebArena from 1.97% to 2.96%. Moreover, we develop a zero-shot ColBERT-based retriever that is able to retrieve the ground-truth element with a recall of 0.52 on Mind2Web and 0.47 on WebArena.

</details>


### [12] [When Parallel Drafter Meets Parallel Speculative Decoding](https://arxiv.org/abs/2609.27396)
*Fuliang Liu,Xue Li,Kun Qian,Zhibin Wang,Wanchun Dou,Wenyuan Yu,Chen Tian*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司/机构及团队所属业务部门。该工作面向LLM生产推理/服务（serving）阶段，属于解码加速与推理系统优化环节。作者提出并行投机解码框架DPara，复用现有高效并行drafter，同时在每一轮保证backbone验证与draft重叠：在目标模型验证期间，由扩散backbone为所有接受边界预计算draft表示但暂不指定bonus token，随后由一个轻量自回归head结合验证结果与匹配的预计算表示，几乎瞬时生成下一轮draft token。DPara消除了现有PSD"猜错则整批回退串行draft"的概率性退化。在Qwen3-8B与Qwen3-14B、七个数学/代码/聊天benchmark上，DPara相较自回归解码平均加速3.21x与3.52x，超过最强串行与并行投机解码基线。


<details>
  <summary>Details</summary>
Motivation: 现有Spark式并行drafter虽使投机解码高效，但其draft阶段在每一轮仍位于关键路径上被串行执行，构成生产推理吞吐瓶颈。并行投机解码（PSD）通过重叠draft与验证来缓解这一问题，但现有方法必须提前猜测接受前缀与bonus token：一旦猜错，整批回退到串行draft，引入了概率性的性能退化，损害实际serving的稳定性与期望吞吐。因此需要一种能在每一轮都保障backbone-验证重叠、彻底消除概率性回退的框架，以稳定地提升生产LLM推理速度。

Method: DPara是一个并行投机解码框架，复用有效的并行drafter并保证backbone验证与draft在每一轮重叠。核心流程：在目标（backbone）模型执行验证期间，DPara的扩散backbone为每一个接受边界预计算draft表示，同时将bonus token留空不指定（"bonus left unspecified"）；随后一个轻量自回归head将已揭示的验证结果与对应的预计算表示结合，几乎即时地生成下一轮的draft token。这样，占主导地位的backbone前向计算与验证完全并行，仅将可忽略的head开销串行化在关键路径上。其关键创新在于把"猜接受前缀与bonus token"的提前决策，改为"预计算所有接受边界的表示 + 事后由自回归head按实际验证结果即时生成"，从而避免猜错导致的整批回退串行draft。（本段为作者声明的复述，论文摘要未披露扩散backbone的具体结构、head参数规模、训练目标与训练数据。）

Result: 离线指标：在Qwen3-8B与Qwen3-14B两个模型、七个数学/代码/聊天benchmark上，DPara相较自回归解码平均加速3.21x（Qwen3-8B）与3.52x（Qwen3-14B），并超过最强的串行投机解码与并行投机解码基线（论文摘要未披露具体基线的量化数值）。在线A/B增益：论文摘要未披露。模型规模：论文摘要未披露（仅提及使用Qwen3-8B与Qwen3-14B作为目标模型）。训练数据、训练硬件/时长、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: DPara解决了并行投机解码中"猜错即整批回退串行draft"的概率性退化问题，通过扩散backbone预计算所有接受边界表示、配合轻量自回归head在验证后即时生成draft，将关键路径上的主要计算（backbone前向）与验证完全并行，仅保留可忽略的head串行开销。其实际价值在于为生产LLM推理serving提供稳定的、无概率回退的加速方案，在多个benchmark上稳定超越最强串行与并行投机解码基线。局限与证据缺口：论文摘要未披露公司/业务场景、在线部署收益、扩散backbone结构、head规模、训练数据与资源、推理硬件及端到端延迟/吞吐/成本，且加速为离线benchmark结果而非生产流量验证。可复用点：工业LLM推理团队可将"预计算接受边界表示 + 事后自回归head即时合成draft"的并行化思路集成到现有投机解码serving栈中，替代依赖猜测的PSD回退逻辑，尤其在长尾/难预测请求上减少回退带来的吞吐抖动。

Abstract: DSpark-style parallel drafters have made speculative decoding highly effective, yet their draft phase remains serialized on the critical path of every round. Parallel speculative decoding (PSD) overlaps drafting with verification, yet existing methods must guess the accepted prefix and bonus token in advance: a wrong guess reverts the whole batch to serial drafting. We present DPara, a PSD framework that reuses effective parallel drafters yet guarantees backbone--verification overlap in every round, thereby eliminating this probabilistic fallback altogether. While the target verifies, DPara's diffusion backbone precomputes draft representations for every acceptance boundary with the bonus left unspecified; a lightweight autoregressive head then combines the revealed verification outcome with the matching precomputed representation to emit the next round's draft tokens almost instantly---fully parallelizing the dominant backbone forward with verification and leaving only the negligible head cost serial. Experiments on Qwen3-8B and Qwen3-14B across seven math, coding, and chat benchmarks show that DPara achieves average speedups of $3.21\times$ and $3.52\times$ over autoregressive decoding, surpassing the strongest serial and parallel speculative decoding baselines alike.

</details>


### [13] [EviStreams: Human-in-the-Loop AI Data Extraction for Systematic Reviews in Medicine](https://arxiv.org/abs/2609.27418)
*Sai Karthik Kosuri,Ankita Shashikant Bhosale,Michael Glick,Alonso Carrasco-Labra,Chris Callison-Burch*

Main category: cs.CL

TL;DR: 作者来自学术团队（Chris Callison-Burch 等，具体公司/企业"论文摘要未披露"）；真实业务场景是医学系统综述的"数据提取"阶段——该阶段依赖两名评审员独立提取、仲裁人裁决分歧、并保留可审计记录，是专家劳动的核心瓶颈；所处环节为 LLM 在真实业务工作流中的"校准与部署/产品化"阶段（表格化字段定义、双盲评审、共识导出）；提出 EviStreams——一个活上线、开源、无代码的 Web 平台，在三个关键环节（程序设计、字段规格化、提取预测）让评审团队掌控 AI 辅助提取；核心发现是提取质量更多由字段规格（field specification）而非模型选择决定。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈：系统综述支撑临床指南，但其数据提取步骤高度依赖专家人工劳动，且受协议化工作流约束——两人独立提取、仲裁人解决分歧、团队保留每项数值产生方式的可审计记录。现有局限：LLM 虽可辅助提取，但辅助必须契合既定评审协议并保持可复现性，否则无法被实际采纳。论文未披露具体规模（文献数量、专家人数、耗时）等量化约束。问题重要性：数据提取是系统综述的关键限速环节，直接决定临床指南产出效率与可审计性。

Method: 系统架构：EviStreams 是一个活上线、开源（Apache-2.0）、无代码 Web 平台，将 AI 辅助提取置于三个关键环节的团队控制之下：(1)程序设计——批准代码运行前先做结构化分解；(2)字段规格化——从试点（pilot）校准的带类型字段定义；(3)提取预测——评审员盲化（reviewer-blinded）的双评审加仲裁。输入输出流程：领域专家通过表单构建器定义带类型字段（而非编写 prompt），在上传的 PDF 上运行提取，逐条检查每个值及其支撑来源段落，最后将盲化双评审解析为可审计的共识导出。核心创新（作者主张）：以"typed field specification"取代直接 prompt，把字段定义、盲化双评审、可审计导出制度化到产品流程中；核心经验结论是 field specification 对提取质量影响远大于模型选择。上述为作者主张，论文摘要未披露具体的模型实现细节、训练目标、字段类型系统细节或仲裁算法。

Result: 离线指标：在四个临床语料库与三个前沿模型家族（three frontier model families）上评估，关键结论是提取质量主要由字段规格（field specification）决定、而非模型选择；论文摘要未披露具体数值（如准确率、F1、一致率、仲裁率）及基线。在线 A/B 增益：论文摘要未披露。模型规模：论文摘要未披露（仅提到"三个前沿模型家族"）。训练数据：论文摘要未披露。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟：论文摘要未披露。吞吐量：论文摘要未披露。内存：论文摘要未披露。成本：论文摘要未披露。

Conclusion: 解决的问题：将 LLM 辅助的医学系统综述数据提取以"无代码、字段规格化、双盲评审、可审计导出"的方式产品化，使 AI 辅助契合既有评审协议并保持可复现性；核心可复用经验是"field specification 比模型选择更影响提取质量"。实用价值：为需要可审计、可复现、需专家掌控的 LLM 辅助标注/信息提取场景（如医学、法律、金融等强监管领域）提供了一个无代码工作流范本和开源平台。局限与证据缺口：论文摘要未披露任何具体评估数值、模型规模、推理成本与延迟，无法判断绝对性能与大模型规模化部署的可行性；团队来自学术机构，无企业级生产规模（并发用户、SLA、成本）数据。对工业团队的迁移点：将"字段规格工程"置于模型选型之前可作为 LLM 落地的高杠杆实践；盲化双评审+可审计导出机制在需要人类专家签核的文档提取/审核类产品中可直接借鉴；无代码 form builder 降低领域专家参与 AI 工作流设计的门槛。

Abstract: Systematic reviews underpin clinical guidelines, yet their data-extraction step is a major expert-labor bottleneck bound by a protocolized workflow: two reviewers extract each study independently, an adjudicator resolves disagreements, and the team keeps an auditable record of how every value was produced. Large language models can assist with extraction, but that assistance must fit established review protocols and preserve reproducibility. We present EviStreams, a live, open-source, no-code web platform that puts review teams in control of AI-assisted extraction at three key stages: program design (a structured decomposition approved before any code runs), field specification (typed field definitions calibrated from a pilot), and extracted predictions (reviewer-blinded dual review with adjudication). Working through a form builder, a domain expert defines typed fields rather than prompts, runs extraction over uploaded PDFs, inspects every value alongside the supporting passage it came from, and resolves a reviewer-blinded dual review into an auditable consensus export. An evaluation across four clinical corpora and three frontier model families, released with the system, shows that extraction quality is shaped far more by the field specification than by the choice of model. EviStreams is live at this https URL and released under Apache-2.0.

</details>


### [14] [Attention Routing Stabilizes Early: Working-Set Inference for Recurrent Language Models](https://arxiv.org/abs/2609.27373)
*Ke Wan,Chen Chen*

Main category: cs.CL

TL;DR: 作者为Ke Wan与Chen Chen（论文摘要未披露所属公司与团队）。面向工业LLM推理/加速场景（论文摘要未披露具体产品与管线阶段，属于语言模型推理阶段的注意力计算优化），提出训练无门槛的两阶段稀疏注意力方法WISE：早期递归步使用无约束全局注意力以发现稀疏工作集，后续步复用已发现的块结构支持并保持递归深度与支持内注意力计算动态化。在多头问答基准上几乎保持全注意力性能，2K上下文内质量几乎无损、4K出现可测损失；优化稀疏实现相对原生FlashAttention在4K下注意力加速最高1.76x，32步注意力全轨迹加速1.36x。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于递归语言模型在推理时每一步递归都要重复计算全局注意力，注意力计算成本随上下文长度和递归深度线性累加，构成长上下文推理的延迟/吞吐负担。作者通过研究注意力动态发现：注意力支持与分布沿递归深度的稳定速度显著早于隐藏状态与注意力输出，即"早期发现、后期精炼"的两阶段结构——这为在后期递归步复用稀疏支持、减少重复全局注意力计算提供了理论依据。论文摘要未披露更具体的产品规模、在线业务约束与现有方案(如原生FlashAttention以外)的量化局限。

Method: WISE(Working-set Inference with Support Exploitation)为训练无门槛（training-free）方法：早期递归步采用无约束全局注意力，用以直接发现块结构化的稀疏工作集（working set，即相关上下文的支持集合）；后期递归步复用该支持，同时保持递归深度不变，并使支持内的注意力计算保持动态（即支持集合固定但支持内部的注意力值仍逐层动态更新）。关键设计点：仅复用"支持"而非复用完整注意力输出，属于支持级（support-only）复用。作者通过受控干预实验对比了多种更严格的注意力复用方案，证明"递归发现"环节重要，且support-only复用比更严格复用方案更能保持模型行为。论文摘要未披露具体块结构划分方式、稀疏阈值选择、底层算子实现细节及与FlashAttention对接的工程方案。

Result: 离线指标：在多头问答（multi-hop QA）基准上，WISE大量保持全注意力性能（"largely preserves full-attention performance"为定性表述，未给出具体分数）；质量在2K上下文内几乎完全保持（"Quality is largely preserved through 2K context"），4K上下文出现可测量损失（"measurable loss at 4K"，具体损失幅度未披露）。上下文扩展实验显示工作集随上下文增长愈发稀疏，效率增益随之扩大（未披露稀疏度具体数值）。加速指标（实测）：优化稀疏实现相对原生FlashAttention，在4K上下文下注意力加速最高1.76x；对完整32步注意力轨迹的加速为1.36x。模型规模、训练数据、训练硬件/时长、推理硬件、延迟、吞吐、内存、成本等：论文摘要未披露。

Conclusion: WISE利用"注意力支持早于注意力值稳定"的现象，在训练无门槛前提下用两阶段稀疏支持复用降低递归LM的全局注意力重复开销，在2K上下文内质量几乎无损并实现相对FlashAttention最高1.76x的注意力加速，证明了支撑企业级长上下文LM推理加速的可行方向。局限与证据缺口：方法针对递归语言模型这一特定架构，未披露模型规模、硬件环境、绝对延迟、内存占用与4K下质量损失的具体量化，加速增益随上下文扩展但4K已出现质量损失，推广到更大上下文与通用Transformer架构需进一步验证；论文摘要未披露公司与团队，企业采用价值需结合具体模型栈评估。可复用经验：通过干预实验区分"支持复用"与"注意力复用"对模型行为的影响，为稀疏推理中"复用哪一层信息"提供了设计判据；"早期全局发现+后期支持复用"的训练无门槛范式可作为生产推理加速的候选方案。

Abstract: Recurrent language models repeatedly apply shared network blocks to refine latent representations, but standard inference recomputes global attention at every recurrent step. We study attention dynamics across recurrent depth and find that attention support and distributions stabilize substantially earlier than hidden states and attention outputs. This suggests a two-stage structure: early steps discover a sparse working set of relevant context, while later steps refine representations over largely the same routing support. Motivated by this structure, we introduce WISE (Working-set Inference with Support Exploitation), a training-free method that uses unrestricted global attention during early recurrence and later reuses directly discovered block-structured support while keeping recurrent depth and within-support attention computation dynamic. Controlled interventions show that recurrent discovery is important and that support-only reuse better preserves model behavior than more restrictive attention-reuse alternatives. Across multi-hop QA benchmarks, WISE largely preserves full-attention performance, while context scaling reveals increasingly sparse working sets and greater efficiency gains. Quality is largely preserved through 2K context, with a measurable loss at 4K. An optimized sparse-attention implementation achieves up to a 1.76x attention speedup over native FlashAttention at 4K and a 1.36x speedup for the full 32-step attention trajectory. Our code is available at this https URL .

</details>


### [15] [Can One Adapted Model Do It All? Fine-Tuning Strategy Selection for Customer Support LLMs](https://arxiv.org/abs/2609.27262)
*Md Tahmid Rahman Laskar,Xue-Yong Fu,Shashi Bhushan TN*

Main category: cs.CL

TL;DR: 论文摘要未披露具体公司和团队名称。研究的是工业客户支持（customer support）系统这一真实业务场景：LLM需要同时支持意图分类、问答、摘要、工具调用决策等多种技能，核心部署疑问是用多个任务专精模型还是单一模型（通过多任务训练、序列化更新或模型合并实现）。工作流阶段为模型训练与推理部署策略选择（fine-tuning strategy selection）。核心结论：在0.6B到32B的5个模型家族13个模型上，多任务全参数微调（multi-task full fine-tuning）是每个模型规模下的最稳健操作默认项；专精模型在其目标任务上强但在非目标任务上退化严重；序列化LoRA比序列化全参数微调更能保留早期技能；大模型上"专家模型+基础模型合并"能提升非目标任务鲁棒性且同任务损失有限。最终输出面向真实环境的微调策略选型指南。


<details>
  <summary>Details</summary>
Motivation: 生产客户支持系统需要单一LLM承载多种技能（意图分类、问答、摘要、工具调用决策），面临的核心部署瓶颈是：每个技能单独训练专精模型会增加模型资产数量、存储与运维成本，且存在路由（routing）错误风险；而单一模型通过不同微调策略组合多技能时，又面临灾难性遗忘、不同技能间权衡不清的问题。现有方案（多任务训练、序列化更新、模型合并）之间缺乏在统一训练协议下的公平对比，且缺乏来自专有生产数据的证据，导致团队难以判断该选哪种策略。该问题直接关系到客户支持业务的模型数量、服务质量和推理部署成本。

Method: 论文构建了一个统一训练协议下的系统性对比实验：覆盖5个模型家族（Qwen3、Qwen3.5、Gemma-3、Llama-3.1、Mistral）共13个模型，规模从0.6B到32B参数；8个客户支持数据集，其中4个公开、4个专有，约74.5k训练样本、8.7k评估样本；在固定训练协议下训练超过200个checkpoint。对比的微调策略包括：多任务全参数微调（multi-task full fine-tuning）、任务专精模型（specialist models）、序列化更新（sequential updates，含序列化LoRA与序列化全参数微调）、模型合并（merging，将专精模型与其基础模型合并）。评估维度包括目标任务表现（same-task）与非目标任务表现（off-task）的鲁棒性。论文摘要未披露具体损失函数、合并算法（如Task Arithmetic/TIES/DARE等）及模型输入输出结构细节。

Result: 实验结论（论文摘要未披露具体离线指标数值，仅给出定性结论）：1）多任务全参数微调在每个测试的模型规模上都是最强操作默认项；2）专精模型在自身目标任务上表现强，但在非目标任务上退化严重（说明可靠路由很重要）；3）序列化LoRA比序列化全参数微调能更好保留早期技能；4）模型合并（专精模型+基础模型）在大模型上提升非目标任务鲁棒性，且同任务损失有限。模型规模：0.6B到32B参数，13个模型、5个家族。训练数据规模：约74.5k训练样本、8.7k评估样本，8个数据集（4公开+4专有）。训练/推理硬件、训练时长、延迟、吞吐、显存、成本均未在摘要披露。论文摘要未披露在线A/B测试结果及具体离线指标数值。

Conclusion: 该研究解决的是多技能客户支持LLM的微调策略选型问题，给出一个可复用的操作性默认结论：在无法确定具体任务分布时，多任务全参数微调是最稳健的起点；若需隔离技能，应优先采用序列化LoRA而非序列化全参数微调以缓解遗忘；对大模型可通过专家模型与基础模型合并来兼顾目标与泛化。对工业LLM团队的可复用价值在于提供了一套跨模型家族、跨规模、含专有数据的统一对比框架和选型指南。局限与证据缺口：摘要未披露具体离线指标数值、模型合并的具体算法、训练与推理资源成本，且未报告在线A/B实验结果，因此"选型指南"的量化力度与线上增益尚不可知。团队落地前仍需结合自身数据分布、延迟预算和路由机制做进一步验证。

Abstract: Production customer-support systems often require LLMs to support multiple skills, such as intent classification, question answering, summarization, or tool-use decisions. A central deployment question is whether these skills should be handled by separate task-specialist models or by a single model trained through multi-task training, sequential updates, or model merging. We study this question using thirteen models spanning five families (Qwen3, Qwen3.5, Gemma-3, Llama-3.1, and Mistral) from 0.6B to 32B parameters across eight customer-support datasets, spanning four public and four proprietary datasets with approximately 74.5k training and 8.7k evaluation samples. Under a fixed training protocol, we train more than 200 checkpoints. Our experiments reveal that multi-task full fine-tuning is the strongest operational default at every model size we test. Specialist models are strong on their target tasks but often degrade sharply off-task, making reliable routing important. Sequential Low-Rank Adaptation (LoRA) preserves earlier skills better than sequential full fine-tuning, while merging a specialist with its base model improves off-task robustness with limited same-task loss for larger models. We conclude with practical guidelines for selecting fine-tuning strategies in real-world settings.

</details>


### [16] [UniDataAgent: An Ontology-Grounded Agent for Enterprise Question-to-Report Automation](https://arxiv.org/abs/2609.27257)
*Yutai Duan,Yahui Zhao,Zhangti Li,Yu Ma,Zhenfeng Qi,Shaoyang Yuan,Jing Fan,Jie Liu*

Main category: cs.CL

TL;DR: 中国联通（ChinaUnicom）团队提出并部署了 UniDataAgent（ChinaUnicom DataAgent），一个面向企业"问题到报告自动化"（question-to-report automation）的本体驱动（ontology-grounded）智能体系统。核心设计是将"语义获取"与"在线执行"解耦：Ontology Acquisition and Validation（OAV）阶段构建版本化企业本体，Question-to-Report Execution（QRE）阶段检索语义契约、协调技能与数据工具、校验结果并生成带证据链接的报告。在 27 张企业表和约数千种指标类型上，本体构建从人工约一周缩短到几小时，报告生成从数个工作天缩短到几分钟；真实业务问题上本体基座取得 95.0% 严格准确率，对比文档 RAG 为 72.5%，尤其在结构化与组合型任务上优势明显。系统已部署并产生成本节约。


<details>
  <summary>Details</summary>
Motivation: 企业数据智能体不能只把自然语言问题翻译成查询，必须保留组织特定的语义（organization-specific semantics）。现有基于文档 RAG 的方案在真实业务问题上严格准确率仅 72.5%，在结构化（structured）与组合型（compositional）任务上表现尤其不足，导致生成的报告不可信、难以直接用于决策。同时人工构建企业本体约需一周、人工生成报告需数个工作天，成本高且不可复用。业务痛点在于：企业语义需沉淀为可版本化、可复用、可校验的资产，并将提效与准确性同时落地到真实生产环境。

Method: 系统采用两阶段解耦架构。第一阶段 Ontology Acquisition and Validation（OAV）：从元数据（metadata）、业务知识（business knowledge）和支持材料（supporting materials）出发，通过专家编写的业务技能（expert-authored business skills）、受约束生成（constrained generation）、问题验证（question verification）和选择性专家评审（selected expert review）构建并校验版本化的企业本体。第二阶段 Question-to-Report Execution（QRE）：为每个问题检索对应的语义契约（semantic contracts），协调技能与数据工具（coordinates skills and data tools），校验结果（validates results），并生成带证据链接的报告（evidence-linked reports）。摘要披露的设计思想是"将语义获取与在线执行分离"（separates semantic acquisition from online execution），以实现语义资产的可复用性与执行阶段的在线效率；具体模型架构、输入输出流、训练目标等细节论文摘要未披露。

Result: 离线指标：在真实业务问题上，本体基座（ontology grounding）取得 95.0% 严格准确率（strict accuracy），对比文档 RAG（document RAG）为 72.5%，摘要特别指出在结构化（structured）与组合型（compositional）任务上提升更显著（未披露具体细分数值）。效率指标：本体构建在 27 张企业表和约数千种指标类型上耗时"几小时"（a few hours），人工方式约需一周（about one week）；报告生成耗时"几分钟"（a few minutes），人工方式需数个工作天（several working days）。在线/部署指标：摘要称系统"已经部署"并"生成成本节约"（generate cost savings），但未披露具体的 A/B 实验增益、成本节约金额或百分比。模型规模、训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存与成本等资源信息：论文摘要未披露。

Conclusion: 该系统解决的核心问题是：如何让企业 LLM 智能体在保留组织特定语义的前提下，把自然语言问题可靠地转化为可复用、可校验、带证据的报告，同时显著降低人工本体构建与报告编制成本。其实用价值在于提供了一条"先离线沉淀版本化本体（语义资产），再在线轻量执行"的工业落地路径，可被其他企业复制（replicated in other enterprises）。对工业团队可迁移的要点包括：语义获取与执行解耦、专家技能+受约束生成+验证+人工评审的混合构建流程、以语义契约组织技能与工具、以及用证据链接增强报告可信度。局限与证据缺口：摘要未披露系统具体模型/架构细节、细分任务准确率、成本节约量化数字，也未披露任何在线 A/B 实验结果或延迟/吞吐/资源开销；"几小时""几分钟""约一周""数个工作天"均为口头描述，缺乏精确测量口径，外推其收益前需审慎。工业搜索、广告、推荐与 LLM 团队可借鉴其"语义资产版本化管理与专家在环校验"的方法论，但需注意该工作属企业数据问答/报表自动化场景，与检索、排序、CTR/CVR 等环节无直接对应。

Abstract: Enterprise data agents must preserve organization specific semantics, not just translate questions into queries. We present ChinaUnicom DataAgent (UniDataAgent), an ontology grounded system for reusable question-to-report analysis that separates semantic acquisition from online execution. Ontology Acquisition and Validation stage (OAV) builds versioned enterprise ontologies from metadata, business knowledge, and supporting materials through expert authored business skills, constrained generation, question verification, and selected expert review. Question-to-Report Execution (QRE) stage retrieves semantic contracts for each question, coordinates skills and data tools, validates results, and produces evidence linked reports. Across 27 enterprise tables and roughly thousands of metric types, ontology construction took a few hours instead of about one week manually. It took just a few minutes to generate the reports, instead of several working days. Ontology grounding achieved 95.0\% strict accuracy on real business questions, versus 72.5\% for document RAG, especially on structured and compositional tasks. The system has already been deployed to generate cost savings and has the potential to be replicated in other enterprises.

</details>


### [17] [Distilling Sequential Computation in Transformer Language Models](https://arxiv.org/abs/2609.27233)
*Zixuan Lan,Jessica Yang,Yanhong Li,Karen Livescu,Jiawei Zhou*

Main category: cs.CL

TL;DR: 论文摘要未披露作者所属公司/机构与团队。该工作面向生产 LLM 的推理服务阶段：针对自回归模型随上下文增长而推理成本递增的问题，提出一种顺序计算蒸馏方法——用轻量级 merge 模块在推理时（on the fly）将连续静态 token 嵌入压缩为单个代理嵌入，使预训练模型在无需架构改动或重训练的情况下处理压缩输入；同时用回滚机制把存储的多 token KV cache 替换为单步代理，从而压缩提示与中间解码步骤。实验显示可将有效序列长度最多降低 40%，在语言建模、问答、摘要、常识推理和长文本数学推理等任务上精度损失极小，轻量适配还可进一步改善压缩-精度权衡。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于 Transformer 语言模型以自回归方式逐 token 处理序列，随着上下文长度增长推理成本迅速上升（KV cache 存储与逐 token 计算均为线性增长）。作者观察到许多相邻 token span 高度可预测或常以稳定单元出现，暗示其表示可被压缩。现有方案（如直接截断、静态剪枝）可能损失信息或需要修改架构/重训练；本工作希望在不改动预训练模型、不重训练的前提下，于推理阶段压缩提示与中间解码步骤，从而降低有效序列长度和推理开销。论文摘要未披露具体业务产品、线上规模、延迟/吞吐指标。

Method: 核心思想是用一个轻量级 merge 模块，在推理时把一段连续 token 的静态嵌入序列压缩成单个代理（surrogate）嵌入，该代理嵌入近似承载多个 token 的功能角色，使预训练模型直接处理压缩后的输入序列。关键组件包括：(1) merge 模块——输入为一段静态 token 嵌入，输出单个代理嵌入，属于即插即用、不改动 Transformer 架构、不需要重训练的小型模块；(2) 两类应用场景——压缩提示（prompt）与压缩中间解码步骤；(3) rollback 回滚机制——将已存储的多 token KV cache 条目替换为其单步代理，实现 KV 层面的压缩；(4) 可选轻量适配——对 merge 模块做轻量微调以改善选定场景下的压缩-精度权衡。上述机制为作者明确表述；至于 merge 模块的具体网络结构（如是否用小型注意力或 MLP）、训练目标与损失函数、代理替换的触发策略等，论文摘要未披露。

Result: 离线指标：在多种模型上，merge 模块可将有效序列长度最多降低 40%，且在语言建模评测及下游任务（问答、摘要、常识推理、长文本数学推理）上精度退化极小；对 merge 模块做轻量适配后，在选定场景下进一步改善压缩-精度（accuracy-compression）权衡。论文摘要未披露具体的模型名称/规模、具体 benchmark 分数与基线数值、具体的性能下降幅度（仅定性为 minimal）。在线 A/B 测试增益：论文摘要未披露（无任何在线实验报告）。模型规模、训练数据规模、训练硬件/时间、推理硬件、延迟、吞吐、内存与成本：论文摘要未披露。

Conclusion: 该工作解决的核心问题是：在不改动预训练 Transformer、不重训练的前提下，通过推理时 token 压缩降低有效序列长度，从而缓解长上下文的计算与 KV cache 开销。其实用价值在于 merge 模块即插即用、通用性强，可跨多个模型和任务（LM、QA、摘要、常识、长文本数学推理）复用，且轻量适配可进一步优化权衡，适合作为工业 LLM 推理加速的候选手段。局限性/证据缺口：论文摘要未披露公司与团队、实际生产系统、模型规模、可量化的精度损失、延迟/吞吐/内存收益，也无在线 A/B 验证；"minimal accuracy degradation" 缺乏具体数字支撑，40% 的序列长度压缩是否能转化为对应比例的端到端延迟或成本下降也未在摘要中验证。工业 LLM 团队可复用的点：merge 模块作为推理期上下文压缩、KV cache 压缩的插件化方案，以及 rollback 回滚式 KV 替换机制的设计思路。

Abstract: Transformer language models process sequences token by token in an autoregressive manner, making growing contexts increasingly expensive. Yet many adjacent token spans are highly predictable or frequently occur as stable units, suggesting that their representations may be compressible. We introduce a method for distilling sequential computation by replacing spans of input tokens with collapsed representations, computed on the fly by a lightweight merge module. This module generates a single surrogate embedding from a sequence of static token embeddings that captures the functional role of the multiple tokens, allowing pretrained models to operate on compressed inputs without architectural changes or re-training. We apply this approach during inference to compress both prompts and intermediate decoding steps, using a rollback mechanism to substitute stored multi-token KV cache entries with their single-step surrogates. Experiments across diverse models show that the merge module can be used to reduce effective sequence length by up to 40% with minimal accuracy degradation across language modeling evaluations and downstream tasks, including question answering, summarization, commonsense reasoning, and long-form mathematical reasoning. Additional lightweight adaptation of the merge module further improves the accuracy-compression trade-off in selected settings. These results demonstrate that sequential token computation in Transformers can be effectively approximated through condensed surrogate representations that approximate the original behavior without model updating.

</details>


### [18] [Experts Rise Where LLMs Disagree: Using Cross-Model Disagreement to Target Expert Effort in LLM Codebook Revision for Large-Scale Annotation](https://arxiv.org/abs/2609.26926)
*Zeyu He,Zhuqian Zhou,Kirk Vanacore,Rene F. Kizilcec,Ting-Hao 'Kenneth' Huang*

Main category: cs.CL

TL;DR: 论文摘要未披露公司/机构及团队名称（作者隶属信息未在摘要中给出）。业务场景是大规模文本标注的codebook（编码手册）修订：传统上编制稳健的codebook需数月，作者提出用早期codebook让多个LLM对数据标注，利用“跨模型分歧（cross-LLM disagreement）”定位疑难case，再有针对性地引导专家反馈（三种方式），从而把codebook修订从数月缩短到数天。该工作属于工业LLM应用中“标注工具链/数据标注效率”环节，与搜索/广告/推荐系统的训练数据生产管线相关（但摘要中标注对象为辅导会话转录文本，非显式广告/推荐数据）。头条结果：Rationale Labeling使LLM标注准确率达64.9%，超过“专家修订后的codebook”的57.8%；最佳Question Answering设置也达60.5%。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈：大规模文本标注依赖codebook（AI标注器遵循的编码手册），但开发一个稳健的codebook“takes months”（耗时数月），成为标注管线投产的关键瓶颈。现有方案局限：纯人工开发codebook周期长、成本高。作者提出用LLM加速：先以早期codebook让LLM对全量数据标注，利用“强LLM分歧（strong LLM disagreement）”作为信号，定位最需要专家介入的case，再有针对地获取专家反馈来修订codebook。业务价值：若能在不牺牲标注性能的前提下把“months”缩短到“days”，将显著提升大规模标注的产出效率，对需要海量训练/评测标签的搜索、广告、推荐及LLM生产系统具有重要意义（该推断基于摘要中“millions of documents”“months to days”表述；摘要未披露具体业务规模数字）。

Method: 核心思路：以“跨模型分歧（cross-LLM disagreement）”为先验信号，定位LLM自身难以达成一致的样本，从而把有限的专家精力聚焦到这些高信息量case上，用于codebook修订（属于主动学习/分歧采样在LLM标注中的一种变体）。对比了三种专家反馈方式作为codebook修订的输入：(i) Codebook Verifying——专家直接编辑由“跨LLM分歧驱动”的LLM生成的codebook修订稿；(ii) Question Answering——专家回答关于LLM分歧点的问题；(iii) Rationale Labeling——专家对分歧case进行打标并给出理由（rationale）。评估方式：以专家标签为ground truth，衡量修订后codebook下LLM标注的准确率。上述为摘要披露的原作者主张；具体架构、输入输出格式、训练/微调目标、模型选择、prompt设计等细节论文摘要未披露。

Result: 离线指标（以专家标签为gold label，实验于“thousands of tutoring-session transcripts”）：Rationale Labeling使LLM标注准确率最高，达64.9%，优于“expert-revised codebook（专家修订后的codebook）”的57.8%；最佳Question Answering设置为60.5%，同样优于57.8%的基线。作者据此宣称可将codebook修订周期从“数月（months）”缩短至“数天（days）”且不牺牲标注性能（该周期缩短为作者定性陈述，摘要未给出具体时间计量）。在线A/B测试增益：论文摘要未披露。模型规模：论文摘要未披露。训练数据：论文摘要未披露（仅提及“thousands of tutoring-session transcripts”用于实验）。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟/吞吐/内存/成本：论文摘要未披露。

Conclusion: 解决的问题：如何在保持标注质量的前提下，大幅缩短大规模文本标注中codebook的开发周期；方法上以“跨LLM分歧”执行专家精力的策略性投放，证明了LLM可作为标注管线的质量控制与加速工具。实用价值：为拥有海量标注需求的生产团队（如搜索/广告/推荐的数据标注、LLM评测数据构建）提供了一条可复用的“分歧采样+轻量专家反馈”范式，Rationale Labeling（带理由标注）经验上最有效。局限与证据缺口：论文摘要未披露公司/团队背景，标注对象为教育领域“tutoring-session transcripts”，与搜索/广告/推荐核心业务数据（query、曝光、点击、竞价等）无直接关联；准确率绝对值虽高于基线但绝对水平（57.8%~64.9%）相对有限；模型规模、训练/推理资源、成本、时间等关键工程指标全部缺披露；“数月到数天”的提升为定性描述，缺少可验证的定量证据，且无在线实验或与真实生产管线集成的验证。可迁移启示：工业搜索/广告/推荐及LLM团队可借鉴“用跨模型一致性/分歧作为标注优先级信号”的思路，将专家人工从全量审阅转向对边缘/分歧样本的定向投入，以加速标注codebook迭代；但落地前需自行验证其在自身业务数据上的准确率增益与成本收益。

Abstract: Large-scale text annotation brings expert insight to millions of documents, often through a codebook that AI annotators follow. Developing a robust codebook, however, takes months. Large language models (LLMs) could speed this process by applying an early codebook to the data, surfacing cases with strong LLM disagreement, and eliciting expert feedback to address them. We examined three ways experts can provide feedback for LLM codebook revision: (i) editing LLM-generated revisions driven by cross-LLM disagreement (Codebook Verifying), (ii) answering questions about LLM disagreements (Question Answering), and (iii) labeling disagreement cases with rationales (Rationale Labeling). Experiments on thousands of tutoring-session transcripts show that Rationale Labeling yielded the highest LLM-labeling accuracy (64.9%) against expert labels, outperforming the expert-revised codebook (57.8%). The best Question Answering setting also outperformed it (60.5%). Our work shows that LLMs can be used to strategically target expert attention, shortening months of codebook revision to days without sacrificing labeling performance.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [19] [Exact Quantile Balancing and Load-Error Injection for Mixture-of-Experts](https://arxiv.org/abs/2609.28053)
*Pit Neitemeier,Jiaze Li,Alessio Serra,Philipp Scholl,Sohir Maskey*

Main category: cs.LG

TL;DR: 论文摘要未披露公司/机构与团队名称；业务场景为大规模MoE（Mixture-of-Experts）LLM的训练与专家并行分布式执行优化，属训练/系统效率环节。作者提出两个方法：Exact Quantile Balancing（EQB，以可忽略通信量计算精确的全局batch BF16分位数以改善全局负载均衡）和Load-Error Injection（LEI，将本地负载误差直接注入router-score梯度以改善本地负载均衡）。在7.5B参数、最多500B tokens训练的MoE上，EQB相较朴素QB改善全局均衡与下游性能，LEI改善本地均衡并在可比质量下优于GShard loss。


<details>
  <summary>Details</summary>
Motivation: MoE训练面临两类负载均衡问题：全局均衡不足会导致专家欠利用（expert under-utilization），本地均衡不足会损害专家并行执行的效率。现有分布式Quantile Balancing（QB）依赖分片相关（shard-dependent）或近似的全局分位数，精度受限；而token无关（token-independent）的专家偏置无法保证microbatch级别的均衡。这些瓶颈直接影响大规模MoE训练的资源利用率、执行效率与下游模型质量，是工业级LLM训练系统的核心痛点。

Method: 论文提出两个核心模块：(1) EQB（Exact Quantile Balancing）——在分布式训练中以可忽略（negligible）的通信开销计算精确的全局batch BF16分位数，替代shard相关或近似的全局分位数，用于改善router的全局负载均衡；(2) LEI（Load-Error Injection）——将本地（local）负载误差直接注入router分数（router-score）的梯度中，从而在microbatch级别实现更精细的负载均衡控制，取代token无关的专家偏置方案。二者分别针对全局与本地两个均衡层级。摘要未披露具体网络结构、输入输出流、训练目标函数细节及创新点的技术推导。

Result: 离线结果：在7.5B参数的MoE上、以最多500B tokens训练的规模下，(a) EQB相较朴素QB（naive QB）改善了全局负载均衡并提升了downstream性能；(b) LEI改善了本地负载均衡，并在可比质量（comparable quality）条件下优于GShard loss。论文摘要未披露具体数值指标、baseline精确数值、评测benchmark、在线A/B实验增益。模型规模：7.5B参数MoE。训练数据：最多500B tokens。训练硬件/时间：论文摘要未披露。推理硬件、延迟、吞吐、内存、成本：论文摘要未披露。

Conclusion: 该工作针对大规模MoE训练中全局与本地两个层级的负载均衡问题给出可落地的解决方案：EQB以低通信开销实现精确全局分位数，LEI通过梯度注入实现microbatch级本地均衡，并在7.5B/500B tokens的真实规模上验证了有效性。可复用价值在于为工业界MoE训练系统提供了一种改善专家利用率与专家并行执行效率、同时不牺牲下游质量（甚至提升）的负载均衡技术，可作为GShard loss的替代或补充。局限与证据缺口：论文摘要未披露公司背景、具体量化指标（均衡度、下游benchmark得分）、硬件/时间/成本、以及EQB"可忽略通信"的具体量级，也未说明在线推理阶段的收益，因此实际部署收益需以原文数据进一步确认。

Abstract: Mixture-of-Experts (MoE) training requires global load balance to prevent expert under-utilization and local balance for efficient expert-parallel execution. Existing distributed Quantile Balancing (QB) uses shard-dependent or approximate global quantiles, while token-independent expert biases cannot ensure microbatch-level balance. We introduce Exact Quantile Balancing (EQB), which computes exact global-batch BF16 quantiles with negligible communication, and Load-Error Injection (LEI), which injects local load errors directly into router-score gradients. On 7.5B-parameter MoEs trained for up to 500B tokens, EQB improves global balance and downstream performance over naive QB, while LEI improves local balance and outperforms the GShard loss at comparable quality.

</details>


### [20] [Learning Collective Dynamics with Differentiable Gaussian Representations](https://arxiv.org/abs/2609.28405)
*Jianxiang Ma,Mingfu Zhang,Xiaocui Yang,Yichen Gao,Junzhao Huang,Yuesong Hou*

Main category: cs.LG

TL;DR: 本文作者单位与团队为“论文摘要未披露”，仅列作者 Jianxiang Ma、Mingfu Zhang 等六人。业务场景为基于群体/集体行为的聚合计数学习与预测（可能服务于推荐系统的用户群体行为建模、聚合行为预测或数据分析环节，具体业务场景摘要未明确披露；管道阶段论文摘要未明确标注，更贴近群体用户建模/行为预测而非检索或排序链路）。系统为 Differentiable Gaussian Dynamics（DGD），以高斯混合表征异质响应倾向，通过可微聚合接触强度与行为概率、并借助反馈递归更新后续响应，实现对群体动态的端到端学习。核心结果为：在 KuaiRand-Pure 的四个窗口与 Online Retail II 上，DGD 的联合行为负对数似然优于带联合行为头的 DeepAR 适配版；在 Retail 2010 上单日行为计数 MAE 为 4.71，对比基线 6.88；在 KuaiRand 标准推荐窗口，学习分布使行为负对数似然相对固定高斯下降 10.82%；移除反馈动态使受控实验联合 KL 从 0.0340 升至 0.2577。


<details>
  <summary>Details</summary>
Motivation: 论文要解决的问题是：集体响应（collective responses）取决于个体差异、接触机会和累积经验，而实际中往往只能观察到聚合计数（aggregate counts），需要从聚合观测中学习群体响应分布并连接“当前观测”与“未来行为”。现有方法的局限在于难以同时刻画群体的异质响应分布、接触/交互强度与时间反馈过程；论文以 DeepAR 改编版（加联合行为头）作为对比基线。对业务的意义在于：在只能拿到聚合级数据（如群体曝光、互动计数）的场景下，仍能还原群体行为分布并进行未来行为计数预测，这对应推荐/零售里群体行为建模与预测的普适瓶颈。论文摘要未披露具体的生产系统规模、数据量级或在线实验约束。

Method: DGD 由三个组件构成（作者声明）：(1) 用高斯混合（Gaussian mixture）表征异质的响应倾向（response propensities）分布；(2) 对接触强度（contact intensity）与行为概率做可微聚合（differentiable aggregation）；(3) 反馈递归（feedback recurrence）用于更新后续响应。关键技术手段为“重参数化积分（reparameterized integration）”与“时间递归（temporal recurrence）”，使聚合预测误差能够联合训练分布、观测函数与反馈参数。整体输入为聚合计数，输出去拟合/预测群体联合行为计数；训练目标为联合行为负对数似然（joint behavioral negative log-likelihood），并辅以 KL 散度等受控实验指标。该方法的核心创新（作者观点）在于用可微的高斯群体表示把群体分布、聚合运算与反馈动态统一到可端到端学习的框架中。论文摘要未披露模型参数规模、训练/推理硬件、吞吐与延迟。

Result: 离线指标：在 KuaiRand-Pure 的四个窗口与 Online Retail II 上，DGD 取得低于“带联合行为头的 DeepAR 适配版”的联合行为负对数似然（joint behavioral negative log-likelihood）；在 Retail 2010 上，单日行为计数 MAE 为 4.71，而该 DeepAR 适配版为 6.88；在 KuaiRand 标准推荐窗口（standard-recommendation window）上，学习分布使行为负对数似然相对固定高斯下降 10.82%；在受控实验中，移除反馈动态使联合 KL 从 0.0340 升到 0.2577。论文摘要未披露在线 A/B 实验收益。模型规模、训练数据量、训练硬件/时间、推理硬件、延迟、吞吐、内存与成本，摘要均未披露。

Conclusion: 论文解决的是“仅凭聚合计数学习群体行为分布并预测未来行为”的建模问题，证明了学习群体分布表征与反馈过程的价值，并给出了可复用的 DGD 框架（高斯混合群体表示 + 可微聚合 + 反馈递归）。实践价值：在只能获取聚合级数据的场景（如隐私受限的群体统计、集团/群体行为预测）中，可用 DGD 类方法重建分布并提升计数预测精度。局限与证据缺口：论文摘要未披露公司/团队、未披露与排序/检索/CTR 等核心推荐链路的直接衔接、未提供在线实验，收益均为离线指标；其 MAE 与 NLL 提升幅度未同时给出完整基线设置，仅对 DeepAR 改编版作对比。可迁移经验：工业搜索/广告/推荐团队可借鉴“可微群体分布表示 + 反馈递归 + 聚合计数端到端训练”的思路用于群体用户建模、聚合行为预测或冷启动类问题，但需自行验证其在具体业务 pipeline（如召回、粗排、精排、CTR）中的实际增益。

Abstract: Collective responses depend on individual differences, contact opportunities, and accumulated experience. Learning their dynamics from aggregate counts requires connecting a population's response distribution to both current observations and future behavior. We introduce Differentiable Gaussian Dynamics (DGD), which learns this connection through three components: a Gaussian mixture representing heterogeneous response propensities, differentiable aggregation of contact intensity and behavioral probabilities, and feedback recurrence that updates subsequent responses. Reparameterized integration and temporal recurrence let aggregate prediction errors jointly train the distribution, observation functions, and feedback parameters. On four windows from KuaiRand-Pure and Online Retail II, DGD achieves lower joint behavioral negative log-likelihood than a DeepAR adaptation with a joint-behavior head. In Retail 2010, its one-day behavioral-count MAE is 4.71 versus 6.88 for this adaptation. Learning the distribution reduces behavioral negative log-likelihood by 10.82% relative to a fixed Gaussian in KuaiRand's standard-recommendation window; removing feedback dynamics raises joint KL from 0.0340 to 0.2577 in a controlled experiment. These results establish the value of learning population representations and their feedback process from aggregate observations. Code is available at this https URL .

</details>


### [21] [Global tree forecasters collapse at the hierarchical aggregate: a five-panel failure characterization](https://arxiv.org/abs/2609.27912)
*Md Rezwanul Islam,Wael Mohammed*

Main category: cs.LG

TL;DR: 论文摘要未披露公司名称与具体团队；业务场景为生产环境B2B市场的层级时间序列预测（作者自报“in our production deployment”和“production business-to-business marketplace”），流水线阶段为训练/预测侧的全球梯度提升树模型失效诊断。作者提出对该设计的系统性失效表征：全球树在层级聚合（hierarchical aggregate）上预测崩溃，生产部署中总聚合被低估30–50倍，公开M5重建中最高低估496倍。其机制是树在训练范围以外输出常数，因聚合总量远超任一训练序列而触发。作者同时给出三种缓解方案（逐序列缩放、加权聚合级训练行、季节差分），并给出部署系统三步诊断预防流程。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈：在生产B2B市场层级预测中，全球梯度提升树对个体序列训练后，对层级聚合总量的预测严重低估（生产部署中低估30–50倍），导致总需求/总额预测崩溃。现有方案局限：作者认为此前文献未记录该“聚合塌缩”失效；标准配置调整均无法阻止——将所有层级混合入训练在规模上失败，唯一能让叶节点拟合线性模型的参数仅能缓解而无法根治。规模约束：聚合总量远超单个训练序列的取值范围（scale gap仅1.15倍即已损失总量的三分之一），触发树外推时的常数输出机制。

Method: 核心机制（作者论证）：梯度提升决策树在训练特征范围以外预测为常数，层级聚合总量远超所有训练序列的取值，故聚合预测塌缩。缓解方案（三条均被作者验证）：①逐序列缩放（per-series scaling，Manso与Hyndman 2021建议的预处理步骤）可防塌缩；②加权聚合级训练行（weighted aggregate-level training row）；③季节差分（seasonal differencing）。此外作者描述了滚动预测（递归多步）下的方案分化：聚合行方案会再次塌缩，逐序列缩放有所退化但仍保持低误差，仅季节差分保持单步精度不变。作者还提出部署系统的三步诊断与预防流程。需要说明的限缩：作者自评其贡献是“表征”（characterization）而非新算法。论文摘要未披露具体模型架构、输入输出形状、训练目标函数、超参数或特征工程细节。

Result: 离线/实证指标（作者报告）：生产部署中层级聚合低估30–50倍；公开M5重建中最高低估496倍；阈值效应——scale gap仅1.15倍即损失总量的三分之一。鲁棒性与显著性：该失效在五个面板上复现（生产B2B市场、合成层级、M5、澳大利亚旅游局Tourism、公开企业买家面板），在三个树库（tree libraries）上均成立，对训练随机种子不变，且统计显著。滚动预测下：聚合行方案重新塌缩、逐序列缩放有所退化但仍低、季节差分保持单步精度不变。在线A/B测试增益：论文摘要未披露。模型规模、训练数据量、训练硬件/时长、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: 作者解决/贡献的问题：系统地表征了全球梯度提升树在层级聚合层级上预测塌缩这一此前未被记录的失效模式，并给出成因机制（树外推常数输出 + 聚合总量超出训练范围）与可操作的预防手段（逐序列缩放、聚合级训练行、季节差分及三步诊断预防流程）。实践价值：对任何将全局树用于层级/聚合时间序列预测的生产系统（供应链、销售、广告投放量、市场成交总额等）具有直接警示价值，尤其当聚合目标量级显著大于训练个体序列时。局限与证据缺口：作者将贡献限定为“表征”而非新算法；论文摘要未披露公司名称、模型规模、数据规模、硬件与推理资源、延迟与成本，也未报告在线A/B验证，泛化到其他业务场景需进一步验证。可迁移经验：部署前应检查聚合层级的scale gap与训练取值范围，优先引入逐序列缩放或季节差分，并警惕聚合行方案在多步递归滚动下的再次塌缩。

Abstract: Global forecasting models pool many series and learn one shared function. Gradient-boosted trees are their most common form. We measure a failure of this design that has not, to our knowledge, been documented. Train a global tree on the individual series of a hierarchy, then ask it for the hierarchical aggregate. The aggregate sits far outside the model's training range, and the forecast collapses. The model under-predicts the total by 30-50x in our production deployment, and by up to 496x in a public M5 reconstruction. The mechanism is known: beyond its training range, a tree predicts a constant. It surfaces at the aggregate because the total dwarfs every training series. The cure is not new. Per-series scaling, the preprocessing step that Montero-Manso and Hyndman (2021) recommend, prevents the collapse. So do a weighted aggregate-level training row and seasonal differencing. Our contribution is the characterization. The collapse reproduces on five panels: a production business-to-business marketplace, a synthetic hierarchy, M5, Australian Tourism, and a public business-buyer panel. It holds on three tree libraries, is invariant across training seeds, and is statistically significant. Its onset is immediate and tracks a simple support bound: a scale gap of only 1.15x already costs a third of the total. No standard configuration change prevents it: pooling every hierarchy level into training fails at scale, and the one knob that fits linear models in the leaves softens it without curing it. Rolling the forecasts forward recursively separates the cures: the aggregate-row cure re-collapses, per-series scaling degrades but stays low, and only seasonal differencing keeps its one-step accuracy unchanged. We close with a three-step procedure for diagnosing and preventing the failure in deployed systems.

</details>


### [22] [Evaluation Choices Decide the Forecasting Leaderboard: Evidence from a Production Marketplace Panel](https://arxiv.org/abs/2609.27867)
*Md Rezwanul Islam,Wael Mohammed*

Main category: cs.LG

TL;DR: 论文摘要未披露具体公司/团队名称；场景是某生产型市场平台(marketplace)对1,887个企业客户、67个月面板数据的时间序列预测（含间歇性需求）。管线阶段为预测基线的评估与选型(evaluation/benchmarking)。核心发现：评估设计选择（分析单位、误差聚合方式、点预测vs预测区间）可完全翻转排行榜结论——生产基线在“市场总量”层面是19个方法中的第2名且无对手胜出，但切换到“单个客户”层面则跌至25个方法中的第23名，19/24的挑战者都胜过它；在间歇性需求上点预测与区间预测的排名相关仅0.02。论文摘要未披露该部署系统的具体名称。


<details>
  <summary>Details</summary>
Motivation: 生产预测系统选型面临一个被忽视的瓶颈：排行榜结论由评估者的设计选择（而非模型本身）预先决定。现有基准通常固定数据、预测期与区间，却允许分析单位、误差池化、点/区间评分等设计自由变动，导致“哪个方法更优”的结论不稳定甚至反转，进而可能误导部署决策。论文用生产市场平台面板（1,887客户×67个月）证明这一脆弱性，并对照公共M5零售面板验证结论非数据特例。

Method: 作者的方法论贡献是一个可控评估实验：固定数据、预测期与区间，仅系统改变三项评估设计——（1）分析单位从“市场总量”改为“单个客户”；（2）误差池化方式（影响Diebold-Mariano检验是否检出任一差异）；（3）点预测评分改为预测区间评分（含conformal预测区间）。在此框架下对比24种预测方法加一个教科书基准，其中包括六个2025年时代的时间序列基础模型（另在M5上追加五个零样本基础模型）。同时测量“部署系统的选择规则”相对“无所作为”到“事后最优”这一floor-to-ceiling距离的捕捉比例，并进行分组细分分析；最后公开评估协议并披露自身曾因错误反转某一结果的失误。上述为作者宣告的方法；论文摘要未披露具体模型架构、训练目标或系统实现细节。

Result: 离线结果：生产面板上，分析单位从市场总量切换到单个客户时，生产基线由19个方法中的第2名（无对手胜出）跌至25个方法中的第23名，24个挑战者中19个胜出；间歇性需求上点预测与区间预测的排名相关系数仅0.02；部署系统的选择规则捕捉了“doing nothing到hindsight”距离的55%。公共M5零售面板（协议未改）上，同样的基线形态在市场总量第1名、按序列则垫底（被全部对手击败），重放的选择规则闭合了64.7%的floor-to-ceiling距离；追加五个零样本基础模型改变总量层冠军但不改变形态；conformal区间在尖峰型（spikiest）条目上覆盖最不足。模型规模：论文摘要未披露。训练数据/硬件/时间：论文摘要未披露。推理硬件、延迟、吞吐、内存、成本：论文摘要未披露。在线A/B实验增益：论文摘要未披露。

Conclusion: 论文解决了“预测方法选型结论被评估设计选择预先决定”这一生产侧关键问题：分析单位（总量vs个体）、误差池化与点/区间评分任一改变都可反转或消解头条结论，且该现象在自有生产面板与公共M5面板上一致复现。实用价值在于提供可复用的评估协议与“选择规则捕捉floor-to-ceiling距离比例”这一部署收益度量，并警示conformal区间在尖峰/间歇需求上的覆盖偏差。证据缺口：未披露公司/团队、模型与系统细节、在线增益、资源成本，且选择规则本身的方法未展开。工业预测/选型团队可借鉴其评估设计清单与“按细分层级量化结论脆弱性”的流程，避免在排行榜误导下做出错误的生产基线替换决策。

Abstract: A forecasting benchmark reports which method won. We show that the answer is set by the evaluator's choices before any model is fitted. We benchmark 24 forecasting methods and one textbook reference, including six 2025-era time series foundation models, on a production marketplace panel of 1,887 business customers over 67 months. We hold the data, the horizon and the period fixed, and vary only the evaluation design. Three choices each reverse or dissolve a headline conclusion. Changing the unit of analysis from the market total to the individual customer moves our production baseline from second of nineteen, beaten by nothing, to twenty-third of twenty-five. Nineteen of its twenty-four challengers beat it there. Changing how much error is pooled decides whether a Diebold-Mariano test finds anything at all. Scoring prediction intervals rather than point forecasts reorders the field almost completely, with a rank correlation of 0.02 on intermittent demand. We then measure what the deployed system gets from this. Its selection rule captures 55% of the distance between doing nothing and choosing with hindsight. The reversal is not a quirk of our data. We ran the released protocol, unchanged, on the public M5 retail panel. The same baseline shape places first at the market total and last per series, beaten by everything, and a replayed selection rule closes 64.7% of the same floor-to-ceiling distance there. Adding five zero-shot foundation models to that roster changes who wins at the total, not the shape. The bands' blind spot travels too: conformal bands under-cover most on the spikiest items. Splitting our own panel into ever smaller groups turns the contrast into a curve: the baseline's rank worsens at every level of disaggregation. We release the evaluation protocol and report an error of our own that inverted a result before we caught it.

</details>


### [23] [FLEET: From Logits Entropy to Enhanced Trajectories in Text Generation](https://arxiv.org/abs/2609.27657)
*Oleksii Streltsov,Oleksandra Vitko*

Main category: cs.LG

TL;DR: 论文摘要未披露具体公司/团队与业务场景。本文针对LLM在生成式应用中依赖温度采样聚合、但该无记忆方式在采样增多时产生语义重复、收益递减这一生产推理瓶颈，提出FLEET方法：将每次生成为熵超过阈值的状态构成的稀疏轨迹，并据此推断逐token效用分数以调整logits。结果表明FLEET在达到与重复采样基线相同准确率的同时实现3x加速，并在复杂编程任务上（LiveCodeBench Pass@32）将59.9%提升至66.2%；其贪婪解码配置确定性、仅需一次校准确定主要超参数，对现有LLM流水线改动极小。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈：基于LLM的解决方案常依赖温度采样并聚合多个完成样本来提升准确率与稳定性，但该方式是“无记忆”的（memoryless），缺乏对先前生成及其评估的感知，导致采样数量增加时语义重复答案占比越来越高，收益递减。这直接涉及LLM推理部署阶段的算力/调用预算与输出多样性的效率矛盾，即在固定采样预算下难以持续提升准确率。现有方案限制：重复采样为次优策略，存在冗余计算与重复输出；具体公司、业务产品、在线指标在论文摘要中未披露。

Method: 核心思想：给生成过程引入“记忆”机制，利用先前生成轨迹来抑制语义重复、提升采样效率。方法流程：将每次生成表示为一条“稀疏轨迹”，该轨迹由熵超过预定义阈值的状态（token位置）构成；基于这些轨迹推断逐token的效用分数（utility scores），用于调整下一步的logits。配置上，本文评估的贪婪解码配置是确定性的，并仅需一次校准（calibration pass）即可推导主要超参数。创新点：将熵阈值轨迹作为记忆表征，并以逐token效用分数调整logits，区别于无记忆的独立温度采样；作者声称对现有LLM流水线仅需极小的工程改动。具体状态/熵的计算方式、轨迹匹配与分数推断的精确算法细节未在摘要中披露。

Result: 离线指标：在与重复采样基线相同准确率的情况下实现3x加速；在复杂编码任务上，LiveCodeBench Pass@32 从59.9%提升至66.2%（相同预算下）。摘要未披露在线A/B测试收益。模型规模、训练数据、训练硬件/时间、推理硬件、绝对延迟、吞吐量、显存与成本等均未披露。摘要亦未说明“3x速度”的底数（响应时间、token数或调用次数）及基线采样数量。

Conclusion: 本文解决的是LLM推理阶段“重复采样→聚合”策略中的无记忆冗余问题，通过内存化轨迹与逐token logits调整，在保持（或提升）准确率的同时显著降低采样/计算开销，对追求推理效率、控制LLM调用预算的生产系统有直接借鉴价值。可复用点：将熵阈值轨迹作为生成记忆、以效用分数修正logits、确定性贪婪解码及单次校准调参，均可嵌入现有推理/服务流水线。局限与证据缺口：摘要未披露任何真实业务场景、在线A/B结果，缺乏绝对延迟/吞吐/成本数据；“3x加速”实验底座、基线采样规模及校准开销均未说明；作者主张的通用工业推广价值仍需在真实生产负载中验证。

Abstract: Solutions based on large language models (LLMs) often rely on temperature sampling to improve accuracy and stability by aggregating multiple samples from the completion distribution. However, this memoryless approach is inherently suboptimal: because it lacks awareness of prior generations and their evaluations, it produces an increasing proportion of semantically duplicate answers as more samples are drawn, leading to diminishing returns. To address this limitation, we introduce FLEET, a novel method that integrates a memory mechanism into the generation process. FLEET represents each generation as a sparse trajectory through states whose entropy exceeds a predefined threshold and uses these trajectories to infer per-token utility scores that adjust the logits. Benchmark evaluations demonstrate that FLEET achieves the same accuracy as the repeated sampling baseline, with a 3x speedup, and substantially improves accuracy on complex coding tasks (LiveCodeBench Pass@32 increases from 59.9% to 66.2%) under the same budget. Furthermore, in the greedy-decoding configuration evaluated here, the approach is deterministic and uses a single calibration pass to derive its principal hyperparameters, requiring only minimal modifications to existing LLM pipelines.

</details>


### [24] [ProCredit: From Outcome Rewards to Progress Credit in Agentic Reinforcement Learning](https://arxiv.org/abs/2609.27532)
*Ming Ma,Yi Zhu,Yiran Zhong,Feida Zhu,Chonghan Liu,Pengkun Jiao,Qichao Wang,Yanhao Jia,Tianming Yang,Steven Hoi*

Main category: cs.LG

TL;DR: 论文摘要未披露公司/团队名称（作者列表含 Ming Ma、Steven Hoi 等，摘要本身未写明所属机构）；业务场景为长程智能体（long-horizon agentic）任务，即让模型通过多轮工具调用修改环境并以最终状态判定成败；管线阶段属于 LLM 的强化学习训练/奖励设计环节。提出 ProCredit，通过在每个回合后重跑验收检查（acceptance checks）把可验证的进度转化为信用，并按回合分配奖励；以 Qwen3.5 基座模型在 AppWorld 做三规模训练，任务完成率在多规模双测试集上均超过 outcome-reward 与 progress-based 基线，4B 规模较最强 outcome-reward 基线高出 4.1 个百分点，第二环境结果方向一致。


<details>
  <summary>Details</summary>
Motivation: 核心瓶颈是长程智能体任务的奖励稀疏与信用分配问题：标准做法只在轨迹末尾给单一 outcome reward，导致三类缺陷——(1) 同一任务的一组采样轨迹若全部失败则无训练信号；(2) 失败的尝试无法按"距离完成的远近"区分；(3) 推进任务的回合与仅查询环境的回合获得相同信用。已有缓解方案各有局限：细化到 step 级比较仍只依赖最终成功信号，训练奖励模型来提供中间信号则是"用模型估计"而非真实验证。论文观察到决定成功的验收检查本身可以作用于中间状态，因而"进度与结果同样可验证"，这构成了设计动机。该问题对需要多步工具调用的生产型智能体（如自动化工作流、客服/运营任务执行）至关重要。

Method: 提出 ProCredit 框架：在每个回合之后重跑验收检查，以该回合带来的进度变化量作为该回合的奖励，并用这些逐回合奖励在"同一任务的不同尝试之间"和"一条轨迹内部的各回合之间"两层分配信用。对比对象包括 outcome-reward 基线（轨迹级单一最终奖励）与 progress-based 基线（将进度细化到 step 级或训练奖励模型提供中间信号）。训练基座为 Qwen3.5 基座模型，在三个规模（含 4B）上进行。论文主张其关键创新在于"把进度信用记到发生该进度的具体回合"——消融表明仅把最终进度加到轨迹总分并不能带来提升，收益来自回合级信用分配。作者声明与读者解读需区分：验证进度可复用验收检查这一观察属于作者主张，整体为可验证进度驱动的回合级奖励塑形。

Result: 离线指标：在 AppWorld 的两个测试集上，ProCredit 在所有三个规模（含 4B 等）均超过 outcome-reward 基线与 progress-based 基线的任务完成率（task completion rate）；在 4B 规模上超过最强 outcome-reward 基线 4.1 个百分点。在第二个（未具名的）环境上改进方向一致。消融：仅在轨迹总分中加入最终进度不提升性能，说明收益来自把进度信用记到对应回合。论文摘要未披露在线 A/B 实验收益。模型规模：三个规模（含 4B），其余规模数值论文摘要未披露。训练数据、训练硬件/时长、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: 论文解决的核心问题是长程智能体强化学习中的稀疏奖励与逐回合信用分配，通过把可验证的进度（复用验收检查）转化为逐回合奖励。实践价值在于：进度与最终结果同源可验证，避免引入额外奖励模型，减少奖励估计的噪声与偏差；其按回合、跨尝试的双层信用分配思路可迁移到需要多步工具调用的生产型智能体（自动化办公、客服、业务运营流程执行）的技术训练环节。不足与证据缺口：论文摘要未披露公司/团队、第二环境的名称、训练与推理资源成本、在线效果、任务完成率之外的指标（如步数效率、稳定性、泛化到未见过任务），且离线提升以"任务完成率"为主的单一维度；4.1 个百分点的提升需结合基线的绝对完成率水平解读。工业团队可复用的是"验收检查即进度信号"的奖励塑形范式及按回合信用分配的机制，而非依赖额外奖励模型。

Abstract: Long-horizon agentic tasks require an agent to modify an environment through a sequence of tool calls, with success determined by the final state. The standard recipe assigns a single outcome reward at the end and compares trajectories sampled for the same task. As a result, a group with no successful trajectory yields no training signal, failed attempts cannot be told apart by how close they came to completion, and turns that advance the task receive the same credit as turns that only query the environment. Prior work refines the unit of comparison from the trajectory to the step, or trains a reward model to supply intermediate signal: the former still derives its signal from final success alone, and the latter estimates it with a model. We observe that the acceptance checks that decide success can also be run on intermediate states, so progress is as verifiable as the outcome. We propose ProCredit, which turns this verified progress into credit: it reruns the acceptance checks after each turn, rewards the turn by its change in progress, and uses these rewards to assign credit both across attempts at the same task and across the turns within a trajectory. Starting from Qwen3.5 base models at three scales on AppWorld, ProCredit outperforms outcome-reward baselines and progress-based baselines in task completion rate at every scale on both test sets, exceeding the strongest outcome-reward baseline by 4.1 percentage points at 4B, and results in a second environment show the same direction of improvement. Ablations show that adding the final progress to the trajectory score alone does not improve performance: the gain comes from crediting progress to the turn where it occurs.

</details>


### [25] [Live Assistant: Learning Whether, When, and Whom to Assist in Real-World Live Social Streams](https://arxiv.org/abs/2609.27303)
*Shujian Gao,Jiamei Yan,Yuchen Yang,Penghao Zhou,Qinglei Wang,Tiehan Fan,Yuan Wang,Zuxuan Wu,Yu-gang Jiang*

Main category: cs.LG

TL;DR: 机构与团队：论文摘要未明确披露公司名称（作者所属单位为学术界，复旦大学，具体公司/生产团队未披露）；业务场景为真实直播平台（livestream）的实时互动辅助，管线阶段为实时流式推理与用户互动决策（每10秒一个决策区间）；系统为 Live Assistant，采用单一自回归策略消费原生音视频、同步评论、礼物、观众动态与房间元数据，输出 OBS/MEM/ANS 三类动作；支撑数据为320小时轨迹引擎与275片段/13,812决策区间的基准；训练方法为 MA-MSFT 加 SM-GSPO；核心结果为 71.14 state accuracy、72.67 recipient accuracy、58.41 task accuracy，并以一致性优势超过代表性流式与多模态基线。


<details>
  <summary>Details</summary>
Motivation: 业务痛点：直播（livestream）是长时交互环境，音视频、观众活动、主播行为与平台信号共同演化，产生的"援助需求"来自直播流本身，需要一个能在正确时机选择性参与（而非持续发言或完全沉默）的助手。现有方案的局限：论文摘要未明确披露具体被超越的系统名称，但以"代表性流式与通用多模态基线"为对照，说明现有方法在状态/收件人/任务三方面精度不足。规模约束：每10秒决策一次，需处理原生音视频与多种并发信号，训练数据达320小时轨迹。该问题重要性在于：选择性参与共享社交流决定了直播助手的可用性、用户互动体验与商业转化（礼物、评论互动）。

Method: 核心思想：把直播交互建模为四个耦合决策的混合主动、角色条件的辅助任务——是否行动、何时行动、面向谁、说什么，并将参与定义为对社交流的选择性介入。架构与流程：每个10秒区间，一个自回归策略消费原生音频、视频与同步的评论、礼物、观众动态、房间元数据，然后选择三种动作之一：OBS（保持沉默）、MEM（记录私有语义更新）、ANS（指定收件人、任务与有依据的消息）。关键模块/创新（作者声明）：① 轨迹引擎，将真实直播会话重建为结构化因果监督，产出320+小时优化轨迹；② 人工审核基准（275片段、13,812决策区间）；③ Marker-Aware Multiturn Supervised Fine-Tuning（MA-MSFT），通过标记增强稀疏结构化决策的多轮监督微调；④ Streaming Multiturn GSPO（SM-GSPO），在自生成轨迹上用 turn 级与 trajectory 级信用优化。需注意：将作者声明与解释区分，以上为摘要原文的技术主张，具体网络结构、输入编码细节、奖励信号设计论文摘要未披露。

Result: 离线指标（held-out benchmark）：state accuracy 71.14、recipient accuracy 72.67、task accuracy 58.41，且相较"代表性流式与通用多模态基线"取得一致性增益。在线A/B实验：论文摘要未披露。模型规模：论文摘要未披露。训练数据规模：320+小时优化轨迹，275个片段、13,812个决策区间（人工审核基准）。训练硬件与时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟：论文摘要未披露（仅说明每个10秒区间决策一次）。吞吐量：论文摘要未披露。内存：论文摘要未披露。成本：论文摘要未披露。

Conclusion: 解决的问题：将直播实时互动辅助形式化为"是否/何时/对谁/说什么"的决策问题，提供轨迹引擎、基准与两阶段训练框架，使助手能在共享社交流中做选择性参与。实用价值：对直播互动、实时内容、社交推荐、用户参与类产品团队可复用其决策式轨迹监督构建思路与稀疏结构化决策的训练技巧。局限与证据缺口：论文摘要未披露公司/生产团队、未披露在线A/B收益、模型规模、训练/推理资源与端到端延迟吞吐成本；任务精度（58.41）显著低于状态与收件人精度，说明"说什么"仍是瓶颈；基准仅275片段，规模有限。可迁移经验：将弱监督交互转化为"动作类型+收件人+任务"结构化因果监督、用 marker 强化稀疏决策、以 turn/trajectory 两级信用做流式RL优化，这些对工业搜索/广告/推荐中的实时决策型助手与流式用户建模有借鉴意义。

Abstract: Livestreams are long-lasting interactive environments where audiovisual content, viewer activity, host behavior, and platform signals evolve together, creating assistance needs that emerge from the stream itself. We introduce \liveassistant, a framework for mixed-initiative, role-conditioned assistance that formulates livestream interaction as four coupled decisions: \textbf{whether to act, when to act, whom to address, and what to communicate}. At each 10-second interval, one autoregressive policy consumes native audio and video with synchronized comments, gifts, viewer dynamics, and room metadata, then selects \textsc{OBS}, \textsc{MEM}, or \textsc{ANS}. \textsc{OBS} remains silent, \textsc{MEM} records a private semantic update, and \textsc{ANS} specifies a recipient, task, and grounded message. To support this task, we build a trajectory engine that reconstructs real livestream sessions into structured causal supervision, yielding over 320 hours of optimization trajectories and a human-reviewed benchmark of 275 clips and 13,812 decision intervals. We train the policy with Marker-Aware Multiturn Supervised Fine-Tuning (MA-MSFT), which strengthens sparse structured decisions, followed by Streaming Multiturn GSPO (SM-GSPO), which optimizes self-generated trajectories with turn- and trajectory-level credit. On the held-out benchmark, \liveassistant reaches 71.14 state accuracy, 72.67 recipient accuracy, and 58.41 task accuracy, with consistent gains over representative streaming and general multimodal baselines. Together, the formulation, benchmark, and training framework establish livestream assistance as selective participation in a shared social stream.

</details>


### [26] [KITE: KV-Invariant Transformer Expansion for Efficient Agentic LLM Scaling](https://arxiv.org/abs/2609.27294)
*Zhiheng Hu,Yixun Wei,Jian Zhou,Yizhuang Zhou,Ji Li,Xing Chen,Yang Li,Bojun Wang,Yibo Zhu,Xiangyu Zhang,Daxin Jiang*

Main category: cs.LG

TL;DR: 论文摘要未明确披露公司/所属团队（作者中含 Daxin Jiang 等产业界研究者，但摘要未写明机构）；业务场景为工业级大规模语言模型扩展，涉及训练、预填充（prompt processing）与自回归解码三个计算环节的成本优化；提出的系统为 KV-Invariant Transformer Expansion (KITE) 及其具体实例 Step Scale Transformer (SST)，一种双塔解码器（一塔产出 KV、另一塔读取 KV），以 upcycling 方式从小模型扩展到大模型，并在预填充阶段仅依赖小部分模型以省推理成本。头号结果：在可比累计训练计算量下，SST 67B MoE（每个解码 token 2.15B 活跃体参数）训练损失低于 47B（1.48B 活跃体）与 63B（2.02B 活跃体）的 MoE Transformer 基线，同时估算推理成本分别降低 6.7% 与 31.6%。


<details>
  <summary>Details</summary>
Motivation: 训练一个大规模语言模型的成本不仅取决于最终质量，还取决于架构选择所决定的训练、预填充（prompt processing）与自回归解码各阶段的计算开销。现有扩展方案的瓶颈在于：扩展后模型虽提升质量，但预填充与解码的计算量随规模同步上升，且从零训练大模型训练成本高。理想的架构应在降低训练、预填充、解码三方面计算成本的同时，确保扩展后的模型确实优于更小的基线，从而支撑更大规模扩展。论文摘要未披露具体产品、线上业务或用户规模等商业场景信息。

Method: KITE 是一种扩展范式：模型从较小规模训练到较大规模（通过 upcycling 节省训练成本），并将新增参数放置在"不影响注意力 KV"的区域，因此推理预填充阶段仅依赖模型的较小部分，从而节省推理成本。具体实例 SST 为双塔解码器（two-tower decoder）：一个塔负责生成 KV，另一个塔负责读取这些 KV。用 MoE 实现，67B 总参数、每个解码 token 2.15B 活跃体（active body）参数。作者声称其核心创新在于"KV 不变"的扩展方式，即新增参数不改变 KV 计算路径。摘要未披露训练目标、输入输出流的更多细节、MoE 路由机制、训练数据与硬件等实现细节。

Result: 离线指标（训练损失）：在可比累计训练计算量下，SST 67B MoE（每解码 token 2.15B 活跃体参数）的训练损失低于 47B MoE Transformer（1.48B 活跃体参数）与 63B MoE Transformer（2.02B 活跃体参数）两个基线。估算推理成本（摘要表述为 estimated inference cost）：相比 47B 基线降低 6.7%，相比 63B 基线降低 31.6%。注意摘要将训练损失列为离线训练指标，推理成本为"估算"值，未披露任何在线 A/B 实验收益。模型规模：SST 为 67B 总参数的 MoE，每个解码 token 活跃体参数 2.15B；基线为 47B（1.48B 活跃体）与 63B（2.02B 活跃体）MoE Transformer。训练数据规模、训练硬件/时长、推理硬件、延迟、吞吐、显存、成本均论文摘要未披露。

Conclusion: 该工作解决了大规模 LLM 扩展中"训练、预填充、解码三阶段计算成本"与"扩展后模型质量"之间的权衡问题，核心价值在于通过 KV 不变的双塔解码器结构，使预填充只依赖小塔、新增参数不增加 KV 计算，从而在扩展规模的同时节省推理成本，并以 67B MoE 在更低训练损失下实现推理成本 6.7%/31.6% 的估算下降。局限与证据缺口：摘要仅给出训练损失与"估算"推理成本，缺乏真实推理延迟/吞吐/显存测量，缺乏在线 A/B 效果，未披露训练数据、硬件与实现细节，且未说明与标准 MoE/路由机制的具体差异。对工业 LLM 团队可复用的点在于：在训练（upcycling）、预填充、解码三环节联合权衡的架构设计思路，以及"KV 生产与消费解耦的双塔"这一可迁移的 scaling 架构模式，适用于需要大规模扩展且关注推理成本的场景。

Abstract: Scaling a language model is not only a question of final quality: the architectural choice determines how much computation is spent during training, prompt processing, and autoregressive decoding to achieve certain model quality. An ideal model architecture should lower all above computation costs to facilitate scaling to a larger model, while ensure the larger model indeed outperforms smaller baselines. We introduce KV-Invariant Transformer Expansion (KITE), a scaling paradigm that achieves this goal. It trains the model from a smaller size to a larger size (i.e., saving training costs via upcycling), while places newly added parameters in regions that do not affect attention KV. Consequently, during inference, prefilling KV only relies on the smaller part of the model, so the inference costs are saved. As a concrete instantiation, we present Step Scale Transformer (SST), a two-tower decoder in which one tower produces KV and the other reads them. At comparable cumulative training compute, SST, a 67B MoE model with 2.15B active body parameters per decode token, achieves lower training loss than 47B and 63B MoE Transformers with 1.48B and 2.02B active body parameters, respectively, while reducing estimated inference cost by 6.7% and 31.6%.

</details>


### [27] [SR-Fraud: An Outcome-Supervised Reflective LLM Agent Framework for Non-Stationary Payment Fraud Detection](https://arxiv.org/abs/2609.27287)
*Xuwei Tan,Yao Ma,Xueru Zhang*

Main category: cs.LG

TL;DR: 作者单位为Xuwei Tan、Yao Ma、Xueru Zhang（具体公司/机构论文摘要未披露），业务场景为实时支付欺诈检测这一非平稳流式预测问题，处于线上实时决策（request-time scoring）与离线适配/规则更新的联合阶段。系统SR-Fraud采用"结果监督的反思型LLM Agent"框架，将请求时决策与离线适配解耦：冻结的无状态 LLM agent 基于 Hybrid Episodic Window 对每笔交易打分追踪行为漂移，离线反思 agent 从成熟错误中提出边界假设，确定性验证器仅将证据支持的假设写入可执行知识状态。在production payment-fraud benchmark上，SR-Fraud相较冻结决策agent提升所有检测指标，点估计高于静态及周期性重训练的CatBoost，并能检测出新出现的欺诈爆发。


<details>
  <summary>Details</summary>
Motivation: 生产痛点在于实时支付欺诈检测是"non-stationary streaming prediction"问题：监督标签成熟滞后（labels mature 延迟），攻击者会抢先适应策略，局部爆发式攻击（localized burst attacks）可能在周期性重训练到来前已造成损失。现有生产系统主要依赖表格分类器与规则，难以在周期性重训练之前捕捉新出现的序列模式。因此需要一种能在请求时即时识别行为漂移、同时以离线方式持续从成熟错误中更新知识、且不被不充分假设干扰的检测机制。

Method: SR-Fraud 的核心架构为"结果监督的反思型 LLM Agent 框架"，将请求时决策与离线适配解耦：一是冻结、无状态的决策 agent，基于 Hybrid Episodic Window（混合情节窗口）对每笔交易打分，以追踪行为偏移；二是离线反思 agent，从已成熟（matured）的错误中提出边界假设（boundary hypotheses）；三是一个确定性验证器（deterministic verifier），只将通过证据支持的假设准入并写入可执行知识状态。该设计以"结果监督"保证反思方向正确，以确定性验证器过滤不可靠假设。以上为作者主张；论文摘要未披露具体的模型尺寸、输入输出字段、训练目标、提示构造、门槛阈值或验证规则细节。相较静态模型和周期性重训练的监督模型，该方法通过可执行知识状态实现持续的、可控的在线时决策能力更新，而非依赖频繁重训练。

Result: 离线/基准评估：在 production payment-fraud benchmark 上，SR-Fraud 相较其 frozen decision agent 在所有检测指标上获得提升；相较静态及周期性重训练的 CatBoost，SR-Fraud 获得更高的点估计（point estimates）；并能检测出新出现的欺诈爆发（emerging fraud burst）。论文摘要未披露各指标的具体名称与数值、未披露是否包含在线 A/B 实验及其收益。模型规模（LLM 参数量）、训练数据规模、训练硬件/训练时长、推理硬件、延迟、吞吐、内存与成本等资源项，论文摘要均未披露。

Conclusion: SR-Fraud 针对支付欺诈检测中标签滞后、攻击快速演化、爆发式攻击三类生产难题，提出"在线无条件决策 + 离线反思 + 确定性验证"的可执行知识增量更新范式，用 LLM 的序列模式捕捉能力弥补表格分类器与规则在新兴模式上的不足。对工业风控/支付团队的可复用点是：将实时打分与离线适配解耦、用成熟错误驱动反思、并通过确定性验证器保证知识状态的可执行性与可靠性。主要证据缺口在于：论文摘要未报告具体指标数值、是否经过真实在线 A/B 验证、LLM 推理的延迟/成本是否满足实时交易要求，以及该框架相较成熟在线学习（如增量梯度提升、streaming anomaly detection）基线是否仍有优势，均需原文进一步确认。

Abstract: Real-time payment fraud detection is a non-stationary streaming prediction problem: adversaries adapt before supervised labels mature, and localized burst attacks can cause losses before retraining. Production systems typically rely on tabular classifiers and rules, which can struggle to capture these emerging sequential patterns before periodic retraining occurs. We present SR-Fraud, an outcome-supervised reflective LLM framework that decouples request-time decisions from offline adaptation. A frozen, stateless agent scores each transaction from a Hybrid Episodic Window to track behavioral shifts, while an offline reflection agent proposes boundary hypotheses from matured errors. A deterministic verifier then admits only supported hypotheses into an executable knowledge state. On a production payment-fraud benchmark, SR-Fraud improves all detection metrics over its frozen decision agent, obtains higher point estimates than static and periodically retrained CatBoost, and detects an emerging fraud burst.

</details>


### [28] [A Systematic Benchmark of Explainable Methods for Temporal Attribution in Sequential Recommendation Systems](https://arxiv.org/abs/2609.27201)
*Akash Pandey,Kanisha Shah,Addrish Roy,Dwipam Katariya,Hongyangyang Shi,Amanda Ding,Kalanand Mishra,Pranab Mohanty*

Main category: cs.LG

TL;DR: 论文摘要未披露具体公司名称与团队（仅列作者姓名，禁止臆断其所属机构）。业务场景为顺序推荐系统（Sequential RecSys）的可解释性审计——面向开发者审计模型行为与用户索取推荐理由的双重需求；pipeline阶段属于推荐系统的"可解释性/模型审计（explainability & model auditing）"环节，而非召回/排序/CTR等核心链路。论文提出一种双模型掩码忠实度度量（dual-model masking metric：一个模型提供逐时间步归因分数，另一个独立训练、对掩码鲁棒的probe模型测量预测概率变化），并据此在CNN、Transformer、SASRec、BERT4Rec四种骨干上、于KuaiRand与MovieLens数据集上基准了10种XAI方法。核心结论：梯度类方法（尤其GradientSHAP与Integrated Gradients）最忠实且鲁棒；原始注意力权重不可靠，但梯度加权注意力在短序列上恢复忠实度、长视野下退化；忠实方法的时序归因反映真实任务结构而非新近性或流行度偏置。


<details>
  <summary>Details</summary>
Motivation: 论文针对的实际痛点：顺序推荐模型因深度非线性本质而成为黑盒，难以将预测结果归因到用户历史交互中的具体交互项，导致两类真实需求无法满足——开发者需要审计模型行为以保障透明与信任，用户需要推荐理由以建立信任。现有梯度类、扰动类、注意力类可解释方法并存，但缺少针对顺序推荐场景的系统性忠实度基准，导致从业者无法判断哪种方法在时序场景下真正可靠。KuaiRand为真实工业数据集，凸显该问题与生产推荐系统的直接关联。论文摘要未披露具体的工业规模约束（如线上延迟、用户量、模型在线实验等）。

Method: 论文提出一种双模型掩码忠实度度量（dual-model masking metric）作为评估框架：由被解释的推荐模型输出逐时间步的归因（attribution）分数，再由一个"独立训练且对掩码鲁棒"的probe模型测量掩码后预测概率的变化，以归因分数与预测变化的一致性衡量忠实度。在此度量下，对四种骨干（CNN、Transformer、SASRec、BERT4Rec）基准了10种XAI方法，并通过三项补充分析增强结论：时序归因模式分析、item流行度混淆（popularity confounding）分析、输入损坏鲁棒性分析。核心创新在于解耦"归因提供方"与"效应测量方"，避免用模型自身输出来自证归因忠实度的循环论证。上述为作者主张；"双模型解耦可减少自证偏差"属于对作者意图的合理解读。

Result: 论文主要产出为定性排序结论而非数值提升（这是可解释性评估类论文的常态）。离线结论：(1) 梯度类方法、尤其GradientSHAP与Integrated Gradients给出最忠实且最鲁棒的归因；(2) 原始（raw）注意力权重不可靠，梯度加权注意力在较短序列上恢复忠实度，但在更长视野（horizon）上退化——其机理被解释为softmax注意力概率趋于均匀的重要性分数，削弱识别信息性交互的能力；(3) 忠实方法的时序归因模式反映真实任务结构而非新近性（recency）或流行度（popularity）偏置。论文摘要未披露线上A/B测试增益；未披露模型参数量、训练数据规模、训练硬件/时长、推理硬件、延迟、吞吐、显存与成本。

Conclusion: 论文解决的是顺序推荐"黑盒难审计、解释可信度无标准"的问题，为开发者审计与用户解释提供了可复用的忠实度度量框架与方法选型指南。实用价值在于：工业推荐团队可直接采用其dual-model masking度量和"梯度类方法优先、原始注意力不可靠"的结论来搭建模型审计流水线。主要局限/证据缺口：摘要未披露所在公司、线上收益、模型与数据规模、计算成本，故其结论在超大规模工业骨干上的泛化性与线上验证尚待证实；作为基准性研究，不直接优化推荐效果本身，属于治理/审计类工具而非核心链路改进。可迁移要点：将归因提供方与效应测量方解耦的masking度量思路，以及对流行度/新近性混淆与输入损坏的鲁棒性消融范式。

Abstract: Sequential RecSys are central to modern personalization, exploiting user's historical interaction sequences to drive next-step decisions. Deep learning models, particularly CNN and Transformer-based architectures, have proven highly effective at capturing temporal dependencies in these histories. For transparency and trust, understanding which past interactions drive a given recommendation is increasingly important --- both for developers auditing model behavior and for users seeking a rationale. However, the non-linearities that give these models their predictive power also render them black boxes, making it difficult to attribute decisions to specific interactions. While gradient-based, perturbation-based, and attention-based explainability methods exist, a systematic benchmark of their faithfulness for sequential recommendation is missing. We address this gap by introducing a dual-model masking metric in which one model supplies per-timestep attribution scores and a separately trained, masking-robust probe measures the resulting change in predicted probability. Using this metric, we benchmark ten XAI methods across CNN, Transformer, SASRec, and BERT4Rec backbones on KuaiRand and MovieLens, complemented by analyses of temporal attribution patterns, item popularity confounding, and robustness to input corruption. Our key findings are: (1) gradient-based methods, particularly GradientSHAP and Integrated Gradients, yield the most faithful and robust attributions; (2) raw attention weights are unreliable, but gradient-weighted attention restores faithfulness on shorter sequences, with degradation on longer horizons as softmax attention probabilities converge toward uniform importance scores, diminishing the method's ability to identify informative interactions; and (3) temporal attribution patterns in faithful methods reflect genuine task structure rather than recency or popularity bias.

</details>


### [29] [ChipMEM: Verification-Grounded Memory for EDA Agents](https://arxiv.org/abs/2609.27067)
*Abdulrahman AlRabah,Joshua Mabry,Dilek Hakkani-Tür,Abdussalam Alawini,Hamid Shojaei,Kartik Hegde,Sandesh Adhikary*

Main category: cs.LG

TL;DR: 论文摘要未披露公司/生产团队。本文研究 EDA 场景下 LLM agent 的记忆增强问题：针对 RTL 设计生成/修订与测试平台（testbench）生成中"反馈仅在同一任务上奖励任务特定修订、知识难以迁移"的问题，提出 ChipMEM——一个验证锚定的记忆层。其结合跨任务过程性记忆与轨迹内统计指引，在不改变各领域工具与验收标准的前提下，通过统一 adapter 同时服务 RTL 优化与 testbench 生成 agent。论文摘要未披露该工作的具体管线阶段归属（属于工业搜索/广告/推荐之外的 EDA agent 工具链自动化）。整体判断为拒绝。


<details>
  <summary>Details</summary>
Motivation: 论文摘要披露的动机：LLM-based agent 利用 EDA 工具在综合（synthesis）与验证（verification）反馈下生成和修订 RTL 设计。现有两类方法各有缺陷——从执行轨迹蒸馏可复用技能、或基于 EDA 工具奖励进行训练，通常只在"产生经验的任务"上评估，反复访问同一任务的基准反馈会奖励"任务特定的修订"而非生成"可迁移的可复用知识"。该问题对 EDA 自动化工作流的生产可用性（知识复用、跨任务泛化）有直接影响，但论文摘要未披露具体业务场景、生产瓶颈量化指标或规模约束。

Method: ChipMEM 是一个"verification-grounded（验证锚定）"的记忆层，包含两大组件：(1) 过程性（procedural）记忆——跨任务存储技能，仅当技能通过综合（synthesis）、仿真（simulation）或形式化（formal）检查后才蒸馏并存储，而非依赖模型自我评估（self-assessments）；(2) 贝叶斯组件——对工具调用结果维护分层 Beta 估计，并对"在可比错误下成功的恢复策略"进行排序（ranking recovery strategies）。统一 adapter 将同一记忆接口应用到 RTL 优化与 testbench 生成两类 agent 上，同时保留各领域的工具与验收标准。论文摘要未披露具体模型架构、训练目标（是否有梯度更新）、输入输出流、记忆检索/写入机制、模型规模或实现细节。

Result: 离线指标（均为离线实验，非线上 A/B）：(1) RTLRewriter-Bench，在模型与工具设置匹配条件下，ChipMEM 在 54 个评分设计中 39/54 通过等价性检查（equivalence-passing），无记忆时 35/54；(2) 49 设计短套件上，平均面积改进（mean area improvement）8.69% vs 无记忆的 5.66%；(3) 留出（held-out）CVDP 任务上，冻结过程性库（frozen procedural library）下 ChipMEM 达 20/20 通过（accepted outcomes），无记忆为 18/20，每设置单次评估。论文摘要未披露任何线上 A/B 实验增益、模型大小、训练数据规模、训练硬件/时长、推理硬件、延迟、吞吐、内存与成本。

Conclusion: 该工作解决了 EDA agent 中"经验难跨任务迁移、验证反馈被任务特定修订所过拟合"的问题，通过验证锚定的过程记忆与贝叶斯结果估计提升可复用性与迁移性。对 EDA/RTL 自动化领域有实际价值。但相对本系统判定的工业搜索/广告/推荐/生产 LLM 范畴而言，本文缺乏公司/生产团队、线上实验、部署、服务、成本或规模证据，属于学术性的 EDA agent 研究，不满足保留标准。可借鉴的思路（验证通过后才沉淀技能、分层 Beta 估计排序恢复策略、统一 adapter 跨域复用）对工业 agent 记忆管理有一定启发性，但论文摘要未披露足以证明其生产落地的事实。

Abstract: Large language model (LLM)-based agents use Electronic Design Automation (EDA) tools to generate and revise register-transfer-level (RTL) designs under synthesis and verification feedback. Recent methods learn from this feedback by distilling reusable skills from execution traces or by training on rewards derived from EDA-tools. Both methods are typically evaluated on the tasks that produced the experience. Repeated access to benchmark feedback on the same task can reward task-specific revision rather than creating reusable knowledge that transfers. We introduce ChipMEM, a verification-grounded memory layer for EDA agents. It combines cross-task procedural memory with within-trajectory statistical guidance. Its procedural component distills and stores a skill only after it passes synthesis, simulation, or formal checks, rather than relying on model self-assessments. A Bayesian component maintains hierarchical Beta estimates over tool-call outcomes and ranks recovery strategies that succeeded under comparable errors. A common adapter applies the same memory interface to RTL optimization and testbench-generation agents while preserving each domain's tools and acceptance criteria. We measure performance on training tasks and evaluate whether learned skills transfer to unseen tasks. On RTLRewriter-Bench, under matched model and tool settings, ChipMEM produces equivalence-passing outputs on 39/54 scored designs versus 35/54 without memory; on the 49-design short suite, mean area improvement is 8.69% versus 5.66%. On held-out CVDP tasks, ChipMEM with a frozen procedural library achieves 20/20 accepted outcomes versus 18/20 without memory in a single evaluation per setting.

</details>


### [30] [CRISP: Scalable Importance-Stratified Coresets for Imbalanced Tabular Learning](https://arxiv.org/abs/2609.26962)
*Hardhik Mohanty,Indrayana Rustandi,Mohamadreza Sheibani*

Main category: cs.LG

TL;DR: 论文摘要未披露公司/机构与团队。业务场景为工业二分类表格数据（生产欺诈检测）的大规模不平衡训练；管线阶段属于训练/数据抽样（coreset 构建）环节。提出 CRISP（Coreset Reduction via Importance-Stratified Pruning），一种线性时间的重要性分层剪枝 coreset 方法：按代理模型分数分位分层分配负类预算，并用逆包含概率加权样本。在 95% 负类缩减下，于生产欺诈数据集上以约 1.70M/25M 训练行保留全量数据 99.7% 的 Average Precision，总训练行缩减 93.2%；在 CriteoPrivateAds 上 90%–99.4% 多数类缩减的各测试比例下取得最高平均 AP。


<details>
  <summary>Details</summary>
Motivation: 大规模不平衡表格数据导致梯度提升树重复训练成本高昂；现有 coreset 方法在移除大量多数类样本后往往损失精度。该方法针对训练效率与不平衡数据精度之间的权衡，对生产欺诈检测等业务中的重复建模/调参场景具有实际意义。

Method: CRISP 依据代理模型（proxy-model）的分数，按分位数对负类（多数类）样本分层（quantile strata），在各层间分配负类采样预算，并依据不等包含概率对样本加权（inverse-propensity weighting）。方法为线性时间，核心组成包括：预算在分位层间的分配策略、基于代理模型的重要性分层，以及逆倾向加权以校正采样偏差。消融实验表明，预算分配与逆倾向加权是生产数据集上增益的主要来源（作者论述）。

Result: 离线指标：生产欺诈数据集上，95% 负类缩减时，CRISP 用约 1.70M/25M 行训练，保留全量数据 Average Precision 的 99.7%，总训练行缩减 93.2%。CriteoPrivateAds 上，CRISP 在 90% 至 99.4% 多数类缩减的每个测试比例下取得最高平均 Average Precision。Sparkov 上，低缩减率下结果好坏参半，但在 99.2% 与 99.4% 缩减率下取得最高平均 AP。在线 A/B 增益：论文摘要未披露。模型规模、训练数据规模细节（仅披露生产欺诈数据集约 25M 行，抽样后约 1.70M 行）、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: CRISP 解决了大规模不平衡表格数据上重复训练梯度提升树时的成本-精度权衡问题，在保持高 AP 的前提下实现约 93.2% 的训练行缩减，可直接用于工业欺诈、广告点击率预测等二分类管道的数据抽取与训练加速。局限性：论文摘要未披露公司/团队、在线实验证据、具体模型规模与资源成本；Sparkov 低缩减率下效果不稳定，说明方法优势主要体现在高缩减（高压缩）区间。可复用要点：基于代理模型分数分位分层的预算分配 + 逆倾向加权的样本构造策略，可复用到工业搜索/广告/风控等需快速迭代 GBDT 模型的训练环节。

Abstract: Large imbalanced tabular datasets make repeated gradient-boosted tree training expensive. Existing coreset methods often lose accuracy when most majority examples are removed. We present CRISP (Coreset Reduction via Importance-Stratified Pruning), a linear-time method that allocates a negative-class budget across quantile strata of a proxy-model score. Sample weights account for unequal inclusion probabilities. At 95% negative-class reduction on a production fraud dataset, CRISP trains on approximately 1.70M of 25M rows and retains 99.7% of full-data Average Precision. This is a 93.2% reduction in total training rows. On public CriteoPrivateAds, CRISP has the highest mean Average Precision at each tested rate from 90% to 99.4% majority reduction. Sparkov results are mixed at lower rates, but CRISP has the highest mean at 99.2% and 99.4%. Ablations identify budget allocation and inverse-propensity weighting as the main sources of the production-dataset gain.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [31] [Query Implied Generative Engine Optimization](https://arxiv.org/abs/2609.27845)
*Shilpa Ramakrishna,William B. Andreopoulos*

Main category: cs.IR

TL;DR: 论文摘要未披露作者所属的具体公司/机构（仅给出作者姓名）；针对生成式搜索引擎（GSE）场景，解决的业务问题为内容创作者在LLM生成回答中被引用的可见性优化，属于搜索流水线中的"内容优化/引用获取"环节（非传统召回或排序，而是面向生成式回答引用的内容端优化）。作者提出QI-GEO方法：不依赖显式用户查询，直接从文档近似其意图空间并补充缺失但相关的潜在问题内容。在GEO-Bench与Extended GEO-Bench上，客观指标最高提升15.9%，主观指标最高提升17.6%，引用增益接近引用损失的两倍。作者/团队、所属机构、是否线上实验等具体信息论文摘要未披露。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈在于：随着传统搜索引擎被生成式搜索引擎取代，内容创作者的可见性不再由搜索结果排名决定，而是由是否被LLM生成的回答引用（cited）决定。现有GEO方法大多依赖显式查询或查询派生信号来对齐内容，而生成式搜索引擎是黑盒，且实际场景中往往无法获取用户的显式查询，导致内容优化难以进行。论文摘要未披露具体的生产规模、用户量、延迟或成本约束。

Method: 作者提出QI-GEO（Query Implied Generative Engine Optimization），核心思想是直接从文档本身推断用户意图（infer user intent directly from the document），而不依赖显式查询输入。方法流程为：近似文档的意图空间（approximates document's intent space），并识别文档中可能缺失但仍与潜在用户查询相关的内容（identifies content that may be missing yet relevant to answer potential user queries）进行补充。论文摘要未披露具体的模型架构、LLM选型、意图空间表示方式、训练目标、输入输出格式及实现细节。

Result: 离线指标：在GEO-Bench与Extended GEO-Bench上，QI-GEO客观分数（objective scores）最高提升15.9%，主观分数（subjective scores）最高提升17.6%；引用增益（citation gains）接近引用损失（citation losses）的两倍（"nearly twice as many citation gains as citation losses"）。在线A/B实验：论文摘要未披露。模型规模、训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: 论文解决了生成式搜索引擎场景下、不依赖显式查询的内容可见性优化问题，证明从文档自身推导用户意图即可提升内容被生成式回答引用的概率。可借鉴价值在于：为搜索/内容生态团队提供一种"查询不可见"条件下的内容端优化思路，其"意图空间近似+缺失内容补充"的框架对生成式搜索的引用优化有一定迁移性。局限与证据缺口：论文摘要未披露作者所属公司/机构与团队、未披露线上业务数据或真实生产GSE的在线收益，评测仅基于公开基准（GEO-Bench/Extended GEO-Bench），指标来源为自建基准的离线分数，主客观指标的具体定义、统计口径及15.9%/17.6%提升对应的基线均未在摘要中说明，工业落地的可行性与规模化收益仍需进一步证据。

Abstract: The landscape of search has changed drastically with how people look for information online. Traditional search engines are being replaced by Generative Search Engines (GSEs), which use Large Language Models (LLMs) to generate natural language responses to user queries. For content creators, visibility is no longer solely determined by ranking in search results but by being cited within generated responses. But Generative Search Engines are black-boxes, leading to the emergence of Generative Engine Optimization (GEO), a set of techniques aimed at improving content visibility in generative search settings. Most existing approaches rely on the explicit queries or query derived signals to align content to better suit user needs. We propose Query Implied Generative Engine Optimization (QI-GEO) to infers user intent directly from the document. Our approach approximates document's intent space and identifies content that may be missing yet relevant to answer potential user queries. Evaluation on GEO-Bench and Extended GEO-Bench demonstrated improvements across objective and subjective metrics. QI-GEO improved objective scores by up to 15.9% and subjective scores by up to 17.6%, while yielding nearly twice as many citation gains as citation losses. These results suggest that document-derived approximations of user intents can improve visibility without relying on explicit query inputs.

</details>


### [32] [The Recall Ceiling of LLM Recommendation Reranking](https://arxiv.org/abs/2609.27953)
*Zhaohui Wang*

Main category: cs.IR

TL;DR: 论文作者为 Zhaohui Wang，具体所属公司/机构与团队论文摘要未披露。研究场景为推荐系统的检索-重排两阶段 pipeline（针对重排/reranking 阶段的评估协议有效性），核心发现是：oracle 评估协议因候选集中注入或保证 ground-truth 项而高估真实 NDCG@10 达 92–95%；其根本原因是召回上限——在八个数据集（三个领域）中，K=100 的真实检索只能覆盖 2–19% 的相关项；在真实的低召回检索下，多种 LLM 重排优化策略（prompt 工程、168× 参数范围的模型缩放、序列模型、监督神经重排器、LoRA 微调、混合检索、评分感知提示、LLM+CF 融合）均未显著优于协同过滤（CF）基线。据此提出 Recall-Aware Evaluation Protocol（RAEP）评估协议。


<details>
  <summary>Details</summary>
Motivation: 论文要解决的瓶颈是：LLM 推荐重排器常采用 oracle 评估协议（将 ground-truth 项注入候选列表或对其与采样负例打分），该协议无法反映真实生产系统中检索阶段召回不足的现实，导致对重排器能力的高估。作者指出真实检索（K=100）仅覆盖 2–19% 相关项，构成所有封闭候选集重排器的确定性 NDCG 上限（E[NDCG@k] ≤ Recall@|W_π|），因此在低召回机制下提升重排器复杂度收益有限，而提升检索召回的收益更大。论文摘要未披露具体业务规模与线上工程约束的更多细节。

Method: 核心方法是先揭示召回天花板（recall ceiling）并给出理论界：在 leave-one-out 评估下，封闭候选集重排器的期望 NDCG@k 不超过召回率 Recall@|W_π|（W_π 为重排器候选窗口）。实证部分在同一协议下对比了多种优化策略：prompt 工程、模型缩放（覆盖 168× 参数范围）、序列模型、监督神经重排器、LoRA 微调、混合检索、评分感知提示、LLM+CF 融合，以及文本感知检索和上游 CF 评分注入。作者指出这些策略在真实检索下均未显著优于 CF 基线，且提供上游 CF 评分主要使 LLM 复现 CF 的排序。论文提出 Recall-Aware Evaluation Protocol（RAEP）：先对检索召回机制进行分类，再仅在召回天花板允许有意义区分的机制下评估重排器。（以上为作者在本摘要中陈述的观点与协议设计；具体架构、训练目标与模块实现的细节论文摘要未披露。）

Result: 离线指标：
- 在三个 Amazon 主数据集上，oracle 评估协议高估真实 NDCG@10 达 92–95%。
- 八个数据集（三个领域）中，K=100 的真实检索覆盖相关项比例为 2–19%。
- 真实检索下，测试的所有优化策略均未显著优于协同过滤（CF）基线。
- 文本感知检索在其中一个数据集上提升了召回率，但未改善端到端 NDCG。

线上 A/B 指标：论文摘要未披露。
模型规模/训练数据/训练硬件与时间/推理硬件/延迟/吞吐/内存/成本：论文摘要未披露（仅提及模型缩放覆盖 168× 参数范围，但未给出具体参数量）。

Conclusion: 论文解决的问题是 LLM 推荐重排器评估协议的系统性偏差，并用召回上限理论解释了为什么在低召回候选集上重排器能力无法体现。其可复用的实践价值在于：工业推荐团队在评估重排/精排模型时应先诊断检索召回机制，再决定是否投入重排器复杂度；在低召回（如 2–19% @ K=100）场景下应优先优化检索而非重排器。局限/证据缺口：论文基于离线 Amazon 数据集与 leave-one-out 评估，作者自己也声明该结论不一定适用于具有更高召回、更丰富特征或在线反馈的生产系统；所用数据集、模型规模、具体实现细节以及线上 A/B 验证均未在摘要中披露。可迁移的教训是引入 RAEP 式的召回感知评估协议，避免 oracle 协议导致的性能高估与资源错配。

Abstract: Some LLM-based recommendation rerankers are evaluated under an oracle protocol that guarantees the ground-truth item is present in the scored set, either by injecting it into the candidate list or by scoring it against sampled negatives. Across three primary Amazon datasets, we show that this protocol overestimates realistic NDCG@10 by 92--95%. The cause is a recall ceiling: realistic retrieval covers only 2--19% of relevant items at $K=100$ across eight datasets in three domains, imposing a deterministic upper bound on any closed-candidate reranker's top-$k$ NDCG. Under leave-one-out evaluation, $\mathbb{E}[\mathrm{NDCG}@k] \leq \mathrm{Recall}@|W_\pi|$, where $W_\pi$ is the reranker's candidate window. Under realistic retrieval, none of the tested optimisation strategies significantly improves over the collaborative-filtering baseline on our primary Amazon datasets. These strategies include prompt engineering, model scaling over a 168$\times$ parameter range, sequential models, supervised neural rerankers, LoRA fine-tuning, hybrid retrieval, score-aware prompting, and LLM+CF fusion. Text-aware retrieval increases recall on one dataset but does not improve end-to-end NDCG, while providing upstream CF scores mainly makes the LLM reproduce the CF order. We therefore propose the Recall-Aware Evaluation Protocol (RAEP): first classify the retrieval-recall regime, then evaluate reranking where the ceiling permits meaningful differentiation. In the low-recall regimes measured here, improving retrieval is more consequential than increasing reranker sophistication; this ordering need not hold in production systems with higher recall, richer features, or online feedback.

</details>


### [33] [Distilling Lexical Product Associations into Deep Transformers: An Extreme Multi-Label Approach for Natural Language E-Commerce Search](https://arxiv.org/abs/2609.26921)
*Sunnidhya Roy,Samarpita Bhaumik*

Main category: cs.IR

TL;DR: 论文摘要未披露作者所属公司/团队。真实业务场景为自然语言电商搜索与对话式产品推荐，处于检索相关性（词法匹配失效/词汇不匹配）与召回表示学习环节。方案：在54,000产品、27类目的Amazon Reviews '23目录上，将产品推荐建模为极端多标签分类（XMLC），用预训练DistilBERT作为神经学生，通过伪标签知识蒸馏把TF-IDF余弦相似度生成的K=50近邻item-to-item拓扑蒸馏进深度上下文表征。结果：85/15切分、8,089留出产品、C=53,923输出类别且严格自排除下，DistilBERT学生达到P@1=93.15%、P@5=90.08%、NDCG@10=0.8845、MRR@10=0.9545，接近TF-IDF教师上界（P@1=98.10%、NDCG@10=0.9419、MRR@10=0.9882），并在十类结构化自然语言查询（情境、跨类目、改写、负向约束）上显著超出词法匹配。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈是传统电商搜索依赖倒排索引与token级词法匹配（BM25、TF-IDF），在对话式、意图驱动、改写或释义查询上出现经典“词汇不匹配”（vocabulary mismatch），无法理解隐含用户意图。作者指出词法模型在这些查询上完全失效（resolve implicit user intent where lexical models fail completely）。业务意义在于电商目录规模达5.4万产品（27类目），需在保留检索相关性（以TF-IDF教师性能为上界）的同时，用深度模型突破词法匹配的泛化局限，并兼顾工业目规模（>10^6 items）下的投影层内存扩展性与向量检索部署路径。论文摘要未披露具体线上指标或CTR/CVR等业务收益。

Method: 整体架构为“TF-IDF教师 → 伪标签知识蒸馏 → DistilBERT神经学生”的极端多标签分类（XMLC）框架。教师侧：对累积元数据做TF-IDF余弦相似度构建稠密item-to-item相似拓扑，取K=50近邻作为伪标签；学生侧：预训练DistilBERT编码器输出深度上下文表征，经极端分类投影层（C=53,923个输出类）学习该拓扑。训练/评估采用严格85/15切分，8,089个留出产品不参与训练，并实施严格自排除（strict self-exclusion）避免泄漏。定性评测覆盖十类结构化自然语言查询原型：情境式、跨类目、改写（paraphrased）、负向约束查询。作者还分析了极端分类投影层在工业目录规模（>10^6 items）下的架构与内存扩展权衡，并给出向Dual-Encoder（Two-Tower）向量检索迁移的具体部署路径。以上为作者披露内容；作者声称的核心创新在于将知识蒸馏与XMLC结合以同时获得词法教师的准确性与Transformer的语义泛化，并将该能力复用为双塔检索的编码器。

Result: 离线指标（85/15切分、8,089留出产品、C=53,923类、严格自排除）：DistilBERT学生 P@1=93.15%、P@5=90.08%、NDCG@10=0.8845、MRR@10=0.9545；TF-IDF教师（校正后）上界 P@1=98.10%、NDCG@10=0.9419、MRR@10=0.9882。注意：学生NDCG/MRR略低于教师，作者表述为“closely recovering the empirical ceiling”。线上A/B测试收益：论文摘要未披露。模型规模：使用预训练DistilBERT transformer编码器，具体参数规模论文摘要未披露。训练数据：Amazon Reviews '23基准，N=54,000产品、27平衡零售类目、C=53,923输出类，训练/验证为85/15。训练硬件与时间：论文摘要未披露。推理硬件、延迟、吞吐、内存、成本：论文摘要未披露（摘要仅定性讨论>10^6 items目录下投影层的架构与内存扩展权衡，无具体数值）。

Conclusion: 论文解决的核心问题是词法匹配在对话式/意图驱动电商查询上的词汇不匹配缺陷，通过把产品推荐形式化为XMLC并用TF-IDF伪标签蒸馏DistilBERT，在5.4万产品规模下取得接近词法教师上界的离线检索质量，同时在十类语义化查询原型上突破词法匹配的泛化边界。可复用点：工业搜索/推荐团队可直接借鉴“轻量词法教师产生近邻伪标签→Transformer学生蒸馏→转双塔向量检索”的渐进式迁移路径，特别适合冷启动语义检索向量化或从倒排索引平滑升级到ANN检索。局限与证据缺口：论文摘要未披露所属公司/团队、模型参数量、训练/推理资源、延迟吞吐、成本及任何线上A/B收益，且学生NDCG@10（0.8845）与MRR@10（0.9545）实际低于教师（0.9419/0.9882），线上业务价值（CTR/CVR/相关性提升）尚未被证明；仅5.4万目录的离线结果与随机定性query示例，外推到>10^6工业目录时的质量退化、投影层内存可行性与蒸馏带来的泛化上限仍需在真实电商流量下验证。

Abstract: Traditional e-commerce search platforms rely heavily on inverted indices and token-level lexical matching algorithms (e.g., BM25 and TF-IDF), which frequently fail on conversational, intent-driven, or paraphrased user queries -- the classic vocabulary mismatch problem. We formulate conversational product recommendation as an Extreme Multi-Label Classification (XMLC) problem over an e-commerce catalog of N = 54,000 products spanning 27 balanced retail categories from the Amazon Reviews '23 benchmark. Using a pre-trained DistilBERT transformer encoder, we distill dense item-to-item similarity topologies (generated via TF-IDF cosine similarity over cumulative metadata with K = 50 nearest neighbours) into a deep contextual representation via a pseudo-label knowledge distillation framework. Evaluated on an exact 85/15 train/validation split (8,089 held-out products across C = 53,923 output classes) with strict self-exclusion enforced, the DistilBERT neural student achieves P@1 = 93.15%, P@5 = 90.08%, NDCG@10 = 0.8845, and MRR@10 = 0.9545, closely recovering the empirical ceiling established by the corrected TF-IDF teacher (P@1 = 98.10%, NDCG@10 = 0.9419, MRR@10 = 0.9882). Furthermore, a qualitative benchmark across ten structured natural language query archetypes -- encompassing situational, cross-category, paraphrased, and negative-constraint queries -- demonstrates that the transformer student generalises substantially beyond keyword matching, successfully resolving implicit user intent where lexical models fail completely. Finally, we analyse the architectural and memory scalability trade-offs of extreme classification projection layers at industrial catalog scale (> 10^6 items) and present a concrete deployment trajectory toward Dual-Encoder (Two-Tower) vector search. Code: this https URL .

</details>


### [34] [ItColBERT: An Italian-Specialised Late-Interaction Retriever](https://arxiv.org/abs/2609.26856)
*Enrico Nello*

Main category: cs.IR

TL;DR: 论文作者为 Enrico Nello，公司与团队信息论文摘要未披露。业务场景是意大利语（低资源语言）神经信息检索，处于检索/召回（candidate retrieval / late-interaction scoring）阶段。作者提出并发布 ItColBERT，一个 135M 参数、专精意大利语的 ColBERT 延迟交互检索器，采用 ColBERT-Zero 配方（从能检索的 checkpoint 初始化 → 监督对比训练 → 单教师蒸馏），训练约 14.5 GPU-小时（单张 RTX 3090）。它在四个意大利语检索基准上超过除 mLateOn 外的所有通用延迟交互基线，且参数比除一个同规模基线外的所有基线少 2–4.4 倍；主要实证结论偏“方法学/负面”：在唯一干净的域外基准 MLDR-it 上，对未改动的 checkpoint 做推理期分块即带来 +0.0602 nDCG@10（p=0.0225），效果超过后续两轮训练带来的任何提升；自挖掘难负样本与原生 1024-token 训练均未通过预设决策门槛。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈：意大利语神经信息检索几乎完全依赖多语言模型；虽已有多个强意大利语稠密嵌入模型，但截至 2026 年 8 月尚无专精意大利语的延迟交互（multi-vector / late-interaction）检索器发布。通用多语言模型在单语种上的参数效率与检索效果存在缺口，且训练与上线成本需要控制。该问题直接对应工业搜索/检索中的多语言低资源线上检索质量与模型部署成本约束。

Method: 系统为 ItColBERT，135M 参数的意大利语 ColBERT（multi-vector 延迟交互检索）。训练遵循 ColBERT-Zero 配方，流程为：从一个“已经能检索”的 checkpoint 初始化，然后进行有监督对比（supervised contrastive）训练，再进行单教师蒸馏（single-teacher distillation）。实现基于 PyLate。核心实证（部分负面）结论：在唯一的干净域外基准 MLDR-it 上，对未改动 checkpoint 应用“推理期分块（inference-time chunking）配方”即可显著提升；而自挖掘难负样本（self-mined hard negatives）与原生 1024-token 训练均未能通过预设（pre-registered）决策门槛。所有对比均采用配对 bootstrap 检验，并以实测噪声地板 0.0030 nDCG@10 为判断基准。作者声明释放权重、训练与评估代码及完整实验记录（含被拒的轮次）。

Result: 离线结果：在四个意大利语检索基准上，ItColBERT 超过测试的所有通用延迟交互基线（唯一例外是 mLateOn）；参数比除一个可比较规模基线外的所有基线少 2–4.4 倍。纯域外基准 MLDR-it 上，推理期分块配方为未改 checkpoint 带来 +0.0602 nDCG@10（p=0.0225），大于后续两轮训练的任何提升；自挖掘难负样本与原生 1024-token 训练均未通过预设决策门槛；噪声地板实测为 0.0030 nDCG@10。在线 A/B 收益：论文摘要未披露。模型规模：135M 参数。训练数据：论文摘要未披露（仅提及自挖掘难负样本，具体语料规模未披露）；训练硬件：单张 RTX 3090；训练时间：约 14.5 GPU-小时；训练其他成本未披露。推理硬件、延迟、吞吐、内存、成本：论文摘要未披露。

Conclusion: 论文解决的是低资源语言（意大利语）专用延迟交互检索器的缺口，提供了一个 135M 的小型高参数效率 ColBERT，并以低成本（单卡约 14.5 GPU-小时）训练完成，可直接作为意大利语检索/召回的基线或底座。其最有迁移价值的经验是：推理期分块策略带来的收益可能超过继续训练，训练侧的一些常见“升级”（自挖掘难负样本、原生长序列训练）可能不划算，工业团队应把推理期技巧纳入评估优先项并设置预设决策门槛与噪声地板以避免过拟合于噪音提升。局限性/证据缺口：公司、业务团队、实际线上指标、训练语料规模均未披露；结论基于离线基准且核心发现偏“负面”，缺乏在线验证与跨语言泛化证据；与 mLateOn 相比仍非全面胜出。

Abstract: Neural information retrieval for Italian is served almost entirely by multilingual models. Several multi-vector (late-interaction) retrievers include Italian among dozens of languages, and several strong Italian dense embedders exist, but as of August 2026 no late-interaction retriever specialised on Italian had been released. We present ItColBERT, a 135M-parameter Italian ColBERT trained with PyLate following the ColBERT-Zero recipe: initialise from a checkpoint that already retrieves, then apply supervised contrastive training followed by single-teacher distillation, for a total of roughly 14.5 GPU-hours on one RTX 3090. Across four Italian retrieval benchmarks it outperforms every general-purpose late-interaction baseline we tested except one (mLateOn), at 2-4.4x fewer parameters than every baseline but one of comparable size. Our principal empirical finding is methodological and partly negative. On the only cleanly out-of-domain benchmark (MLDR-it), an inference-time chunking recipe applied to an unchanged checkpoint yields +0.0602 nDCG@10 (p = 0.0225), a larger effect than anything two further rounds of training produced. Self-mined hard negatives and native 1024-token training were both evaluated against pre-registered decision gates and both failed. We report every comparison with paired bootstrap tests against an empirically measured noise floor of 0.0030 nDCG@10, and we release the weights, the training and evaluation code, and the complete experimental record including the rejected rounds.

</details>


### [35] [Test-Time Adaptation with Query-Dependent Residuals for Visual Document Retrieval](https://arxiv.org/abs/2609.27688)
*Zeliang Li,Xiaofen Xing,Kailing Guo,Xiangmin Xu*

Main category: cs.IR

TL;DR: 论文摘要未披露公司/团队信息。该工作面向视觉文档检索（VDR）系统部署后的测试时自适应问题，处于检索管线中的召回/重排阶段（利用有限 reranker 反馈改进页面检索得分，且保持编码器与页面索引固定）。提出的 Q-REACT 是一种查询侧测试时自适应方法：学习共享低秩变换生成查询相关残差，将自适应查询得分与文档级上下文结合，并采用在完整任务页面索引上归一化的学生分布蒸馏 reranker 偏好。在 8 个 ViDoRe V3 任务与 5 个开放权重及闭源骨干模型上，Q-REACT 在稀疏与全覆盖预算下均提升平均检索性能，可迁移到留出查询与任务，且推理开销很小。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于 VDR 系统的页面嵌入在部署前离线计算，部署后若编码器参数不可修改、也无法对语料重编码，系统难以针对新查询分布或任务进行自适应改进。现有 reranker 能提供相关性信号，但传统重排仅能作用于"被选中的查询和候选页面"（selected queries and candidate pages），反馈覆盖有限，无法惠及未参与评分的页面。核心问题是如何把有限的 reranker 反馈摊销到整个查询集合，在不重训练、不重建检索器的前提下提升检索质量，同时控制推理开销。

Method: Q-REACT 为查询侧测试时自适应方法，核心思想是把有限 reranker 反馈转化为可复用的检索改进，而非对单查询做一次性重排。作者声称的技术要点包括：(1) 学习一个共享的低秩变换，产生"查询相关残差"（query-dependent residuals）；(2) 将自适应后的查询得分与文档级上下文（document-level context）结合；(3) 用在一个"完整任务特定页面索引"上归一化的学生分布（student distribution normalized over the complete task-specific page index）来蒸馏 reranker 偏好。该设计使未被评分（unscored）的页面也能通过缓存嵌入参与竞争，同时保持编码器与页面索引固定。输入输出流、具体训练目标函数形式、低秩维度及文档上下文的具体建模方式等细节，论文摘要未披露。

Result: 离线指标：在 8 个 ViDoRe V3 任务、5 个开放权重与闭源骨干模型上，Q-REACT 在稀疏（sparse）与全覆盖（full-coverage）两种反馈预算下，平均检索性能相较被评估的基线均有所提升；并报告可迁移至留出查询与任务，以及推理开销增加很少。论文摘要未披露具体的数值指标（如 nDCG、Recall、MAP 等）、基线名称、各预算下的具体提升幅度、显著性检验结果，也未披露模型规模、训练数据与规模、训练硬件/时间、推理硬件、延迟、吞吐、内存或成本等任何资源信息。

Conclusion: Q-REACT 解决的是检索系统部署后无法重训练/重编码时的测试时自适应问题，将有限 reranker 反馈摊销到整个查询集合，避免重建 retriever，具有工程可落地价值：尤其适用于嵌入缓存不可变更、在线追加自适应改进的场景。其可迁移到留出查询与任务、推理开销低的特性对工业检索团队有吸引力。局限与证据缺口：论文摘要未披露任何具体指标数值、在线 A/B 结果、模型/数据规模与成本，也缺少与"重新编码或重训练"这一强基线的直接量化对比；"adds little inference overhead"为定性描述，缺少延迟/吞吐等硬数据。工业搜索/文档检索团队可借鉴其"查询侧低秩残差 + 全索引归一化蒸馏"的思路，用于免重训练、免重编码的召回/重排自适应，但落地前需自行验证在线收益与推理成本。

Abstract: Visual document retrieval (VDR) systems depend on page embeddings computed before deployment, which makes adaptation difficult when encoder parameters or corpus re-encoding are unavailable. Rerankers provide useful relevance signals, but conventional reranking applies them only to selected queries and candidate pages. We introduce Q-REACT, a query-side test-time adaptation method that converts limited reranker feedback into reusable retrieval improvements. Q-REACT learns a shared low-rank transformation that produces query-dependent residuals, combines adapted query scores with document-level context, and distills reranker preferences with a student distribution normalized over the complete task-specific page index. This design lets unscored pages compete through cached embeddings while keeping the encoders and page index fixed. Across eight ViDoRe V3 tasks and five open-weight and proprietary backbones, Q-REACT improves average retrieval over evaluated baselines at sparse and full-coverage budgets, transfers to held-out queries and tasks, and adds little inference overhead. The results show that finite reranker feedback can be amortized across a query collection without retraining or rebuilding the retriever.

</details>


### [36] [EidosDoc: Implicit Structure Encoding for Cost-Effective Semi-Structured Document QA](https://arxiv.org/abs/2609.27784)
*Teng Lin,Yuyu Luo,Nan Tang*

Main category: cs.IR

TL;DR: 论文摘要未披露具体公司/团队；作者隶属机构为论文摘要未披露。真实业务场景为半结构化文档（科学报告、财务报表、技术手册）的问答；管线阶段为文档结构编码、检索召回（多路Hybrid Retrieval Pipeline）与LLM推理优化（通过减少LLM调用降低成本和延迟）。系统为EidosDoc，核心包括隐式结构编码器、无LLM参与的混合检索、动态证据扩展，在四个基准上达到SOTA精度，并报告较前一SOTA方法"成本降低50倍、延迟降低4倍"。


<details>
  <summary>Details</summary>
Motivation: 业务瓶颈：半结构化文档问答需同时理解文本、表格、图表和复杂层级布局。现有方案要么反复调用大语言模型做结构解析和检索，导致高成本、高延迟；要么将文档展平，丢失布局与层级信息，牺牲答案精度。EidosDoc旨在在精度、成本、速度三者间建立更优折中，提供可规模化落地的路径。

Method: EidosDoc提出三项创新（作者声明）：(1) 隐式结构编码器（Implicit Structure Encoder），通过对比学习与结构一致性损失训练，把层级关系、空间位置、文本内容联合嵌入同一稠密向量空间，整体刻画文档结构，且无需人工定义、易出错的构造；(2) 混合检索流水线（Hybrid Retrieval Pipeline），组合BM25、布局指纹（layout fingerprints）与轻量交叉编码器（cross-encoder），完全不经LLM完成高精度检索，从而大幅降低成本与延迟（对应检索/召回阶段）；(3) 动态证据扩展（Dynamic Evidence Expansion），自适应检索空间相邻、结构相关的证据，以缓解固定路径检索常见的信息遗漏问题。输入输出流、具体网络结构、训练目标细节及模块规模等纸质摘要未详细披露。

Result: 离线指标：在四个基准上（benchmark名称与具体精度数值论文摘要未披露）取得新的SOTA精度。相对前一SOTA方法：成本降低50倍（cost: 50 times reduction）、延迟降低4倍（latency: 4 times lower）。需要注意：成本和延迟对比基准为"前一SOTA方法"，但具体绝对成本、延迟数值、测试环境未披露。在线A/B实验：论文摘要未披露。模型规模、训练数据、训练硬件/时间、推理硬件、吞吐、显存均论文摘要未披露。

Conclusion: EidosDoc解决半结构化文档问答中精度与成本/延迟不可兼得的矛盾，核心可复用价值在于：用可训练的隐式结构编码替代规则式结构解析、用BM25+布局指纹+轻量交叉编码器的多路混合检索替代LLM化检索以削减成本延迟、用动态证据扩展缓解召回信息遗漏。这些思路对工业文档QA、知识库问答、RAG检索阶段及LLM推理降本具有迁移意义。局限与证据缺口：摘要未披露具体公司/产品、四个基准名称及精度数值、"50倍成本、4倍延迟"的绝对基线与测量口径、模型与数据规模、是否经过线上验证；"cost reduction/latency"为作者自报结果，无法从摘要判断其统计严谨性与可复现性，需阅读正文核实对比基准的公平性。

Abstract: Semi-structured documents are ubiquitous in scientific reports, financial statements, and technical manuals. Question answering over such documents requires simultaneous understanding of text, tables, charts, and complex hierarchical layouts. Existing methods either rely on repeatedly calling large language models for structure parsing and retrieval, leading to high cost and large latency, or they flatten the document and lose layout and hierarchy information, sacrificing answer accuracy. To address this, we propose EidosDoc, a novel system that achieves state-of-the-art accuracy with minimal computational expense. Our approach introduces three core innovations. (1) An Implicit Structure Encoder trained via contrastive learning and a structure consistency loss. This module jointly embeds hierarchical relationships, spatial positions, and textual content into a dense vector space, capturing document structure holistically without the need for manually defined and error-prone constructions. (2) A Hybrid Retrieval Pipeline that leverages BM25, layout fingerprints, and a lightweight cross-encoder to perform high-precision retrieval entirely without invoking an LLM, drastically reducing cost and latency. (3) A Dynamic Evidence Expansion mechanism that adaptively retrieves spatially adjacent and structurally related evidence, overcoming the evidence omission common in fixed-path retrieval methods. We evaluate EidosDoc on four benchmarks, and comprehensive evaluations show that EidosDoc achieves a new state-of-the-art accuracy on the four benchmarks. Crucially, it does so with a 50 times reduction in cost and 4 times lower latency compared to the previous state-of-the-art Method. These results demonstrate that EidosDoc establishes a new optimal trade-off among accuracy, cost, and speed, offering a practical and scalable path for accurate semi-structured document analysis.

</details>


### [37] [When LLM-Based User Profiling Adds Value in Production Streaming Recommendation](https://arxiv.org/abs/2609.27183)
*Milad Sabouri,Neeraj Sharma,Sardar Hamidian,Shaghayegh Agah*

Main category: cs.IR

TL;DR: 作者所属公司及团队论文摘要未披露；业务场景为生产环境下的流式内容推荐，所处pipeline阶段为用户画像构建（user profiling / 用户表征）。论文提出对四种语义用户画像策略进行系统对比：表示类型（聚合式 numerical aggregate vs. LLM式自然语言摘要）与时间处理（近期 vs. 历史行为解缠）的因子交叉。核心结论是揭示不同策略在用户行为类型、准确性与超准确性维度、以及时间窗口设置上的差异，从而回答LLM画像的额外成本何时值得承担；具体指标数字论文摘要未披露。


<details>
  <summary>Details</summary>
Motivation: 生产推荐系统依赖用户历史行为构建用户表征，存在两种语义画像范式：一是将语义item embedding做数值聚合的aggregate方法，二是用LLM生成自然语言偏好摘要再经文本编码的LLM-based方法；二者还可叠加近期/历史行为的时间解缠。业务瓶颈在于LLM画像生成成本显著高于聚合方法，生产团队需要判断额外成本在何种条件下才被证明合理。论文旨在回答该生产决策问题，但摘要未披露具体规模、延迟或成本数值。

Method: 论文对四种语义用户画像策略做因子交叉的系统对比，因子包括（1）表示类型：聚合式（语义item embedding的数值聚合）vs. LLM式（自然语言偏好摘要+文本编码器）；（2）时间处理：是否对近期行为与历史行为做时间解缠。在真实生产数据集上从三个维度评估差异：不同用户行为类型、精度（accuracy）与超精度（beyond-accuracy）推荐质量维度、以及控制解缠的时间窗口设置。摘要未披露具体模型架构、文本编码器、训练目标或LLM具体规模等实现细节。

Result: 论文摘要未披露任何具体离线指标数值或在线A/B实验增益，仅作定性结论：四种策略在不同行为类型、不同推荐质量维度及不同时间窗口设置下存在表现差异，并据此判断LLM画像的额外成本何时合理。模型规模、训练数据规模、训练硬件/时间、推理硬件、延迟、吞吐、内存及成本等均论文摘要未披露。

Conclusion: 论文的实用价值在于为生产流式推荐团队提供一个成本-收益决策框架：何时值得用昂贵的LLM式用户画像替代廉价的聚合式画像，并指出其优势取决于用户行为类型与时间解缠配置。局限在于摘要不含任何量化指标、数据集规模、公司背景或工程资源数据，无法独立验证"增加价值"的具体幅度。可复用经验：画像建模的选型需同时评估accuracy与beyond-accuracy维度，并将时间解缠作为独立因子纳入消融，而非笼统用LLM替换所有画像路径。

Abstract: Personalized recommendation depends critically on how user representations are constructed from historical behavior. Two paradigms have emerged for constructing semantic user profiles in content-based recommendation. First, aggregate methods derive user representations as numerical aggregates of semantic item embeddings. Second, LLM-based methods generate natural-language summaries of user preferences and encode them through a text encoder. Each paradigm can be combined with temporal disentanglement of recent versus historical behavior. LLM-based profile generation is significantly more expensive than aggregate approaches, raising the question of when this additional cost is justified. We present a systematic comparison of four semantic user-profiling strategies, factorially crossed across representation type and temporal handling, evaluated on a real-world production dataset. The comparison reveals how these strategies differ across user behavior types, across both accuracy and beyond-accuracy dimensions of recommendation quality, and across the temporal-window setting that governs the disentanglement.

</details>


### [38] [BoundaryMORPH: Budgeted Reranking via Active Set Selection for Diffuse Retrieval](https://arxiv.org/abs/2609.27213)
*Eylon Caplan,Shamik Roy,Shib Sankar Dasgupta,Yingfan Wang,Rashmi Gangadharaiah*

Main category: cs.IR

TL;DR: 论文摘要未披露具体公司/团队。真实业务场景为面向开放式查询（open-ended queries）的 RAG 检索系统，处于检索后的精排（reranking）阶段。系统提出 BoundaryMORPH 算法：在交叉编码器（CE）预算 B 受延迟严格限制、且 B 小于 LLM 上下文容量 k 的 mismatch 下，通过高斯过程（Gaussian Process）利用双塔初排作为结构先验，将 CE 调用集中在 top-k 集合成员关系的"边界"判定上，而非追求单个最相关文档。头条结果：在多个模型和开放式查询数据集上达到 state-of-the-art 集合检索质量，相较最强基线 +5.4 nCG@100。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于 RAG 系统中开放式（diffuse）查询需要将大量文档装配进有限的 LLM 上下文窗口，因此需要既快又准的检索。现有方案用快速双塔（dual-encoders）初排、再用昂贵交叉编码器（CE）精排，但 CE 预算 B 受 latency 严格约束，且 B 常小于上下文窗口容量 k。这一 mismatch 使标准 reranking 存在结构性缺陷：把算力浪费在验证明显排在前面的候选上，却忽略了初排靠后但实际相关的文档。问题对业务的价值在于：在延迟受限下最大化 LLM 输入上下文的整体检索质量（集合级质量），而非单文档相关度。

Method: BoundaryMORPH 是一个面向预算受限 reranking 的算法。核心思路：将双塔（dual-encoder）初排结果作为结构先验（structural prior），用高斯过程（Gaussian Process）建模，把 CE 调用预算智能地花在判定 top-k 集合成员关系的"边界"上，而非寻求单一最相关文档。每次 CE 调用得到的信息会传播到尚未打分的文档（propagates to unscored documents），从而最大化预算效用。输入输出流：初始双塔排序 → 高斯过程结构先验 → 按边界不确定性分配 CE 预算 → 更新 top-k 成员关系估计。作者主张的创新点是"以 top-k 集合成员关系为目标、以边界主动集选择（active set selection）分配预算"这一全新范式，区别于传统单文档 reranking。该理解基于摘要，论文摘要未披露更细的训练目标、损失函数或实现细节。

Result: 离线指标：BoundaryMORPH 在多个模型和开放式查询数据集上取得 state-of-the-art 集合检索质量，相较最强基线 +5.4 nCG@100（nCG 为 normalised Cumulative Gain，@100 表示前 100 个文档，作者未在摘要中披露 G 的具体定义）。在线指标：论文摘要未披露。模型规模：论文摘要未披露。训练数据：论文摘要未披露。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟：论文摘要未披露（仅提及 CE 预算 B "strictly bounded by latency"，但未给出具体延迟数值）。吞吐、内存、成本：论文摘要未披露。

Conclusion: 该工作解决了延迟受限下 CE 预算小于 LLM 上下文容量 k 时的集合级（top-k）检索质量问题，用主动集选择 + 高斯过程把有限交叉编码器算力集中在边界确定性上。实用价值：对工业 RAG、搜索召回精排、广告候选集截断等"预算内最大化集合质量"的场景有直接可迁移性，尤其是需要喂给 LLM 固定上下文窗口的检索链路。局限/证据缺口：摘要未披露数据集规模、CE 模型种类、延迟对比、在线 A/B 效果、预算 B 与容量 k 的具体取值，以及方法对超参数（如高斯过程核、边界宽度）的敏感性；"最强基线"的具体名称未披露。可复用点：将 reranking 目标从"单文档排序"改为"top-k 集合成员判定"的思路、以及以结构先验 + 主动预算分配替代全量或 top-B 重排的做法，可供工业搜索/广告/推荐/RAG 团队在延迟约束精排与候选集选择环节借鉴。

Abstract: Open-ended queries in modern Retrieval-Augmented Generation (RAG) are increasingly "diffuse," requiring a large set of documents to be assembled into a finite LLM context window. To ensure retrieval quality, systems use fast dual-encoders and more expensive cross-encoders (CEs) to score candidates. However, the CE budget $B$ is strictly bounded by latency and is often smaller than the context window capacity $k$. This mismatch makes standard reranking structurally flawed: it wastes compute verifying obvious top candidates while ignoring relevant documents further down the initial ranking. To address this, we introduce BoundaryMORPH, a novel algorithm that allocates CE budget specifically for the LLM's context capacity $k$. Using a Gaussian Process, BoundaryMORPH treats the initial dual-encoder ranking as a structural prior and intelligently spends CE calls on resolving top-$k$ set membership at the boundary, rather than seeking a single most-relevant document. Information from each CE call propagates to unscored documents, maximizing the utility of the budget. We demonstrate that BoundaryMORPH achieves state-of-the-art set retrieval quality across multiple models and datasets with open-ended queries ($+5.4$ nCG@100 over the strongest baseline).

</details>


### [39] [Beyond a Scalar: Distributional Serving Interfaces for Watch-Time Prediction](https://arxiv.org/abs/2609.28383)
*Xuan Liu,Jingbin Qian,Zhanyu Liu,Hefeng Zhou*

Main category: cs.IR

TL;DR: 论文摘要未明确披露公司/团队所属部门（但 KuaiRec、KuaiRand-1K 数据集指向快手，作者团队身份论文摘要未披露）；业务场景为短视频信息流的观看时长预测，处于排序（ranking）/曝光（exposure）及下游检索（retrieval）环节的在线服务（serving）接口阶段。作者提出"分布化服务接口 DSI"，包含分布提供者、紧凑低维摘要与轻量读出模块：提供者学习基于观看比例的四种观看状态联合分布及其事件时间，用视频时长规则剔除不兼容组合，并以恢复损失保持秒级精度；摘要将其压缩为事件概率、相对时长的尺度与不确定性统计量；读出模块组合摘要与原始上下文。在 KuaiRec、KuaiRand-1K、WeChat21 三个数据集上，DSI 取得最低 MAE（比九个基线中的最强结果低 1.9%–8.5%），在两个数据集上取得最佳 XAUC，并在考虑视频时长的检索指标上领先。


<details>
  <summary>Details</summary>
Motivation: 现有观看时长预测方法虽通过修正时长偏差或建模更丰富分布来改进，但在服务时刻大多只暴露一个期望值或去偏后的观看时长标量，即使下游模型能拿到视频时长，接口也只提供一个观看时长估计，缺少完工、超播等下游任务所需区域的概率信息。这限制了下游排序、检索等任务对观看行为不确定性和分布细节的利用，是短视频信息流排序/曝光环节的实际生产瓶颈。

Method: DSI 由三部分组成：(1) 分布提供者（distribution provider）：学习基于观看比例的四种观看状态的联合分布及其事件时间，利用基于视频时长的规则剔除不兼容的状态组合，并通过恢复损失（restoration loss）保持以秒为单位的精度；(2) 紧凑低维摘要（summary）：将该分布压缩为一小组事件概率、相对视频时长的尺度（time scales relative to duration）以及不确定性统计量；(3) 轻量读出模块（readouts）：在固定提供者参数后，训练价值（value）与排序（ranking）读出，将摘要与原始上下文结合。文中还对比了"摘要+视频时长"与"预测均值+视频时长"的信息保留能力，并用相同的轻量线性头验证对新观看时长目标及单独记录的互动目标（engagement target）的迁移效果，同时以随机初始化的提供者作为对照。上述为作者表述；作者未给出具体模型网络结构、训练目标公式细节或各状态的定义，论文摘要未披露。

Result: 线下指标：在 KuaiRec、KuaiRand-1K、WeChat21 三个数据集上，完整 DSI 系统取得最低 MAE，相对九个基线中的最强结果降低 1.9% 至 8.5%；在两个数据集上取得最佳 XAUC；在考虑视频时长的检索指标上，完整系统对比中领先。在保持读出模块不变时，摘要相对"预测均值+视频时长"保留了更多与任务相关的信息。使用相同轻量线性头时，在新观看时长目标上表现最佳，并改善一个单独记录的互动目标，而随机初始化的提供者无法复现该增益。在线 A/B 实验增益：论文摘要未披露。模型规模、训练数据规模、训练硬件/时长、推理硬件、延迟、吞吐量、内存、成本：论文摘要均未披露。

Conclusion: 论文针对短视频信息流观看时长预测在服务接口上只暴露标量、丢失分布信息的问题，提出了可嵌入在线服务的分布化服务接口 DSI，通过联合分布建模、时长约束与低维摘要，为下游排序/检索任务提供完工/超播等区域概率和不确定性信息，并在多数据集上取得 MAE、XAUC 及检索指标领先。对工业推荐/排序团队的借鉴价值在于：将"预测接口"从单一标量升级为结构化分布摘要，并以轻量读出适配多样下游目标。局限与证据缺口：论文摘要未披露是否包含在线 A/B 实验及线上收益，也未披露模型规模、训练/推理资源与延迟成本，无法评估其线上工程可行性与开销；"四个观看状态""恢复损失"等具体设计细节与真实生产收益尚需正文核实。

Abstract: Watch time is the primary engagement signal in short video feeds, and its prediction directly affects ranking and exposure. Existing methods improve watch time prediction by correcting duration bias or modeling richer distributions, but most expose only an expected or debiased watch time at serving time. Even when video duration is available to later models, the interface gives only one estimate of watch time and no probabilities for completion, overplay, or other regions relevant to downstream tasks. To address this limitation, we propose the Distributional Serving Interface (DSI), which has a distribution provider, a compact, low-dimensional summary, and lightweight readouts tailored to each task. The provider learns a joint distribution over four watch states derived from watch ratio and their event times; rules based on video duration remove incompatible combinations, while a restoration loss preserves accuracy in seconds. The summary reduces this distribution to a small set of event probabilities, time scales relative to duration, and uncertainty statistics. After training the provider, we fix its parameters and train value and ranking readouts that combine the summary with raw context. Across KuaiRec, KuaiRand-1K, and WeChat21, the complete DSI system achieves the lowest MAE on all three datasets, beating the strongest result among nine baselines by 1.9% to 8.5%, and achieves the best XAUC on two. It also leads retrieval metrics that account for video duration when complete systems are compared. With matched readouts held constant, the summary retains information relevant to each task beyond a predicted mean paired with video duration. Using the same lightweight linear heads for each new target, it also performs best on two new watch-time targets and improves a separately logged engagement target, while a randomly initialized provider does not reproduce this gain.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [40] [Agentic Governance and Adversarial Verification for Policy-Constrained LLM Healthcare Appeal Generation](https://arxiv.org/abs/2609.27844)
*Harshil Lodhiya,Alex McManus,Reese Walker*

Main category: cs.AI

TL;DR: 作者团队所属公司与机构在论文摘要中未披露；业务场景为美国医疗报销拒赔申诉（claim denial management）的医学必要性申诉信生成；所处流程阶段为检索增强生成（RAG）与多智能体受控生成（training前的生成/推理与验证环节）；提出AGVF（Agentic Governance and Adversarial Verification Framework）五智能体框架，将申诉合成建模为受限MDP。核心结果：在1,000个合成申诉案例上实现零引用接地违规、单调证据缺陷下降；消融接地门控后违规率升至100%。论文摘要未披露团队、在线A/B收益、模型规模、训练数据、硬件与推理成本。


<details>
  <summary>Details</summary>
Motivation: 业务痛点：美国医疗拒赔管理每年产生约2,600亿美元的行政开销，申诉信书写成本高；现有单智能体LLM/RAG架构在医疗等高风险场景中存在缺陷，会引入无依据的临床细节，并丢失分层支付方政策的逻辑结构。需要一种在显式政策与证据约束下生成申诉文本、且能保证引用接地与证据充分性的架构。

Method: AGVF将申诉合成建模为受限马尔可夫决策过程（CMDP），由五个智能体构成：政策形式化（policy formalization）、证据检索（evidence retrieval）、差距分析（gap analysis）、对抗性批判（adversarial critique）、门控合成（gated synthesis）。论文宣称：在固定政策约束图上迭代精炼可以单调降低证据缺陷，并以完整满足前沿或局部化证据缺口终止；确定性引用接地门控阻止无据断言进入共享状态。论文摘要未披露各智能体的具体模型实现、输入输出细节、训练目标及迭代精炼的具体算法。上述门控有效性结论为作者在合成数据上的实验结论。

Result: 离线指标：在1,000个由去标识公开医院出院数据参数化生成的合成申诉案例上，全部AGVF案例零引用接地违规，且每个episode均单调降低证据缺陷；消融接地门控后违规率升至100%，证实门控是关键组件。在线A/B实验：论文摘要未披露。模型规模：论文摘要未披露。训练数据：研究未使用真实患者记录，摘要称"uses no real patient records"；未披露训练数据规模。训练硬件/时间：论文摘要未披露。推理硬件/时延/吞吐/内存/成本：论文摘要均未披露。

Conclusion: 论文解决了高风险政策约束下LLM生成中证据可控性与层次结构保持的问题，提出理论支撑的多智能体CMDP架构与可验证的引用接地门控机制，对医疗理赔、法律、保险等强合规场景的检索增强生成有可复用的架构思路。局限与证据缺口：全部验证基于合成数据，未使用真实患者记录，未测量临床有效性，未披露在线收益、规模、成本与生产部署指标，因此其工业可用性仍属参考实现层面的验证。可迁移点：多智能体分工（政策形式化—检索—差距分析—批判—门控合成）与"确定性引用接地门控"可作为搜索/推荐/LLM生产系统中事实约束生成的可复用模式。

Abstract: Claim denial management costs U.S. healthcare approximately $260 billion annually in administrative overhead. Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) can produce fluent clinical text, but single-agent architectures fail in high-stakes healthcare: they introduce unsupported clinical details and lose the logical structure of hierarchical payer policy. We propose AGVF (Agentic Governance and Adversarial Verification Framework), a multi-agent architecture for medical-necessity appeal generation under explicit policy and evidence constraints. AGVF models appeal synthesis as a Constrained Markov Decision Process (CMDP) over five agents: policy formalization, evidence retrieval, gap analysis, adversarial critique, and gated synthesis. We prove that refinement over a fixed policy constraint graph monotonically reduces evidence-deficiency and terminates with either a complete satisfying frontier or a localized evidence gap. A deterministic citation- grounding gate prevents assertions without admissible evidence from entering shared state. We provide a reference implementation and validate it on 1,000 synthetic appeal cases parameterized from de-identified public hospital discharge data. The validation confirms zero citation-grounding violations across all AGVF cases and monotone deficiency reduction in every episode; ablating the gate raises violations to 100%, confirming it is load-bearing. The study uses no real patient records and does not measure clinical efficacy. AGVF thus contributes a theory-backed agentic architecture and verified reference implementation for policy-constrained LLM generation in healthcare.

</details>


### [41] [Large Knowledge Model: From Papers to a Scientific Reasoning Landscape](https://arxiv.org/abs/2609.27297)
*Yuan Huang,Sihan Hu,Hongyu Gu,Chao Ma,Jiaxing Zhang,Zhiyong Zou,Caiyu Fan,Yan Xiao,Mingjun Xu,Chenyu Xie,Mingzhen Ju,Zhehao Ma,Qi Zhang,Baozong Wang,Yu Li,Zhiyuan Yao,Ruoxue Liao,Xinyu Li,Linfeng Zhang,Kun Chen,Weinan E*

Main category: cs.AI

TL;DR: 本文由 LKM 团队提出（Weinan E 等，论文摘要未披露具体公司名/业务部门），面向科学文献的大规模检索与知识密集型问答这一生产场景，处于检索（召回/候选生成）与问答评估阶段。系统为 Large Knowledge Model (LKM)：将论文表示为 source-grounded 推理图，结合结构遍历与语义检索，构建 Question / Workflow / Evidence 三视图的 Scientific Reasoning Landscape。核心成果（离线）是固定回答模型时，LKM 检索分别使 ChemBench、PubMedQA、SciBench 准确率提升 9.30%、4.20%、14.69%。


<details>
  <summary>Details</summary>
Motivation: 科学知识只有在先前发现能帮助研究者选题、设计实验、解读结果时才产生价值，但传统文献检索只能按关键词/文本相似度匹配，无法检索"科学意图"——即连接研究问题、科学流程、结论与证据的推理链条。这导致跨论文的相关问题、声明、推理链难以对齐，可复用实验流程难以暴露，结论与其支撑/分歧/适用条件难以关联。要规模化实现这一价值，需要一种把文献组织成可计算、可共享的推理资源的基础设施，这正是生产科学文献检索系统在召回质量与证据可解释性上的瓶颈。

Method: LKM 将每篇论文表示为 source-grounded 推理图（source-grounded reasoning graphs），并基于同一组对象同时做结构遍历（structural traversal）与语义检索（semantic retrieval），对齐跨论文的相关问题、声明与推理链。由此生成 Scientific Reasoning Landscape，包含三个互联视图：Question Landscape（组织研究问题与开放方向）、Workflow Landscape（暴露可复用的科学流程）、Evidence Landscape（把结论与其支撑、分歧、条件关联起来）。在统一基座上支持 reasoning-aware 科学检索、证据-grounded 问答、比较性证据分析与研究规划；研究者/智能体可按科学意图检索相关工作，合成带可检查支撑论据的答案，并基于既有工作流与未解决证据制定研究计划。作者称其为"科学知识基础设施"，是"corpus-scale system"。摘要未披露具体的模型架构、输入输出流程、训练目标、参数规模与工程细节。

Result: 离线指标：固定回答模型（answering model fixed）时，LKM 检索使准确率在 ChemBench 提升 9.30%、PubMedQA 提升 4.20%、SciBench 提升 14.69%。线上 A/B 增益：论文摘要未披露。模型规模、训练数据规模、训练硬件/时长、推理硬件、延迟、吞吐、内存、成本：全部未披露。

Conclusion: 本文解决的核心问题是：如何把海量科学文献从"文本相似度检索"升级为"基于推理意图与证据结构的检索"，从而支撑证据-grounded 问答与研究规划。其可迁移价值在于 source-grounded 推理图 + 结构遍历/语义检索双通道 + 问题/流程/证据三视图的统一表示方法，对工业检索团队（尤其是科学、法律、医疗等知识密集型领域的召回/候选生成与证据可解释问答）有方法论借鉴意义。主要证据缺口：仅报告了 ChemBench/PubMedQA/SciBench 三个离线准确率增益（9.30%/4.20%/14.69%），未披露 corpus 规模、系统架构细节、检索延迟与吞吐、任何在线 A/B 效果或成本指标，且"提升"的具体基线与实验设置（除固定回答模型外）未在摘要中说明，因此工业可复现性有限。工业团队可借鉴其"把实体/结论/证据组织成图并同时做结构遍历与语义检索"的召回增强思想，但需自行验证在生产数据与延迟约束下的收益。

Abstract: Accumulated scientific knowledge advances inquiry when prior findings help researchers choose new questions, design investigations, and interpret results. Realizing this value at scale requires access to the reasoning that connects research problems, scientific procedures, conclusions, and evidence. We introduce the Large Knowledge Model (LKM), a scientific knowledge infrastructure that transforms the literature into a shared, computationally accessible reasoning resource. LKM represents papers as source-grounded reasoning graphs, couples structural traversal with semantic retrieval over the same objects, and aligns related questions, claims, and reasoning chains across papers. This representation forms a Scientific Reasoning Landscape with three connected views: a Question Landscape that organizes research problems and open directions, a Workflow Landscape that exposes reusable scientific procedures, and an Evidence Landscape that connects conclusions to their support, disagreement, and conditions. The unified substrate supports reasoning-aware scientific search, evidence-grounded question answering, comparative evidence analysis, and research planning. Researchers and agents can retrieve relevant work through its scientific intent, synthesize answers with inspectable supporting arguments, and develop research plans informed by established workflows and unresolved evidence. We describe a corpus-scale system and evaluate scientific retrieval and knowledge-intensive question answering. With the answering model fixed, LKM retrieval improves accuracy by 9.30%, 4.20%, and 14.69% on ChemBench, PubMedQA, and SciBench, respectively. By connecting knowledge access to scientific reasoning and action, LKM provides a common foundation for discovering relevant research, reusing scientific knowledge, and coordinating cumulative inquiry across researchers, agents, and research cycles.

</details>


### [42] [Learning the Cost of Reliable Inference](https://arxiv.org/abs/2609.28322)
*Dimitrios Rontogiannis,Ander Artola Velasco,Manuel Gomez Rodriguez*

Main category: cs.AI

TL;DR: 作者机构与团队：论文摘要未披露（作者为 Dimitrios Rontogiannis、Ander Artola Velasco、Manuel Gomez Rodriguez，机构未在摘要中给出）。真实业务场景为 LLM 推理服务市场中连接 LLM 提供商与终端用户的基准测试与路由中介平台；所处流水线阶段可归为 LLM 的推断服务/路由与成本优化阶段。论文提出一个由提供商竞争驱动按任务定价的采购平台：平台通过逆向二价拍卖激励提供商真实投标其服务用户查询的平均成本，在路由查询过程中在线学习各提供商的质量，并把查询渐进式路由给满足质量阈值的提供商中最具成本竞争力者。在 Llama 与 Qwen 多个模型、数学推理与问答基准上的实验显示，最具成本竞争力提供商的定价边际随任务与质量阈值不同在 10% 到 71% 之间显著变化，表明固定单价市场存在可观无效率，用户可在竞争充分时通过该平台获取最大节省。


<details>
  <summary>Details</summary>
Motivation: 现有 LLM 中介/路由平台中，提供商普遍采用每 token 固定价格（fixed price per token），这种统一定价机制使用户无法针对其具体任务获得最具竞争力的价格，造成市场效率损失。论文指出现有方案（固定定价）导致用户在保证质量的前提下无法按任务捕获成本节省，属于对生产环境中推断计费与成本优化环节的改造。其价值在于将经济学机制（竞争性定价、拍卖）引入 LLM 服务采购，使成本与质量在任务粒度上对齐。论文摘要未披露具体平台规模、用户量、请求量、延迟约束或涉及的提供商数量等规模细节。

Method: 论文设计的核心是一个 LLM 采购/路由平台，其机制包括：（1）逆向二价拍卖（reverse second-price auction），用于激励提供商真实投标其服务某个用户查询的平均成本估计；（2）平台在顺序路由查询的过程中，在线学习每个提供商所能提供的质量（quality）；（3）渐进式路由：将查询路由到所有满足预设质量阈值的提供商中"最具成本竞争力（most cost-competitive）"的一家。作者声称该设计的核心思想是把按任务动态定价与经济激励相结合，实现"以竞争性价格换取有保证的质量水平"。输入-输出流为：用户查询进入平台 → 拍卖定价 → 选择满足质量阈值的成本最优提供商 → 返回结果，同时平台更新对提供商质量的估计。论文摘要未披露具体的学习算法（如多臂老虎机/在线学习等）、质量估计的建模方式、定价的具体公式（除"逆向二价拍卖"外）以及训练目标等细节，这些属于作者的机制设计而非本文摘要明确陈述。

Result: 离线结果：在多个来自 Llama 与 Qwen 家族的 LLM、以及在数学推理与问答流行基准上，最具成本竞争力提供商的定价边际（pricing margin）随任务与质量阈值的不同在 10% 到 71% 之间显著变化，作者据此推断当前固定价格市场存在显著无效率，且该平台在竞争充分时能让用户捕获最大节省。在线 A/B 增益：论文摘要未披露。模型规模：论文摘要未披露（仅提及采用 Llama 与 Qwen 家族的多个 LLM）。训练数据、训练硬件/时间：论文摘要未披露（该工作针对推断路由而非模型训练）。推断硬件、延迟、吞吐量、内存、成本：论文摘要未披露，仅报告了定价边际这一经济学/成本指标。

Conclusion: 该论文解决的核心问题是 LLM 服务中介平台中固定单价造成的成本错配，提出以逆向二价拍卖加质量在线学习与渐进路由的机制，实现"任务级竞争性定价 + 保证质量阈值"，并在 Llama/Qwen 多模型基准上验证了固定价格市场存在 10%–71% 的定价边际空间。对工业界（LLM 服务部署、API 聚合/路由平台、成本工程团队）的可复用价值在于：可将竞争性定价/拍卖机制与在线质量学习结合，用于降低用户采购成本、优化路由的性价比；对搜索/广告团队也能提供"机制设计 + 在线路由 + 成本-质量权衡"的借鉴思路。主要局限与证据缺口：论文摘要未披露任何在线 A/B 或真实生产流量验证，未给出平台规模、请求量、延迟/吞吐/内存/成本等生产指标，也未说明质量估计与学习算法的具体实现；实验仅基于公开基准（数学推理与问答），其"用户节省可达 10%–71%"的结论有待在生产流量与真实工作量分布下进一步验证。

Abstract: Benchmarking and routing platforms increasingly act as intermediaries connecting large language model providers with end-users. However, providers on these platforms typically use a fixed price per token, preventing users from achieving the most competitive price for their tasks. % workloads. In this work, we design a procurement platform where token prices for each task are driven by provider competition, enabling users to secure competitive pricing for guaranteed quality levels. To this end, the platform sequentially routes queries via a reverse second-price auction that incentivizes model providers to truthfully bid their best estimate of the average cost to serve a user's query. As it routes queries, the platform learns the quality offered by each provider and progressively routes queries to the most cost-competitive provider among those meeting a desired quality threshold. To validate our design, we conduct experiments with multiple LLMs from the \texttt{Llama} and \texttt{Qwen} families on popular mathematical reasoning and question-answering benchmarks. The results show that the pricing margin of the most cost-competitive provider on our platform varies significantly---from $10\%$ to $71\%$---depending on the task and quality threshold. This suggests a substantial inefficiency in the current fixed-price market, and it demonstrates that our platform may enable users to capture maximum savings whenever competitive market conditions permit.

</details>


### [43] [Ask Which, Not How Good: Sizing Benchmarks Scored by an LLM](https://arxiv.org/abs/2609.27787)
*Atul Anand*

Main category: cs.AI

TL;DR: 作者Atul Anand（论文摘要未披露所属公司/机构与团队）。该研究针对工业生产LLM系统中"用LLM-judge选模型/评估迭代"的评估环节：现有做法依赖LLM评判器鉴别相差0.1分的模型差异，但从未测量过这些基准本身的分辨率（测量下限）。核心贡献是结构性地证明：单点式rubric打分下generalizability随样本量增加而归一化于σ²_s/(σ²_s+σ²_sj)这一天花板（受系统×judge交互项限制，与题目数n_i无关），而改用双向呈现的pairwise偏好可将σ²_sj压低两个数量级、ceiling升至0.986，但pairwise又引入位置偏差（先呈现者胜率高8.6个百分点）；实测0-5量表下测量下限为0.41-1.24分，而53篇已发表win-rate比较的中位改进仅0.28分、17个MT-Bench改进全部低于其自身下限。对628篇arXiv论文的审计显示不足四分之一声明评估是否重复运行。


<details>
  <summary>Details</summary>
Motivation: 现有LLM-as-judge基准被用来裁决0.1分的模型差异，但基准自身的分辨率（测量下限/可重复性）从未被量化，导致工业界可能在低于噪声水平的尺度上做决策；已有样本复杂度研究只覆盖accuracy类基准，judged类基准是空白。生产瓶颈在于：单judge下即使无限增加题目（items）也无法提升generalizability，因为系统×judge交互项的方差不随n_i衰减；这直接影响企业用评测集选模型、判断产品迭代是否有效时是否能得到统计上可靠的结论。

Method: 以被测量系统为对象，采用generalizability theory对373,019条LLM判断做方差分解，拆出system、item、judge及交互（如system×judge）分量。结构性结论：单judge下单点rubric的generalizability以σ²_s/(σ²_s+σ²_sj)为渐进上限，与题目数无关（系统×judge项不含n_i），故items饱和而judges不饱和；目标分辨率在接近ceiling时所需题目成本发散。对比方案为pairwise preference：在两种呈现顺序下重跑，σ²_sj较单点式下降两个数量级、低于σ²_s，ceiling升至0.986（11个系统上bootstrap区间[0.934,1.000]），单个judge即足够。同时量化pairwise位置偏差：同一系统先呈现比后呈现胜率高8.6个百分点。审计部分：对628篇arXiv论文由两个独立模型双重编码，并与盲态人工编码对照（kappa=0.73）。（上述为原文主张，方差分解结构与"items饱和而judges不饱和"为论文核心贡献。）

Result: 离线指标：单judge单点rubric时generalizability渐近上限为σ²_s/(σ²_s+σ²_sj)（受系统×judge交互项限制，与题目数无关）；pairwise双向呈现下σ²_sj下降两个数量级、ceiling=0.986，bootstrap 95%区间[0.934,1.000]（基于11个系统）。偏差/下限指标：pairwise位置偏差为8.6个百分点（先呈现者胜率更高），为53篇已发表win-rate比较中位改进（论文摘要披露为"median improvement"，比值1.23x）的1.23倍；0-5量表下测量下限（floors）为0.41-1.24分（at native item counts），而中位报告改进为0.28分；17个MT-Bench改进全部低于MT-Bench自身下限，70%的win-rate声明低于pairwise下限。审计指标：628篇论文中不足四分之一声明评估是否重复运行，仅46-67%报告任何形式的不确定性，双模型编码与盲态人工编码一致性kappa=0.73。在线A/B收益：论文摘要未披露。模型规模：论文摘要未披露。训练数据：373,019条判断（论文摘要未披露其来源与构成）。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟/吞吐/内存/成本：论文摘要未披露。

Conclusion: 该文解决了"LLM-judge基准本身分辨率未被测量"的问题，给出结构性结论：协议设计（单点rubric vs. pairwise）对可靠性的主导作用超过panel规模（judge数量）。可复用要点：(1)单judge+单点rubric存在与题目数无关的generalizability天花板，工业评估不应仅靠加题目来提升可靠性；(2)双向呈现的pairwise可大幅压低σ²_sj，但必须校正或规避位置偏差（8.6个百分点）；(3)评估改进声明需对照基准自身的测量下限，许多已发表的0.1分级别改进实际低于噪声。局限/证据缺口：作者所属公司/团队未披露，判断数据集来源与构成未披露，样本复杂度理论原论文未给出闭合解（pairwise因位置偏差引入新问题），benchmark数量有限（MT-Bench为唯一有足够重复的基准），未给出在线业务闭环证据。对工业LLM/搜索/推荐团队的直接价值在于评测协议设计与模型选型决策的统计可靠性。

Abstract: Benchmarks scored by an LLM judge routinely adjudicate differences of a tenth of a point, but the resolution of those benchmarks has never been measured. Existing sample-complexity work covers accuracy benchmarks and leaves the judged case open. Treating the system as the object of measurement, we decompose 373,019 judgments into system, item, judge and interaction components using generalizability theory. The central result is structural: under a single judge, generalizability asymptotes to sigma2_s/(sigma2_s+sigma2_sj) regardless of item count, because the system-by-judge term carries no n_i. Items saturate; judges do not. The item cost of a target diverges as the target nears that ceiling. The ceiling is a property of pointwise rubric scoring, not of LLM judging. Run as a pairwise preference in both presentation orders, sigma2_sj falls two orders of magnitude below sigma2_s and the ceiling rises to 0.986 (bootstrap [0.934, 1.000] on 11 systems), so one judge suffices. Pairwise buys a different problem: a system presented first wins 8.6 percentage points more often than the same system presented second, a bias 1.23x the median improvement claimed in the 53 published win-rate comparisons we recovered. Protocol design dominates panel size. Measured floors are 0.41-1.24 points on a 0-5 scale at native item counts, against a median reported improvement of 0.28 points; on the one benchmark recurring often enough for an exactly matched comparison, all 17 recovered MT-Bench improvements fall below MT-Bench's own floor, and 70% of the win-rate claims fall below the pairwise floor. An audit of 628 arXiv papers, double-coded by two independent models and validated against blind human coding (kappa=0.73), finds fewer than one paper in four states whether its evaluation was run more than once, and only 46-67% report uncertainty of any kind.

</details>


### [44] [State-Grounded Conditioning: Wrapping User-Facing LLM Agents Where Direction Depends on Live State](https://arxiv.org/abs/2609.27606)
*Qi Liu,Xiaoyang Yuan,Yubin Ruan,Zhuomeng Zhang,Wenjin Wang,Di Wu,Mingye Xu,Xinyi Mou,Xingxi Yin,Ke Feng,Zixun Sun*

Main category: cs.AI

TL;DR: 作者团队与公司论文摘要未披露；真实业务场景是游戏内对话式教练 agent，在连续竞技对局中引导玩家，管线阶段属于用户侧 LLM agent 的推理/conditioning 与状态感知（state grounding）环节。提出 State-Grounded Conditioning (SGC) 设计原则，将状态依赖控制外化为规则核（rule kernels）加三层 wrapper（Perception、Grounding、Interaction）。在 200 场会话（约 1,000 个 assistant turn）的匿名基准上，开启全部 wrapper 使 turn-level grounded accuracy 从 61.1%/69.8%（Prompting / PE-Agent）提升到 96.7%，session-level 从 20.0%/26.5% 提升到 83.5%；Perception wrapper 将平均首字延迟从生产 tool-use harness 中 PE-Agent 的 6.1s 降至 1.5s。


<details>
  <summary>Details</summary>
Motivation: 业务场景为面向用户的游戏内 LLM 对话教练，需在连续竞技对局中依据实时用户状态（游戏状态、会话历史、实时库存）给出正确方向。核心问题是作者定义的"direction drift"失败类别：模型输出在形式上完整（task-complete）但所选方向与当前状态错位。现有方案（直接 Prompting、PE-Agent）在 state grounding 上准确率不足，session-level grounded accuracy 仅 20.0%/26.5%，且 PE-Agent 在生产 tool-use harness 中平均首字延迟高达 6.1s，存在准确率与延迟的双重瓶颈。

Method: SGC 将状态依赖控制外部化为三个结构化输入切片（state slices）之上的规则核，并通过三个具有显式条件依赖的 wrapper 实现：Perception wrapper（负责状态感知，实验显示对延迟贡献显著——平均首字延迟 1.5s vs PE-Agent 6.1s）、Grounding wrapper（将模型输出与状态切片对齐以提升 grounded accuracy）、Interaction wrapper。输入-输出流程与各 wrapper 的训练目标、模型架构、底层 LLM 型号论文摘要未披露。核心创新是将"状态依赖的方向控制"从隐式提示指令中解耦，外化为可显式管理依赖的规则核与切片结构；作者还提出"状态切片近似正交性"的观察，但未声称能建立各 wrapper 的独立效果（论文摘要未披露单 wrapper 独立因果分析的实验设置）。

Result: 离线指标（200 场会话、约 1,000 assistant turn 的匿名基准，人类标注）：turn-level grounded accuracy——Prompting 61.1%、PE-Agent 69.8%、开启全部三个 wrapper 的 SGC 96.7%；session-level grounded accuracy——Prompting 20.0%、PE-Agent 26.5%、SGC 83.5%；session-level grounding 失败事件相对最强基线（PE-Agent）降低约 78%；累计消融显示随 wrapper 加入呈互补性增量收益。在线 A/B 收益论文摘要未披露。延迟：Perception wrapper 平均首字延迟 1.5s，对比生产 tool-use harness 中 PE-Agent 的 6.1s。模型规模、训练数据、训练硬件/时间、推理硬件、吞吐、内存、成本均论文摘要未披露。

Conclusion: 论文解决的核心问题是用户侧 LLM agent 在实时状态依赖下的"方向漂移"失效，以 wrapper + 规则核 + 状态切片的外部化设计将端到端 grounded accuracy 从约 70% 提升到 96.7%（turn-level），并将首字延迟从 6.1s 降至 1.5s，对实时交互的场景（游戏教练、带会话/库存状态的客服与推荐对话）有直接借鉴价值。局限与证据缺口：模型具体架构与底层 LLM 未披露；仅 200 场会话、约 1,000 turn 的基准规模较小且为单一游戏领域；无在线 A/B 数据；作者明确说明实验确立了"状态切片近似正交性"但未建立各 wrapper 的独立因果贡献；延迟对比仅覆盖 Perception wrapper 与 PE-Agent 基准。工业团队可复用的核心思路是把状态依赖的方向控制外化为结构化状态切片 + 规则核，并以显式依赖的 wrapper 分层，兼顾可控性与首字延迟。

Abstract: We introduce State-Grounded Conditioning (SGC), a design principle for user-facing LLM agents that must condition on live user state (game state, session history, live inventory), and a distinct failure class we call direction drift: task-complete responses whose chosen direction misaligns with the current state. SGC externalises state-dependent control into rule kernels over structured inputs and three primary state slices, via Perception, Grounding, and Interaction wrappers with explicit conditioning dependencies. We evaluate SGC on a 200-session anonymised benchmark ($\approx$1,000 assistant model turns) from an in-game conversational coaching agent that guides players through consecutive competitive matches, reporting mean first-token latency and five human-annotated dialogue-quality metrics that jointly cover factual grounding and coach-like guidance progression. The Perception wrapper holds mean first-token latency at 1.5s (vs. 6.1s for PE-Agent inside a production tool-use harness); enabling all three wrappers lifts turn-level grounded accuracy from 61.1%/69.8% (Prompting / PE-Agent) to 96.7% and session-level grounded accuracy from 20.0%/26.5% to 83.5%; session-level grounding-failure incidents drop by $\approx$78% relative to the strongest baseline. A cumulative ablation shows complementary incremental gains as the wrappers are added. These results inform approximate state-slice orthogonality, without establishing independent per-wrapper effects.

</details>


### [45] [CART: Closed-Loop Adaptive Red Teaming for Large Language Models](https://arxiv.org/abs/2609.27336)
*Dongdong Zhang,Tengchao Lv,Yilin Jia,Yuzhong Zhao,Yupan Huang,Wenshan Wu,Xiangyang Zhou,Shaohan Huang,Nan Yang,Li Dong,Lei Cui,Furu Wei*

Main category: cs.AI

TL;DR: 本文由微软团队提出（论文摘要未披露具体部门/业务产品，但作者含微软 Li Dong、Furu Wei 等），业务场景为生产 LLM 与受限工具使用 Agent 的安全红队测试；所处环节属于"评估/安全测试"阶段。核心系统 CART（Closed-Loop Adaptive Red Teaming）将固定提示词回放升级为闭环自适应测试：以每次测试结果指导下一步测试，起始覆盖广泛风险、追踪暴露出的弱点、保持新探针多样性，并记录每个发现的证据与来源。整体结果：在三类评估基准（Frontier、JAH、Agentic）上，CART 相比静态种子回放发现更多失败且平均风险更高，对每个有可用基线的 Target 均如此；在工具介导的 Agent 测试中增益同样成立。


<details>
  <summary>Details</summary>
Motivation: 现有自动化红队测试通常反复回放固定提示词集合，只能度量已知风险，无法从测试过程中新发现的失败中学习，导致安全评估停留在"一次性清单"状态，难以覆盖语言模型及工具使用 Agent 在实际部署中可能被逐步攻破的薄弱点。CART 要解决的核心问题是：让测试策略具备闭环学习能力，把红队测试从静态重放转变为连续的、自适应的、可审计的弱点搜索。所涉及的模型规模、训练/推理资源等业务约束论文摘要未披露。

Method: CART 框架采用角色分离设计：Challenger 负责生成测试探针，Target 是被测模型（可为纯文本模型或受限工具使用 Agent），Judge 负责评估测试结果。闭环流程为：从广泛风险覆盖起步，依据每次评测结果动态引导后续测试方向，追踪新兴弱点，同时维持新探针多样性，并为每项发现记录证据与来源。核心创新在于：1）闭环自适应——测试内容由上一次结果驱动；2）三角色解耦，使 Challenger、Judge 等组件可独立研究；3）证据可审计。作者声称该设计相比静态种子回放能系统性发现更多失败与更高风险弱点（此为本解读对方法目标的概括，非原文逐字引用）。具体模型结构、训练目标、输入输出格式、参数规模等论文摘要未披露。

Result: 离线指标：在 Frontier、JAH、Agentic 三类评估数据集上，CART 发现更多失败且平均风险更高，且对所有具有可用基线的 Target 均优于静态种子回放；在工具介导的 Agent 测试中增益依旧成立。论文明确指出这些结果描述的是"测试策略所发现的内容"，而非失败在真实部署中出现的频率（即无真实线上频率估计）。在线 A/B 收益：论文摘要未披露。模型规模、训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本：论文摘要均未披露。

Conclusion: CART 将 LLM 红队测试从一次性静态清单升级为连续、自适应、可审计的弱点搜索，解决了固定提示词重放"只见已知风险、无法从失败中学习"的问题，且通过 Challenger/Target/Judge 角色分离支持独立审计与组件级研究，对生产环境 LLM 及工具使用 Agent 的安全评估具有直接复用价值。主要局限与证据缺口：摘要明确指出测试结果是聚合层面的发现量，不能用于推断真实部署中的实际失败频率；未给出具体竞争基线名称之外的定量提升幅度、也未披露任何模型/资源规模信息，且缺乏真实业务在线的对照验证。工业 LLM 团队可借鉴其闭环自适应探针生成与角色解耦、证据留痕的设计，将其嵌入安全评估流水线，取代纯静态红队清单。

Abstract: Automated red teaming often replays a fixed set of prompts, which measures known risks but cannot learn from failures found during testing. We present CART (Closed-Loop Adaptive Red Teaming), a framework that uses each result to guide what it tests next. CART begins with broad risk coverage, follows weaknesses that emerge, keeps new probes diverse, and records the evidence and source of every finding. It separates the Challenger that creates tests, the Target being tested, which may be a text-only model or a bounded tool-using agent, and the Judge that evaluates the results, allowing these roles to be studied independently. Across three evaluation families (Frontier, JAH, and Agentic), CART discovers more failures and higher average risk than static seed replay for every Target with an available baseline. The gains extend to tool-mediated agent tests, suggesting that contextual adaptation can reveal weaknesses that direct prompt replay does not exercise. These results describe what the test policies discover, not how often failures occur in real deployments. We also find that Challenger-Judge choices affect the evidence uncovered, highlighting the need for role separation and independent review. Overall, CART turns red teaming from a one-time checklist into a continuous, adaptive, and auditable search for model and agent weaknesses.

</details>


### [46] [Verifiable Hidden Dynamics Play: Generating Agentic RL Environments from Solved Mechanisms](https://arxiv.org/abs/2609.27321)
*Xinjie Shen,Wei Fan,Xudong Guo,Jianhong Tu,Yang Su,Chuqiao Kuang,Yinger Zhang,Dayiheng Liu*

Main category: cs.AI

TL;DR: 本文作者来自阿里巴巴达摩院（团队专注于 LLM 训练，论文摘要未披露具体部门名）；业务场景是面向长时程、状态演化、决策相互依赖且结果延迟的智能体任务，需要为工业级 LLM 智能体训练生成多样化、低成本、信号可靠的训练环境。论文对应训练数据/环境生成与训练阶段。所提系统 VHD-Play 通过“先采样并求解数学模型，再由语料grounded的setter渲染为有状态工具”的方式，使可执行动态与轨迹评分参考继承自同一已求解模型，生成 3,300 个多样化智能体环境，单个环境成本仅几分钱。训练 Qwen3.6-35B-A3B 后，五个家族诊断中的平均智能体得分从 0.204 提升至 0.815；在外部基准上（通用函数调用、旅行规划、365 天电商）亦有增益，其中 E-Commerce Bench 上训练后的 checkpoint 全程不破产并超过 Qwen3.7-Max。


<details>
  <summary>Details</summary>
Motivation: 业务痛点：语言模型智能体面临长时程任务中的演化状态、相互依赖的决策与延迟结果，扩展其训练需要三类条件——多样化的智能体环境、可信的结果信号、低扩展成本。现有生成流水线的局限在于“先构建环境、后定义结果规则或标注轨迹”，导致动态机制与评估目标需要事后对齐（post hoc），难以保证结果信号的一致性与低成本扩展。论文通过反转这一依赖（先求解数学模型、再渲染环境）来同时满足多样性、信号可信性与低成本三大需求，直接服务于生产级 LLM 智能体训练数据供给。

Method: 核心思想：VHD-Play 反转传统“先环境后规则”的生成顺序，改为“先采样并求解一个数学模型，再由语料grounded的 setter 将该决策过程渲染为有状态工具”，使可执行动态与轨迹评分参考共同继承自同一个已求解模型，保证动态与评测天然对齐。输入输出流：数学模型的求解结果既提供可执行的状态转移动态，也提供轨迹评分参考。关键模块：语料grounded setter（负责渲染）、可执行动态、轨迹评分参考。创新点（作者主张）：将动态与评估的生成统一到同一已求解机制上；并设计了对比实验区分“书面化问题（written-out problems）”与“有状态版本（stateful versions，显式/隐藏参数）”，用于归因学习增益主要来自有状态交互而非底层问题求解。追加实验：冻结的 35B setter 可生成更大环境，规模匹配的训练在机制规模与时程增长下仍保持增益。

Result: 离线指标：3,300 个多样化智能体环境，单个环境成本约“几分钱（a few cents）”；训练 Qwen3.6-35B-A3B 后，在“五家族诊断（five-family diagnostic）”中平均智能体得分从 0.204 提升至 0.815；训练后增益出现在全部三个训练家族的 held-out 实例与八个未见机制家族上；外部基准/离线评测中，在通用函数调用、旅行规划、365 天电商上均有增益，其中 E-Commerce Bench 上训练后的 checkpoint“完成每次运行而不破产，且超过 Qwen3.7-Max”。消融/归因：书面化问题与有状态版本的对比显示，大部分可学习差距来自有状态交互而非底层问题求解。规模实验：冻结 35B setter 实现更大环境；规模匹配训练随机制规模与时程增长仍保持增益。
在线 A/B 收益：论文摘要未披露。
模型规模：主训练模型 Qwen3.6-35B-A3B；冻结 setter 为 35B；基线与外部对比模型包括 Qwen3.7-Max（未披露参数量）。
训练数据：3,300 个生成环境，分为三个训练家族，机制家族共约 11 个（3 训练 + 8 未见）；具体 token 量论文摘要未披露。
训练硬件/时间：论文摘要未披露。
推理硬件：论文摘要未披露。
延迟/吞吐/内存/成本：单个环境的生成成本约“几分钱”有披露；训练与推理的延迟、吞吐、内存及总体成本论文摘要未披露。

Conclusion: 本文解决的核心问题是为工业级长时程智能体训练提供“动态与评测天然对齐、低成本、可扩展”的环境生成方案，并通过 Qwen 系列模型训练与电商/函数调用/旅行规划等外部基准验证其可迁移性。实践价值：为需要大规模多样训练环境的生产 LLM 智能体团队提供了一种低扩展成本的管线，且证明学习增益主要来自有状态交互，提示训练应侧重状态交互而非纯符号推理。局限与证据缺口：论文摘要未披露在线 A/B 测试收益、训练与推理的资源/延迟/吞吐、训练数据 token 量，以及在真实生产业务（如搜索、广告、推荐、实际电商在线的函数调用）中的端到端效果，“超过 Qwen3.7-Max”为离线基准比较，不能等同于线上业务指标。可复用点：工业智能体/LLM 团队可借鉴“先求解机制、再渲染环境、动态与评分同源”的生成范式来降低训练环境扩展成本，并采用“书面化 vs 有状态”对比实验来定位训练增益的真正来源。

Abstract: Language-model agents increasingly face long-horizon tasks with evolving state, interdependent decisions, and delayed outcomes. Scaling their training requires diverse agentic environments, dependable outcome signals, and low extension cost. Existing generation pipelines commonly construct an environment before defining its outcome rule or annotating its trajectories, leaving dynamics and evaluation to be aligned post hoc. VHD-Play reverses this dependency by sampling and solving a mathematical model before a corpus-grounded setter renders its decision process as stateful tools. The executable dynamics and trajectory-scoring reference are inherited from the same solved model. The pipeline produces 3,300 diverse agentic environments at a cost of a few cents each. Training Qwen3.6-35B-A3B on three families raises its mean agentic score from 0.204 to 0.815 in a five-family diagnostic. Gains also appear on held-out instances from all three training families and eight unseen mechanism families, then extend beyond the generated substrate to external benchmarks for general function calling, travel planning, and 365-day e-commerce. On E-Commerce Bench, the trained checkpoint completes every run without bankruptcy and exceeds Qwen3.7-Max. We compare written-out problems with stateful versions that reveal or hide their parameters. The comparison shows that most of the learnable gap lies in stateful interaction rather than underlying problem solving. A frozen 35B setter realizes larger environments, and scale-matched training retains gains as mechanism size and horizon grow, indicating the potential for an evolving training substrate.

</details>


### [47] [Hunyuan-A13B Technical Report](https://arxiv.org/abs/2609.27284)
*Tencent Hunyuan Team,Ao Liu,Botong Zhou,Can Xu,Chayse Zhou,ChenChen Zhang,Chengcheng Xu,Chenhao Wang,Decheng Wu,Dengpeng Wu,Dian Jiao,Dong Du,Dong Wang,Feng Zhang,Fengzong Lian,Guanghui Xu,Guanwei Zhang,Hai Wang,Haipeng Luo,Han Hu,Huilin Xu,Jiajia Wu,Jianchen Zhu,Jianfeng Yan,Jiaqi Zhu,Jihong Zhang,Jinbao Xue,Jun Xia,Junqiang Zheng,Kai Liu,Kai Zhang,Kai Zheng,Kejiao Li,Keyao Wang,Lan Jiang,Lixin Liu,Lulu Wu,Mengyuan Huang,Peijie Yu,Peiqi Wang,Qian Wang,Qianbiao Xiang,Qibin Liu,Qingfeng Sun,Richard Guo,Ruobing Xie,Saiyong Yang,Shaohua Chen,Shihui Hu,Shuai Li,Shuaipeng Li,Shuang Chen,Suncong Zheng,Tao Yang,Tian Zhang,Tinghao Yu,Weidong Han,Weijie Liu,Weijin Zhou,Weikang Wang,Wesleye Chen,Xiao Feng,Xiaoqin Ren,Xingwu Sun,Xiong Kuang,Xuemeng Huang,Xun Cao,Yanfeng Chen,Yang Du,Zhen Yang,Yangyu Tao,Yaping Deng,Yi Shen,Yigeng Hong,Yiqi Chen*

Main category: cs.AI

TL;DR: 腾讯混元团队（Tencent Hunyuan Team）发布开源的Hunyuan-A13B大语言模型技术报告，面向大语言模型训练与推理部署场景。模型采用Mixture-of-Experts架构，总参数80B、推理时仅激活13B，在20T-token严格过滤语料（含增强STEM数据治理）上预训练，并通过高质量监督微调与大规模强化学习优化；引入双模式思维链（CoT）框架——快思考处理常规查询、慢思考处理复杂多步问题。评估显示其在数学、科学、编程、通用语言理解及智能体任务上接近更大模型，且推理吞吐高、适合延迟敏感应用。论文摘要未披露具体的在线A/B测试收益；具体业务产品与具体部署流水线阶段论文摘要未披露。


<details>
  <summary>Details</summary>
Motivation: 论文摘要披露的核心诉求是在"模型能力、计算效率与部署成本"之间取得平衡：全参数大模型能力虽强，但推理计算与部署开销高，难以满足延迟敏感型应用。Hunyuan-A13B通过MoE架构在总参数80B的前提下仅激活13B，以降低推理成本并提升吞吐。同时，预训练语料经过严格过滤并强化STEM数据治理，以提升事实可靠性与推理能力，解决大模型在专业领域的知识可靠性瓶颈。论文摘要未披露具体业务场景、具体的延迟敏感产品形态，以及面向哪个具体生产流水线环节（如何种在线服务）。

Method: Hunyuan-A13B采用Mixture-of-Experts（混合专家）架构，总参数量80B，推理时仅激活13B参数。训练流程包括：（1）在20T-token的严格过滤语料上进行预训练，并对STEM（科学、技术、工程、数学）数据进行增强治理；（2）高质量监督微调（SFT）；（3）大规模强化学习（RL）。其核心创新之一是双模式思维链（dual-mode Chain-of-Thought）框架，根据任务复杂度自适应调整推理深度：快思考（fast thinking）用于常规查询、慢思考（slow thinking）用于复杂多步问题。输入输出流、具体MoE专家数量与路由策略、RL的具体奖励设计等细节论文摘要未披露。

Result: 论文摘要未披露具体离线评测数值、基线模型名称及对比的实验设定，仅表述为"在数学、科学、编程、通用语言理解及智能体任务上性能具有竞争力，常接近远超其规模的大模型"。论文摘要未披露任何在线A/B测试收益。模型规模：总参数80B，推理时激活13B；预训练语料为20T-token。训练数据规模、训练硬件、训练时间、推理硬件、延迟、吞吐量、显存占用、成本等具体数值论文摘要均未披露；仅定性表述"高推理吞吐""适合延迟敏感型应用"。

Conclusion: Hunyuan-A13B针对的是大模型能力与推理成本/延迟的矛盾，通过MoE稀疏激活（80B总参数/13B激活）、严格语料治理、STEM数据增强、SFT+大规模RL以及双模式CoT，在保持较高能力的同时降低部署门槛，适合对延迟敏感的工业应用。可复用的思路包括：稀疏MoE降低推理成本与提升吞吐、按任务复杂度切换推理深度的双模式CoT框架、面向事实可靠性的STEM语料治理。局限与证据缺口明显：论文摘要未给出任何具体评测分数、未提供与工业基线的逐项对比、未披露吞吐/延迟/显存/成本等可量化的推理指标，也未提供在线实验或实际产品中的收益证据，因此其"适合延迟敏感应用"的结论尚缺可验证的量化支撑。工业LLM团队可借鉴其架构与训练范式，但需自行验证在自身业务场景下的能力与吞吐权衡。

Abstract: We present Hunyuan-A13B, an open-source large language model based on a Mixture-of-Experts architecture. It contains 80 billion total parameters but activates only 13 billion during inference, balancing model capability, computational efficiency, and deployment cost. The model is pretrained on a rigorously filtered 20T-token corpus with enhanced STEM data curation, improving factual reliability and reasoning ability. High-quality supervised fine-tuning and large-scale reinforcement learning further enhance its overall performance. Hunyuan-A13B also introduces a dual-mode Chain-of-Thought framework that adapts reasoning depth to task complexity: fast thinking for routine queries and slow thinking for complex, multi-step problems. Evaluations show competitive performance across mathematics, science, programming, general language understanding, and agent tasks, often approaching that of much larger models. Its high inference throughput makes it suitable for latency-sensitive applications. We release Hunyuan-A13B to support open research and practical LLM deployment.

</details>


### [48] [EnSIMem: Entity-Structured Indexing for Long-Term Agent Memory](https://arxiv.org/abs/2609.27279)
*Xuanyu Meng,Xing Fan,Xinyi Fan,Chenlei Guo,Yixuan Xie,Jiawei Han*

Main category: cs.AI

TL;DR: 论文摘要未披露具体公司/团队信息（作者含Xuanyu Meng、Xing Fan、Xinyi Fan、Chenlei Guo、Jiawei Han等）。业务场景为长期与用户交互的智能体需要从持续增长的历史中回忆事实、偏好、事件与变化；所处流水线阶段为长期记忆检索与证据抽取（offline索引构建 + online证据检索 + 响应生成）。系统EnSIMem采用实体结构化索引（[entity][entity type][property:value]）配合episode级溯源，实现point/temporal/compositional/aggregation四类推理的证据收集；核心效果为在长期智能体记忆基准上取得高答题准确率，同时保持紧凑上下文和良好在线效率（具体数值摘要未披露）。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈：长期交互的智能体需持续积累并回忆海量历史，现有记忆系统常将交互压缩为通用摘要或检索匿名文本块，导致难以准确定位正确的实体、属性和支撑证据（entity/property/supporting evidence识别困难）。抽象中未给出具体的用户规模、交互条数、上下文长度或延迟等规模约束，也未披露线上指标或业务收益。该问题对业务的意义在于：长期对话产品需要agent在持续增长的记忆中回溯事实、偏好、事件及其变更。

Method: 系统分为两阶段。离线构建阶段：将交互组织为theme-coherent episodes（主题连贯的episode），并构建以对话为grounding的索引条目，形式为[entity][entity type][property:value]，每条记录保留其source turns（来源轮次）、temporal information（时间信息）及可用的multimodal fields（多模态字段）。在线交互阶段：将用户请求分解为evidence requirements（证据需求），其property与记忆索引对齐；随后通过entity-property lookup（实体-属性查找）与adaptive retrieval（自适应检索）收集point、temporal、compositional、aggregation四类推理所需的证据；最终agent基于保留的source evidence生成响应，而非基于有损的记忆摘要。核心创新点：1) 实体+实体类型+属性:值的三级结构化索引；2) episode级provenance（溯源）机制，保留原始证据并携带时间与多模态信息；3) 证据需求驱动的自适应检索，支持四类推理。（以上为摘要作者声明；具体模型架构、训练目标、索引存储结构、检索算法细节论文摘要未披露。）

Result: 离线/在线效果：论文摘要仅陈述"achieves high answer accuracy while maintaining compact contexts and favorable online efficiency"（高答题准确率、紧凑上下文、良好在线效率），未披露具体指标、基线、数据集名称或消融数据，无法区分离线指标与在线A/B收益。模型规模、训练数据、训练硬件/时间、推理硬件、延迟、吞吐、内存、成本等全部论文摘要未披露。

Conclusion: 解决的问题：长期智能体记忆的实体/属性/证据定位困难，通过实体结构化索引与episode级溯源，让agent从原始证据而非有损摘要生成响应，兼顾准确率、上下文紧凑性与在线效率。实践价值：为工业对话/助手产品的长期记忆检索与证据溯源提供了可复用的架构范式（实体-属性索引 + 证据需求分解 + episode provenance）。局限与证据缺口：摘要未披露具体benchmark名称与数值、未披露线上收益、模型与资源规模、延迟/吞吐等成本数据，无法独立验证"compact contexts"和"online efficiency"的量化水平；也未说明多模态字段如何参与以及针对大规模历史时的可扩展性。可复用经验：长期记忆系统应保留source turns与时间信息、按实体-属性组织索引并以证据需求驱动检索，使用原始证据而非有损摘要做最终生成。

Abstract: An agent that interacts with users over long periods must recall facts, preferences, events, and changes from a continuously growing interaction history. Existing memory systems often compress interactions into generic summaries or retrieve anonymous text chunks, making it difficult for an agent to identify the correct entity, property, and supporting evidence. We present EnSIMem, an entity-structured long-term memory architecture for an agent. During offline construction, the system organizes interactions into theme-coherent episodes and builds dialogue-grounded index entries of the form [entity][entity type][property:value]. Each entry preserves its source turns, temporal information, and available multimodal fields. During online interaction, the agent's request is decomposed into evidence requirements whose properties are aligned with the memory index. Entity-property lookup and adaptive retrieval then collect the evidence needed for point, temporal, compositional, and aggregation reasoning. The agent generates its response from the preserved source evidence rather than from lossy memory summaries. On long-term agent-memory benchmarks, EnSIMem achieves high answer accuracy while maintaining compact contexts and favorable online efficiency. These results show that entity-structured indexing and episode-level provenance provide a reliable foundation for long-term memory in agents. The code of our model is available at this https URL .

</details>


### [49] [Enhancing Small Language Models for Power Outage Report Generation via Minimum Risk Training](https://arxiv.org/abs/2609.27197)
*Hung Phan,Waqwoya Abebe,Youssef Hussein,Supriya Chinthavali,Dalton Lunga,Ali Jannesari*

Main category: cs.AI

TL;DR: 论文来自橡树岭国家实验室（多名作者隶属 ORNL，论文摘要未逐字披露机构名但可从 ODIN/CIM 背景与作者信息判断）及其合作团队，面向美国全国停电数据倡议 ODIN 的停电报告标准化生产流程，属于工业 LLM 的训练/适配阶段任务。系统采用最小风险训练（MRT）对 Qwen2.5-7B-Instruct 进行序列级优化，使其能将异质停电报告自动转换为符合 CIM IEC 61968-3 标准的 XML。核心结果：整体准确率从 16.20% 提升至 68.95%。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈在于 ODIN 需要将来自不同来源、格式异质的停电报告统一转换为符合 CIM IEC 61968-3 标准的 XML，人工或规则化转换成本高且难以规模化。现有方案局限：传统 token 级最大似然（MLE）训练目标与最终评价指标不一致，导致模型在结构化生成任务上准确率极低——基线 Qwen2.5-7B-Instruct 整体准确率仅 16.20%，说明仅靠通用指令模型无法胜任该领域任务。规模约束与业务价值：摘要强调"异构报告"到"标准化 XML"的转换是电力行业数据互操作的核心，准确率提升直接关系停电数据的可用性与下游分析质量。论文摘要未披露具体的报告体量、转换吞吐或人工成本数据。

Method: 核心方法为最小风险训练（MRT）：不再仅依赖 token 级的最大似然目标，而是直接以序列级评价指标（准确率等结构化生成指标）为优化目标，通过对采样候选序列的风险期望进行梯度优化。输入输出流：输入为异质停电报告（文本），输出为符合 CIM IEC 61968-3 的标准化 XML。模型采用 Qwen2.5-7B-Instruct 作为基座进行领域适配与 MRT 优化。作者的核心主张是：虽 MRT 十年前即在神经机器翻译中提出，但最近研究显示其对现代语言模型的序列级结构化生成同样有效；本文将该技术迁移到电力领域结构化生成任务。创新点在于把 MRT 的序列级奖励优化应用于特定领域的结构化/标准化生成场景，并验证其在真实生产格式（CIM/XML）上的有效性。论文摘要未披露 MRT 的具体采样策略、候选数量、风险函数形式、训练目标细节及训练配置。

Result: 离线指标：MRT 优化后 Qwen2.5-7B-Instruct 的整体准确率从 16.20% 提升至 68.95%（基线为优化前的 Qwen2.5-7B-Instruct；该指标为结构化生成的整体准确率，属于离线评测）。论文摘要未披露任何在线 A/B 实验或线上效果。模型规模：7B 参数（Qwen2.5-7B-Instruct）。训练数据规模：论文摘要未披露。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟/吞吐/内存/成本：论文摘要未披露。

Conclusion: 论文解决的问题：将通用 7B 指令模型通过 MRT 序列级优化适配到电力停电报告标准化生成，使整体准确率大幅提升（16.20%→68.95%），证明针对领域结构化生成任务，token 级 MLE 与目标评价指标错配是准确率低下的主要瓶颈。实用价值：为工业界"异质文本→标准化结构化格式"类任务提供了现成的优化范式，MRT 可直接迁移到其他领域的数据标准化、结构化抽取、代码/协议合规生成等场景。局限与证据缺口：摘要未披露训练数据规模、训练/推理资源、采样与风险函数细节，无法评估训练成本与可复现性；仅有单一离线准确率指标，缺少对整体成功率细分字段准确率、格式合规率及在线部署效果的报告。可复用经验：工业 LLM 团队在做领域结构化生成适配时，应优先考虑以最终评价指标（而非 token 似然）直接指导训练目标，MRT 是低成本、高收益的候选方案。

Abstract: Minimum Risk Training (MRT) enables neural machine translation models to directly optimize sequence-level evaluation metrics instead of relying only on token- level maximum-likelihood objectives Shen et al. [2016]. Although introduced a decade ago, recent work shows renewed potential for risk-based optimization in modern language models Yang et al. [2024], Jinnai et al. [2025]. We apply MRT to power outage report generation for the Outage Data Initiative Nationwide (ODIN), transforming heterogeneous reports into standardized XML compliant with CIM IEC 61968-3. Our MRT approach improves Qwen2.5-7B-Instruct overall accuracy from 16.20% to 68.95%, demonstrating the effectiveness of sequence- level optimization for domain-specific structured generation

</details>


### [50] [Policy-as-Skill: Governed LLM Decision Support with Evidence, Deterministic Control, and Audit](https://arxiv.org/abs/2609.27087)
*Kabeh Mohsenzadegan,Vahid Tavakkoli,Kyandoghere Kyamakya*

Main category: cs.AI

TL;DR: 论文摘要未披露具体公司/机构团队（作者 Kabeh Mohsenzadegan 等三人，机构未在摘要中说明）；业务场景是组织将 LLM 用于政策、合规、风险与运营决策支持；所处阶段为生产决策支持系统的治理运行时/评测（论文摘要未披露检索-召回-排序等搜索链路角色）；提出 Policy-as-Skill（PaS）模块化运行时，把证据验证、审查路由、版本控制、审计等功能打包为可执行、带版本的“政策技能”；在固定 Gemma4 后端、600 个开发任务上，PaS+Audit 取得 exact accuracy 53.8%、macro-F1 0.346、review F1 0.854、citation precision 1.000、policy-reference recall 0.984、audit completeness 1.000，在多数治理与审查指标上优于 LLM+RAG；叠加确定性控制后聚合准确率提升至 61.2%，但效果强烈依赖任务，支持选择性而非普遍性的规则介入。


<details>
  <summary>Details</summary>
Motivation: 生产瓶颈：组织在政策、合规、风险与运营决策中越来越多地使用 LLM，需要证据验证（evidence validation）、审查路由（review routing）、版本控制（version control）与可审计性（auditability），而这些治理能力通常散落或缺位，导致 LLM 决策输出难以验证、追踪与合规审查。现有方案的局限：摘要以 LLM+RAG 作为对照基线，暗示朴素 RAG 在治理与审查类指标上表现不足（PaS+Audit 在多数治理与审查指标上 outperform LLM+RAG）。规模约束：600 个开发任务、固定 Gemma4 后端，属于开发规模的治理评测。业务重要性：合规与风险管理场景对可解释、可追溯、可审计的要求强于普通问答，缺少确定性控制与审计闭环会直接阻碍 LLM 在受监管业务中的落地。

Method: 系统架构：Policy-as-Skill（PaS）是一个模块化运行时，将政策能力封装为“可执行、带版本的 policy skills”，内部包含证据验证、审查路由、版本控制、审计等模块；在此基础上 PaS+Audit 叠加审计模块，进一步有“Deterministic Control（确定性控制）”作为可选择叠加的规则层。输入-输出流：摘要未逐项披露输入输出细节，仅可确认系统接收政策/合规/风险/运营类任务，输出决策并附带证据引用（citation）、政策引用（policy-reference）与审计记录。核心思想：把治理与合规要求（验证、路由、版本、审计）以可执行技能的形式与 LLM 决策运行时统一起来，使决策过程具备证据支撑、确定性控制与审计追踪。训练目标/模型：固定 Gemma4 后端，共评估 13 种方法，摘要未披露是否训练、微调或仅工程编排。关键创新点（作者主张）：将治理功能打包为版本化可执行技能；确定性控制作为可选层提升准确性但建议选择性介入。作者主张与解读的区分：摘要明确主张的是模块化、版本化、可审计与确定性控制的架构设计及其在治理/审查指标上对 RAG 的优势；“audit completeness 1.000、citation precision 1.000”等强指标是作者报告的结果，未披露评测协议细节，需谨慎对待。

Result: 离线指标（均未披露为在线 A/B）：PaS+Audit 在 600 个开发任务、固定 Gemma4 后端上取得——exact accuracy 53.8%、macro-F1 0.346、review F1 0.854、citation precision 1.000、policy-reference recall 0.984、audit completeness 1.000；在多数 governance 与 review 指标上优于 LLM+RAG。叠加确定性控制后，聚合准确率由 53.8% 提升至 61.2%，但效果强烈依赖任务（strongly task dependent），作者据此主张选择性而非普遍性规则介入。在线 A/B 增益：论文摘要未披露。模型规模：固定 Gemma4 后端，但未披露具体参数量或版本口径。训练数据：论文摘要未披露（600 为开发/评测任务数，非训练数据规模）。训练硬件/时间：论文摘要未披露。推理硬件：论文摘要未披露。延迟/吞吐/内存/成本：论文摘要未披露。

Conclusion: 解决的问题：为使用 LLM 做政策/合规/风险/运营决策的组织提供一套带证据验证、审查路由、版本控制与审计的可执行治理运行时，并在治理与审查类指标上证明其相对朴素 LLM+RAG 的优势。实用价值：对需要合规、可追溯、可审计的工业 LLM 决策系统（如金融合规、风控、政策问答、运营自动化）有直接借鉴意义，尤其是把审计与引用作为一等公民、以技能化/版本化管理政策逻辑、用确定性规则做“选择性兜底”而非全量替换的思路。局限与证据缺口：（1）未披露公司与业务落地证据，属方法/系统论文；（2）600 个开发任务为离线评测，无在线 A/B 或生产指标；（3）Gemma4 未披露参数量与版本；（4）citation precision 与 audit completeness 达到 1.000 等指标缺少评测协议细节，真实性需看原文复核；（5）macro-F1 仅 0.346、exact accuracy 53.8% 说明任务本身难度大、精度仍有较大提升空间；（6）确定性控制的“任务依赖性”结论缺少数值分解支撑。可迁移点：将治理（证据、审查、版本、审计）解耦为模块化技能并与 LLM 运行时集成的架构；用 audit completeness 与 citation precision 作为 LLM 决策系统上线前的评测门禁；以及“选择性确定性控制”的介入策略，对企业的搜索/推荐/LLM 产品化团队在合规场景下均可复用。

Abstract: Organizations increasingly use LLMs for policy, compliance, risk, and operational decision support, requiring evidence validation, review routing, version control, and auditability. We introduce Policy-as-Skill (PaS), a modular runtime that packages these functions as executable, versioned policy capabilities. Thirteen methods are evaluated with a fixed Gemma4 backend on 600 development tasks. PaS+Audit achieves 53.8% exact accuracy, macro-F1 0.346, review F1 0.854, citation precision 1.000, policy-reference recall 0.984, and audit completeness 1.000, outperforming LLM+RAG on most governance and review metrics. Deterministic control raises aggregate accuracy to 61.2% but is strongly task dependent, supporting selective rather than universal rule-based intervention.

</details>


### [51] [Propose, Don't Judge: An Anytime-Valid Referee for LLM Agents That Mine Investment Factors](https://arxiv.org/abs/2609.27051)
*Bo Qu,Mingguang Chen,Licheng Wang*

Main category: cs.AI

TL;DR: 论文摘要未披露具体工业公司；作者 Bo Qu、Mingguang Chen、Licheng Wang 从事的是量化金融因子挖掘研究，而非搜索/广告/推荐或工业 LLM 产品。核心工作是用一个"冻结的、不可被 agent 触碰"的统计裁判（通过 betting 实现 anytime-valid 的假发现控制）对 LLM agent 提出的投资因子进行筛选，在合成世界、probe-authoring 环境和 CSI 500 十年 walk-forward 上比较三类提案者（脚本、bandit、语言模型）与四类裁判（一个冻结裁判加三个故意泄漏的裁判）。该研究属于金融统计与量化投资方法，且未提供工业搜索/广告/推荐或生产 LLM 部署的任何证据，不符合本系统的工业相关性范围。


<details>
  <summary>Details</summary>
Motivation: 论文要解决的是量化因子挖掘中"谁来判断因子真假"的治理问题：LLM agent 可以提因子、回测、筛选并退役因子，但当提案与判断都由 agent 掌控时，会产生过度挖掘与假发现（false admission）——论文用"let leaky referees"来对比说明自身裁判若参与数据会让低于阈值的因子被错误准入。这与搜索/广告/推荐的召回、排序、CTR/CVR、竞价、广告分配以及工业 LLM 的训练/推理/serving/产品化瓶颈均无直接对应。论文摘要未披露任何工业业务场景、生产数据规模或线上实验约束。

Method: 方法核心是"governed self-evolution"：agent 只负责提案（及撰写自己的诊断探针 diagnostic probes），判断权和统计保障由一个冻结的、agent 无法修改的裁判承担。该裁判对每个候选因子仅基于提交之后揭示的市场结果进行打分，通过 betting（赌博/下注式在线检验）构建，使其 false-discovery 保证在任意停止时间、对任意提案策略都成立（anytime-valid）。实验将三个提案者（script、bandit、language model）与一个冻结裁判及三个被故意泄漏数据的裁判交叉组合，在合成世界（含植入真相）、probe-authoring 环境和 CSI 500 十年 walk-forward 三个场景中评估，并以"被准入的真因子数量、等待时间、certified portfolio 的 Sharpe"等度量结果。以上均为作者在论文自述中的方法框架，论文摘要未披露具体网络结构、训练目标、模型规模或任何生产级实现细节。

Result: 离线结果（论文明确报告）：在被准入的 sub-threshold 因子数量上，冻结裁判比泄漏裁判少 5–11 倍（在脚本提案者下），且没有提案者能缩小这一差距；在产出 yield 上，语言模型提案者优于脚本、与 bandit 持平，并额外具备 bandit 所没有的能力——撰写自己的诊断探针。代价是时间：一个被准入的真因子要等待约 500 个交易日，因此 certified portfolio 的 Sharpe ratio 落后于未加门控（ungated）的组合。在线 A/B 测试增益：论文摘要未披露。模型规模：论文摘要未披露。训练数据：论文摘要未披露。训练硬件/时长：论文摘要未披露。推理硬件、延迟、吞吐量：论文摘要未披露。内存与成本：论文摘要未披露。

Conclusion: 论文解决的是量化因子挖掘中的统计治理问题：将"提案"与"判断"分离，用 anytime-valid 的 betting 裁判以 5–11 倍的假发现抑制换取因子准入的高可靠性，代价是约 500 交易日的等待与组合 Sharpe 的下降。对工业搜索/广告/推荐或生产 LLM 团队的直接可复用价值有限：其 anytime-valid false-discovery 控制思想在概念上可迁移到"新策略/新特征需通过独立冻结评估器才能上线"的治理场景，但论文未提供任何工业数据、生产系统、在线实验或规模化资源的证据，作者身份与公司背景亦未披露。证据缺口明显：无工业部署、无线上收益、无成本/延迟数据，且实验局限于金融因子与合成世界。综合判断：不符合本系统设定的工业搜索/广告/推荐/生产 LLM 范围，拒绝保留。

Abstract: Language-model agents now run the whole of quantitative factor research: they propose investment factors, backtest them, select the survivors and retire them. We ask which of those jobs an agent should keep. Our answer is governed self-evolution: the agent may propose, and a frozen statistical referee that the agent cannot touch must judge. The referee scores each candidate only on market outcomes revealed after submission, by betting, so its false-discovery guarantee holds at every stopping time for any proposal policy. We cross three proposers (a script, a bandit and a language model) with this referee and with three deliberately leaky ones, in a synthetic world with planted truth, a probe-authoring environment and a ten-year walk-forward on the CSI 500. Who judges sets the number of false admissions: the frozen referee admits 5-11 times fewer sub-threshold factors than the leaky referees under a scripted proposer, and no proposer closes that gap. Who proposes sets the yield: the language model beats the script, matches the bandit, and adds the one capability a bandit lacks, writing its own diagnostic probes. The certificate's price is time: an admitted true factor waits about 500 trading days, and the certified portfolio's Sharpe ratio therefore trails an ungated one. Judging belongs to the procedure; proposing and instrument-making belong to the agent.

</details>


### [52] [Harness as a Language: A Minimalist Agent Framework With Maximal Expressivity](https://arxiv.org/abs/2609.26891)
*Zhening Li,Joshua Liu,Mateja Vukelic,Nicole Shen,Supriya Lall,Amitayush Thakur,Alex Zhang,Omar Khattab,Jonathan Light,Armando Solar-Lezama*

Main category: cs.AI

TL;DR: 论文摘要未披露具体公司/机构名称及团队所属业务线；该工作构建了一个极简 LLM 智能体框架 JAZ，聚焦于智能体循环（agent loop）这一智能体工作流的核心编排阶段，针对长期记忆、上下文窗口召回、持续自改进等通常需要专门外部系统才能实现的工作流。核心是单一 LLM 原语 invoke（可写递归调用 invoke 的任意代码、所有可见信息均为代码环境变量），仅靠提示、无手工工具或外部系统即在这些工作流上超过专用系统：在 StuLife 的召回密集部分比 Letta (MemGPT) 高 8%、成本减半；在 AppWorld 上比 ACE 高 4%、成本更低。


<details>
  <summary>Details</summary>
Motivation: 现代语言模型智能体围绕"agent loop"构建，LLM 在暴露工具集的环境中通过交替工具调用与观测输出掌控工作流；但某些工作流（如记忆系统、自改进系统）仍需要 agent loop 之外的额外工程（hand-crafted harness、外部记忆、文件系统等）。作者从第一性原理出发，探究一个"几乎只包含 agent loop 本身"的极简 harness 能在多大程度上完成这些专门系统所服务的工作流，从而减少专用外部系统的工程复杂度。具体瓶颈为：超出上下文窗口的长期召回与持续自改进通常依赖专门的外部 harness，实现成本高、系统复杂。

Method: JAZ 暴露单一 LLM 原语 invoke，并提供一组内置 hooks 供程序员施加约束与监控。invoke 是对现有 code-mode agent loop 的泛化，是满足两条定义性质的最简 loop：(1) LLM 可写任意可执行代码，其中包括递归 invoke；(2) 所有对 LLM 可见的内容——invoke 的全部输入以及其与代码环境的交互历史——都是代码环境中的变量。作者将 invoke 视为一个语言原语，代表一个"每次调用时由 LLM 在运行时提供实现"的函数。评测时 invoke 仅使用提示（no manually designed tools, harness, or external systems such as memory or file system），以验证核心原语本身的能力。上述为作者主张；论文摘要未披露训练目标、模型架构、输入输出 schema、hook 具体实现等更多工程细节。

Result: 离线评测（论文摘要未披露是否含在线 A/B 测试）：
- 长期工作流（需超出上下文的召回）：在 StuLife 的 recall-heavy 部分，JAZ invoke 比 Letta (MemGPT) 高 8%，且成本为其一半。
- 持续自改进：在 AppWorld 上，JAZ invoke 比 ACE 高 4%，且成本更低。
模型规模：论文摘要未披露。
训练数据：论文摘要未披露。
训练硬件/时间：论文摘要未披露。
推理硬件：论文摘要未披露。
延迟：论文摘要未披露。
吞吐量：论文摘要未披露。
内存：论文摘要未披露。
成本：仅给出相对表述——StuLife 部分为 Letta 的一半成本，AppWorld 上"更低"，但未披露绝对成本数值或计价单位。

Conclusion: 该工作解决的核心问题是：用一个极简 invoke 原语（本质即 agent loop 本身）替代记忆系统、自改进系统等专用外部 harness，在长期召回与持续自改进工作流上以更低成本取得更好（或相当）效果。对工业 LLM 团队的可复用价值在于：提供了一个"少即是多"的 harness 设计原则——通过语言级原语与递归调用、代码环境变量暴露，把本需专用外部系统的能力收敛到单一原语上，从而降低工程复杂度与成本。主要局限/证据缺口：论文摘要未披露公司/机构、评测所用的模型底座、绝对成本与延迟数据、是否做过生产环境或在线实验验证，且收益幅度（8%、4%）的统计显著性与任务覆盖范围未在摘要中说明；摘要未说明 framework 的团队归属及是否与实际业务产品绑定，可作为智能体编排层架构参考，但直接迁移到搜索/广告/推荐系统的证据不足。

Abstract: Modern language-model agents are built around the \textit{agent loop}, where the LLM is placed in an environment exposing a set of tools, and the LLM has full control over the workflow by alternating between tool calls and observing their output. However, certain workflows currently require additional engineering beyond the agent loop itself, such as memory systems and self-improving systems. We built an LLM agent framework, JAZ, to explore the extent to which a minimal harness that is little more than the agent loop itself can accomplish tasks these specialized systems are built for. JAZ exposes a single LLM-based primitive invoke and provides a set of built-in hooks that allow the programmer to apply constraints and monitoring. Generalizing existing code-mode agent loops, \texttt{invoke} is the simplest loop that satisfies two defining properties: (1) the LLM can write arbitrary executable code that can include recursive \texttt{invoke}; (2) everything visible to the LLM --- all inputs to \texttt{invoke} as well as its interaction history with the code environment --- are variables in the code environment. We motivate our design from first principles, viewing \texttt{invoke} as a language primitive representing a function whose implementation is provided at runtime by an LLM every time it is called. To validate the design of our core \texttt{invoke} primitive, we evaluate \texttt{invoke} --- with only prompting, no manually designed tools, harness, or external systems (e.g., memory or the file system) --- on workflows traditionally implemented through specialized external harnesses. On long-horizon workflows requiring recall beyond the context window, JAZ invoke outperforms Letta (MemGPT) by 8\% at half its cost on the recall-heavy portion of StuLife. On continual self-improvement, JAZ invoke outperforms ACE by 4\% at a lower cost on AppWorld.

</details>
